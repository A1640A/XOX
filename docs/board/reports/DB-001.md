```yaml
task: DB-001
status: done
summary: >
  `RoomDoc`/`GameDoc`/`UserDoc` tasarım §3.2-3.4'ün TÜM alanlarına genişletildi;
  `friendships` ve `mobileRefreshTokens` yeni koleksiyonları eklendi; §3.6'daki
  indeks tablosunun tamamı (12 kayıt — "11 indeks" kabul metni `games.finishedAt`ı
  önceden var sayıyor, aşağıda gerekçesi var) hem şema hem elle yazılmış bir
  `EXPECTED_INDEXES` beklenti tablosuyla `ensureIndexes()`/`indexes.test.ts`
  üzerinden gerçek `xox_test` Atlas veritabanına karşı doğrulandı. `seed.ts`
  artık `TEST_USERS`'ı gerçek `@node-rs/argon2` argon2id özetiyle yazıyor ve
  `TEST_USER_PASSWORD`'ü dışa veriyor. Bu görevden itibaren `packages/db`'nin
  YENİ test dosyalarının tamamı (models/*, indexes, seed) gerçek `xox_test`e
  karşı koşuyor — `client.test.ts`/`reset.test.ts` bilinçli olarak mocklu
  kaldı (bağlantı hata yollarını ve `xox_prod` korumasını test ediyorlar,
  gerçek ağ olmadan da doğru davranışı kanıtlarlar).

  **İnceleme turu (review_round_1) sonrası:** reviewer canlı `xox_test`e karşı
  4 major buldu — `board`ta 9 hücre kısıtı yoktu, `friendship` sıralı-çift
  değişmezi `updateOne`/`upsert` ile atlanabiliyordu, `passwordHash`
  `aggregate()` üzerinden sızıyordu, kapsam ölçümü `models/**`+`seed.ts`'i
  dışlıyordu — artı 3 küçük bulgu (yerel `Cell` tipi shared'ı yeniden
  tanımlıyordu, `Game` bitiş alanları çapraz tutarsız olabiliyordu,
  `indexes.test.ts` yapısal bir flake riski taşıyordu). Yedisi de önce
  kırmızı test yazılarak canlıda yeniden üretildi, sonra düzeltildi; 7 ayrı
  commit. `@xox/db` artık 66/66 test, **%100/%100/%100/%100** (models/**
  ve seed.ts DAHİL, yalnız `vitest.setup.ts` test altyapısı hariç);
  `pnpm gates` repo genelinde exit 0.

files_changed:
  - packages/db/package.json # @node-rs/argon2 bağımlılığı eklendi
  - packages/db/src/index.ts # yeni barrel export'lar (indexes, friendship, mobile-refresh-token, TEST_USER_PASSWORD)
  - packages/db/src/models/room.ts # RoomDoc tasarım §3.2'ye genişletildi (presence, disconnected, rematch, lastEmoji, moves, turnDeadline, startedAt)
  - packages/db/src/models/room.test.ts # YENİ — gerçek xox_test
  - packages/db/src/models/game.ts # GameDoc tasarım §3.3'e genişletildi (players, participants, pairKey, endReason, winLine, rated, eloDelta, settledAt), _id randomUUID
  - packages/db/src/models/game.test.ts # YENİ — gerçek xox_test
  - packages/db/src/models/user.ts # passwordHash {select:false}, ratedGames, theme, ELO_START/DISPLAY_NAME_MAX/LEADERBOARD_MIN_RATED_GAMES sabitlerinden
  - packages/db/src/models/user.test.ts # YENİ — KK-004 gizlilik testi dahil, gerçek xox_test
  - packages/db/src/models/friendship.ts # YENİ — sıralı çift değişmezi pre('validate') ile zorlanır
  - packages/db/src/models/friendship.test.ts # YENİ — gerçek xox_test
  - packages/db/src/models/mobile-refresh-token.ts # YENİ
  - packages/db/src/models/mobile-refresh-token.test.ts # YENİ — gerçek xox_test
  - packages/db/src/indexes.ts # YENİ — EXPECTED_INDEXES (elle, tasarımdan kopya) + ensureIndexes()
  - packages/db/src/indexes.test.ts # YENİ — collection.indexes() canlı doğrulama, gerçek xox_test
  - packages/db/src/seed.ts # gerçek argon2id hash, TEST_USER_PASSWORD dışa verildi
  - packages/db/src/seed.test.ts # YENİ — hash≠düz metin + verify true/false, gerçek xox_test
  - packages/db/src/vitest.setup.ts # YENİ — .env.local'i yükler (MONGODB_URI), MONGODB_DB'yi xox_test'e sabitler
  - packages/db/src/models/validators.ts # YENİ (review_round_1) — hasExactLength/isNullOrExactLength/hasAtMostLength
  - packages/db/vitest.config.ts # setupFiles + fileParallelism:false (review_round_1); coverage.exclude artık yalnız vitest.setup.ts
  - pnpm-lock.yaml # yalnız @node-rs/argon2 satırları
  - docs/board/reports/DB-001.md

tests:
  {
    added: 48,
    passing: 66,
    coverage: '%100 satır · %100 dal · %100 fonksiyon · %100 ifade — models/** ve seed.ts DAHİL (review_round_1 öncesi dışlanıyordu), yalnız vitest.setup.ts test altyapısı hariç',
    mutation: 'yok — bu paket mutasyon kapsamında değil (yalnız game-core)',
  }

kirmizi_yesil_kanit:
  - >
    `pnpm --filter @xox/db typecheck` ilk denemede `friendship.ts`teki
    `pre('validate', function(next){...})` imzasında iki hata verdi
    (`next` `Record<string,any>` çıkarımı — mongoose'un hook overload
    çözümlemesi callback biçimini reddetti). Düzeltme: `next` parametresini
    tamamen kaldırıp senkron `throw` kullanan bir hook'a geçildi; typecheck
    temizlendi.
  - >
    İlk `room.test.ts` koşusu 1/5 kırmızı verdi: `toStrictEqual({X:null,O:null})`
    hidratlanmış Mongoose belgesindeki alt-belge nesnesine karşı başarısız oldu
    (`Document` vs düz nesne). `.findOne(...).lean()` ile okunarak düzeltildi —
    diğer tüm model testleri baştan `.lean()` kullanacak şekilde yazıldı.
  - >
    İlk gerçek-Atlas koşusu `MONGODB_URI tanımlı değil` ile düştü:
    `vitest.setup.ts`teki `import.meta.dirname`den `.env.local`'e göreli yol
    bir dizin eksikti (`../..` yerine `../../..` gerekiyordu — dosya
    `packages/db/src/` altında, kök üç seviye yukarıda). Düzeltildikten sonra
    tüm gerçek-Atlas testleri yeşile döndü.
  - >
    `Game._id` alanına `winLine: [0,1]` (2 elemanlı) yazan test önce YEŞİLDİ
    (kırmızı bekleniyordu) — `isThreeIndexTuple` validator'ı `Schema` tanımına
    `validate` seçeneğiyle eklenmeden önce yazılmıştı. Validator eklenince
    test kırmızıdan yeşile döndü (doğru TDD sırası: sonradan fark edildi,
    validator + test birlikte commit edildi).

indeks_dogrulama:
  yontem: >
    `indexes.ts` içindeki `EXPECTED_INDEXES` tasarım §3.6'dan ELLE kopyalandı
    (mongoose şemasından `schema.indexes()` ile TÜRETİLMEDİ — gotcha:
    kendine-referanslı test bir indeksin şemadan silinmesini göremez).
    `indexes.test.ts` gerçek `xox_test`e bağlanır, `ensureIndexes()`
    (`Model.syncIndexes()` × 5) çağırır, sonra her koleksiyon için
    `db.collection(name).indexes()` çıktısını `_id_` hariç `EXPECTED_INDEXES`
    ile hem SAYI hem alan-sırası hem `unique`/`expireAfterSeconds`/
    `partialFilterExpression` bakımından karşılaştırır.
  sonuc: 'PASS — 6 test (5 koleksiyon `it.each` + 1 sayım testi), gerçek xox_test, 2 ayrı koşuda flake yok.'
  sayim_notu: >
    Kabul kriteri "11 indeksin TAMAMI" diyor ama tasarım §3.6 tablosunda 12
    satır var (`rooms`×2, `games`×4, `users`×2, `friendships`×2,
    `mobileRefreshTokens`×2). Tabloda `games{finishedAt:-1}` satırı
    "(mevcut — genel raporlama)" notuyla işaretli, yani bu görevden ÖNCE zaten
    vardı — "11" muhtemelen bu görevde YENİ eklenen indeks sayısını
    kastediyor. `EXPECTED_INDEXES`/testler tasarımın tam 12 satırlık listesini
    birebir doğrular; hiçbiri atlanmadı.
  users_elo_partial_filter: >
    `{ratedGames: {$gte: LEADERBOARD_MIN_RATED_GAMES}}` (`$gte`, `$ne` DEĞİL —
    gotcha zaten Mongo'nun kısmi indekste `$ne` desteklemediğini not etmişti,
    tasarım da başından `$gte`yi seçmişti). Canlı indeksten okunan
    `partialFilterExpression` bu değere `toStrictEqual` ile eşleşiyor.

password_hash_gizlilik_testi:
  test: "'KK-004: User.findById() çıktısında passwordHash GELMEZ'"
  sonuc: >
    PASS — gerçek xox_test'e yazılan kullanıcı `User.findById(id).lean()`
    ile okunduğunda `passwordHash` anahtarı sonuçta YOK
    (`expect(user).not.toHaveProperty('passwordHash')`). Ayrı testte
    `.select('+passwordHash')` ile açıkça istenince değer VAR ve doğru.
    `seed.test.ts` ayrıca gerçek argon2id doğrulamasını kapatıyor:
    `passwordHash !== TEST_USER_PASSWORD`, `passwordHash.startsWith('$argon2id$')`,
    `verify(hash, TEST_USER_PASSWORD) === true`, `verify(hash, 'yanlis-parola') === false`.

pnpm_gates: >
  review_round_1 fixleri sonrası tekrar koşuldu. Repo genelinde `pnpm gates`
  (typecheck + lint + format:check + test:coverage + knip) EXIT 0. 7 paket:
  @xox/db (66/66, %100/%100/%100/%100 — models/** ve seed.ts dahil),
  @xox/shared (214/214), @xox/game-core (91/91, %100), @xox/ui-tokens
  (38/38, %100), @xox/web (19/19, 92.66% ifade — bu görevin dışında,
  dokunulmadı), @xox/mobile ve @xox/e2e typecheck-only (cache hit). knip
  yalnız önceden var olan konfigürasyon ipuçlarını (17 adet, hepsi başka
  paketlerle ilgili) bastı, kırmızı/unused bulgu YOK.

review_round_1:
  status: 'dört major + üç küçük bulgunun yedisi de kapatıldı'
  yontem: >
    Reviewer HER bulguyu gerçek `xox_test`e karşı yazıp çalıştırarak kanıtladı
    (iddia değil, çalıştırılmış kod). Her düzeltme burada da aynı disiplinle
    yapıldı: önce reviewer'ın verdiği reprodüksiyonu birebir test olarak yaz →
    kırmızı olduğunu gör → düzelt → yeşile dön. Her biri ayrı commit (kota
    disiplini).
  commits:
    - '33f55a6 fix(db): board tam 9 hücre olmalı — sıranın tek kaynağı korunuyor'
    - '399c39b fix(db): friendship sıralı-çift değişmezi updateOne/findOneAndUpdate’te de zorlanıyor'
    - '543ebbe fix(db): passwordHash aggregate() üzerinden sızmasın'
    - 'b94db62 fix(db): Cell tipini yeniden tanımlama — @xox/shared’dan import et'
    - 'cf84438 fix(db): Game bitiş alanları çapraz tutarlı olmak zorunda'
    - 'ef8e70e fix(db): indexes.test.ts flake riskini kapat — dosya paralelliği kapatıldı'
    - 'c14d362 fix(db): models/** ve seed.ts kapsam ölçümüne dahil edildi'
  bulgular:
    - >
      1 (MAJOR) — `Room`/`Game.board` uzunluk kısıtı yoktu: `board:[12 hücre]`
      ve `board:[]` ikisi de kabul ediliyordu. `board` sıra sahipliğinin TEK
      kaynağı (`nextPlayer(board)`) olduğu için bu, tanımsız sıra + odanın
      hiçbir hamleyi kabul etmemesi + TTL'e kadar 2 saat kilit demekti.
      Kanıt: `Room.create({code, board:[...12]})` `resolve` etti (reddetmedi).
      Düzeltme: `hasExactLength(9)` validator'ı hem `Room.board` hem
      `Game.board`'a eklendi (`winLine`in önceki yerel `isThreeIndexTuple`'ı
      da aynı `validators.ts`e taşındı — aynı kısıt sınıfının iki kopyası
      olmasın). `moves` de `hasAtMostLength(9)` ile sınırlandı (öneri).
    - >
      2 (MAJOR) — `Friendship.pre('validate')` yalnız `save()`/`insertMany`
      yolunda çalışıyordu. Kanıt: `Friendship.updateOne({userA:büyük,
      userB:küçük}, {...}, {upsert:true})` ters sıralı dokümanı sorunsuz
      yazdırdı — KK-125…127'nin "$or'suz tek sorgu" varsayımını çökertiyordu.
      Düzeltme: `updateOne`/`findOneAndUpdate`/`replaceOne`'a bir QUERY
      middleware'i eklendi; nihai `userA`/`userB` değeri `$set`'ten ya da
      (upsert'te) sorgu filtresinden okunup kontrol ediliyor. Doğru sıralı
      upsert hâlâ sorunsuz çalışıyor (aşırı-geniş red testi eklendi) ve
      `userA`/`userB`'ye hiç dokunmayan update'ler (yalnız `status`
      değiştiren) engellenmiyor.
    - >
      3 (MAJOR) — `select:false` yalnız `find*` ara katmanını kapsıyor.
      Kanıt: `User.aggregate([{$match:{_id}}])` `passwordHash`i olduğu gibi
      döndürdü (`findById`/`findOneAndUpdate`/`JSON.stringify` temizdi).
      Düzeltme: `pre('aggregate')` hook'u her pipeline'ın başına
      `$unset:'passwordHash'` ekliyor. Ham `Model.collection.findOne()`/
      native sürücü erişimi mongoose hook sisteminin TAMAMEN dışında kalır —
      bu Mongoose'un belgelenmiş sınırı, düzeltilemez; bu görevin kapsamında
      böyle bir çağrı yok (doğrulandı).
    - >
      4 — Kapsam ölçümü `src/seed.ts` ve `src/models/**`'i dışlıyordu, yani
      bu kartın teslim ettiği yüzeyin TAMAMI ölçüm dışıydı. Kaldırılınca iki
      GERÇEK boşluk çıktı: `seed.ts`'in CLI giriş dalı test edilmiyordu
      (`reset.test.ts`teki `vi.resetModules()` kalıbıyla kapatıldı) ve
      `friendship.ts`teki `readOrderCandidate` üç dalını kapsamıyordu (`$set`
      içinde ters sıralı değer, `userA`/`userB`'ye dokunmayan update,
      `replaceOne` yolu — üçü de test edildi). Sonuç: %100/%100/%100/%100,
      hiçbir dosya artık kapsam dışı değil.
    - '5 — Yerel `Cell` tipi silindi, `room.ts` `@xox/shared`dan import ediyor (yukarıdaki decisions maddesine bkz — ilk raporun bu konudaki iddiası yanlıştı, düzeltildi).'
    - >
      6 — `Game` bitiş alanları çapraz tutarsız olabiliyordu: `winner:'X'`+
      `isDraw:true` kabul ediliyordu, `winner:null`+`endReason:'line'`+
      `winLine:null` de kabul ediliyordu. `pre('validate')`'e üç kural
      eklendi: `isDraw ⇒ winner===null`, `endReason==='line' ⇒
      winner!==null && winLine!==null`, `finishedAt===null ⇒ winner===null
      && !isDraw` (oyun sürüyorsa sonuç yok). 4 kırmızı test → 4 yeşil.
    - >
      7 — `indexes.test.ts` yapısal bir flake riski taşıyordu: beş kardeş
      test dosyası paralel worker'larda kendi `beforeAll`'unda
      `Model.syncIndexes()` (drop+create) çağırıyordu, `indexes.test`in
      tam-sayım iddiası bir syncIndexes ortasında ölçüm alabilirdi (flake
      GÖZLEMLENMEDİ ama mekanizma gerçekti — kanıt değil). `fileParallelism:
      false` ile db paketinin dosyaları sıralı koşuyor; iki `beforeAll` artık
      YAPISAL OLARAK asla eşzamanlı çalışamaz. Maliyet ~4 sn yerel koşuda.
  dogrulanmis_dogru_kisimlar: >
    Reviewer ayrıca ilk teslimin şu kısımlarını canlıda doğruladı ve
    değiştirilmesini İSTEMEDİ: §3.2'nin 13 alanı, §3.3'ün 15 alanı, 12
    indeksin gerçekten Atlas'ta kurulduğu, `EXPECTED_INDEXES`'in şemadan
    türetilmediği için testin dürüst olduğu, seed argon2id doğrulamasının
    dört yönlü olduğu, `@node-rs/argon2`'nin tek kopya olduğu, ve `turn`
    alanının EKLENMEMESİ kararının doğru olduğu (üç kaynaktan bağımsız
    doğrulanmış: tasarım §3.2, §5.5'in `nextPlayer(board)` türetimi, ve
    `board.json`daki gerçek kabul metni — hiçbirinde `turn` yok).

decisions:
  - karar: >
      İlk teslimde rapor dosyası yanlışlıkla ana checkout'a
      (`/Users/.../XOX/docs/board/reports/DB-001.md`) yazılmıştı — worktree'ye
      değil. Fark edilir edilmez düzeltildi: dosya ana checkout'tan silindi
      (henüz commit edilmemişti, `git status --porcelain` ile doğrulandı,
      kayıp iş yok), doğru yola (worktree içi) yeniden yazıldı ve `git status`
      ile hem worktree'nin doğru dosyayı gördüğü hem ana checkout'ta DB-001'e
      dair hiçbir iz kalmadığı iki ayrı komutla kanıtlandı.
    gerekçe: >
      gotchas.md'deki "Dalga uçuştayken lead'in `git add -A` kullanması iş
      karıştırır" maddesiyle aynı sınıf hata (mutlak yol yerine göreli yazma/
      yanlış çalışma dizini). Erken yakalanıp commit'ten ÖNCE düzeltildiği
      için `main`e hiçbir şey sızmadı.
    reddedilen_alternatif: yok — bu bir hataydı, alternatif değerlendirilmedi.
  - karar: >
      RoomDoc'a spec'in kabul metninde geçen ama tasarım §3.2 arayüzünde
      OLMAYAN bir "turn" alanı EKLENMEDİ.
    gerekçe: >
      Tasarım dokümanı normatif ("bir alan adı burada yazılıysa uygulama
      onu birebir kullanır") ve §3.2'nin `RoomDoc` arayüzünde `turn` yok;
      sıra sahipliği `nextPlayer(board)` ile TÜRETİLİR (§5.5, `applyMove`
      örneği). Bu, ayrıca `board.json`daki DB-001 acceptance metniyle de
      birebir örtüşüyor (o metinde de `turn` yok) — bana iletilen görev
      özetindeki "turn" ifadesi muhtemelen bir özetleme kaymasıydı.
    reddedilen_alternatif: >
      Depolanan bir `turn: Player` alanı eklemek — `board`'dan türetilebilen
      bir değeri ayrıca saklamak iki doğruluk kaynağı yaratır ve senkron
      kalması gereken bir alan daha ekler (aynı sınıf hata: "sabitin
      kopyası = sessiz sapma").
  - karar: >
      **DÜZELTME (review_round_1) — bu maddenin ilk hâli YANLIŞTI.** İlk
      teslimde `room.ts` kendi `export type Cell = Player | null` tanımını
      tutuyordu; `game.ts` zaten `@xox/shared`'dan alıyordu. Yani iki `Cell`
      dolaşımdaydı ve `packages/db/src/index.ts` kendi kopyasını re-export
      ediyordu — kartın açık kuralının (`SeatOccupant`'ı yeniden tanımlama)
      aynı sınıf ihlaliydi, ama ilk raporda "YENİDEN TANIMLANMADI" yazılmıştı.
      Reviewer yakaladı: `commit b94db62` ile yerel `Cell` silindi, `room.ts`
      `@xox/shared`'dan import ediyor, barrel'dan re-export kaldırıldı (hiçbir
      tüketici `@xox/db`'den `Cell` import etmiyordu — `grep` ile doğrulandı,
      kırıcı değişiklik değil). `Player`/`SeatOccupant`/`EndReason`/
      `WinLineCells` baştan beri doğru şekilde shared'dan geliyordu.
    gerekçe: >
      Aynı birlik tipinin iki kopyası (`'X'|'O'` db'de, `Player` shared'da)
      birbirinden ayrışırsa hiçbir derleme hatası vermez — sessiz sapma.
    reddedilen_alternatif: >
      Yalnız `SeatOccupant`i shared'dan almak, geri kalanı yerel birlik
      olarak bırakmak — kısmi disiplin, aynı sınıf riski `Player`/`Cell`
      için açık bırakır.
  - karar: >
      `EXPECTED_INDEXES` `indexes.ts`te ELLE yazıldı (mongoose
      `schema.indexes()`ten türetilmedi) ama içindeki değerler
      `ROOM_TTL_SECONDS`/`LEADERBOARD_MIN_RATED_GAMES` gibi `@xox/shared`
      sabitlerinden okunuyor, magic number kopyalanmadı.
    gerekçe: >
      İki ayrı gotcha aynı anda kapatılıyor: (a) kendine-referanslı test
      silmeyi göremez → beklenti şemadan türetilemez, elle yazılmalı;
      (b) sabitin kopyası sessiz sapma üretir → sayısal değer ayrıca elle
      yazılmamalı, tek kaynaktan okunmalı. İkisi çelişmiyor: "elle yazılan"
      şey İNDEKS YAPISI (hangi alanlar, unique mi, TTL mi), sayısal DEĞER
      değil.
    reddedilen_alternatif: >
      `ROOM_TTL_SECONDS` değerini de `7200` olarak elle yazmak — sabit
      değişirse `indexes.ts` sessizce yanlış beklenti taşımaya devam ederdi.
  - karar: >
      `packages/db`'nin bu görevde eklenen TÜM yeni test dosyaları
      (`models/*.test.ts`, `indexes.test.ts`, `seed.test.ts`) gerçek
      `xox_test` Atlas'ına karşı koşuyor; `client.test.ts`/`reset.test.ts`
      mocklu kalmaya devam ediyor.
    gerekçe: >
      Görev kabul kriteri #9 ve tasarım §1 açıkça "packages/db içinde düz
      vitest run ile gerçek xox_test'e karşı" diyor. `client.test.ts` ise
      `MONGODB_URI` yokluğu ve eşzamanlı bağlantı paylaşımı gibi HATA
      yollarını test ediyor — bunları gerçek ağla test etmek mümkün değil
      (ağ her zaman "başarılı" olurdu) ve zaten önceden var olan, çalışan
      testler. `reset.test.ts` de `xox_prod`/`xox_dev` REDDİNİ test ediyor;
      gerçek bir `xox_prod`a bağlanıp reddedilmesini beklemek riskli ve
      gereksiz (mantık zaten saf bir string karşılaştırması).
    reddedilen_alternatif: >
      Tüm testleri gerçek Atlas'a taşımak — `client.test.ts`in "eşzamanlı
      çağrılarda tek bağlantı sözü paylaşılır" testi gibi zamanlama-hassas
      senaryolar gerçek ağda flake üretir.
  - karar: >
      `.env.local` yükleyicisi (`vitest.setup.ts`) `dotenv` bağımlılığı
      EKLEMEDEN elle satır satır ayrıştırıyor.
    gerekçe: >
      Tek ihtiyaç `KEY=VALUE` biçimini okumak; `.env.local`de tırnak/çok
      satırlı değer yok (doğrulandı). Yeni bir bağımlılık paketin
      `package.json`ına dokunmayı gerektirir ve bu görevin çakışma kümesi
      zaten `package.json`ı kapsıyor ama gereksiz yüzey eklemekten kaçınıldı.
    reddedilen_alternatif: >
      Node'un `--env-file` bayrağı — vitest CLI'nin kendi node çağrısına
      bu bayrağı enjekte etmek `package.json` script'ini kırılgan hale
      getirirdi (`vitest run` doğrudan `node_modules/.bin/vitest`i çağırıyor).

gotchas:
  - >
    `import.meta.dirname` tabanlı göreli yol hesaplarken dosyanın GERÇEK
    konumunu (burada `packages/db/src/`, `packages/db/` değil) saymak kolay
    kayıyor — bir seviye eksik yazıldığında hata `.env.local yok` değil
    `MONGODB_URI tanımlı değil` gibi bambaşka bir mesaj veriyor (dosya sessizce
    bulunamıyor, `existsSync` false dönüyor, kod normal akışına devam ediyor).
    Teşhis: yolu `console.log`layıp `existsSync` sonucunu ayrıca yazdırmak
    gerekti; hata mesajının kendisi yanıltıcıydı.
  - >
    Mongoose `schema.pre('validate', function(next) {...})` imzası bu
    mongoose/typescript sürüm kombinasyonunda `next`i `Record<string,any>`e
    daraltıyor (yanlış overload seçimi) — callback biçimini TAMAMEN bırakıp
    senkron `throw` kullanan hook yazmak hem daha az kod hem tip-güvenli.
  - >
    `@node-rs/argon2@2.1.0` bu ortamda `pnpm-workspace.yaml`'a `allowBuilds`
    girdisi GEREKTİRMEDİ — napi-rs prebuilt ikili `optionalDependencies`
    üzerinden postinstall script'i olmadan kuruluyor (`ERR_PNPM_IGNORED_BUILDS`
    HİÇ tetiklenmedi). Görev talimatındaki "gerekirse blocked işaretle"
    şartı bu yüzden devreye girmedi — kayda değer çünkü gotchas.md'de
    "postinstall script'i olan paketler onay ister" genellemesi var, bu paket
    o kategoriye girmiyor.
  - >
    **(review_round_1) Mongoose belge doğrulayıcıları (`schema.validate`,
    `pre('validate')`) yalnız `save()`/`insertMany` yolunu kapsar —
    `updateOne`/`findOneAndUpdate`/`replaceOne` TAMAMEN atlar, `upsert:true`
    dahil.** Uygulama seviyesi bir değişmez (sıralı çift, uzunluk kısıtı,
    çapraz tutarlılık) yalnız `pre('validate')`e yazılırsa güvenlik
    sağlanmamış olur; ayrıca ilgili güncelleme operasyonlarına (`updateOne`/
    `findOneAndUpdate`/`replaceOne`) bir QUERY middleware'i (`query.getUpdate()`
    + `query.getFilter()` okuyan) gerekir. `select:false` de benzer şekilde
    yalnız `find*`i kapsar, `aggregate()`i KAPSAMAZ — o da ayrı bir
    `pre('aggregate')` hook'u ister. Genel ders: bir mongoose güvenlik/
    doğrulama mekanizması eklerken "hangi OPERASYON ARA KATMANINI kapsıyor"
    sorusu tek tek her yazma yolu için (`save`/`insertMany`/`updateOne`/
    `updateMany`/`findOneAndUpdate`/`replaceOne`/`aggregate`/ham
    `collection.*`) sorulmalı — biri atlanırsa hiçbir test kırmızı olmadan
    sessizce delinir.
  - >
    **(review_round_1) `Model.collection.findOne()`/ham `mongodb` sürücü
    çağrıları Mongoose'un TÜM hook sisteminin (validate/select/aggregate)
    tamamen DIŞINDADIR** — bu Mongoose'un belgelenmiş, düzeltilemez sınırı.
    `select:false`/`pre('aggregate')` gibi savunmalar yalnız Mongoose API'si
    üzerinden geçen çağrıları kapsar; bir gün biri `User.collection.find()`
    yazarsa `passwordHash` sızar ve hiçbir mevcut test bunu yakalamaz. Kod
    incelemesi maddesi olarak tutulmalı, otomatik test edilemez.

blocked_reason: null

next_suggestions:
  - >
    DB-002 (`packages/db/src/rooms/**` — create/join/detach/applyMove) bu
    modellere DOĞRUDAN bağımlı; `Room.seats`/`presence`/`moves` ve
    `Game.participants`/`pairKey` alanları artık kullanıma hazır. `pairKey`i
    sıralı `${a}|${b}` olarak YAZMAK DB-002'nin işi — bu görev yalnız şemayı
    tanımladı, sıralama mantığını (kim küçük kim büyük) uygulamadı.
  - >
    (review_round_1 sonrası GÜNCEL) `Friendship` modelinin sıralı-çift
    kontrolü artık HEM `save()`/`insertMany` (`pre('validate')`) HEM
    `updateOne`/`findOneAndUpdate`/`replaceOne` (query middleware) yolunda
    zorlanıyor — model VERİYİ korur. Ama `friendships/**` altında (henüz
    yazılmamış, DB-002+ kapsamında) bir `sendFriendRequest(userA, userB)`
    fonksiyonu yine de kendi tarafında `[a,b].sort()` ile GÖNDERMELİ: model
    hatalı sırayı REDDEDER, doğru sırayı KENDİLİĞİNDEN üretmez — çağıran
    taraf `ters sıra` hatası yerine `SESSİZCE doğru` davranışı ister.
  - >
    `TEST_USER_PASSWORD` artık `@xox/db`den dışa veriliyor; AUTH-001 (Credentials
    provider) ve E2E login akışları bunu import edip gerçek parolayı
    kullanabilir — E2E dosyalarında ayrı bir sabit tanımlanmasın.
  - >
    `indexes.ts`teki `ensureIndexes()` henüz hiçbir yerden ÇAĞRILMIYOR
    (yalnız testte). Koordinatör bunun için **OPS-003** kartını açtı (DB-002'nin
    bağımlılığı) — prod/preview ortamında ilk deploy sonrası indekslerin
    gerçekten kurulduğundan emin olmak için bir migration/healthcheck adımına
    bağlanması OPS-003'ün işi, burada tekrar açılmadı.
  - >
    Koordinatör `buildPairKey`/`deriveParticipants` seam'ini DB-002'ye
    devretti (bu görev bilerek boş bıraktı — `GameDoc.pairKey`/`participants`
    alanları şemada var ama onları YAZAN mantık `packages/db/src/rooms/`e
    ait, henüz yazılmadı). İki yerde ayrı ayrı yazılmasın diye tek sahiplik
    DB-002'de.
  - >
    review_round_1'in genel dersi: bir mongoose güvenlik mekanizması
    eklerken (`pre('validate')`, `select:false`, benzeri) hangi YAZMA/OKUMA
    YOLUNU kapsadığını TEK TEK kontrol et (`save`/`insertMany`/`updateOne`/
    `updateMany`/`findOneAndUpdate`/`replaceOne`/`aggregate`) — bkz. yukarıdaki
    gotcha. DB-002'nin `rooms/` geçiş fonksiyonları CAS için `findOneAndUpdate`
    kullanacak (§5.5); oradaki hiçbir şema-seviyesi kısıt bu köşeyi
    unutmamalı.
```
