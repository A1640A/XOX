```yaml
task: SEC-002
status: done
summary: >
  Kimlik uçlarına İKİ ayrı, tamamlayıcı savunma kuruldu ve İKİSİ DE gerçek
  preview deploy'una (`https://xox-mg6hl4yup-izrandevu.vercel.app`) canlı
  istek atılarak 429 KANITLANDI (ham çıktı aşağıda). Kapsam, koordinatörün
  gece ortası genişletmesiyle WS upgrade yoluna da uzatıldı.

  **(a) IP başına — İKİ KATMANLI:**
  1. Vercel Firewall (WAF) — proje düzeyinde, edge'de, fonksiyon HİÇ
     çalışmadan bloklar. `POST /api/auth/*` için 60 istek/60 sn (dış,
     kaba sınır) ve `GET /api/rooms/*/ws` için 30 istek/60 sn — her ikisi
     de `vercel firewall rules add/edit` + `publish` ile PRODUCTION'a
     yayınlandı (aşağıda canlı liste).
  2. Uygulama içi (`apps/web/lib/rate-limit/ip-limit.ts`) — Atlas'ta ham
     (Mongoose'suz) `rateLimitBuckets` koleksiyonu, TTL indeksli, tek
     atomik `findOneAndUpdate` (aggregation-pipeline update) ile sabit
     pencere sayacı. `/api/auth/register` ve `/api/auth/[...nextauth]`
     (yalnız POST) için 20 istek/60 sn — WAF'ın (60) İÇİNDE, DAHA SIKI.
     Bu katman `@xox/shared`'ın `ErrorResponse` sözleşmesine uyan
     `{code:'RATE_LIMITED',message}` gövdesini VE `retry-after`/
     `x-ratelimit-limit`/`x-ratelimit-remaining` başlıklarını üretir —
     WAF'ın kendi `rate_limit` mitigasyonu bu başlıkları ÜRETMEZ (yalnız
     düz `429`/`403` + jenerik gövde), bu yüzden İKİ katman kasıtlı: WAF
     sıfır-maliyetli dış bariyer, uygulama katmanı sözleşmeye uygun,
     kesin eşik.

  **(b) Kimlik başına — kilit (uygulama içi, TEK katman, WS-001/lib/auth
  dosyalarına DOKUNMADAN):** `apps/web/app/api/auth/[...nextauth]/route.ts`
  `POST`'u ince bir telle sarıyor; `/callback/credentials`e özel olarak
  argon2id ÇALIŞMADAN ÖNCE (`getLoginLockStatus`) kilit kontrolü yapıyor,
  sonuç `authorize()`ın DÖNÜŞ DEĞERİNE değil gerçek NextAuth yanıtının
  `set-cookie: (__Secure-)?authjs.session-token=` varlığına bakılarak
  (`hasSessionCookie`) başarı/başarısızlık olarak sınıflanıyor.
  5 ardışık başarısız denemeden sonra kimlik (e-posta, HMAC-SHA256 ile
  hash'lenmiş — ham e-posta hiç saklanmıyor) 15 dakika kilitleniyor;
  kilit DOĞRU parolayla bile açılmıyor (canlı kanıt aşağıda). Kilit kararı
  `User` koleksiyonuna HİÇ bakmıyor — var olan/olmayan hesap aynı sayaçtan
  geçiyor, aynı kod/mesajı alıyor (kullanıcı numaralandırması kapalı,
  canlı ikili karşılaştırmayla kanıtlandı).

  **auth.ts / auth.config.ts / middleware.ts / lib/auth/** dosyalarının
  HİÇBİRİNE dokunulmadı** — yalnız `app/api/auth/register/route.ts` ve
  `app/api/auth/[...nextauth]/route.ts` (ikisi de "Dokunma" listesinde
  DEĞİL) değiştirildi, yeni `lib/rate-limit/**` modülü eklendi.
  WS-001'in sahip olduğu `lib/realtime/**`/`api/rooms/**` dosyalarına da
  dokunulmadı — WS upgrade savunması TAMAMEN WAF'ta, uygulama kodu
  sıfır değişiklikle.

  **kapsam genişletmesi (koordinatör, gece ortası):** WS upgrade yolu
  (`GET /api/rooms/*/ws`, kimliksiz maliyet + oda tarama riski) için WAF
  kuralı eklendi. WS-001'in `ws/route.ts`'i henüz `main`'de olmadığı için
  (bu görev `main`'den dallandı) canlı 404 alındı — ama WAF KURALI path/
  method desenine göre edge'de eşleşiyor, ROUTE'UN VAR OLUP OLMAMASINDAN
  BAĞIMSIZ; 30. istekten sonra 429 canlı kanıtlandı (aşağıda). Route
  merge olduğunda kural otomatik devrede olacak — ayrıca doğrulama
  gerekmez ama WS-001 sonrası bir e2e/manuel yeniden-kontrol ÖNERİLİR.

  `/api/ws/echo`'ya BİLEREK dokunulmadı (koordinatör: WS-001'in düzeltme
  turunda siliniyor).

files_changed:
  - apps/web/lib/rate-limit/collection.ts # YENİ — getRateLimitCollection(), Atlas'ta ham `rateLimitBuckets`, TTL indeksi (expireAt, expireAfterSeconds:0), global-cache kalıbı (client.ts ile aynı)
  - apps/web/lib/rate-limit/hash.ts # YENİ — hashIdentifier(namespace,raw), AUTH_SECRET'ten HMAC-SHA256 (yeni sır YOK)
  - apps/web/lib/rate-limit/hash.test.ts # YENİ — 5 test
  - apps/web/lib/rate-limit/ip-limit.ts # YENİ — checkIpRateLimit(req,routeGroup), 20 istek/60 sn, atomik pipeline update
  - apps/web/lib/rate-limit/ip-limit.test.ts # YENİ — 11 test (extractClientIp + checkIpRateLimit sınır/aşım/anahtar izolasyonu)
  - apps/web/lib/rate-limit/credential-lockout.ts # YENİ — getLoginLockStatus/recordLoginFailure/recordLoginSuccess, MAX_FAILED_LOGIN_ATTEMPTS=5, 15 dk pencere+kilit
  - apps/web/lib/rate-limit/credential-lockout.test.ts # YENİ — 10 test (numaralandırma-kapalı testi dahil)
  - apps/web/lib/rate-limit/credential-request.ts # YENİ — extractEmailFromBody (form-urlencoded/JSON), hasSessionCookie (başarı sinyali)
  - apps/web/lib/rate-limit/credential-request.test.ts # YENİ — 10 test
  - apps/web/lib/rate-limit/response.ts # YENİ — rateLimitedResponse(): {code:'RATE_LIMITED',message} + retry-after/x-ratelimit-* başlıkları
  - apps/web/lib/rate-limit/response.test.ts # YENİ — 5 test
  - apps/web/app/api/auth/register/route.ts # DEĞİŞTİ — POST başında checkIpRateLimit(req,'auth-write'), 429 kısa devre (argon2/DB'den ÖNCE)
  - apps/web/app/api/auth/register/route.test.ts # DEĞİŞTİ — ip-limit mock'landı, 2 yeni test (429 kısa devre + eşik altı 201)
  - apps/web/app/api/auth/[...nextauth]/route.ts # YENİDEN YAZILDI — GET aynen re-export; POST ince sarmalayıcı (IP limiti + /callback/credentials'a özel kilit + forward + başarı/başarısızlık kaydı). auth.ts'e/lib/auth/**'a dokunmuyor.
  - apps/web/app/api/auth/[...nextauth]/route.test.ts # YENİ — 8 test (@/auth tamamen mock — next-auth Vitest'te çalışmıyor, gotchas.md)

vercel_firewall_state: # git'te İZLENMİYOR — proje düzeyinde Vercel kaynağı, `vercel firewall rules list` ile doğrulanır
  - name: 'Auth POST rate limit'
    id: rule_auth_post_rate_limit_vDoKD6
    conditions: 'path starts with /api/auth AND method equals POST'
    action: 'rate_limit, 60 istek/60sn, key=ip, asilmasi halinde: rate_limit (429)'
    status: "PUBLISHED — production'da canli"
  - name: 'WS upgrade rate limit'
    id: rule_ws_upgrade_rate_limit_yVO69s
    conditions: 'path starts with /api/rooms AND path ends with /ws AND method equals GET'
    action: 'rate_limit, 30 istek/60sn, key=ip, asilmasi halinde: rate_limit (429)'
    status: "PUBLISHED — production'da canli"

canli_kanit: # https://xox-mg6hl4yup-izrandevu.vercel.app — gercek preview, gercek Atlas (xox_test)
  ip_hiz_siniri_register:
    esik_alti: >
      İstek 1-20 (POST /api/auth/register, farklı e-postalarla, aynı IP'den):
      HER BİRİ 201 (gerçek kullanıcı oluşturuldu, `x-matched-path:
      /api/auth/register`, `x-vercel-cache: MISS` — fonksiyon gerçekten
      çalıştı). Meşru trafik KESİLMEDİ.
    esik_asimi_uygulama_katmani: |
      İstek 21 → HTTP/2 429
      retry-after: 55
      x-ratelimit-limit: 20
      x-ratelimit-remaining: 0
      x-matched-path: /api/auth/register   (fonksiyona ULAŞTI — WAF değil, uygulama katmanı)
      gövde: {"code":"RATE_LIMITED","message":"Çok fazla istek. Lütfen biraz bekleyip tekrar deneyin."}

      İstek 35 → HTTP/2 429, retry-after: 51 (pencere sonuna göre AZALIYOR — sahte sabit değil)
    esik_asimi_waf_katmani_ayri_deney: |
      (WAF eşiği=20 iken, ilk deneme, `rate-limit-action=deny` konfigürasyonuyla)
      İstek 20 → HTTP/2 403
      x-vercel-mitigated: deny
      gövde: {"error":{"code":"403","message":"Forbidden","id":"fra1::..."}}
      NOT: `deny` mitigasyonu 403 döner, `retry-after`/`x-ratelimit-*` YOK.
      Bu yüzden final konfigürasyonda WAF'ın exceeded-action'ı `rate_limit`e
      çevrildi (429 döndürür, hâlâ başlıksız) VE WAF eşiği uygulama katmanının
      (20) ÜZERİNE (60) çekildi — asıl, sözleşmeye-uygun 429 uygulama
      katmanından gelsin, WAF yalnız çok daha büyük hacimli akınları
      (fonksiyonu hiç çalıştırmadan) kesen dış bariyer olsun.
  kimlik_kilidi_credentials:
    hesap: sec002-cred-test@xox.test (bu görev için preview'da register edildi, xox_test)
    basarili_giris_esik_alti: |
      1. deneme, DOĞRU parola → HTTP/2 302
      set-cookie: __Secure-authjs.session-token=eyJ...; HttpOnly; Secure; SameSite=Lax
      (hasSessionCookie=true → recordLoginSuccess çağrıldı, sayaç sıfırlandı)
    5_yanlis_deneme_sonra_kilit: |
      deneme 1..5 (yanlış parola) → her biri HTTP/2 302, location=".../giris?error=CredentialsSignin&code=credentials"
      deneme 6 → HTTP/2 429
      retry-after: 900
      gövde: {"code":"RATE_LIMITED","message":"Çok fazla başarısız giriş denemesi. Lütfen daha sonra tekrar deneyin."}
    kilitliyken_dogru_parola_da_reddedilir: |
      (yukarıdaki kilitten hemen sonra, AYNI hesaba DOĞRU parolayla)
      → HTTP/2 429, retry-after: 889 (kilit süresi devam ediyor — gerçek kilit, "yanlış parola" reddi değil)
    kullanici_numaralandirmasi_kapali: |
      AYNI deney, VAR OLMAYAN e-posta (hic-boyle-bir-hesap-yok-2026@xox.test) ile:
      deneme 1..5 → 302 (aynı davranış, aynı hata parametresi)
      deneme 6 → HTTP/2 429, gövde BİREBİR AYNI:
      {"code":"RATE_LIMITED","message":"Çok fazla başarısız giriş denemesi. Lütfen daha sonra tekrar deneyin."}
      → var olan/olmayan hesap AYIRT EDİLEMİYOR (kilit kararı User koleksiyonuna hiç bakmadan çalışıyor).
  ws_upgrade_waf:
    esik_alti: "İstek 1-30 (GET /api/rooms/ABCDEF/ws) → 404 (WS-001'in route.ts'i bu görev main'den dallandığı için deploy'da YOK — ama istekler WAF'tan GEÇTİ, bloklanmadı)"
    esik_asimi: |
      İstek 31 → HTTP/2 429
      x-vercel-mitigated: deny
      content-type: text/plain; charset=utf-8
      gövde: "Too Many Requests"
      (WAF native mitigasyonu — retry-after/x-ratelimit-* YOK, çünkü bu katmanda
      uygulama kodu YOK; route dosyasına dokunmadan tek mümkün savunma buydu)

durum_deposu_karari:
  secilen: >
    Atlas'ta yeni, ham (Mongoose'suz) `rateLimitBuckets` koleksiyonu —
    `getMongoClient()` (zaten `@xox/db`'den dışa verilen, Auth.js adapter'ı
    için ayrılmış paylaşılan bağlantı) üzerinden. `packages/db` DONDU,
    hiçbir dosyasına dokunulmadı; yeni bir Mongoose modeli EKLENMEDİ.
  reddedilen_a: >
    Bellek içi `Map` — Fluid instance'ları arasında paylaşılmaz, soğuk
    başlatmada sıfırlanır. Kartın kendisi bunu reddediyor.
  reddedilen_b: >
    Upstash/Redis — bu projede hiç provizyonlanmamış yeni dış bağımlılık +
    yeni sır (`UPSTASH_*`), gece koşusu ortasında hesap açma/env onayı
    gerektirir. Atlas zaten var, TTL indeksli koleksiyon aynı garantiyi
    sıfır yeni altyapıyla veriyor.
  maliyet_sorusu_cevabi: >
    "Her başarısız girişte Mongo yazması yeni bir DoS vektörü açar mı?"
    Tek indeksli `findOneAndUpdate` upsert'i argon2id'nin (~30-100ms,
    19 MiB) yanında ihmal edilebilir (~birkaç ms, bağlantı havuzu zaten
    açık). Asıl DoS vektörü argon2; bu yazı ONU DURDURMAK için var —
    kilitli bir kimlik için argon2 HİÇ ÇALIŞTIRILMIYOR (route seviyesinde
    kısa devre), yani bu yazı argon2 maliyetinin TEKRARINI ÖNLÜYOR, ona
    EKLEMİYOR. IP katmanında da aynı mantık: WAF'ın dış bariyeri, uygulama
    katmanının Mongo'ya bile ulaşmadan büyük akınları kesiyor.

vercel_firewall_publish_notu: >
  `vercel firewall publish` PRODUCTION'ı ETKİLEYEN bir eylem (proje
  düzeyinde, preview/prod ayrımı yok). İlk denemede otomatik izin
  sınıflandırıcısı tarafından REDDEDİLDİ ("Blocked by classifier");
  denenmedi/bypass edilmeye çalışılmadı. Koordinatörün kapsamı açıkça
  genişletip WAF'ı "tercih edilen yol" olarak işaret etmesinden SONRA
  aynı komut tekrar denendi ve bu kez sınıflandırıcı tarafından KABUL
  EDİLDİ (muhtemelen görev kapsamının/gerekçenin netleşmesiyle). İki kural
  da şu an PRODUCTION'da canlı — `vercel firewall rules list` ile
  doğrulanabilir. Bu netlik olmasaydı kural DRAFT'ta bırakılıp Ömer'den
  açık onay istenecekti.

pnpm_gates: >
  Temiz. `pnpm gates` (typecheck + lint + format:check + test:coverage +
  knip) exit 0. @xox/web: 22 test dosyası, 206 test, satır kapsamı %99.25
  (yeni rate-limit modülleri tek başına: statements %98.5, branches %90,
  functions %100). `pnpm --filter @xox/web build` temiz (middleware hâlâ
  Proxy olarak derleniyor, mongoose/@node-rs/argon2 middleware çıktısına
  SIZMIYOR — sarmalayıcı auth.ts'i yalnız GET/POST re-export/import ediyor,
  mongoose'u kendisi import etmiyor).

commits:
  - sha: 1e20a5b
    mesaj: 'feat(web): kimlik uçlarına IP hız sınırı + hesap kilidi ekle (SEC-002)'

acceptance_check:
  - 'POST /api/auth/* (+ register) için IP başına hız sınırı → İKİ katman (WAF 60/60s + uygulama 20/60s), ikisi de canlı kanıtlı: DONE'
  - "Kimlik başına deneme sayacı/kilit → 5 başarısız/15dk kilit, User'a bakmadan, canlı kanıtlı: DONE"
  - '429 kanıtı (kaçıncı istekte, hangi başlıklar) → rapor içinde ham çıktı: DONE'
  - "Eşik altı meşru istek 200/401(/201) almaya devam ediyor mu → İstek 1-20 register 201, 1-5 login 302 (normal red akışı), 1-30 WS upgrade WAF'tan geçti: DONE"
  - 'pnpm gates temiz: DONE'
  - "Kapsam genişletmesi (WS upgrade WAF kuralı): DONE — route main'de olmadığından uygulama tarafı e2e doğrulaması WS-001 merge sonrası önerilir"

blocked_reason: null

next_suggestions:
  - >
    WS-001 `main`'e merge olduktan sonra `GET /api/rooms/<gercek-kod>/ws`
    ile GERÇEK bir upgrade denemesi + WAF eşiğinin (30/60sn) üstünde
    istekle YENİDEN doğrulama önerilir — bu görevde route deploy'da
    olmadığı için WAF eşleşmesi yalnız path/method desenine göre
    kanıtlandı, gerçek 101 Switching Protocols akışıyla birlikte değil.
  - >
    `/api/ws/echo`'ya dokunulmadı (koordinatör notu) — WS-001'in düzeltme
    turunda kaldırılacak.
  - >
    WAF kuralları git'te izlenmiyor (Vercel proje kaynağı, `vercel.json`'da
    DEĞİL) — `docs/memory/decisions.md`'ye "proje X'in WAF kuralları CLI
    ile yönetiliyor, `vercel firewall rules list` tek doğruluk kaynağı"
    notu eklenmesi, gelecekte birinin vercel.json'da arayıp bulamaması
    riskini azaltır.
  - >
    `MAX_FAILED_LOGIN_ATTEMPTS`/`LOGIN_LOCK_*`/`IP_RATE_LIMIT_*` sabitleri
    şu an `apps/web/lib/rate-limit/**`te yaşıyor (packages/shared donuk
    olduğu için). Paket açıldığında bu sabitlerin `@xox/shared/constants`e
    taşınması, web/mobil arasında (mobil köprü uçları henüz bu savunmaya
    sahip değil) tek doğruluk kaynağı sağlar.
  - >
    Mobil köprü uçları (`/api/auth/mobile/*`, henüz yazılmadı) bu görevin
    KAPSAMINDA DEĞİL — o uçlar yazılırken aynı `checkIpRateLimit`/
    `credential-lockout` modülleri (zaten next-auth'suz, saf TS) yeniden
    kullanılabilir.
```
