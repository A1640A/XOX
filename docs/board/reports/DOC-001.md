# DOC-001 — `OPS-004` sonrası eskimiş `middleware.ts` yorum referansları (lead yürüttü)

**Tarih:** 2026-08-29 · **Yürüten:** lead · **Dalga:** gece koşusu #2, dalga 2

## Neden lead yaptı

Bu kart, daha önce **kör bir `perl` değiştirmesiyle** neredeyse bozacağım karttı: `proxy.ts`
içindeki `middleware.ts` geçişleri **tarihseldir ve doğrudur** (geçişin kendisini anlatırlar),
otomatik değiştirme onları da bozacaktı. Tuzağı fark edip iptal etmiş ve karta yazmıştım.

## Yöntem — kör değiştirme YOK, üç aşamalı ayıklama

**1. Gerçek dosya adları doğrulandı:**

```
apps/web/middleware.ts        → YOK
apps/web/middleware.test.ts   → YOK
apps/web/proxy.ts             → VAR
apps/web/proxy.test.ts        → VAR
```

**2. Kapsam dışı dosyalardaki atıflar okundu ve DOKUNULMADI** — `proxy.ts` ve `proxy.test.ts`
içindekiler açıkça geçmişi anlatıyor:

```
proxy.test.ts:7  `proxy.ts` (OPS-004 öncesi adıyla `middleware.ts`) …
proxy.ts:14      OPS-004: bu dosya Next.js 16'nın `middleware.ts` → `proxy.ts` …
proxy.ts:28      … `proxy.test.ts` (eski adıyla `middleware.test.ts`) …
```

**3. Beş dosyadaki her atıf tek tek sınıflandırıldı.** Değiştirme yalnız **tam dosya adı
belirteçlerine** uygulandı (`\bmiddleware\.ts\b`, `\bmiddleware\.test\.ts\b`) — bu, kavramsal
ifadelere ve sabit adlarına bilerek dokunmaz.

## BİLEREK DEĞİŞTİRİLMEYEN iki yer

| Yer                      | Metin                                                      | Neden bırakıldı                                                                                                                                                         |
| ------------------------ | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth.config.test.ts:69` | `MIDDLEWARE_MATCHER kart metnindeki middleware matcher'ı…` | Bir dosya adı **değil** — `MIDDLEWARE_MATCHER` **sabitinin** adına atıf. Sabit hâlâ o adı taşıyor (`auth.config.ts:40`). "proxy" yazmak testi sabitle tutarsız yapardı. |
| `HomeActions.tsx:29`     | `… zaten middleware'de …`                                  | **DEĞİŞTİRİLDİ** → `` `proxy.ts`'te `` — burası kavramsal değil, yönlendirmenin yapıldığı dosyayı kastediyor.                                                           |

## Kod davranışı değişmedi — ölçüldü

```
5 files changed, 18 insertions(+), 18 deletions(-)     ← birebir simetrik
```

Diff'in **her satırı** yorum. Tek istisna gibi görünen satır bir **JSX yorumu**
(`{/* … */}`) — grep deseni onu tanımadığı için "kod" diye işaretlemişti, elle doğrulandı:

```tsx
- {/* `prefetch={false}` — AUTH-004. `/oyna/:path*` `middleware.ts`in matcher'ında,
+ {/* `prefetch={false}` — AUTH-004. `/oyna/:path*` `proxy.ts`in matcher'ında,
```

## Definition of Done

| Kapı                     | Sonuç                                     |
| ------------------------ | ----------------------------------------- |
| `pnpm gates` (altı kapı) | **exit 0**                                |
| Yalnız yorum değişti     | ✅ 18/18 simetrik, hepsi elle doğrulandı  |
| Tarihsel atıflar korundu | ✅ `proxy.ts`/`proxy.test.ts` dokunulmadı |

## Öneri (bu kartın kapsamı DIŞINDA)

`MIDDLEWARE_MATCHER` sabiti hâlâ eski adı taşıyor (`auth.config.ts:40`,
`TopBar.tsx:20`, `proxy.ts:21`). Yeniden adlandırmak bir **kod** değişikliğidir ve
`auth.config.test.ts`'in parity zincirine dokunur — bu kartın "yalnız yorum" sınırının
dışında. İstenirse ayrı kart.
