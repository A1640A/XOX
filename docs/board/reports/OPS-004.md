```yaml
task: OPS-004
status: done
severity: bakim
summary: >
  Next.js 16'da `middleware.ts` dosya konvansiyonu DEPRECATED — bu, ölçümle
  doğrulandı (yerel `pnpm --filter @xox/web build` çıktısında canlı uyarı
  gözlendi, aşağıda). Bu yüzden karar "geçir": `apps/web/middleware.ts` →
  `apps/web/proxy.ts`, `apps/web/middleware.test.ts` → `apps/web/proxy.test.ts`
  olarak taşındı. Davranış DEĞİŞMEDİ — yalnız dosya adı ve içindeki dokümantasyon
  yorumları güncellendi. İki güvenlik mekanizması (AUTH-004 prefetch nöbetçisi,
  matcher literal kilidi) ihlal enjekte edilerek kırmızıya düşürüldü ve doğrulandı.

olcum_gerekli_mi:
  next_surumu: '16.3.2 (apps/web/package.json → "next": "16.3.2"; `next --version` → "Next.js v16.3.2")'
  kanit: >
    Geçiş ÖNCESİ `pnpm --filter @xox/web build` çıktısında şu uyarı canlı
    olarak gözlendi:
      "⚠ The "middleware" file convention is deprecated. Please use "proxy" instead.
       To migrate automatically, run:
       npx @next/codemod@canary middleware-to-proxy .
       Learn more: https://nextjs.org/docs/messages/middleware-to-proxy"
    Ayrıca `next` paketinin kendi derlenmiş kaynağında (`node_modules/next/
    dist/build/index.js`) hem `MIDDLEWARE_FILENAME`/`PROXY_FILENAME` sabitleri
    hem de ikisi birden varsa build'i SERT reddeden bir hata yolu bulundu
    ("Both middleware file ... and proxy file ... are detected. Please use
    ... only.") — yani bu resmi, kalıcı bir dosya-adı konvansiyonu, geçici bir
    öneri değil.
  karar: >
    ZORUNLU DEĞİL (henüz build'i kırmıyor) ama RESMİ DEPRECATION UYARISI VAR
    ve codemod resmen sunuluyor — kartın "deprecation uyarısı varsa geçir"
    koşulu karşılandı. Geçiş yapıldı.
  gecis_sonrasi_dogrulama: >
    `pnpm --filter @xox/web build` çıktısında uyarı TAMAMEN kayboldu (aşağıda
    tam çıktı). Route tablosunda `ƒ Proxy (Middleware)` etiketi göründü —
    Next'in kendisi de artık bunu "Proxy" olarak raporluyor.

degisiklikler:
  - dosya: 'apps/web/middleware.ts → apps/web/proxy.ts (git mv, davranış AYNI)'
    ne: >
      `export default auth` ve `export const config = { matcher: [...] }`
      HİÇBİR SATIR mantık değişmedi. Yalnızca dosya başındaki Türkçe yorum
      güncellendi: OPS-004 geçiş notu eklendi (deprecation uyarısı + ölçüm
      referansı), `middleware.test.ts` referansları `proxy.test.ts` olarak
      düzeltildi.
  - dosya: 'apps/web/middleware.test.ts → apps/web/proxy.test.ts (git mv)'
    ne: >
      `readFileSync(join(process.cwd(), 'middleware.ts'), ...)` →
      `readFileSync(join(process.cwd(), 'proxy.ts'), ...)`. Hata mesajları
      ("middleware.ts içinde..." ) `proxy.ts` olarak güncellendi. `describe`
      başlığı `'middleware.ts — split config...'` → `'proxy.ts — split
      config... (OPS-004)'`. `toStrictEqual` kilidi ve `MIDDLEWARE_MATCHER`
      importu (`./auth.config`'ten) DOKUNULMADAN korundu — bu, iki testin
      kilit mekanizması, davranış değişmedi.
  - dosya: 'knip.json'
    ne: >
      `workspaces["apps/web"].entry` içindeki `"middleware.ts"` girdisi
      `"proxy.ts"` olarak güncellendi (kök konfig dosyası — varsayılan yazma
      alanım). Bu güncellenmeden bırakılsaydı knip `proxy.ts`'i "kullanılmayan
      entry" olarak işaretlemezdi ama gerçek entry noktası eksik kalırdı;
      şimdi doğru.

dokunulmayan_ama_stale_kalan_yorumlar:
  - >
    `apps/web/auth.config.ts`, `apps/web/auth.config.test.ts`,
    `apps/web/components/TopBar.tsx`, `apps/web/components/home/
    HomeActions.tsx`, `apps/web/lib/theme.ts` içindeki DOKÜMANTASYON
    yorumlarında hâlâ `middleware.ts` ismi geçiyor (davranışa etkisi YOK —
    hepsi salt-metin açıklama, hiçbiri dosya yolunu programatik okumuyor).
    Kartın açık talimatı gereği (`auth.config.ts`'e dokunman gerekirse bana
    söyle) BU DOSYALARA DOKUNULMADI — çakışma kümem dışında. Lead isterse
    ayrı, küçük bir dokümantasyon-temizliği kartı açabilir; işlevsel risk
    sıfır, yalnız isim tutarlılığı.

iki_mekanizmanin_calistiginin_kaniti:
  mekanizma_1_matcher_literal_kilidi:
    yontem: >
      `proxy.ts`teki `matcher` dizisinden `'/arkadaslar'` GEÇİCİ olarak
      silindi (6 → 5 öğe), `pnpm --filter @xox/web exec vitest run
      proxy.test.ts` koşuldu, sonra dosya YEDEKTEN (`cp` öncesi alınan
      yedek) TAM olarak geri yüklendi.
    sonuc_kirmizi: >
      "matcher literal DİZİSİ auth.config.ts'teki MIDDLEWARE_MATCHER ile TAM
      eşit" testi KIRMIZI: `toStrictEqual` diff'i eksik `"/arkadaslar"`
      öğesini gösterdi (1 failed | 2 passed).
    geri_yukleme_dogrulamasi: >
      `git status --porcelain` → yalnız beklenen rename'ler (proxy.ts,
      proxy.test.ts) ve `knip.json` değişikliği görünüyor, `proxy.ts`te
      kalıntı diff YOK.
  mekanizma_2_auth004_prefetch_nobetcisi:
    yontem: >
      `apps/web/components/home/HomeActions.tsx`teki `/oyna/bilgisayar`
      `<Link>`inden `prefetch={false}` GEÇİCİ olarak kaldırıldı, `pnpm
      --filter @xox/web exec vitest run components/prefetch-guard.test.ts`
      koşuldu, sonra dosya YEDEKTEN TAM olarak geri yüklendi.
    sonuc_kirmizi: >
      "korumalı rotaya giden her <Link> prefetch={false} taşır" testi
      KIRMIZI: `ihlaller` dizisi `["components/home/HomeActions.tsx →
      /oyna/bilgisayar"]` döndü (beklenen `[]`), 1 failed | 2 passed.
    onemli_gozlem: >
      Bu test `MIDDLEWARE_MATCHER`'ı `../auth.config`'ten import ediyor,
      dosya adını (`middleware.ts`/`proxy.ts`) HİÇ okumuyor — yani AUTH-004
      nöbetçisi bu geçişten YAPISAL OLARAK etkilenmiyordu; yine de kartın
      talimatı gereği kırmızıya düşürülerek MEKANİK kanıt üretildi.
    geri_yukleme_dogrulamasi: >
      `git diff apps/web/components/home/HomeActions.tsx` → boş.

build_kaniti:
  gecis_oncesi_ozet: >
    "⚠ The "middleware" file convention is deprecated. Please use "proxy"
    instead." uyarısı + `npx @next/codemod@canary middleware-to-proxy .`
    öneri satırı, ardından build normal başarıyla tamamlandı (route tablosu
    aynı, `ƒ Proxy (Middleware)` satırı zaten o zaman da vardı).
  gecis_sonrasi_ozet: >
    Uyarı YOK. "✓ Compiled successfully", "Finished TypeScript",
    "Generating static pages using 9 workers (9/9)" — route tablosu ve
    `ƒ Proxy (Middleware)` satırı AYNI, sıfır fark. Komut: `pnpm --filter
    @xox/web build` → exit 0.

gates:
  komut: 'pnpm gates (worktree .claude/worktrees/OPS-004)'
  sonuc: 'EXIT 0 — beş kapının hepsi yeşil.'
  typecheck: 'turbo run typecheck — başarılı (proxy.ts/proxy.test.ts dahil).'
  lint: 'eslint . — 0 hata.'
  format: 'prettier --check . — temiz.'
  test_coverage: >
    90 test dosyası, 901/901 test yeşil (proxy.test.ts 3/3, prefetch-guard.test.ts
    3/3, auth.config.test.ts 17/17 dahil). İstatistik %94.34 · Dal %89.46 ·
    Fonksiyon %94.1 · Satır %96.42.
  knip: >
    Temiz — `proxy.ts` için "Remove redundant entry pattern" ipucu göründü
    (aynısı önceden `middleware.ts` için de vardı, bu görevle ilgisiz ön
    var olan bir konfig önerisi, hata DEĞİL).

commit_shas:
  - 'henüz commit atılmadı — bu YAML'ın altındaki gerçek SHA raporun SON
    sürümünde güncellenecek (bkz. aşağıdaki not).'

worktree: ".claude/worktrees/OPS-004 (branch feat/OPS-004, main'den ayrıldı, main'e merge/push YAPILMADI)"

izin_reddi: 'YOK — bu görevde hiçbir izin isteği reddedilmedi.'

production_dokunuldu_mu: 'HAYIR — deploy edilmedi, env değiştirilmedi, Vercel ayarı açılmadı.'

blocked_reason: null

kalan_risk_next_suggestions:
  - >
    `auth.config.ts`, `auth.config.test.ts`, `TopBar.tsx`, `HomeActions.tsx`,
    `theme.ts` içindeki yorumlarda hâlâ "middleware.ts" ismi geçiyor
    (davranışa etkisiz, salt dokümantasyon borcu). Çakışma kümem dışında
    olduğu için dokunulmadı.
  - >
    Next'in kendi build hatası kanıtladığı gibi (`Both middleware file ...
    and proxy file ... are detected`), eğer başka bir paralel görev
    yanlışlıkla `apps/web/middleware.ts`'i yeniden oluşturursa build SERT
    reddeder — bu doğal bir koruma, ek önlem gerekmiyor.
```
