```yaml
task: CTR-003
status: done
summary: >
  `GET /api/rooms/[code]` yerel `state === 'waiting' && bosKoltukVar` kopyasını
  sildi ve `@xox/shared`'ın tek türetme noktası olan `canJoinRoom(state,
  seats)`'ini çağırmaya başladı (fonksiyonun kendisi CTR-BOARD-001 tarafından
  `rest-contract.ts:77`'ye zaten yazılmıştı — bu kartın işi yalnızca route'u
  ona BAĞLAMAKtı). Ayrıca `roomStateResponseSchema`'nın CTR-BOARD-001'de
  zorunlu hâle gelen `size`/`winLength` alanları, route'ta `?? 3` gibi üçüncü
  bir kopya YAZILMADAN, `getRoomSummary`'nin (DB-BOARD-001'in `resolveBoardConfig`
  ile ÇÖZÜLMÜŞ olarak döndürdüğü) `room.size`/`room.winLength`'inden doğrudan
  aktarıldı. Zincirin başında kırmızı olan 12 test (3 dosya) artık yeşil;
  `pnpm gates` yedi paketin hepsinde temiz.

kok_neden_analizi:
  neden_1_yerel_kopya: >
    `route.ts:72`'deki `canJoin: room.state === 'waiting' && bosKoltukVar`
    CTR-001'in bilinen kusuruydu (bkz. `rest-contract.ts`taki CTR-003 yorumu):
    mantık `packages/shared` dondurulduğu için route'ta yerel yazılmıştı.
    CTR-BOARD-001 penceresi açıkken `canJoinRoom` shared'a taşındı ve
    `roomStateResponseSchema.superRefine`'ı ona bağlandı — ama route'un
    KENDİSİ hâlâ eski `&&` kopyasını tutuyordu, iki türetme aynı mantığı
    ayrı ayrı kodluyordu (biri güncellenip diğeri unutulabilirdi).
  neden_2_zorunlu_alanlar: >
    `roomStateResponseSchema`'ya CTR-BOARD-001'de eklenen `size: boardSizeSchema`
    ve `winLength: winLengthSchema` ZORUNLU (opsiyonel değil). Route bu
    alanları hiç vermiyordu → `roomStateResponseSchema.parse(...)`
    `invalid_union`/`invalid_type` ile fırlıyordu → route'un `catch` bloğu
    bunu yakalayıp 500 SERVER_ERROR'a çeviriyordu. 12 kırmızının 12'sinin de
    GERÇEK nedeni buydu — schema/route senkronsuzluğu, iş mantığı hatası
    değil.

degisiklikler:
  - dosya: 'apps/web/app/api/rooms/[code]/route.ts'
    ne: >
      `canJoinRoom` `@xox/shared`'dan import edildi. Yanıt gövdesi kurulurken
      `canJoin: room.state === 'waiting' && bosKoltukVar` satırı SİLİNDİ,
      yerine `canJoin: canJoinRoom(room.state, room.seats)` YAZILDI. `size:
      room.size, winLength: room.winLength` eklendi — `room` zaten
      `getRoomSummary`'den (ki o `resolveBoardConfig(doc)`'u ÇAĞIRIYOR,
      DB-BOARD-001) ÇÖZÜLMÜŞ değerlerle geliyor; route'ta ikinci bir
      varsayılan/dönüştürme YOK. JSDoc, artık `canJoin`'in NEREDE
      hesaplandığını (route'ta DEĞİL) ve tek kaynağın ne olduğunu (CTR-003,
      `canJoinRoom`) belirtecek şekilde güncellendi.
    silinen_satir: >
      `const bosKoltukVar = room.seats.X === null || room.seats.O === null`
      ve `canJoin: room.state === 'waiting' && bosKoltukVar` — ikinci kopya
      tamamen kaldırıldı, route artık `bosKoltukVar` değişkenini tutmuyor.
  - dosya: 'apps/web/app/api/rooms/[code]/route.test.ts'
    ne: >
      7 test `getRoomSummary` mock'una `size`/`winLength` eklenmesini
      gerektiriyordu (schema artık zorunlu kılıyor, aksi hâlde parse 500'e
      düşüyordu) — GERÇEK üretim davranışıyla senkronize edildi, gevşetme
      YAPILMADI. `toStrictEqual` kullanan 2 test (Auth.js çerezi testi, AC5
      normalleştirme testi) beklenen gövdeye `size`/`winLength` eklendi.
      AC5 testinde bilinçli olarak DEFAULT OLMAYAN bir konfig (size:6,
      winLength:4) kullanıldı — route'un bu alanları sabit/varsayılana
      düşürmediğini, gerçekten `getRoomSummary`den geçirdiğini kanıtlamak
      için (nötr eleman körlüğüne karşı savunma).
  - dosya: 'apps/web/components/JoinCodeField.test.tsx'
    ne: >
      İstemci `roomStateResponseSchema.safeParse` ile sunucu yanıtını
      doğruluyor (`JoinCodeField.tsx:65`) — 4 tane `ok:true` fetch mock'u
      `size`/`winLength` içermediği için `safeParse` başarısız oluyor,
      bileşen `SERVER_ERROR`'a düşüyordu (görünen semptom: ROOM_FULL/GAME_OVER
      testleri de yanlış hata koduyla başarısız oluyordu). 4 fixture'a
      `size: 3, winLength: 3` eklendi.
  - dosya: 'apps/web/app/oda/katil/page.test.tsx'
    ne: >
      Aynı kök neden — 1 başarılı fetch mock'una `size`/`winLength` eklendi.
  - dosya: 'packages/shared/src/rest-contract.ts'
    ne: >
      DOKUNULMADI. `canJoinRoom` ve `roomStateResponseSchema`'nın
      `superRefine`'ı CTR-BOARD-001 tarafından zaten yazılmıştı; bu kart
      yalnızca route'u ona bağladı, ikinci bir unfreeze gerekmedi.

kanit_and_operand_testleri_ayakta:
  test_1: >
    "MAJOR-1 fix: canJoin false — state waiting DEĞİL, koltuk boş (TEK operand
    false)" (route.test.ts:167) — state:'playing', seats'ten biri null (boş
    koltuk VAR). Yalnız `state==='waiting'` operandı false; `canJoin:false`
    bekleniyor. Silinmedi, DEĞİŞTİRİLMEDİ (yalnız mock'a size/winLength eklendi).
  test_2: >
    "MAJOR-1 fix: canJoin false — state waiting AMA koltuk boş DEĞİL (TEK
    operand false)" (route.test.ts:186) — state:'waiting', iki koltuk da
    dolu. Yalnız "boş koltuk var" operandı false; `canJoin:false` bekleniyor.
    Silinmedi, DEĞİŞTİRİLMEDİ.
  ikisi_de_false_testi: >
    "canJoin: bitmiş oda ve her iki koltuk doluysa da false döner (iki
    operand birden false)" (route.test.ts:205) — state:'finished' + iki
    koltuk dolu. Ayakta, DEĞİŞTİRİLMEDİ.

test_kaniti:
  kirmizi_once: >
    Görev başında ölçülen kırmızı: 3 test dosyası, 12 test kırmızı
    (route.test.ts 7, JoinCodeField.test.tsx 4, page.test.tsx 1) — hepsi
    `AssertionError: expected 500 to be 200` ya da `ZodError: invalid_union`.
  yesil_sonra: >
    `pnpm --filter @xox/web test -- --run` (yalnız üç dosya, izole): 69/69
    dosya, 707/707 test yeşil. Tam `pnpm gates` koşusunda da @xox/web
    69 test dosyası, 707/707 test yeşil; @xox/db 28 dosya, 230/230 yeşil.
  gates_ciktisi: |
    $ pnpm typecheck && pnpm lint && pnpm format:check && pnpm test:coverage && pnpm knip
     Tasks:    7 successful, 7 total   (typecheck)
    $ eslint . --max-warnings=0        (temiz, uyarı yok)
    $ prettier --check .               (All matched files use Prettier code style!)
    @xox/web:test:coverage:  Test Files  69 passed (69)
    @xox/web:test:coverage:       Tests  707 passed (707)
    @xox/db:test:coverage:   Test Files  28 passed (28)
    @xox/db:test:coverage:        Tests  230 passed (230)
     Tasks:    5 successful, 5 total   (test:coverage — db+web+shared+game-core+ui-tokens)
    $ knip                              (yalnız var olan konfigürasyon ipuçları, hata yok)
    exit code: 0
  coverage: >
    @xox/web %93.76 ifade / %88.58 dal (mevcut eşiklerin üzerinde,
    değişiklik öncesiyle aynı seviyede — bu kart yeni dal eklemedi, mevcut
    bir `&&`'i bir fonksiyon çağrısıyla değiştirdi). @xox/db %95.77 ifade.
  gecici_flake_notu: >
    İlk `pnpm gates` denemesinde `components/computer/game-engine.test.ts`
    ("9 açılış varyantının tamamında 207 oyunda insan hiç kazanamaz") 90
    saniyelik test timeout'una takıldı — bu dosyaya bu kartta HİÇ
    dokunulmadı ve CORE-AI-001 ile paylaşılan makinede CPU çekişmesinden
    kaynaklandığı, dosyayı TEK BAŞINA çalıştırarak (707/707 yeşil, aynı test
    83s'de bitti) ve `pnpm gates`'i TEKRAR koşarak (aynı test 83.3s'de
    normal bitti, tüm süit yeşil) doğrulandı. Rapor edilen final `pnpm
    gates` çıktısı bu ikinci, temiz koşudur.

commit_shas:
  - "114eb85 fix(web): GET /api/rooms/[code] canJoin'i tek kaynaktan (canJoinRoom) türetir"
  - '91382c4 test(web): size/winLength artık zorunlu — sabit/mock yanıtlar güncellendi'

worktree: ".claude/worktrees/CTR-003 (branch feat/CTR-003, feat/DB-BOARD-001'den ayrıldı, main'e merge/push YAPILMADI)"

blocked_reason: null

next_suggestions:
  - >
    Zincir (CTR-BOARD-001 → DB-BOARD-001 → CTR-003) artık uçtan uca yeşil;
    lead sırayla main'e merge edip `turbo run typecheck --force` +
    `turbo run test:coverage --force` ile Cached:0 doğrulamasını
    yapabilir.
  - >
    `route.test.ts`teki "connectDb() DB sorgusundan ÖNCE çağrılır" testinin
    `getRoomSummary` mock'u hâlâ `size`/`winLength` İÇERMİYOR — bu, test
    yalnız `mockConnectDb` çağrısını doğruladığı (`schema.parse` sonrası
    500'e düşse bile testin kendisi bunu YAKALAMAZ) için bugün YEŞİL, ama
    kırılgan: gelecekte biri bu teste bir gövde assertion'ı eklerse
    sessizce kırmızıya düşebilir. Kapsam dışı bırakıldı (raporlanan 7
    kırmızıya dahil değildi) ama küçük bir tutarlılık borcu olarak not
    düşülüyor.
```
