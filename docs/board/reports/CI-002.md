```yaml
task: CI-002
status: done
summary: >
  `gates` işi 5 saattir MONGODB_URI eksikliğinden kırmızıydı. Kök neden İKİ
  KATMANLIYDI, ikisi de gerçek CI koşularıyla kanıtlandı:

  (1) CI iş akışında `MONGODB_URI` hiç tanımlı değildi.
  (2) Bunu iş akışına eklemek TEK BAŞINA yetmiyor — turbo 2.10'un varsayılan
  `strict` env modu, `turbo.json`'da `env`/`globalEnv` listesinde olmayan
  değişkenleri alt görev sürecine hiç geçirmiyor. Job-level `env:` eklendiğinde
  bile `pnpm test:coverage` (turbo üzerinden çalışıyor) aynı "tanımlı değil"
  hatasını vermeye devam etti; bunu `turbo.json`'a `env: ["MONGODB_URI",
  "MONGODB_DB"]` eklenerek doğruladım (önce kasıtlı geçersiz bir adrese
  ayarlayıp ECONNREFUSED almakla önceki "tanımlı değil" hatasından ayırt ettim).

  (3) Bu ikisini düzelttikten sonra ÜÇÜNCÜ bir regresyon ortaya çıktı:
  `@xox/db`'nin branch coverage eşiği (%85) job-level `env:` yaklaşımıyla
  %84.89'a düştü. Neden: `packages/db/src/load-env.ts` kasıtlı olarak
  `process.env.MONGODB_URI` ZATEN tanımlıysa `.env.local` dosyasını hiç
  okumadan döner (kendi yorumu: "CI ortam değişkeni... dokunulmaz"). Job-level
  `env:` ile değeri Node başlamadan ÖNCE ortama koyunca, dosya okuma/satır
  ayrıştırma dallarının TAMAMI (14 dal) CI'da hiç çalışmıyor. Çözüm: değeri
  job-level `env:` yerine, geliştiricinin yerelde kullandığı AYNI `.env.local`
  dosyası mekanizmasıyla verdim (mongod ayağa kalktıktan sonra sırsız bir
  `.env.local` yazılıyor). Sonuç: branch coverage CI'da %90.1 — yereldeki
  %90.62 ile pratik olarak eşleşiyor (fark, Atlas'taki kalıcı verinin tetiklediği
  tek bir dal — eşiğin çok üzerinde, sorun değil).

  MongoDB kurulumu: `docker run` ile tek düğümlü `mongod --replSet rs0
  --bind_ip_all`, `rs.initiate()` AÇIKÇA `127.0.0.1:27017` host'una bağlanarak
  (aksi halde üye adresi container hostname'ine düşer, runner'dan erişilemez).
  Replika seti seçildi (standalone değil) çünkü orijinal görev tanımı bunu P0
  gereksinim olarak verdi ve kurulum zaten çalışıyordu; paralel bir incelemenin
  "bugün hiçbir test gerçek `.watch()`/transaction kullanmıyor, hepsi mock'lu"
  bulgusu doğru ama replica set'i standalone'a düşürmenin kazancı (birkaç
  saniye kurulum) riskine değmiyor — biri ileride gerçek bir change-stream testi
  yazdığı gün standalone sessizce farklı bir hatayla düşer ve "CI neden kırıldı"
  avı yeniden başlar. Kriter 3 buna göre netleştirildi: BUGÜN gerçek bir
  change-stream/transaction testi YOK (W1-03'ün presence akışı `createFanoutHub()`
  ile deterministik taklit ediliyor); replica set ileriye dönük güvence olarak
  tutuldu, uydurma bir "değiştirir" iddiası YOK.
files_changed:
  - .github/workflows/ci.yml (gates işine mongod replica-set bootstrap + .env.local yazma adımı)
  - turbo.json (test/test:coverage görevlerine env: [MONGODB_URI, MONGODB_DB] — savunma amaçlı, gelecekteki paketler için)
  - docs/board/reports/CI-002.md
gercek_ci_kosulari:
  - run: "https://github.com/A1640A/XOX/actions/runs/32799470175 — İLK deneme: mongo replica set bootstrap başarılı ama job-level env turbo'nun strict modunda siliniyor, @xox/db hâlâ 'MONGODB_URI tanımlı değil'"
  - run: "https://github.com/A1640A/XOX/actions/runs/32800079959 — turbo.json env fix sonrası: MONGODB_URI artık geçiyor (133/133 test yeşil) ama branch coverage %84.89 < %85 eşiği kırdı"
  - run: "https://github.com/A1640A/XOX/actions/runs/32801123845 — .env.local mekanizmasına geçiş sonrası: gates=success, mutation=success, 133/133 test, branch coverage %90.1, Cached: 0 cached/5 total"
  - run: "https://github.com/A1640A/XOX/actions/runs/32801490936 — KRİTER 4 SONDASI: yeni bir __ci-canary__.test.ts (packages/db, mevcut dosyalara dokunmadan) ile kasıtlı yanlış iddia (`expect(1+1).toBe(3)`) eklendi → gates=failure, log: 'AssertionError: expected 2 to be 3'. Kapı gerçekten ateşleniyor."
  - run: "https://github.com/A1640A/XOX/actions/runs/32801688479 — sonda geri alındıktan sonra: gates=success, mutation=success. Bu koşu PR merge-ref üzerinden main'in güncel hâliyle birleşerek çalıştı (main'de bu sırada başka dalgalar merge olmuştu) — @xox/db 23 dosya/169 test yeşil, branch coverage %89.01; 'Kalite kapıları' ve 'game-core mutasyon testi' başarılı, yalnız 'Derleme' (build) işi ÖNCEDEN VAR OLAN, bu görevle ilgisiz bir size-limit aşımından kırmızı (aşağıya bakın)."
  pr: "https://github.com/A1640A/XOX/pull/1 (feat/CI-002 → main, YALNIZ CI'ı gerçek koşuya zorlamak için açıldı — main'e MERGE EDİLMEDİ, lead entegrasyon sırasını kendisi yönetiyor)"
tests:
  xox_db_ci: "133/133 (main'in bu branch'e denk geldiği an) → sonraki koşuda 169/169 (main ilerledikçe); CI 'Cached: 0 cached' ile gerçekten koşuyor, replay değil"
  xox_db_coverage_ci: "statements %95.88/95.11, branches %90.1/89.01, functions %100/98.52, lines %98.31/97.54 — eşik (90/85/90/90) her ikisinde de geçti"
  yerel_baseline: "pnpm --filter @xox/db test:coverage (gerçek Atlas xox_test) → 19 dosya, 133 test, branch %90.62 — CI'daki %90.1 ile pratik eşleşme"
  pnpm_gates_yerel: "temiz (typecheck, lint, format:check, test:coverage, knip)"
kriter_4_sonda:
  yontem: >
    Mevcut hiçbir dosyaya dokunmadan (paralel W1-01/02/03 ile çakışma riski
    sıfır) `packages/db/src/__ci-canary__.test.ts` adında YENİ, geçici bir
    dosya eklendi: `expect(1 + 1).toBe(3)`. Push edildi, CI'ın kırmızı
    döndüğü doğrulandı, ardından dosya silinip geri commit edildi.
  kanit_kirmizi: "run 32801490936, log: 'AssertionError: expected 2 to be 3 // Object.is equality' → src/__ci-canary__.test.ts:10:19 → gates=failure"
  kanit_yesile_donus: "run 32801688479, sonda kaldırıldıktan sonra gates=success"
mongo_kurulum_secimi:
  secim: "Tek düğümlü MongoDB REPLICA SET (docker run mongo:8, --replSet rs0, rs.initiate host=127.0.0.1:27017)"
  neden: >
    Görev tanımı bunu P0 gereksinim olarak verdi ve kurulum ilk denemede
    çalıştı (PRIMARY seçimi ~1-2 sn). Paralel bir incelemenin bulgusu doğru:
    bugünkü kod tabanında hiçbir test gerçek `.watch()`/`startTransaction`/
    `withTransaction` kullanmıyor — `room-hub.ts`'in change stream'i TÜM
    testlerde mock'lu, W1-03'ün presence akışı `createFanoutHub()` ile
    deterministik taklit ediliyor. Yani standalone mongod BUGÜN teknik olarak
    yeterli olurdu. Replica set'i yine de tuttum çünkü: (a) maliyeti ihmal
    edilebilir (kurulum saniyeler içinde), (b) `packages/db`'nin CAS
    (`findOneAndUpdate` koşullu) yolları zaten replica set semantiğiyle test
    ediliyor ve bu davranış korunuyor, (c) standalone'a düşürüp ileride biri
    gerçek bir change-stream/transaction testi yazdığı gün CI'ın SESSİZCE
    farklı bir hatayla kırılmasını (yeni bir "CI neden kırık" avı) önlemek
    istedim — bu tam olarak bu görevin teşhis ettiği örüntünün kendisi.
  reddedilen_alternatif: "Standalone mongod (bugün yeterli ama ileriye dönük kırılgan, kazanç ihmal edilebilir)"
mongodb_uri_teslimat_secimi:
  secim: ".env.local dosyası (job-level `env:` DEĞİL)"
  neden: >
    `packages/db/src/load-env.ts` açıkça İKİ modu destekleyecek şekilde
    yazılmış: gerçek CI ortam değişkeni (varsa dokunmaz) VEYA `.env.local`
    dosyası (yoksa okur). Job-level env vermek "CI ortam değişkeni" modunu
    seçmiş olurdu ama bu, dosyanın kendi satır-ayrıştırma dallarını CI'da
    hiç çalıştırmadığı için `packages/db`'nin coverage eşiğini kırdı (ölçüldü:
    %84.89 → %90.1). `.env.local` dosyası yazmak, geliştiricinin yerelde
    ZATEN test edilip yeşile döndürülmüş AYNI kod yolunu tetikliyor — hem
    sıfır regresyon hem `packages/db`'ye HİÇ dokunmadan (çakışma kümesi
    ihlali yok) çözüm. Dosya sır İÇERMEZ (yalnız 127.0.0.1 replica set adresi,
    ephemeral runner'da yaşıyor, commit edilmiyor).
  turbo_json_notu: >
    `turbo.json`'daki `env: [MONGODB_URI, MONGODB_DB]` bildirimi yine de
    korundu — `.env.local` mekanizması turbo'nun env geçişini bypass ettiği
    için `@xox/db` için artık ZORUNLU değil, ama "iş genelinde kur" gerekliliği
    için savunma amaçlı bırakıldı: ileride gerçekten `process.env.MONGODB_URI`'yi
    doğrudan (dosya değil) okuyan bir paket/test eklenirse turbo onu SESSİZCE
    silmesin diye.
e2e_preview_teshisi:
  sonuc: "KAPI SESSİZCE HİÇ ATEŞLENMEMİŞ (mekanizma çalışıyor, ama koşullar hiç oluşmamış) — kod DEĞİŞTİRİLMEDİ, yalnız teşhis edildi."
  kanit: >
    `gh api repos/A1640A/XOX/deployments` → main'e yapılan SON 21 deploy'un
    TAMAMI `environment: "Production"`. Bunun nedeni: main'e her push, Vercel'in
    Git entegrasyonu tarafından otomatik deploy ediliyor ve main = Vercel
    projesinin "Production Branch"'i olduğu için bu deploy'lar GitHub
    Deployments API'sinde HER ZAMAN "Production" olarak işaretleniyor —
    devops'un `vercel deploy` (preview, --prod OLMADAN) CLI çağrısı olsa
    bile main'in HEAD'inden deploy edildiği için Vercel/GitHub bunu Production
    sayıyor. `E2E (preview)` işinin `if` koşulu (state==success && environment
    != Production) TASARIM GEREĞİ doğru — sadece gerçek bir Preview
    deployment_status hiç oluşmadığı için hep skip ediyor.
  kanit_calisiyor: >
    Bu görev sırasında `feat/CI-002`'den açtığım PR (main DIŞINDA bir dal)
    ilk deployment_status'ünde `environment: "Preview"` üretti ve `E2E
    (preview)` GERÇEKTEN koştu ve BAŞARILI oldu (run 32799473453 ve
    32800110538 ve 32801157505 ve 32801526214 — hepsi success, playwright
    reset+migrate+seed+e2e adımlarının tümü tamamlandı). Yani mekanizmanın
    kendisi SAĞLAM; sorun yalnızca main'e doğrudan push eden mevcut dalga
    akışının hiçbir zaman bir "Preview" ortamı GitHub'a bildirmemesi.
  oneri_kapsam_disi: >
    Düzeltme yapmadım (kapsam dışı, ayrı kart istendi). Olası yönler: (a)
    dalga entegrasyonunu bir PR üzerinden yürütüp merge etmek (main'e
    doğrudan push yerine) — bu hem bu E2E akışını hem gelecekteki branch
    koruması/review akışlarını doğal olarak çalıştırır, (b) devops'un manuel
    preview deploy'unu ayrı bir GitHub Deployment kaydı olarak işaretlemek
    (Vercel projesi ayarında "Preview" hedefini zorlamak), (c) bu workflow'u
    `deployment_status` yerine devops'un deploy sonrası döndürdüğü URL'i
    `workflow_dispatch` ile tetiklemeye çevirmek.
mutation_job_durumu: >
  Yeşil — her CI koşusunda `game-core mutasyon testi` (Stryker) `success`
  döndü (runs 32799470175 hariç tümünde tamamlandı; o run'da gates hatası
  yüzünden diğer job'lar erken durumdaydı ama mutation kendisi başarıyla
  bitti). CI-002 kapsamında dokunulmadı, bulgu yok.
kapsam_disi_bulgu_derleme_isi:
  sonuc: >
    `build` ("Derleme") işi `pnpm exec size-limit` adımında kırmızı — CI-002'nin
    MONGODB_URI düzeltmesiyle İLGİSİZ, ÖNCEDEN VAR OLAN bir sorun. Lead'in
    "gates yeşile dönünce arkasındaki görünür oldu" tespiti doğru ama sebep
    W1-01/02/03 DEĞİL: sorun bu dalgadan ÖNCE, `main`'in CI-002'nin dallandığı
    commit'inde (`80cd3d8`, DALGA 0 kapanışından hemen sonra, W1-* hiçbiri
    merge olmadan) ZATEN VARDI. Bunu iki commit'i ayrı ayrı checkout edip
    (`80cd3d8` vs. W1-01/02/03 merge'lerinden sonraki `6db5500`) gerçek
    `pnpm build && pnpm exec size-limit` ile ÖLÇEREK doğruladım — bütçe
    yükseltmedim, sadece teşhis ettim.
  olcum_w1_oncesi: "commit 80cd3d8 → limit 180 kB, ölçülen 260.35 kB, aşım 80.35 kB (CI'daki İLK kırmızı koşuyla BİREBİR aynı sayı)"
  olcum_w1_sonrasi: "commit 6db5500 (W1-01+W1-02+W1-03 merge sonrası, main HEAD) → limit 180 kB, ölçülen 263.57 kB, aşım 83.58 kB"
  yorum: >
    W1-01/02/03 toplamda yalnız +3.22 kB eklemiş (/oyna/bilgisayar rotası +
    game-core minimax'ı client'a taşıyor) — asıl 80.35 kB'lik aşımın kaynağı
    DEĞİL, üzerine eklenen küçük bir artış. Asıl neden muhtemelen
    `.size-limit.json`'daki glob'un kendisi: `path: "apps/web/.next/static/chunks/**/*.js"`
    TEK bir girdi olarak TÜM chunk dosyalarının (framework + HER rotanın kendi
    JS'i: /, /giris, /kayit, /profil, /oda/[kod], /oyna/bilgisayar, vb.)
    gzip boyutunu TOPLAR — gerçek bir kullanıcının TEK bir sayfa ziyaretinde
    indirdiği "ilk yükleme JS" değil, uygulamadaki TÜM rotaların JS'inin
    TOPLAMI. Rota sayısı arttıkça bu sayı yapısal olarak büyümeye mahkum;
    muhtemelen bütçe (180 kB) daha az rota varken/glob'un ne anlama geldiği
    netleşmeden kondu. Bu bir hipotez — kesin doğrulama için `@size-limit`'in
    route-bazlı ölçüme (örn. `.next/build-manifest.json` üzerinden yalnız
    gerçekten paylaşılan/first-load chunk'ları hedeflemek) geçmesi gerekir.
  yapilmadi: "Bütçe YÜKSELTİLMEDİ, glob DEĞİŞTİRİLMEDİ. Görev kapsamım gates/mutation/E2E-preview idi; bu yalnız teşhis, PERF-001'e bağlanmalı (lead'in talimatı)."
decisions:
  - karar: "MongoDB, GH Actions `services:` bloğu yerine elle `docker run` ile başlatıldı"
    gerekce: >
      `services:` bloğu container'a `--replSet` gibi ek komut argümanları
      geçirmeye izin vermiyor (yalnız `image`/`env`/`ports`/`options`,
      `options` docker CREATE bayrakları için, CMD override için değil).
      `docker run` tam kontrol veriyor: replSet bayrağı, bind_ip_all,
      readiness polling, rs.initiate'in host'unu açıkça 127.0.0.1:27017'ye
      sabitleme.
    reddedilen_alternatif: "services: bloğu (mongod'a replSet bayrağı geçirilemiyor)"
  - karar: "MONGODB_URI/MONGODB_DB job-level env yerine CI'da yazılan .env.local dosyasıyla verildi"
    gerekce: "Yukarıda mongodb_uri_teslimat_secimi — coverage regresyonunu packages/db'ye dokunmadan önledi."
    reddedilen_alternatif: "Job-level env: (basit ama load-env.ts'in dosya-okuma dallarını CI'da öldürüp branch coverage'ı %84.89'a düşürüyor)"
gotchas:
  - >
    Turbo 2.10'un varsayılan `envMode: strict`'i, `turbo.json`'da `env`/
    `globalEnv`'de listelenmeyen ortam değişkenlerini alt görev sürecine HİÇ
    geçirmez — GitHub Actions job'unun kendisi değişkeni görse bile
    `turbo run test:coverage`'ın çağırdığı `vitest` bunu göremez. CI job
    env'i "doğru" görünmesi kanıt DEĞİL; asıl kontrol noktası `turbo.json`.
  - >
    Bir env değişkenini iskeleye (CI) job-level `env:` ile vermek, o değişkenin
    varlığına göre dallanan kodun (`load-env.ts` gibi "eğer zaten tanımlıysa
    dosyayı okuma" desenleri) coverage'ını SESSİZCE düşürebilir. Yereldeki
    %90.62 → CI'da %84.89 düşüşü test FLAKY'liği ya da DB içeriği farkı DEĞİL,
    salt "değişken NASIL sağlandı" farkıydı — coverage eşiği kırmızıya
    dönmeden önce ASLA fark edilmezdi.
  - >
    `gh api repos/OWNER/REPO/deployments`'ın `environment` alanı Vercel'in
    KENDİ preview/production ayrımını değil, deploy edilen COMMIT'in git
    dalının Vercel projesinin "Production Branch" ayarına eşit olup olmadığını
    yansıtır. main'e doğrudan push eden bir akış, `vercel deploy` (--prod
    OLMADAN) çağırsa bile GitHub'a hep "Production" bildirir.
next_suggestions:
  - "Ayrı kart: build işindeki size-limit aşımı (260.35 kB / 180 kB limit) — hangi dalganın bundle'a ne eklediği araştırılmalı."
  - "Ayrı kart (kapsam dışı bırakıldım): E2E (preview)'in gerçekten koşması için dalga entegrasyonunun bir PR üzerinden mi yürütüleceğine ya da Vercel proje ayarlarının mı değiştirileceğine lead karar vermeli."
  - "PR #1 (feat/CI-002 → main) yalnız CI'ı gerçek koşuya zorlamak için açıldı — lead entegrasyon sırasını kendi yönetimine göre ele almalı (merge ya da kapatma kararı lead'de)."
```
