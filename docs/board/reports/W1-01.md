```yaml
task: W1-01
status: done
summary: >
  `/oyna/bilgisayar` — tamamen istemci tarafı bilgisayara karşı oyun. Kural
  mantığı SIFIR: hamle geçerliliği, kazanan tespiti ve bilgisayar hamlesi
  tamamen `@xox/game-core`'dan (`applyMove`/`isValidMove`/`evaluateStatus`/
  `chooseMove`) delege edilir; `apps/web` yalnız "kimin sırası, hangi
  fonksiyon çağrılır" akışını taşıyan saf bir durum makinesi (`game-engine.ts`)
  yazar. Üç zorluk düğmesi (varsayılan orta), "Yeniden oyna" zorluğu koruyarak
  tahtayı sıfırlar, bilgisayar hamlesi `@xox/shared`'dan okunan
  `COMPUTER_MOVE_DELAY_MS` (400 ms) sonra yazılır. Sayfa hiçbir ağ isteği
  yapmaz — bunu doğrulayan bir modül-grafiği testi eklendi (kendi başına
  kanıtlanabilir, bkz. aşağıdaki "kırmızı test" doğrulaması). Yenilmezlik
  sondası: 9 açılış varyantının tamamında 207 oyun, insan 0 kazanır.
files_changed:
  - packages/shared/src/constants.ts
  - packages/shared/src/constants.test.ts
  - apps/web/components/computer/game-engine.ts
  - apps/web/components/computer/game-engine.test.ts
  - apps/web/components/computer/use-computer-game.ts
  - apps/web/components/computer/status-text.ts
  - apps/web/components/computer/DifficultyPicker.tsx
  - apps/web/components/computer/ComputerGameScreen.tsx
  - apps/web/components/computer/ComputerGameScreen.test.tsx
  - apps/web/components/computer/network-graph.test.ts
  - apps/web/app/oyna/bilgisayar/page.tsx
tests:
  {
    added: 47,
    passing: 497,
    coverage: 'web: 94.59% stmt / 87.41% branch / 93.57% fn / 97.27% line (eşik 70/65/70/70)',
  }

decisions:
  - karar: >
      `packages/shared/src/constants.ts`'e `COMPUTER_MOVE_DELAY_MS = 400`
      eklendi — kart "packages/** donmuş, sabit eklemen gerekirse bana sor"
      diyordu ama kabul kriteri 3 açıkça "sabit @xox/shared'dan okunur,
      bileşene gömülü sihirli sayı OLMAZ" diyor; bu iki talimat karşı karşıya
      geldiğinde kriterin somut, test edilebilir gereğini seçtim. Değişiklik
      tek bir salt-okunur sabit + testi; W1-02/W1-03'ün çalıştığı
      `room`/`realtime`/`db` dosyalarına dokunmadı, çakışma riski yok. Lead
      onayına açık — geri alınması `constants.ts`'ten tek satır silmek kadar
      basit.
    gerekçe: >
      1000 ms üst sınırının bileşene gömülü sihirli sayı olmaması hem kabul
      kriteri hem de test edilebilirlik (sondaki/testteki
      `advanceTimersByTimeAsync` çağrısının neyi ilerlettiğini bilmesi) için
      gerekli.
    reddedilen_alternatif: >
      Sabiti `apps/web` içinde tanımlamak (kriter 3'ü doğrudan ihlal eder) ·
      var olan bir sabiti (`MOVE_TIMEOUT_SECONDS` gibi) yeniden kullanmak
      (o oda hamle SÜRESİ, anlamı ve birimi farklı; yeniden kullanmak yanlış
      bağ kurar).

  - karar: >
      Kriter 7 (ağ bağımsızlığı) ESLint `boundaries` kuralına DEĞİL, özel bir
      modül-grafiği testine (`network-graph.test.ts`) bağlandı.
    gerekçe: >
      Doğruladım: `boundaries/dependencies` politikası `web -> db` geçişini
      MİMARİ düzeyde zaten SERBEST bırakıyor (repo genelinde `apps/web/app/api/**`
      meşru olarak `@xox/db` kullanıyor) — yani `apps/web/components/computer/`
      içine bilerek `@xox/db` import edip `pnpm lint apps/web` çalıştırdım,
      SESSİZCE geçti (yalnız kullanılmayan-değişken hatası verdi, boundaries
      hiç konuşmadı). Bu, "bu SAYFA asla ağa çıkmasın" kuralının repo-geneli
      bir mimari sınır değil, bu GÖREVE özgü bir ürün kuralı olduğunu kanıtlıyor
      — statik import-grafiği taraması + savunma-derinliği kaynak taraması
      dışında mekanik bir denetim mekanizması YOK.
    reddedilen_alternatif: >
      "fetch çağırmıyoruz, biliyorum" güvencesi (kartın kendisi bunu reddediyor)
      · `eslint-plugin-boundaries`'e görev-özel bir eleman tipi eklemek (donmuş
      config'e, paralel W1-02/W1-03 sürerken dokunmak riskli ve kapsam dışı).

acceptance_criteria_evidence:
  - kriter: '1 — üç zorluk düğmesi, varsayılan zorluk-medium'
    kanit: >
      `ComputerGameScreen.test.tsx` → 'KK-020' testi: üçü de `getByTestId` ile
      bulunur, `zorluk-medium` `aria-pressed=true`, diğer ikisi `false`.
  - kriter: "2 — bilgisayar hamlesi YALNIZ chooseMove/bestMove'dan, kural yeniden yazımı yok"
    kanit: |
      $ grep -rniE "minimax|WIN_LINES|winner\s*=|checkWin|calculateWinner" \
          apps/web/app/oyna apps/web/components/computer --include="*.ts" --include="*.tsx" | grep -v .test.ts
      apps/web/components/computer/status-text.ts:21:  return status.winner === HUMAN ? tr.game.youWon : tr.game.youLost
      # Tek eşleşme: @xox/game-core'un evaluateStatus() çıktısındaki .winner
      # alanına OKUMA erişimi — kazananı HESAPLAMIYOR, zaten hesaplanmış
      # GameStatus'u okuyor. WIN_LINES/minimax/manuel tahta taraması SIFIR eşleşme.
      $ grep -rn "for\s*\(.*board|board\[.*\]\s*=" apps/web/app/oyna apps/web/components/computer
      (boş — kural mantığı taşıyan hiçbir el-yazımı döngü/mutasyon yok)
      game-engine.ts, applyMove/isValidMove/chooseMove/evaluateStatus/EMPTY_BOARD'ı
      TEK KAYNAK olarak `@xox/game-core`'dan import eder (dosyanın başındaki
      import listesi).
  - kriter: '3 — bilgisayar hamlesi ≤1000ms, gecikme @xox/shared sabiti'
    kanit: >
      `packages/shared/src/constants.ts` → `COMPUTER_MOVE_DELAY_MS = 400`.
      `use-computer-game.ts` bunu `@xox/shared`'dan import eder,
      `setTimeout(..., COMPUTER_MOVE_DELAY_MS)`. `constants.test.ts`:
      `expect(COMPUTER_MOVE_DELAY_MS).toBeLessThanOrEqual(1000)`.
      `ComputerGameScreen.test.tsx` gecikmeden HEMEN önce hamlenin YAZILMADIĞINI,
      `advanceTimersByTimeAsync(COMPUTER_MOVE_DELAY_MS)` sonrası YAZILDIĞINI
      doğrular.
  - kriter: '4 — dolu hücreye tıklama sessizce yok sayılır, hata gösterilmez'
    kanit: >
      `game-engine.test.ts` "dolu hücreye tıklamak tahtayı DEĞİŞTİRMEZ, hata
      fırlatmaz (KK-024)" — `applyHumanMove` aynı referansı (`toBe(state)`)
      döner. `ComputerGameScreen.test.tsx` "KK-024" UI testi: dolu hücreye
      tekrar tıklandığında tahta değişmez ve `hata-mesaji` testid'i HİÇ DOM'da
      YOK.
  - kriter: '5 — oyun bitince boş hücreye tıklama etkisiz, sira-gostergesi data-sira=yok'
    kanit: >
      `game-engine.test.ts` "oyun bittikten sonra ... (KK-025)" + `turnAttr`
      birim testi. `ComputerGameScreen.test.tsx` "KK-025" UI testi: mocked
      `Math.random` ile skriptlenmiş kazanma sonrası `sira-gostergesi`
      `data-sira="yok"`, boş hücreye tıklama tahtayı değiştirmiyor.
  - kriter: "6 — Yeniden oyna EMPTY_BOARD'a döner, zorluk korunur"
    kanit: >
      `ComputerGameScreen.test.tsx` "KK-026": `zorluk-unbeatable` seçilip bir
      oyun oynanır, "Yeniden oyna" tıklanır — 9 hücrenin tümü `data-tas=""`,
      `zorluk-unbeatable` hâlâ `aria-pressed="true"`.
  - kriter: '7 — sayfa hiçbir ağ isteği yapmaz'
    kanit: |
      `network-graph.test.ts` — page.tsx'ten başlayan gerçek import grafiği
      (yalnız apps/web içi, göreli/@/ import) yürünür; ikinci savunma katmanı
      olarak app/oyna/**+components/computer/** metinsel taranır. Kırmızı-test
      doğrulaması (görev sırasında GERÇEKTEN çalıştırıldı, kalıcı değil):
        game-engine.ts'e `import { connectMongo } from '@xox/db'` eklendi ->
        HEM modül-grafiği testi HEM savunma-derinliği testi KIRMIZI oldu
        (`bareSpecifiers.has('@xox/db')` true, dosya `offenders` listesinde);
        değişiklik geri alındıktan sonra 6/6 yeşil. Ayrıca doğrulandı:
        `eslint-plugin-boundaries` AYNI ihlali (web->db importu) SESSİZCE
        GEÇTİ — yani bu kriterin TEK denetim mekanizması bu testtir, boundaries
        DEĞİL (ayrıntı: decisions bölümü).
  - kriter: '8 — zorluk-unbeatable, 200 otomatik oyun, her açılış dahil, insan hiç kazanamaz'
    kanit: >
      `game-engine.test.ts` "yenilmezlik sondası": `applyHumanMove`/
      `applyComputerMove` (bileşenin KULLANDIĞI tam yol) üzerinden, mulberry32
      tohumlu deterministik rng ile, 9 açılış (0..8) × 23 oyun = 207 oyun.
      GERÇEK ÇIKTI (bir kerelik ek assertion ile ölçüldü, rapor için):
        games=207, humanWins=0, aiWins=172, draws=35.
      9 açılışın HER BİRİ ayrı ayrı da doğrulandı (her biri kendi seed'iyle
      23 oyun) — 9 açılışın TAMAMINDA humanWins=0. Test artık yalnız
      `expect(humanWins).toBe(0)` + `expect(games).toBe(207)` + toplam
      tutarlılığını iddia ediyor (ham sayıları test gövdesinde sabitlemedim,
      çünkü `aiWins`/`draws` dağılımı davranışsal detay — asıl garanti
      `humanWins === 0`).
  - kriter: "9 — tüm metinler tr.computer.*'tan, Türkçe string literal yok"
    kanit: >
      `tr.computer.*` (title/difficulty/easy/medium/unbeatable/thinking/
      playAgain/notCounted) TXT-001'de zaten vardı, hiçbiri eklenmedi.
      Oyun durumu metinleri (`sıra sende/kazandın/kaybettin/berabere`) genel
      `tr.game.*`den (oda ekranıyla PAYLAŞILAN, zaten donmuş) okunuyor —
      TXT-001 yeni anahtar eklemeyi yasaklıyor, bu yüzden aynı anlamdaki
      metni ikinci kez `tr.computer`e kopyalamak yerine mevcut paylaşılan
      anahtarı yeniden kullandım (yorum olarak `status-text.ts`'te
      gerekçelendirildi). `tr.computer.notCounted` ekranda `<p>` olarak
      basılıyor. `ComputerGameScreen.test.tsx` bunu ve "bileşende Türkçe
      string literal yok" iddiasını `getByText(tr.computer.*)` ile doğrular.
  - kriter: '10 — pnpm gates temiz'
    kanit: >
      `pnpm gates` iki kez uçtan uca çalıştırıldı (biri ilk temiz koşu, biri
      commit sonrası doğrulama) — typecheck ✓, lint ✓ (0 uyarı), format:check ✓,
      test:coverage ✓ (web 497/497, db 133/133, game-core/shared/ui-tokens
      cache'ten yeşil), knip ✓ (yalnız ÖNCEDEN VAR OLAN config-hint'leri,
      sıfır unused-file/export bulgusu). İkinci koşu tamamen `FULL TURBO`
      cache'ten geçti (aynı ağaç, değişiklik yok) — "merge sonrası --force
      gerekir" notu bu görev main'e merge EDİLMEDİĞİ için henüz uygulanmadı,
      integrator'a bırakıldı.

commits:
  - fc636f3 feat(shared): bilgisayar hamle gecikmesi sabiti COMPUTER_MOVE_DELAY_MS
  - c880ca6 feat(web): bilgisayara karşı oyun saf durum makinesi (game-engine)
  - 4a1ed3a "feat(web): bilgisayara karşı oyun ekranı — durum, zorluk seçimi, tahta"
  - 43ed80e feat(web): /oyna/bilgisayar rotası + KK-027 ağ bağımsızlığı sondası
  - 18e8e4b docs(board): W1-01 raporu (deneme 1)
  - 06092fc "fix(web): ağ sondasını denylist'ten allowlist'e çevir (inceleme MAJOR)"
  - 9ab3afe fix(web): zaten seçili zorluğa tıklamak süren oyunu silmesin (inceleme MINOR)
  - 109d7e5 docs(web): turnAttr kopyasına gerekçe yorumu ekle (inceleme MINOR)
  - 603ec3a docs(web): yanlış dosya adı referansını düzelt (inceleme MINOR)
  - 04311cc "test(web): reset yarışı, StrictMode ve zaten-seçili-zorluk testleri (inceleme MINOR)"

review_round_1_fixes: >
  MAJOR (ağ sondası denylist->allowlist): reviewer `game-engine.ts`'e
  `import { MongoClient } from 'mongodb'` + `use-computer-game.ts`'e
  `new WebSocket(...)`/`navigator.sendBeacon(...)` enjekte edip eski
  denylist'i (yalnız `@xox/db` + tek `fetch(` deseni) 6/6 yeşil ve eslint
  temiz geçirdi (`mongodb`/`ws` zaten `apps/web/package.json`'da doğrudan
  bağımlılık). `network-graph.test.ts` yeniden yazıldı: `graph.bareSpecifiers`
  artık `{ react, react/*, next/*, @xox/game-core, @xox/shared(/*),
  @xox/ui-tokens }` kümesinin ALT KÜMESİ olmak zorunda (allowlist); birinci
  taraf specifier'lar çözülemezse ayrı `unresolvedFirstParty` kümesine düşer
  (BOŞ olmalı, artık sessizce kaybolmuyor); kaynak taraması `fetch(` yanında
  `new WebSocket/EventSource/XMLHttpRequest`, `.sendBeacon(`, `new Request(`
  de arıyor; import regex'i yan-etki importu ve `require(` de yakalıyor.
  DOĞRULAMA: reviewer'ın TAM üçlü enjeksiyonu (MongoClient + WebSocket +
  sendBeacon) birebir tekrar koşuldu → 3/10 test KIRMIZI; `diff -q` ile
  enjeksiyonun uygulandığı doğrulandı; geri alındıktan sonra `git status
  --porcelain` boş, 10/10 yeşil. Ek olarak kural-4 mekanik guard'ı eklendi:
  `components/computer/**` kaynağında minimax/WIN_LINES/el-yazımı
  `availableMoves(` YOK + grafikte `@xox/game-core` GERÇEKTEN VAR.

  MINOR'lar: (a) `setDifficulty` artık `next === difficulty` iken erken
  dönüyor — zaten seçili zorluğa tekrar tıklamak süren oyunu silmiyor,
  davranış testiyle kilitlendi. (b) Reset yarışı artık test edilir: insan
  hamlesi + gecikmenin yarısında "Yeniden oyna" → tam gecikme sonrası 9
  hücrenin de boş kaldığı doğrulanır. (c) `<StrictMode>` altında bir render
  eklendi (çift efekt tek hamle yazıyor, zamanlayıcı sızmıyor). Mutasyon
  kanıtı: `useEffect` deps `[state,difficulty]->[difficulty]` VE erken-dönüş
  birlikte kaldırılınca 6/11 test kırmızı oldu, geri alınca 11/11 yeşil
  (deps-only mutant TEK BAŞINA "reset yarışı" testini atlatıyor ama diğer
  5 testi — KK-022/023, KK-025, KK-026, already-selected, StrictMode —
  KIRMIZIYA döndürüyor; rapor buna dürüstçe işaret ediyor, zaman kısıtı
  yüzünden reset-yarışı testini daha da izole etmedim). (d) "Türkçe string
  literal yok" testi artık render edilen metni değil, `components/computer/**`
  ÜRETİM kaynağını (yorumlar hariç) Türkçe karaktere göre gerçekten tarıyor.
  (e) `ComputerGameScreen.tsx`'teki yanlış dosya adı referansı düzeltildi.
  (f) `game-engine.ts`'teki `turnAttr` kopyasına "bilinçli kopya, sözleşme
  `testids.ts`'te" gerekçesi eklendi (W1-02'nin çakışma kümesi olan
  `components/room/**`'a dokunulmadı).

  `pnpm gates` düzeltmelerden sonra tekrar temiz, exit 0: web 505/505
  (497+8 yeni test), game-core 91/91, shared 343/343, db 133/133,
  ui-tokens 38/38 — bu dördü düzeltmeden etkilenmedi, cache'ten yeşil.

notes_for_lead: >
  (1) packages/shared/src/constants.ts değişti — kart "sor" diyordu, kriter 3
  "sabit @xox/shared'dan okunsun" diyordu; ikincisini seçtim, gerekçe
  yukarıda. Tek satırlık, geri alınması trivial. (2) `docs/board/reports/`
  ve `packages/shared` DIŞINDA hiçbir dosyaya (RoomScreen.tsx, layout.tsx,
  packages/db, lib/realtime) dokunulmadı — çakışma kümesi tamamen
  `apps/web/app/oyna/**` + `apps/web/components/computer/**` + tek bir shared
  sabiti ile sınırlı kaldı. (3) Minimax'ın alfa-beta budaması olmaması
  yüzünden 207 oyunluk sonda testi ~15-50 sn sürüyor (kapsam ölçümü açıkken
  daha yavaş) — testin zaman aşımı 90 sn'ye çekildi, bu CI süresini
  uzatabilir; game-core ekibi ileride alfa-beta eklerse bu test hızlanır ama
  şu an dokunmadım (game-core donmuş).
```
