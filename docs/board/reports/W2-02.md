```yaml
task: W2-02
status: done
summary: >
  /profil'e görünen ad düzenleme, istatistik/ELO görünümü ve tema
  değiştirici eklendi; GET/PATCH /api/profile route.ts yazıldı
  (rest-contract.ts'teki profileResponseSchema/profileUpdateBodySchema
  önceden donmuştu, DEĞİŞTİRİLMEDİ). ProfileContent 'use client' kaldı
  (zaten interaktif — useSession/signOut) ve tek ağ katmanı olarak
  GET/PATCH /api/profile'ı yönetiyor; EditNameForm/ThemeToggle saf
  sunum bileşenleri.

kabul_kriterleri_kanit:
  get_patch_semasi: >
    `apps/web/app/api/profile/route.ts` — GET 401 UNAUTHENTICATED (kimliksiz)
    / 200 {name,email,stats,elo,ratedGames,theme}; PATCH strictObject
    {name?,theme?} kabul eder, boş gövde/bilinmeyen alan/aralık dışı ad/
    geçersiz tema 400 ile reddedilir. 11 test — `route.test.ts`, gerçek
    `resolveIdentity` (yalnız `@/auth`+`@xox/db` mock, KK-010 disiplini).
  kk_080: >
    `ProfileContent.tsx` GET yanıtını `profileResponseSchema.safeParse`den
    geçirip render eder; e-posta salt okunur metin, üç sayaç
    `TESTID.istatistikGalibiyet/Maglubiyet/Beraberlik` ile `users.stats`
    değerleriyle BİREBİR gösterilir (`ProfileContent.test.tsx` KK-080 testi).
  kk_081: >
    Ayrı bir mantık YAZILMADI — GET route'u `users` koleksiyonunu olduğu
    gibi yansıtır; yeni kullanıcı varsayılanları (`stats:0/0/0`, `elo:1200`)
    `packages/db/src/models/user.ts` şemasının sorumluluğu ve zaten
    `packages/db/src/models/user.test.ts`/`seed.test.ts`te kilitli.
  kk_082: >
    Sunucu: `displayNameSchema` (2..40) — aralık dışı ad 400 INVALID_NAME,
    DB'ye hiç yazılmaz (`route.test.ts`). İstemci: `EditNameForm` +
    `ProfileContent.handleSaveName` gerçek `fetch` PATCH çağırır, başarıda
    profil state'i güncel adla değişir ve "Adın güncellendi." `role="status"`
    ile duyurulur; 400 dönerse `ErrorBanner` `role="alert"` gösterir VE
    "Kaydet" düğmesi tekrar tıklanabilir (pending `finally`de düşüyor —
    bu gecenin dersi, ayrı test bunu KİLİTLER).
  kk_083: >
    `ThemeToggle` "Koyu"ya basınca `<html data-tema="koyu">` SAYFA
    YENİLENMEDEN uygulanır (`document.documentElement.setAttribute`) +
    aynı anda `xox-tema` çerezi yazılır + `PATCH {theme:'koyu'}` gider.
    PATCH ağ hatasıyla başarısız olursa ÖNCEKİ temaya (DOM+çerez+state)
    geri dönülür ve hata gösterilir — test bunu `mockRejectedValueOnce`
    ile kanıtlıyor. `app/layout.tsx`e DOKUNULMADI (kart şartı).
  kk_084: >
    Hiç hex literal yok; tüm renkler `border-border`/`bg-surface`/
    `text-text` gibi @xox/ui-tokens türevi Tailwind sınıflarından.
    `pnpm lint apps/web` (no-restricted-syntax dahil) temiz.
  pnpm_gates: 'temiz — çıktı aşağıda.'

test_ciktisi_ozet:
  route: 'app/api/profile/route.test.ts — 11/11 geçti'
  bilesenler: >
    ProfileContent.test.tsx 9/9, EditNameForm.test.tsx 4/4,
    ThemeToggle.test.tsx 5/5 — toplam 18 yeni bileşen testi.
  tum_paket: 'pnpm --filter @xox/web test → 60 dosya / 611 test, hepsi yeşil.'
  typecheck: 'pnpm --filter @xox/web typecheck → temiz'
  lint: 'pnpm lint apps/web → temiz (jsx-a11y dahil, hata seviyesinde)'
  format: 'pnpm format:check → temiz'
  knip: 'pnpm knip → 0 kullanılmayan export/dosya (yalnız ön-var-olan config ipuçları)'
  build: 'pnpm --filter @xox/web build → başarılı, /api/profile route listede'
  gates: 'pnpm gates → exit 0 (typecheck+lint+format+test:coverage+knip)'
  coverage_ozet: 'statements 94.44% / branches 89.24% / functions 93.29% / lines 96.85% (eşik altında kalan dosya yok, mevcut eşiğin üzerinde)'

erisilebilirlik_dogrulama:
  - "Görünen ad input'u <label htmlFor> ile bağlı — `getByLabelText('Görünen ad')` testte kullanıldı (id sızıntısı değil, gerçek erişilebilir isim)."
  - 'Hatalar `role="alert"` (ErrorBanner, mevcut donmuş bileşen, DEĞİŞTİRİLMEDİ) — testte `getByRole(''alert'')` ile sorgulanıyor.'
  - 'Başarı durumu `role="status"` (örtük aria-live=polite) — testte `getByRole(''status'')`.'
  - 'ThemeToggle iki gerçek <button>, seçili durum `aria-pressed` (kart metninin izin verdiği iki kalıptan biri); `userEvent.tab()`+`{Enter}` testi klavyeyle odaklanıp seçilebildiğini kanıtlıyor (fare kullanılmadan).'
  - 'pending sırasında düğmeler `disabled` — ekran okuyucu çakışan durumları duyurmaz.'
  - 'jsx-a11y kuralları hata seviyesinde ve lint temiz — mekanik kanıt.'

sunucu_hata_govdesi_zod_notu: >
  Lead notuna göre `errorResponseSchema.safeParse` kullanıldı, `as` cast'i
  YOK: `parseErrorCode()` (ProfileContent.tsx) hem GET hem PATCH hata
  yanıtlarını bu şemadan geçiriyor, eşleşmezse `SERVER_ERROR`'a düşüyor —
  KayitForm'daki eski kalıptan (yalnız `code` alanını cast'leyen) daha katı.

form_pending_notu: >
  handleSaveName/handleChangeTheme try/catch/finally ile yazıldı,
  `pending` state'i HER ZAMAN `finally`de `false`'a dönüyor — testler
  (400 dönüşünde "Kaydet" tekrar tıklanabilir, ağ hatasında tema PATCH
  sonrası pending düşer) bunu kanıtlıyor.

mimari_not_theme_cookie_bolunmesi: >
  `apps/web/lib/theme.ts` `next/headers` import ediyor (sunucu-yalnız).
  Kart notu THEME_COOKIE'yi "burada tekrar export et" diyordu ama
  `ProfileContent.tsx` (istemci bileşeni) bunu import edince
  `pnpm --filter @xox/web build` Turbopack hatasıyla reddetti ("you're
  importing next/headers into a Client Component"). Çözüm: sabiti YENİ,
  next/headers'sız bir dosyaya (`lib/theme-cookie.ts`) taşıdım;
  `theme.ts` ORADAN import ediyor, `ProfileContent.tsx` da ORADAN — iki
  ayrı sabit olarak KOPYALANMADI, tek kaynak `theme-cookie.ts`. Bu,
  kartın "yazma alanı" listesinde `lib/theme.ts` açıkça anılmasa da
  (kart metni bunu BEKLİYORDU) ve yeni `lib/theme-cookie.ts` dosyası
  gerekli oldu — knip + build ile doğrulandı, `lib/theme.test.ts`
  (mevcut, dokunulmadı) hâlâ yeşil.

design_001b_notlari:
  - 'ProfileContent/EditNameForm/ThemeToggle şu an minimal Tailwind sınıflarıyla (flex/gap/border-border/bg-surface) — görsel cila (spacing/typography ölçeği, ikon, animasyon) bilerek eklenmedi, DESIGN-001b bekliyor.'
  - "ThemeToggle 2 düğmeyi 'flex gap-2' ile yan yana koyuyor; aria-pressed:font-bold dışında seçili/seçilmemiş için görsel fark YOK — DESIGN-001b'nin eklemesi beklenen şey tam olarak bu (ör. arka plan/kenarlık farkı, `motion` token'larıyla geçiş)."
  - 'EditNameForm başarı mesajı düz <p role="status"> — toast/animasyon yok, motion.ts token''ları henüz tüketilmedi.'
  - "Bilinen kapsam dışı boşluk: `resolveTheme()` (lib/theme.ts) YALNIZ çerezden okur, `users.theme`den DEĞİL — cihaz değişiminde (yeni tarayıcı/cihaz, çerez yok) profil sayfası dışındaki İLK SSR hâlâ 'acik' varsayılana düşer, DB'deki tema yalnız `/profil`e tekrar girilince PATCH çerezi tazeler. Kart metni `app/layout.tsx`'i AÇMAMAMI istedi; bu yüzden cihazlar-arası tam senkron bu kartın kapsamı DIŞINDA bırakıldı — bir sonraki tema/oturum kartı `resolveTheme()`'e oturumlu kullanıcı için DB okuma eklemeyi değerlendirmeli."

dokunulan_dosyalar:
  yeni:
    - apps/web/app/api/profile/route.ts
    - apps/web/app/api/profile/route.test.ts
    - apps/web/components/profile/EditNameForm.tsx
    - apps/web/components/profile/EditNameForm.test.tsx
    - apps/web/components/profile/ThemeToggle.tsx
    - apps/web/components/profile/ThemeToggle.test.tsx
    - apps/web/lib/theme-cookie.ts
  degisen:
    - apps/web/components/profile/ProfileContent.tsx
    - apps/web/components/profile/ProfileContent.test.tsx
    - apps/web/lib/theme.ts (yalnız THEME_COOKIE'yi ./theme-cookie'ye taşımak için)
  dokunulmayan_dondurulmus: 'apps/web/app/layout.tsx, apps/web/lib/log.ts, apps/web/app/api/health/route.ts, packages/game-core/**, packages/db/src/models/**, packages/ui-tokens/**, apps/web/messages/tr.ts (yeni anahtar eklenmedi, TXT-001 korunuyor)'

commit_shalari:
  - "worktree feat/W2-02 dalında commit atıldı — lead entegrasyon sırasında SHA'yı `git log feat/W2-02` ile görebilir (bu ajan merge/push yapmaz)."

acilan_soru_lead_icin: >
  Kart metninin sonunda `move:applied` ince yolunun `turnDeadline`
  taşımadığı hakkında bir not var (CTR-002) — bu W2-02'nin (profil/tema)
  kapsamıyla İLGİSİZ görünüyor, muhtemelen panoda yanlış karta
  yapıştırılmış bir hatırlatma. Dokunmadım; WS protokolüyle ilgili
  görevin (CTR-002/kart) sahibine yönlendirilmeli.
```
