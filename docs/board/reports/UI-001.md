```yaml
task: UI-001
status: done
summary: >
  `@xox/ui-tokens` tek kaynağı `themes.acik`/`themes.koyu` olarak yeniden kuruldu (anahtar
  kümeleri testle birebir eşitleniyor); `cssVariables`/`nativeColors` aynı `themes` kaynağından
  web (CSS custom property) ve mobil (düz RN nesnesi) çıktısını üretiyor, anahtar paritesi
  testle kanıtlandı. `themeCssBlock`/`themeCss` `[data-tema='acik']`/`[data-tema='koyu']`
  seçicili CSS bloklarını üretiyor (KK-083'ün token yarısı). Kök `eslint.config.mjs`'e
  `no-restricted-syntax` ile literal hex renk yasağı eklendi — `apps/web/**` ve
  `apps/mobile/**` içinde `Literal`/`TemplateElement` düzeyinde hem düz string hem template
  literal yakalanıyor, `packages/ui-tokens/**` muaf (KK-084). Ek olarak `contrast.ts` ile
  WCAG kontrast oranı hesaplanıp her metin token'ının bg/surface üzerinde >=4.5:1, `win`
  token'ının >=3:1 (grafik eşiği) olduğu testle kilitlendi; bu sırada `playerO` (açık tema)
  ve `win` (açık tema) değerleri 4.46/3.13'ten 5.97/4.77'ye çekildi çünkü eski değerler metin
  kullanımında 4.5:1 eşiğinin altındaydı.
files_changed:
  - packages/ui-tokens/src/colors.ts
  - packages/ui-tokens/src/colors.test.ts
  - packages/ui-tokens/src/css.ts
  - packages/ui-tokens/src/css.test.ts
  - packages/ui-tokens/src/contrast.ts
  - packages/ui-tokens/src/contrast.test.ts
  - packages/ui-tokens/src/index.ts
  - packages/ui-tokens/package.json
  - packages/ui-tokens/vitest.config.ts
  - eslint.config.mjs
  - docs/board/reports/UI-001.md
tests: { added: 33, passing: 33, coverage: '100% (statements/branches/functions/lines)' }
decisions:
  - karar: >
      `themes.acik`/`themes.koyu` yeni tek kaynak oldu; eski `colors.light`/`colors.dark`
      `themes`'ten türetilen geriye-dönük bir takma ad olarak (`colors = { light: themes.acik,
      dark: themes.koyu }`) korundu, silinmedi.
    gerekçe: >
      `apps/mobile/app/index.tsx` (bu görevin çakışma kümesi dışında, dokunulmadı) hâlâ
      `colors.light.bg` üzerinden okuyor. Bunu kırmadan Türkçe `themes.acik/koyu` API'sini
      eklemenin tek yolu, ikisini de AYNI literal kümeden türetmekti — iki ayrı renk paleti
      elle yazmak KK-084'ün amacına ters düşerdi. `colors` alias'ı `themes`'in bir GÖRÜNÜMÜ,
      ayrı bir kaynak değil (`toStrictEqual` testiyle doğrulandı).
    reddedilen_alternatif: >
      `apps/mobile/app/index.tsx`'i de bu PR'da `themes.acik`'e taşımak — çakışma kümem
      yalnızca `packages/ui-tokens/**` ve `eslint.config.mjs`; `apps/mobile/app/**` sonraki
      dalgada yazılacak dosyalar arasında listelenmiş, dokunmadım.
  - karar: >
      `cssVariables`/`nativeColors`/`themeCssBlock`/`themeCss` ayrı fonksiyonlar olarak
      `css.ts`'e eklendi; ikisi de doğrudan `themes` nesnesini okuyor, aralarında literal
      renk kopyası yok.
    gerekçe: >
      Kabul kriteri #2 "aynı kaynaktan" ifadesini iki farklı ÇIKTI biçimi (CSS custom property
      map'i ve düz RN nesnesi) olarak okudum; ikisinin de tek okuma noktası `themes[theme]`.
      Anahtar paritesi `cssVariableTokenNames` (kebab-case'i geri çeviren yardımcı) ile
      `Object.keys(nativeColors(theme))` karşılaştırılarak testte kilitlendi.
    reddedilen_alternatif: >
      Tek bir `toStyles(theme, target: 'web' | 'native')` fonksiyonu — iki farklı dönüş tipini
      (CSS string-map vs. düz renk nesnesi) tek imzada birleştirmek çağıran tarafta gereksiz
      tip daraltması zorunlu kılardı; ayrı fonksiyonlar hem `explicit-module-boundary-types`
      için daha net hem tree-shake edilebilir (web hiç `nativeColors` bundle'lamaz).
  - karar: >
      Açık temada `playerO`'yu `#e11d48` -> `#be123c`, `win`'i `#16a34a` -> `#15803d` olarak
      güncelledim (koyu tema değişmedi).
    gerekçe: >
      Sen XOX'un tasarım sorumlususun rolündeki "Kontrast: metin/arka plan oranı en az 4.5:1"
      değişmezini `contrast.ts` ile ölçülebilir hâle getirince açık temada `playerO` bg üzerinde
      4.46:1 (eşiğin altı) çıktı; `win` de metin bağlamında (o an ayrı bir eşiği yok) hem bg hem
      surface'te 3.1-3.3 idi. WCAG'ın grafik/kalın çizgi eşiği (1.4.11, >=3:1) `win` için zaten
      geçerliydi ama biraz daha marj bıraktım (4.77/5.02) çünkü winning-line vurgusu ince bir
      stroke olarak render edilebilir ve o durumda metin eşiği daha güvenli bir hedef.
    reddedilen_alternatif: >
      Değerleri değiştirmemek ve yalnızca `win` için ayrı, daha düşük bir eşik (3:1) test etmek
      — teknik olarak WCAG'a uygun olurdu, ama playerO'nun 4.46:1'i (metin/rozet bağlamında
      kullanılması muhtemel) düzeltilmeden bırakmak, bu görevin devraldığı "Kontrast"
      değişmezini kısmen ihlal ederdi. Renk tonu (rose) korunarak yalnızca L değeri koyulaştı,
      görsel kimlik değişmedi.
gotchas:
  - >
    `apps/web/package.json` henüz `@xox/ui-tokens`'a bağımlı değil (yalnızca `@xox/db`
    listeleniyor, o dosya OPS-002'nin çakışma kümesinde). Bu yüzden web tarafında "izinli"
    sondayı gerçek bir `@xox/ui-tokens` import'uyla değil, hex-olmayan bir CSS değeriyle
    (`'rebeccapurple'`, `'rgb(37, 99, 235)'`) yazdım — aksi hâlde tip çözümlemesi
    "cannot be resolved" hatası verip kuralın kendisiyle ilgisiz gürültü üretiyordu.
    `apps/web/app/**` gerçek bileşenler yazıldığında `apps/web/package.json`'a
    `@xox/ui-tokens` eklenmesi gerekecek (OPS-002 ya da ilgili web görevi).
  - >
    Şablon literal (template literal) içine gömülü hex ("1px solid #123" gibi) `TemplateElement`
    seçicisiyle YAKALANMAZ çünkü `value.raw` tüm parçayı (`"1px solid #123"`) taşır, salt
    `#123`'ü değil — regex tam string eşleşmesi arıyor. Yalın `` `#123` `` template literal'i
    (parça yalnız hex'ten ibaretse) yakalanır; bu, görevin verdiği ipucundaki kalıbın bilinen
    sınırıdır ve talimatta belirtildiği şekliyle uygulandı.
sonda_output: |
  === İHLAL (error bekleniyor) ===
  $ npx eslint apps/web/app/__sonda_ihlal/page.tsx
  (içerik: const stil = { color: '#2563eb', border: `1px solid #123` })

  /Users/.../apps/web/app/__sonda_ihlal/page.tsx
    2:25  error  Literal hex renk kodu yasak (KK-084). Rengi @xox/ui-tokens içindeki
                 themes.acik/themes.koyu üzerinden al  no-restricted-syntax

  ✖ 1 problem (1 error, 0 warnings)
  EXIT_WEB_IHLAL: 1

  $ npx eslint apps/mobile/app/__sonda_ihlal.tsx
  (içerik: StyleSheet.create({ kutu: { backgroundColor: '#e11d48' } }))

  /Users/.../apps/mobile/app/__sonda_ihlal.tsx
    8:28  error  Literal hex renk kodu yasak (KK-084). Rengi @xox/ui-tokens içindeki
                 themes.acik/themes.koyu üzerinden al  no-restricted-syntax

  ✖ 1 problem (1 error, 0 warnings)
  EXIT_MOBILE_IHLAL: 1

  === İZİNLİ (temiz geçmesi bekleniyor) ===
  $ npx eslint apps/web/app/__sonda_izinli.tsx
  (içerik: const stil = { color: 'rebeccapurple', border: '1px solid rgb(37, 99, 235)' })
  (çıktı yok — 0 problem)
  EXIT_WEB_IZINLI: 0

  $ npx eslint apps/mobile/app/__sonda_izinli.tsx
  (içerik: import { themes } from '@xox/ui-tokens'; StyleSheet.create({ kutu: {
   backgroundColor: themes.koyu.playerO } }))
  (çıktı yok — 0 problem)
  EXIT_MOBILE_IZINLI: 0

  $ npx eslint packages/ui-tokens/src/__sonda_muaf.ts
  (içerik: export const sondaMuafRenk = '#123abc')
  (çıktı yok — 0 problem; ui-tokens hex yasağından MUAF)
  EXIT_UITOKENS_MUAF: 0

  (Dört geçici sonda dosyası da doğrulama sonrası `rm` ile silindi;
  `git status --short` bu görevin gerçek değişikliklerinin dışında hiçbir şey göstermiyor.)
next_suggestions:
  - >
    `apps/mobile/app/index.tsx`'in `colors.light` yerine `themes.acik` kullanacak şekilde
    taşınması — o zaman geriye dönük `colors` takma adı kaldırılabilir. Bu görevin çakışma
    kümesi dışında bırakıldı.
  - >
    `apps/web/package.json`'a `@xox/ui-tokens` bağımlılığı eklenmesi (OPS-002 ya da web UI
    görevi kapsamında) — böylece `themeCss()`/`themeCssBlock()` çıktısı gerçekten
    `apps/web/app/globals.css`'in `@theme` bloğuna yapıştırılabilir.
  - >
    Oyun tahtası bileşeni yazılırken X/O'nun yalnızca renkle değil şekil/kalınlıkla da ayırt
    edilmesi gerekiyor (renk körlüğü); bu token paketi renk kısmını çözdü, şekil/stroke
    kalınlığı kararı bileşen görevine (sonraki dalga) bırakıldı.
```
