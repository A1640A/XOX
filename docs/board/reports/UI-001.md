```yaml
task: UI-001
status: done
summary: >
  `@xox/ui-tokens` tek kaynağı `themes.acik`/`themes.koyu` olarak kuruldu (anahtar kümeleri
  testle birebir eşitleniyor); `cssVariables`/`nativeColors` aynı `themes` kaynağından web
  (CSS custom property) ve mobil (düz RN nesnesi) çıktısını üretiyor, her ikisinin anahtar
  kümesi `ColorToken` birleşimine karşı BAĞIMSIZ bir referans listeyle doğrulanıyor (derleme
  zamanı egzoz kontrollü). `themeCssBlock`/`themeCss` `[data-tema='acik']`/`[data-tema='koyu']`
  seçicili CSS bloklarını üretiyor (KK-083'ün token yarısı). Kök `eslint.config.mjs`'e literal
  hex renk yasağı üç ayrı kalıpla eklendi: (1) tam hex literal (3/6/8 hane), (2) Tailwind v4
  keyfi-değer sözdizimi (`bg-[#2563eb]`), (3) template literal içine gömülü CSS bildirimi
  (`` `color: #2563eb` ``) — `apps/web/**`/`apps/mobile/**`'de error, `packages/ui-tokens/**`
  muaf (KK-084). `contrast.ts` WCAG kontrast oranı hesaplıyor; metin token'ları (win dahil)
  >=4.5:1, `border` WCAG 1.4.11 anlamlı-UI-bileşeni eşiği >=3:1 ile testte kilitli. Bu turda
  reviewer'ın bulduğu dört gerçek delik kapatıldı — bkz. `review_fixes`.
files_changed:
  - packages/ui-tokens/src/colors.ts
  - packages/ui-tokens/src/colors.test.ts
  - packages/ui-tokens/src/css.ts
  - packages/ui-tokens/src/css.test.ts
  - packages/ui-tokens/src/contrast.ts
  - packages/ui-tokens/src/contrast.test.ts
  - packages/ui-tokens/src/index.ts
  - packages/ui-tokens/package.json
  - packages/ui-tokens/vitest.config.ts
  - eslint.config.mjs
  - pnpm-lock.yaml
  - docs/board/reports/UI-001.md
tests: { added: 38, passing: 38, coverage: '100% (statements/branches/functions/lines)' }
review_fixes:
  - bulgu: >
      MAJOR — `win` düzeltmesini hiçbir test korumuyordu. Eski değer (`#16a34a`, 3.13:1) geri
      konduğunda testin tamamı yeşil kalıyordu çünkü `contrast.test.ts` `win`'i yalnızca 3:1
      eşiğiyle sınıyordu, oysa değeri değiştirme gerekçesi 4.5:1'in altında kalmasıydı.
    duzeltme: >
      `win`, `TEXT_TOKENS` listesine eklendi — artık `meetsTextContrast` üzerinden diğer metin
      token'larıyla AYNI 4.5:1 iddiasıyla sınanıyor. Ampirik doğrulama: `win`'i elle eski
      değerine (`#16a34a`) geri alıp `pnpm --filter @xox/ui-tokens test` koştum —
      `acik.win, hem bg hem surface üzerinde >= 4.5:1` testi KIRMIZI oldu (`AssertionError:
      expected false to be true`), sonra geri aldım ve 38/38 yeşile döndü. Artık testin eşiği
      ile değerin seçilme gerekçesi aynı sayı (4.5).
  - bulgu: >
      MAJOR — Tailwind v4 keyfi-değer sözdizimi (`bg-[#2563eb]`), 8 haneli alfa kanallı hex
      (`#2563eb80`) ve template literal içine gömülü CSS bildirimi (`` `color: #2563eb` ``)
      hex yasağının altından geçiyordu. Reviewer'ın 9 satırlık sondasında 6 vakadan yalnız 3'ü
      yakalanıyordu.
    duzeltme: >
      `eslint.config.mjs`'e iki yeni regex kalıbı eklendi: `TAILWIND_ARBITRARY_HEX`
      (`-\[#hex\]`, `Literal` ve `TemplateElement`'te substring eşleşmesiyle) ve
      `CSS_DECLARATION_HEX` (`:\s*#hex\b`, aynı şekilde substring). Ana `HEX_COLOR_LITERAL`
      3/6/8 haneli hex'i kapsayacak şekilde genişletildi. "İçinde hex geçen HERHANGİ bir
      string" gibi genel bir kalıba BİLEREK gidilmedi (git SHA'sı/kimlik yanlış pozitifi riski)
      — `CSS_DECLARATION_HEX` yalnızca `:` + boşluk + geçerli hex uzunluğu + kelime sınırı
      arıyor, bu da normal düzyazıda/kimliklerde rastlanmayan, CSS bildirimine özgü bir kalıp.
      Reviewer'ın 9 satırlık sondası AYNEN tekrar koşuldu — 6/6 vaka artık `error` veriyor
      (bkz. `sonda_output`). Ayrıca kasıtlı bir "izinli" sonda (className string, template
      literal içinde `oran: 16/9`, 40 haneli sahte git SHA'sı `commit: <sha>` biçiminde) SIFIR
      yanlış pozitifle geçti.
  - bulgu: >
      Özdeşlik iddiaları test değildi. `nativeColors(theme) toStrictEqual themes[theme]`
      (gövde zaten `return themes[theme]`) ve `colors.light toStrictEqual themes.acik` (gövde
      zaten `{ light: themes.acik }`) tanım gereği asla kırılamaz; kabul kriteri #2'nin
      dayandığı "kanıt" buydu.
    duzeltme: >
      Her iki özdeşlik testi silindi. Yerine `css.test.ts`'te `Record<ColorToken, true>` tipine
      atanan bağımsız bir `EXPECTED_COLOR_TOKENS_RECORD` sabiti eklendi — TS'in fazla özellik
      denetimi sayesinde bu nesne `ColorToken` birleşiminden eksik ya da fazla herhangi bir
      anahtarda DERLEME HATASI verir, yani `themes`'ten bağımsız, derleme zamanında kilitli bir
      referans noktası. `cssVariableTokenNames`/`Object.keys(nativeColors(...))` artık
      birbirleriyle değil bu bağımsız listeyle karşılaştırılıyor. Kapsam 33'ten 38'e çıktı
      (silinenlerin yerine gerçek risk hedefleyen testler kondu, sayı düşmedi çünkü border/win
      için de yeni testler eklendi).
  - bulgu: >
      Küçük ama gerçek üç bulgu: (a) `nativeColors` mutable referans dağıtıyordu — dönüş tipi
      `Record<ColorToken,string>` olduğu için `nativeColors('acik').bg = '#000'` tsc'den
      geçip tek kaynağı süreç boyunca bozabilirdi. (b) `contrastRatio` 6 haneli olmayan girdide
      sessizce `NaN` dönüyordu (`NaN < 4.5` -> false, çağıran "geçti" sonucu alırdı) ve 3 haneli
      hex'i (kendi yasağının izin verdiği biçim) hiç desteklemiyordu. (c) `themeCssBlock`
      docstring'i "`@theme` bloğuna yapıştırılmak üzere" diyordu ama ürettiği
      `[data-tema='acik'] { ... }` bir SEÇİCİ bloğu — Tailwind v4'te `@theme` seçici
      içeremeyeceği için `@theme`'in içine gömülürse build kırılır. Ayrıca `vitest`/
      `@vitest/coverage-v8` `packages/ui-tokens`'ta beyan edilmemişti (yalnız hoisting'e
      güveniyordu, `game-core`/`db` ikisi de açıkça yazıyor). `border` de kontrast
      kontrolünün tamamen dışındaydı (ölçüldü: acik 1.23:1, koyu 1.43:1 — 3x3 tahtanın hücre
      sınırları az gören kullanıcı için görünmez).
    duzeltme: >
      (a) `nativeColors` dönüş tipi `Readonly<Record<ColorToken, string>>` yapıldı.
      (b) `contrast.ts`'te `hexToRgb` artık 3/6 haneli hex dışında (KK-084 ile aynı biçim,
      alfa hariç) her şeyde açıkça `throw` ediyor; 3 haneli kısaltma `charAt` ile 6 haneliye
      genişletiliyor (`noUncheckedIndexedAccess` + string indeksleme gotcha'sına uygun).
      `contrastRatio('#fff','#000')` artık `contrastRatio('#ffffff','#000000')` ile özdeş
      sonuç veriyor, testle doğrulandı. (c) `themeCssBlock` docstring'i "`@theme`'in İÇİNE
      DEĞİL, YANINA kardeş kural olarak" diye düzeltildi. `packages/ui-tokens/package.json`'a
      `vitest`/`@vitest/coverage-v8@4.1.11` `devDependencies` olarak eklendi (lockfile'da
      yalnız ui-tokens importer girdisi güncellendi, yeni paket inmedi). `border` renkleri
      hem açık hem koyu temada WCAG 1.4.11 (>=3:1) hedefiyle yeniden seçildi
      (acik `#e5e2dd`->`#857f79`, koyu `#35323c`->`#78727e`) ve `contrast.test.ts`'te
      `>= 3:1` iddiasıyla kilitlendi — bu bir tasarım kararı, gerekçesi `decisions`'ta.
decisions:
  - karar: >
      `themes.acik`/`themes.koyu` yeni tek kaynak oldu; eski `colors.light`/`colors.dark`
      `themes`'ten türetilen geriye-dönük bir takma ad olarak (`colors = { light: themes.acik,
      dark: themes.koyu }`) korundu, silinmedi. Bu tur bilerek TEST EDİLMEDİ (reviewer bulgusu
      #3) — `colors.ts` içindeki modül-seviyesi atama zaten import anında çalıştığı için satır
      kapsamı etkilenmiyor, ama davranışsal bir iddia artık yok.
    gerekçe: >
      `apps/mobile/app/index.tsx` (çakışma kümem dışında, dokunulmadı) hâlâ `colors.light.bg`
      üzerinden okuyor. `colors.light === themes.acik` (aynı referans) olduğu için buna dair
      bir test tanım gereği kırılamaz; reviewer'ın "özdeşlik iddialarını sil" talimatına uyarak
      kaldırıldı. Alias'ın kaldırılması `next_suggestions`'ta W2-03'e bırakıldı (lead'in notu).
  - karar: >
      Hex yasağını "içinde hex geçen herhangi bir string" gibi genel bir regex'e ÇEVİRMEDİM;
      bunun yerine iki AYRI, sözdizimine özgü kalıp (Tailwind keyfi-değer, CSS bildirimi)
      ekledim.
    gerekçe: >
      Reviewer'ın kendisi bunu açıkça istedi (git SHA/kimlik yanlış pozitifi riski). Örnek:
      40 haneli bir git SHA'sı ya da `#anchor-id` gibi bir HTML çapa kimliği "hex'e benzeyen"
      bir alt dizi içerebilir ama `-\[...\]` ya da `:\s*#...` bağlamında GEÇMEZ. İzinli
      sondada tam olarak bunu sınadım (sahte 40 haneli SHA + `commit: <sha>` biçiminde), sıfır
      yanlış pozitif.
  - karar: >
      Açık temada `border`'ı `#e5e2dd` -> `#857f79`, koyu temada `#35323c` -> `#78727e` olarak
      güncelledim; ikisi de artık hem `bg` hem `surface` üzerinde >=3:1 (acik: 3.76/3.96, koyu:
      3.86/3.5).
    gerekçe: >
      Reviewer ölçtü: eski değerler 1.23:1 (acik) ve 1.43:1 (koyu) — WCAG 1.4.11'in "anlamlı
      UI bileşeni" eşiğinin (3:1) çok altında, ızgara çizgisi az gören bir kullanıcı için
      pratikte görünmez. Bu görevin devraldığı "Kontrast" değişmezi metinle sınırlı değil;
      3x3 tahtanın hücre sınırları KK-090/KK-091'in sözünü ettiği tahta bileşeninin temel bir
      parçası. Renk tonu her iki temada da bg/surface ile aynı sıcak/soğuk aile içinde kalacak
      şekilde (acik: kahve-gri, koyu: mor-gri) seçildi, palet kimliği bozulmadı.
    reddedilen_alternatif: >
      Yalnızca `win` için olduğu gibi `border`'ı da düşük bir eşikte (ör. 2:1) bırakmak — WCAG
      1.4.11 spesifik olarak 3:1 istiyor, daha düşük bir sayı seçmek "gerçek eşik" ilkesini
      (bulgu #1'in dersi) ben de ihlal ederdi.
gotchas:
  - >
    `apps/web/package.json` henüz `@xox/ui-tokens`'a bağımlı değil (yalnızca `@xox/db`
    listeleniyor, o dosya OPS-002'nin çakışma kümesinde). Bu yüzden web tarafında "izinli"
    sondayı gerçek bir `@xox/ui-tokens` import'uyla değil, hex-olmayan CSS değerleriyle yazdım
    — aksi hâlde tip çözümlemesi "cannot be resolved" hatası verip kuralın kendisiyle ilgisiz
    gürültü üretiyordu. `apps/web/app/**` gerçek bileşenler yazıldığında `apps/web/package.json`
    'a `@xox/ui-tokens` eklenmesi gerekecek (OPS-002 ya da ilgili web görevi).
  - >
    `CSS_DECLARATION_HEX` (`:\s*#hex\b`) TAM ÇÖZÜM DEĞİL — hâlâ atlanan bir kalıp var: hex'in
    öncesinde `:` OLMADAN serbestçe geçtiği düzyazı (`bu renk #2563eb'ye benziyor` gibi). Bunu
    da yakalamak "içinde hex geçen her string" kalıbına geri döner ve reviewer'ın reddettiği
    yanlış pozitif riskini taşır. Bilinçli bir sınır: gerçek CSS-in-JS/Tailwind kullanım
    biçimlerini kapsıyor, serbest metin içinde hex YAZMAK isteyen biri (ör. dokümantasyon
    stringi) hâlâ kaçabilir — bu, lint'in son savunma hattı değil, `themes` kaynağının kendisi
    olduğu gerçeğiyle kabul edilebilir bir sınır.
sonda_output: |
  === REVIEWER'IN 9 SATIRLIK SONDASI — 6/6 VAKA YAKALANMALI (tekrar koşuldu) ===
  $ cat apps/web/app/__probe/probe.tsx
  export default function Probe(): React.ReactElement {
    const a = '#2563eb'
    const b = `#2563eb`
    const c = '#abc'
    const d = 'bg-[#2563eb]'
    const e = '#2563eb80'
    const f = `color: #2563eb`
    return <div className={d} style={{ color: a, border: b + c + e + f }} />
  }

  $ npx eslint apps/web/app/__probe/probe.tsx
  apps/web/app/__probe/probe.tsx
    2:13  error  Literal hex renk kodu yasak (KK-084)...  no-restricted-syntax   <- a: tam hex
    3:13  error  Literal hex renk kodu yasak (KK-084)...  no-restricted-syntax   <- b: template tam hex
    4:13  error  Literal hex renk kodu yasak (KK-084)...  no-restricted-syntax   <- c: 3 haneli
    5:13  error  Literal hex renk kodu yasak (KK-084)...  no-restricted-syntax   <- d: Tailwind bg-[#..]
    6:13  error  Literal hex renk kodu yasak (KK-084)...  no-restricted-syntax   <- e: 8 haneli alfa
    7:13  error  Literal hex renk kodu yasak (KK-084)...  no-restricted-syntax   <- f: `color: #..` template
  ✖ 6 problems (6 errors, 0 warnings)
  EXIT_PROBE: 1   (6/6 vaka yakalandı)

  === İZİNLİ — YANLIŞ POZİTİF OLMAMALI ===
  $ cat apps/web/app/__probe/probe_izinli.tsx
  export default function ProbeIzinli(): React.ReactElement {
    const stil = { color: 'rebeccapurple', border: '1px solid rgb(37, 99, 235)' }
    const siniflar = 'flex items-center justify-center gap-4 rounded-lg border p-4'
    const aciklama = `oran: 16/9, konum: merkez`
    const commitSha = 'a1b2c3d4e5f6789012345678901234567890abcd'
    const bagli = `commit: ${commitSha}`
    return <div className={siniflar} style={stil} title={aciklama} data-commit={bagli}>sonda</div>
  }
  $ npx eslint apps/web/app/__probe/probe_izinli.tsx
  (çıktı yok — 0 problem)
  EXIT_IZINLI: 0

  $ cat apps/mobile/app/__probe_izinli.tsx
  import { themes } from '@xox/ui-tokens'
  const styles = StyleSheet.create({
    kutu: { backgroundColor: themes.koyu.playerO, borderColor: themes.koyu.border },
  })
  $ npx eslint apps/mobile/app/__probe_izinli.tsx
  (çıktı yok — 0 problem)
  EXIT_MOBILE_IZINLI: 0

  $ cat packages/ui-tokens/src/__probe_muaf.ts
  export const sondaMuafRenk = '#123abc'
  export const sondaMuafTailwind = 'bg-[#123abc]'
  $ npx eslint packages/ui-tokens/src/__probe_muaf.ts
  (çıktı yok — 0 problem; ui-tokens hem tam-hex hem Tailwind kalıbından MUAF)
  EXIT_UITOKENS_MUAF: 0

  (Tüm sonda dosyaları doğrulama sonrası `rm` ile silindi; `git status --short` bu görevin
  gerçek değişikliklerinin dışında hiçbir şey göstermiyor.)

  === AMPİRİK MUTASYON KANITI (bulgu #1) ===
  $ sed -i '' "s/win: '#15803d'/win: '#16a34a'/" packages/ui-tokens/src/colors.ts
  $ pnpm --filter @xox/ui-tokens test
  FAIL src/contrast.test.ts > ... > acik.win, hem bg hem surface üzerinde >= 4.5:1
  AssertionError: expected false to be true
  Test Files  1 failed | 2 passed (3)   Tests  1 failed | 37 passed (38)
  $ sed -i '' "s/win: '#16a34a'/win: '#15803d'/" packages/ui-tokens/src/colors.ts   # geri alındı
  $ pnpm --filter @xox/ui-tokens test
  Test Files  3 passed (3)   Tests  38 passed (38)
next_suggestions:
  - >
    `apps/mobile/app/index.tsx`'in `colors.light` yerine `themes.acik` kullanacak şekilde
    taşınması — o zaman geriye dönük `colors` takma adı kaldırılabilir. Lead notuna göre W2-03.
  - >
    `apps/web/package.json`'a `@xox/ui-tokens` bağımlılığı eklenmesi (OPS-002 ya da web UI
    görevi kapsamında) — böylece `themeCss()`/`themeCssBlock()` çıktısı gerçekten
    `apps/web/app/globals.css`'teki `@theme`'in YANINA yapıştırılabilir. Lead notuna göre
    UI-SKEL-001 bunu üstleniyor (globals.css elle yazılmayacak, `themeCss()` çıktısıyla
    karşılaştıran bir test olacak).
  - >
    Oyun tahtası bileşeni yazılırken X/O'nun yalnızca renkle değil şekil/kalınlıkla da ayırt
    edilmesi gerekiyor (renk körlüğü, ölçülen ~1.22:1 doğrudan renk farkı) — bu token paketi
    renk kısmını çözdü, şekil/stroke kararı UI-SKEL-001'e bırakıldı (lead zaten kritere koydu).
```
