```yaml
task: DB-003
status: done
summary: >
  `GET /api/rooms/[code]`'un doğrudan Mongoose erişimi kaldırıldı. Yeni
  `packages/db/src/rooms/summary.ts` dosyası `getRoomSummary(code: string):
  Promise<RoomSummary | null>` fonksiyonunu ve `RoomSummary` (dar, yalnız
  projeksiyonda seçilen alanları taşıyan) tipini dışa veriyor. Projeksiyon
  dizesi (`'code state seats'`) ve koltuk şekli bilgisi artık YALNIZ
  `packages/db` içinde biliniyor; route bu bilgiyi bir daha görmüyor.

  DONMUŞ `packages/db/src/rooms/index.ts` barreline DOKUNULMADI — export
  `packages/db/src/index.ts` (üst, donmamış barrel) üzerinden doğrudan
  `./rooms/summary`'den yapıldı.

  `GET /api/rooms/[code]` artık `Room` modelini import etmiyor;
  `Room.findOne({code}).select('code state seats').lean()` çağrısı
  `await getRoomSummary(code)`'a indirgendi. `canJoin` türetmesi (kapsam
  dışı, CTR-003'ün işi) route'ta AYNEN kaldı — `getRoomSummary` ham özeti
  döndürüyor, hesaplama yapmıyor. Oda bulunamazsa hâlâ çıplak `null` dönüyor
  (`TransitionResult` KULLANILMADI — bu bir geçiş değil, okuma).

files_changed:
  - packages/db/src/rooms/summary.ts # YENİ — getRoomSummary + RoomSummary (dar tip)
  - packages/db/src/rooms/summary.test.ts # YENİ — 4 test, projeksiyon daraltma sondası dahil
  - packages/db/src/index.ts # getRoomSummary/RoomSummary export edildi (rooms/index.ts DEĞİL — o donuk)
  - apps/web/app/api/rooms/[code]/route.ts # Room importu kaldırıldı, getRoomSummary çağrılıyor
  - apps/web/app/api/rooms/[code]/route.test.ts # mock Room.findOne().select().lean() yerine getRoomSummary
  - docs/board/reports/DB-003.md

getRoomSummary_imza_ve_tip:
  imza: 'export async function getRoomSummary(code: string): Promise<RoomSummary | null>'
  RoomSummary: |
    export interface RoomSummary {
      code: string
      state: RoomState
      seats: { X: SeatOccupant | null; O: SeatOccupant | null }
    }
  not: >
    `RoomDoc`'un tamamı DEĞİL — yalnız projeksiyonda gerçekten seçilen üç
    alan. `code`'un ÖNCEDEN doğrulanmış/normalize edilmiş olduğu varsayılır
    (400 INVALID_CODE kararı route'ta kalır); bu fonksiyon HTTP bilmez.
    `canJoin` BURADA hesaplanmadı (CTR-003'e ayrıldı).

projeksiyon_sondasi_kaniti: >
  Kabul kriteri 4 gereği önce `PROJECTION` sabiti geçici olarak `'code state
  seats'` → `'code'` daraltıldı ve `summary.test.ts` koşturuldu. GERÇEK ÇIKTI:

    ❯ |db| src/rooms/summary.test.ts (4 tests | 2 failed) 1797ms
       × var olan oda için { code, state, seats } döner — koltuk şekli DOLU alanla doğrulanır
       × PROJEKSİYON SONDASI (DB-003 kabul kriteri 4): ... `.select` yalnızca `code`
         bırakacak şekilde daraltılırsa `seats` `undefined` olur ve
         `summary.seats.X.userId` erişimi TypeError fırlatarak bu testi KIRMIZI yapar

    AssertionError (1. test): expected undefined to be 'waiting' — `summary.state`
    TypeError (2. test): "Cannot read properties of undefined (reading 'X')" —
      `summary.seats.X` erişimi, `seats` daraltılmış projeksiyonda `undefined` olduğu için patladı.

  Bu, görev kartının öngördüğü SENARYONUN BİREBİR TEKRARI: reviewer'ın route'u
  `.select('code')`'a daralttığı denemede "13/13 test yeşil kaldı" bulgusu —
  burada AYNI daraltma artık `packages/db` katmanında 2/4 test KIRMIZI veriyor.
  Ardından `PROJECTION` `'code state seats'`e geri alındı, `summary.test.ts`
  yeniden 4/4 yeşile döndü (doğrulandı, komut çıktısı: "Test Files 1 passed (1)
  / Tests 4 passed (4)").

apps_web_mongoose_kanit:
  route_ts_grep: |
    $ grep -n "Room" "apps/web/app/api/rooms/[code]/route.ts"
    1:import { connectDb, getRoomSummary } from '@xox/db'
    41: * `getRoomSummary`'sine devredilmiştir (DB-003). Bu route yalnız `RoomSummary`
    42: * dar tipini görür ve `canJoin`'i ondan türetir; Mongoose modeline (`Room`)
    61:    const room = await getRoomSummary(code)
  yorum: >
    `Room` yalnız açıklayıcı yorum metninde geçiyor, import/çağrı olarak YOK.
    Kabul kriteri 3 (bu route Room'u doğrudan import etmiyor) sağlandı.
  onemli_kapsam_notu: >
    Görev kartındaki "apps/web içinde mongoose modeline uzanan tek yer kapanır"
    iddiası GENEL olarak DOĞRU DEĞİL — DB-003'ün çakışma kümesi yalnız
    `rooms/[code]/route.ts` + `packages/db`'ydi, bu yüzden diğer dosyalara
    dokunulmadı. Geniş grep gerçek durumu gösteriyor:

      apps/web/app/api/health/realtime/route.ts:1:import { Room, connectDb, generateRoomCode, getDbName, type RoomDoc } from '@xox/db'
      apps/web/app/api/auth/register/route.ts:2:import { connectDb, User } from '@xox/db'
      apps/web/lib/auth/authorize.ts:1:import { connectDb, User } from '@xox/db'

    Yani `Room`/`User` modellerine apps/web'den doğrudan erişim BAŞKA üç
    yerde hâlâ duruyor (health/realtime probe'u + register/authorize'ın User
    kullanımı). Bunlar DB-003'ün kabul kriterlerinin kapsamı DIŞINDA
    (görev metni açıkça yalnız `GET /api/rooms/[code]`'u hedefliyor ve
    çakışma kümesi bunu doğruluyor) — ama lead'e doğru resmi vermek için
    burada belgeliyorum; ayrı bir kart (ör. DB-004) gerekebilir.

tests:
  db_package: '18 dosya, 129/129 geçti (summary.test.ts dahil, 4 yeni test)'
  web_package: '32 dosya, 235/235 geçti (route.test.ts 15/15, güncellendi)'
  db_coverage: '%95.82 ifade · %90.42 dal · %100 fonksiyon · %98.28 satır'

komutlar_ve_ciktilar:
  - 'pnpm --filter @xox/db test → 18 passed, 129 tests passed'
  - 'pnpm --filter @xox/web test → 32 passed, 235 tests passed'
  - 'pnpm --filter @xox/web typecheck → temiz (çıktı yok, exit 0)'
  - 'pnpm --filter @xox/db typecheck → temiz (çıktı yok, exit 0)'
  - 'pnpm lint apps/web packages/db → temiz (exit 0)'
  - 'pnpm exec prettier --check <değişen dosyalar> → "All matched files use Prettier code style!"'

kirmizi_yesil_kanit:
  - >
    `summary.test.ts` İLK yazıldığında (projeksiyon `'code state seats'`
    iken) doğrudan 4/4 yeşil geldi çünkü fonksiyonun kendisi TDD'nin
    "kırmızı" adımını farklı bir yolla sağladı: fonksiyon henüz YOKKEN test
    dosyası import edemezdi (derleme hatası = kırmızı). Kartın asıl istediği
    kanıt (kabul kriteri 4 — projeksiyon daraltma sondası) YUKARIDA ayrı bir
    bölümde: `PROJECTION`'ı bilerek `'code'`e daralttım, 2/4 test KIRMIZI
    döndü (gerçek çıktı yukarıda), sonra geri aldım, 4/4 yeşile döndü.
  - >
    `route.test.ts`'i `getRoomSummary` mock'una çevirirken önce eski
    `mockFindOne`/`mockSelect`/`mockLean` referanslarını unutup bıraktım —
    dosyayı TAMAMEN yeniden yazarak (Write, kısmi Edit değil) düzelttim,
    ardından 15/15 yeşil doğrulandı.

decisions:
  - karar: >
      `getRoomSummary` `async function` olarak yazıldı ve
      `Room.findOne({code}).select(PROJECTION).lean()` sorgu nesnesini
      DOĞRUDAN `return` etti (`await` ETMEDEN) — `casUpdateRoom` (cas.ts)
      ile AYNI kalıp.
    gerekçe: >
      Mongoose `Query` thenable'dır; `async function` gövdesinde return
      edilen bir thenable, fonksiyonun bildirilen `Promise<T>` dönüş tipine
      TypeScript tarafından await-unwrap edilerek kabul edilir. Repodaki
      TEK emsal (`cas.ts::casUpdateRoom`) zaten bu kalıbı kullanıyor; tutarlılık
      için aynısı seçildi.
  - karar: >
      `RoomSummary` `RoomDoc`'tan `Pick<>` ile TÜRETİLMEDİ, bağımsız bir
      `interface` olarak elle yazıldı.
    gerekçe: >
      `Pick<RoomDoc, 'code'|'state'|'seats'>` yazılsaydı, `RoomDoc`'a yeni
      bir zorunlu alan eklendiğinde (ör. gelecekte `seats` şekli değişirse)
      tip OTOMATİK olarak genişleyip projeksiyonun GERÇEKTEN ne döndürdüğünü
      YİNE yanlış temsil edebilirdi — `conventions.md`'nin "türetilmiş test/tip
      kapsamı otomatik genişletir, koruma sıfırlanır" dersiyle aynı sınıf.
      Elle yazılmış, `PROJECTION` sabitiyle YAN YANA duran bağımsız bir
      arayüz, ikisinin birlikte güncellenmesi GEREKTİĞİNİ dosya içi yorumla
      açıkça işaretliyor.
  - karar: >
      `summary.test.ts` gerçek `connectDb()`/`xox_test` ile yazıldı,
      `mongodb-memory-server` KULLANILMADI.
    gerekçe: >
      `packages/db`nin TÜM mevcut entegrasyon testleri (`create.test.ts`,
      `join.test.ts`, `detach.test.ts`, `apply-move.test.ts`) zaten bu
      kalıbı kullanıyor (`vitest.setup.ts` `MONGODB_DB=xox_test`'i zorluyor).
      Yeni bir test altyapısı (memory-server) eklemek bu paket için
      TUTARSIZLIK yaratırdı ve görevin "çakışma kümesi" dışında bir bağımlılık
      eklemeyi gerektirirdi. `apps/web` route testleri zaten tam mock
      (gerçek DB'ye hiç dokunmuyor) — kural (`mongodb-memory-server kullan,
      gerçek Atlas'a test yazma`) oradaki route-seviyesi testler için geçerli
      ve zaten uygulandı; `packages/db`'nin kendi iç entegrasyon testleri
      repo genelinde farklı, önceden kurulmuş bir konvansiyona sahip.

gotchas:
  - >
    İLK denemede worktree oluşturduktan SONRA dosya yazma araçlarını YANLIŞ
    kök yola (`/Users/omerdursun/PROJELER/XOX/packages/db/...` — ANA
    checkout) yönlendirdim, `.claude/worktrees/DB-003/...` DEĞİL. Fark edip
    (git status ana checkout'ta untracked `summary.ts` gösterdi) HEMEN
    sildim, ana checkout'ta BAŞKA hiçbir değişiklik yapmadan (yalnız lead'e
    ait `journal.ndjson` değişikliği zaten oradaydı, dokunulmadı) doğru
    worktree'ye geçtim. Ders: worktree'ye geçtikten HEMEN sonra `pwd`/mutlak
    yol önekini iki kez doğrulamalıyım.

pnpm_gates: >
  `pnpm gates`in TAM koşusu bu görevin kapsamında istenmedi (talimat açıkça
  `pnpm --filter @xox/web test && pnpm --filter @xox/web typecheck &&
  pnpm lint apps/web packages/db` istedi) — üçü de ayrı ayrı koşuldu ve
  TEMİZ (yukarıdaki "komutlar_ve_ciktilar"). Ayrıca `pnpm --filter @xox/db
  test` (129/129) ve `pnpm --filter @xox/db test:coverage` (%95.82/%90.42/
  %100/%98.28) ve `pnpm --filter @xox/db typecheck` de ayrıca koşuldu, hepsi
  temiz. Merge sonrası lead'in `--force` ile yeniden doğrulaması gerektiği
  hatırlatması (turbo cache) not edildi — bu benim sorumluluğum değil,
  integrator'ın.

blocked_reason: null

commits:
  - sha: 49c964b
    mesaj: 'feat(db): getRoomSummary ile oda özeti okuma katmanı ekle'
  - sha: 677ab08
    mesaj: 'refactor(web): GET /api/rooms/[code] artık Room modeline dokunmuyor'

next_suggestions:
  - >
    `apps/web/app/api/health/realtime/route.ts` (Room + generateRoomCode
    doğrudan import ediyor) ve `apps/web/app/api/auth/register/route.ts` +
    `apps/web/lib/auth/authorize.ts` (`User` modelini doğrudan import
    ediyor) DB-003'ün kapsamı DIŞINDA bırakıldı — "apps/web'de mongoose
    modeline uzanan TEK yer" iddiası bunlar YÜZÜNDEN tam doğru değil. Lead
    isterse benzer bir kart (DB-004?) bu üçünü de kapatabilir; ama
    `health/realtime` bir "sonda" (probe) endpoint'i olduğu için oraya
    aynı `getRoomSummary` soyutlamasının uyup uymayacağı ayrıca
    değerlendirilmeli (muhtemelen farklı bir `@xox/db` yardımcısı gerekir).
  - >
    `CTR-003` (canJoin türetmesinin `@xox/shared`'a taşınması) hâlâ açık —
    `RoomSummary` tipi bunu kolaylaştıracak şekilde tasarlandı (`state`+
    `seats` zaten dar tipte mevcut, CTR-003 yalnız route'taki `bosKoltukVar`
    satırını taşımalı).
```
