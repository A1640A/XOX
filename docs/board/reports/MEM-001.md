```yaml
task: MEM-001
status: done
summary: >
  Dalga 1-5 hafızası damıtıldı. gotchas.md'nin başına 5 isimlendirilmiş tekrar-eden-örüntü
  bölümü eklendi (76 madde artık 5 kategoriye indirgenip taranabiliyor); 5 duplike/geçersiz
  madde birleştirilip silindi (en kritiği: "boşluksuz takas" önerisi canlıda yanlış çıktığı
  hâlde ayrı bir maddede hâlâ "yapılacak" olarak duruyordu — düzeltilip tek doğru maddede
  birleştirildi). decisions.md'ye bu geceden 5 yeni mimari/güvenlik kararı (reddedilen
  alternatifleriyle) eklendi; conventions.md'ye 4 yeni konvansiyon + 1 düzeltme (import type
  disiplini, gerçek bir hataya yol açmıştı); api-contract.md'ye OPS-003'ün iki yeni uç noktası
  eklendi. CLAUDE.md'ye 3 satırlık "ölümcül ders" işaretçisi eklendi (93 satır, bütçe 200).
added: { gotchas: 1, decisions: 5, conventions: 4 }
pruned: 5
patterns_named:
  [
    'Kural yazılmış ama ateşlenmiyor',
    'Test yeşil ama hiçbir şey doğrulamıyor',
    'Tip doğru ama çalışma zamanı yalan söylüyor',
    'Mekanizma var ama kimse çağırmıyor',
    'Vitest gizliyor, üretim kırık (ve tersi de olur)',
  ]
claude_md_lines: 93
```

## Ayrıntı

### 1. Tekrar eden örüntüler (gotchas.md başlığı)

76 maddenin (73 hedeflenen + gece boyunca eklenen birkaç yeni madde) çoğunu kapsayan 5 örüntü
isimlendirildi ve her biri altındaki somut madde başlıklarına işaret edildi — artık bir agent
70+ maddeyi baştan taramak yerine önce 5 satırlık özeti okuyup ilgili sınıfa inebiliyor:

1. **Kural yazılmış ama ateşlenmiyor** — ESLint çözümleyicisi (en pahalı ders: `boundaries` +
   `import-x/no-cycle` aylarca hiçbir şey korumadı), pnpm symlink + `boundaries`,
   `projectService`+kapsam-dışı-dosya, ESLint `.css` ayrıştırmıyor, Vercel `functions` anahtarı
   doğrulanmıyor, Claude Code hook'larına mutlak yol (aynı hastalık farklı yüzey), `Stop` hook'u.
2. **Test yeşil ama hiçbir şey doğrulamıyor** — kaynak metni okuyan test, kendine-referanslı
   beklenti, özdeşlik iddiası, sabitten türetilmiş beklenti, seçim-gerekçesinden farklı eşik,
   zod'un mutlu-yol testi, hatayı kilitleyen test, tam mock'lanmış bağımlılık.
3. **Tip doğru ama çalışma zamanı yalan söylüyor** — `Model.watch().resumeToken`, Auth.js `jwt`
   callback'inin `user`'ı, `import.meta.dirname`, mongoose model cast'i.
4. **Mekanizma var ama kimse çağırmıyor** — `ensureIndexes()` yalnız testten çağrılıyordu
   (OPS-003'ün kendi acceptance-1 bulgusu), lefthook/unrs-resolver postinstall onaylanmazsa
   git hook'ları hiç kurulmaz.
5. **Vitest gizliyor, üretim kırık (ve tersi de olur)** — mongoose CJS+tsx ESM (vitest gizler,
   CLI kırık) ile `next-auth`/`jose` (vitest'te patlıyor, gerçek Node'da sorun yok) aynı köke
   ("test ortamı = üretim" varsayımı) bağlandı.

Hiçbir yeni madde uydurulmadı; hepsi zaten dosyada var olan olaylardan gruplandı.

### 2. Budama (5 madde, hepsi gerçek gerekçeyle)

- **En kritik:** "Mongo AYNI key pattern'ine ikinci indeksi reddeder" (25 Ağu, "boşluksuz takas
  ÇALIŞMAZ" kanıtı) ile "Düşür-sonra-kur penceresi geri alınamaz" (24 Ağu, hâlâ "boşluksuz takas
  yap" diye ÖNERİYORDU) birleştirildi. İkinci madde birinciyle DOĞRUDAN ÇELİŞİYORDU — biri
  "çalışmaz" derken diğeri hâlâ o yaklaşımı tavsiye ediyordu. Tek, düzeltilmiş maddede birleşti
  (SEC-003'ün gerçek tasarımına işaret ediyor); decisions.md'ye de aynı düzeltme karar olarak
  eklendi.
- Auth.js `jwt` callback kısa maddesi (25 Ağu) ile aynı konunun kapsamlı/kanıtlı hâli
  (`session.js:28`, `sessionStore.clean()` ayrıntılı) birleştirildi — kısa olan silindi.
- İki "Stop hook" maddesi (genel 3-parçalı ders + `in_wave` özelinde tekrar) birleştirildi;
  `in_wave` maddesinin "kuru koşu bunu yakalayamaz" notu ana maddeye taşındı, ayrı madde silindi.
- İki `readFileSync`+test maddesi (AUTH-001'in kısa özeti + detaylı matcher-kısaltma analizi)
  tek maddede birleştirildi; her iki mutasyon örneği (token.sub + matcher.slice) korundu.
- İki `import.meta.dirname`/`@types/node` maddesi (temiz kurulum vakası + lockfile-sonrası-merge
  vakası) tek maddede birleştirildi — aynı kökün iki farklı oluşu olarak işaretlendi.

Hiçbir ölçülmüş sayı (p95, kontrast oranı, argon2/mongo hata kodları) silinmedi.

### 3. decisions.md — 5 yeni karar (hepsi reddedilen alternatifiyle)

ARCH-001 (taşıma durumu tipi `game-core`'a sızmaz) · OPS-003 CI/CLI vs HTTP (sır ağdan hiç
geçmez) · OPS-003 SEC-003 indeks çakışma çözümü (mükerrer-tarama+telafi, "boşluksuz takas"ın
YANLIŞ olduğu kanıtlandıktan sonra) · SPEC-001 30 sn grace + oturum devralma · OPS-002
`-L/--local` bayrağı.

### 4. conventions.md — 4 yeni + 1 düzeltme

Mongoose model kaydı kalıbı (5 model dosyasında birebir aynı satır) · Auth.js ince-tel +
next-auth'suz iş mantığı ayrımı (authorize.ts/session-callback.ts emsali) · iki katmanlı
şema/liste testi kuralı (6+ olayın ortak dersi) · `pnpm why` tek-kopya doğrulaması. **Düzeltme:**
"tip importları `import { type X }` biçiminde satır içi" kuralı SAF paket importları için
YANLIŞ olduğu kanıtlandı (`verbatimModuleSyntax` altında modül grafiğine girmeye devam ediyor) —
düzeltilmiş ayrım eklendi.

### 5. api-contract.md

OPS-003'ün iki yeni ucu tabloya eklendi: `POST /api/admin/migrate` (production-only runbook,
`timingSafeEqual` + `?db=` pozitif doğrulama) ve `GET /api/health/realtime` (RT-PROBE-001
gecikme sondası, production'da 404). "Planlanan yüzey" özetindeki `/api/health/realtime`
girdisinin aslında zaten UYGULANMIŞ olduğu, Dalga 0'da yeniden yazılmaması gerektiği notu
eklendi.

### 6. CLAUDE.md

93 satır (bütçe 200, hâlâ rahat). 3 satırlık "Bu geceden 3 ölümcül ders" işaretçisi eklendi —
ESLint çözümleyicisi/kural 4 denetimi, Auth.js `jwt` callback tanımlama, next-auth Vitest'te
çalışmaz. Her satır ayrıntıya değil ilgili dosyaya işaret ediyor, kopyalamıyor.
