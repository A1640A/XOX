```yaml
task: W3-03
status: done
summary: >
  Emoji tepkileri (palet + balon + yayın), emojiye ÖZEL hız sınırı ve davet
  linki/iniş sayfası kapandı. Kartın sekiz kriterinin tamamı **davranışsal
  testle** kilitlendi; hız sınırı ayrıca **canlı bir akış sondasıyla** (sahte
  saat, gerçek `createRoomSession`, gerçek çerçeveler) gösterildi —
  yapılandırma değil, ateşlenen kural.

  **Kartın en kritik parçası (KK-124) için üç ayrı kanıt katmanı var:**
  (a) 14 birim/oturum testi, (b) 8 çerçevelik canlı akış çıktısı (6.-8.
  çerçeveler `RATE_LIMITED`, ilk 5'i otoriteye ULAŞTI, bağlantı KAPANMADI,
  pencere kayınca meşru emoji YİNE geçti), (c) üç mutasyon sondası
  (kapıyı devre dışı bırak / off-by-one / kayan pencereyi sabit pencereye
  çevir) — üçü de kırmızı üretti.

  **Genel sınırla (WS-001) etkileşim ölçüldü ve teste yazıldı:** 5 < 20
  olduğu için emoji seli DAİMA önce emoji kapısına çarpar; o kapı bağlantıyı
  kapatmaz. Israrcı istemci reddedilen çerçeveleriyle genel pencereyi de
  doldurur (genel sayaç kabul/ret ayırmaz) ve 40'ta 4400 alır — kademelenme
  kasıtlı. Meşru kullanım (5 emoji + nabız + hamle = 7 çerçeve) iki sınıra da
  değmiyor; bu bir tezle değil, aynı pencerede 7 çerçeve gönderen bir testle
  gösterildi.

  **`RoomScreen.tsx` AÇILMADI** (mekanik olarak doğrulandı, aşağıda).
  `connection.ts`, `handlers/index.ts`, `packages/shared/**`,
  `packages/game-core/**`, `packages/db/src/models/**` de dokunulmadı.

files_changed:
  - packages/db/src/rooms/emoji.ts # pushEmoji gövdesi — version ARTIRMAZ
  - packages/db/src/rooms/emoji.test.ts # YENİ — 7 test, gerçek Atlas xox_test
  - packages/db/src/rooms/skeletons.test.ts # iskelet kilidi kalktı, barrel tamlığına dönüştü
  - apps/web/lib/realtime/handlers/emoji.ts # handler + emojiye özel kayan pencere
  - apps/web/lib/realtime/handlers/emoji.test.ts # YENİ — 14 test (11'i GERÇEK oturum yolundan)
  - apps/web/lib/realtime/handlers/index.test.ts # iskelet kilidi kalktı; R1 sonda listesi türetilmiş olmaktan çıkarıldı
  - apps/web/components/room/EmojiTray.tsx # 8'li palet + balon
  - apps/web/components/room/EmojiTray.test.tsx # YENİ — 7 test
  - apps/web/components/room/InviteLink.tsx # <origin>/davet/<KOD>
  - apps/web/components/room/InviteLink.test.tsx # iskelet testi gerçek davranışla değişti — 4 test
  - apps/web/app/davet/invite-target.ts # YENİ — next-auth'suz iş mantığı
  - apps/web/app/davet/invite-target.test.ts # YENİ — 7 test (biri middleware muafiyeti)
  - apps/web/app/davet/[kod]/page.tsx # YENİ — iniş sayfası (ince tel)
  - apps/web/app/davet/[kod]/page.test.tsx # YENİ — 5 test
  - apps/web/app/davet/oturumsuz-akis.test.tsx # YENİ — 3 test, davet→giriş→oda UÇTAN UCA
  - docs/board/reports/W3-03.md

tests:
  {
    added: 47,
    passing: '668 (@xox/web · 66 dosya) · 177 (@xox/db · 24 dosya) · 343 (@xox/shared) · 91 (@xox/game-core) · 57 (@xox/ui-tokens)',
    coverage: '@xox/web %94.34 ifade · %89.03 dal · %93.33 fonksiyon · %96.69 satır (eşik 70/65/70/70) · @xox/db %95.19/%89.55/%98.52/%97.57 (eşik 90/85/90/90)',
    mutation: '8 elle sonda, hepsi `diff -q` ile UYGULANDIĞI doğrulandı, hepsi kırmızı üretti, hepsi geri alındı (`git status --porcelain` BOŞ)',
    gates: 'pnpm gates EXIT=0 (typecheck + lint + format:check + test:coverage + knip)',
    build: 'pnpm --filter @xox/web build ✓ — /davet/[kod] rotası ƒ (dynamic) olarak kayıtlı',
  }

commits:
  - e6ea113 # feat(db): pushEmoji — version artırmadan lastEmoji yazar (KK-122)
  - 66e23b1 # feat(web): chat:emoji handler'ı + emojiye özel kayan pencere hız sınırı (KK-124)
  - 3180ca5 # feat(web): EmojiTray 8'li palet + balon, InviteLink davet linki (KK-120/122)
  - c5ca211 # feat(web): /davet/[kod] iniş sayfası — oturumsuz kullanıcıda kod kaybolmaz (KK-121)
  - b938a8d # test(db): skeletons.test.ts iskelet kilidi kalktı — barrel tamamlandı

hiz_siniri_canli_sonda:
  neden: >
    "Kural yazılmış ama ateşlenmiyor" örüntüsüne karşı. Yapılandırmayı
    (EMOJI_RATE_LIMIT = {count:5, windowMs:10_000}) göstermek KANIT DEĞİL;
    aşağıdaki çıktı GERÇEK `createRoomSession` üzerinden, gerçek
    `clientMessageSchema` ayrıştırmasıyla, gerçek `checkRate` genel kapısıyla
    ve sahte bir saatle üretildi. Sonda dosyası çalıştırıldıktan sonra
    SİLİNDİ (repoda yok; `git status --porcelain` boş).
  cikti: |
    === AŞIM: 10 sn içinde 8 emoji çerçevesi ===
    #1 (+0ms)    → SESSİZ (kabul, yankı change stream'den)
    #2 (+200ms)  → SESSİZ (kabul, yankı change stream'den)
    #3 (+400ms)  → SESSİZ (kabul, yankı change stream'den)
    #4 (+600ms)  → SESSİZ (kabul, yankı change stream'den)
    #5 (+800ms)  → SESSİZ (kabul, yankı change stream'den)
    #6 (+1000ms) → {"type":"error","code":"RATE_LIMITED","message":"Biraz yavaş — çok hızlı emoji gönderiyorsun."}
    #7 (+1200ms) → {"type":"error","code":"RATE_LIMITED","message":"Biraz yavaş — çok hızlı emoji gönderiyorsun."}
    #8 (+1400ms) → {"type":"error","code":"RATE_LIMITED","message":"Biraz yavaş — çok hızlı emoji gönderiyorsun."}
    otoriteye ULAŞAN çağrılar:
      pushEmoji(ABC234, X, 🔥) @ +0ms
      pushEmoji(ABC234, X, 🔥) @ +200ms
      pushEmoji(ABC234, X, 🔥) @ +400ms
      pushEmoji(ABC234, X, 🔥) @ +600ms
      pushEmoji(ABC234, X, 🔥) @ +800ms
    kapanışlar: []

    === PENCERE KAYDI: +10 sn sonra MEŞRU emoji ===
    meşru emoji (+20000ms) → SESSİZ (KABUL EDİLDİ)
    otoriteye ULAŞAN son çağrı: pushEmoji(ABC234, X, 👏) @ +20000ms
    toplam otorite çağrısı: 6

    === EŞİK ALTI: yeni pencerede 4 emoji ===
    #1 → SESSİZ (KABUL)
    #2 → SESSİZ (KABUL)
    #3 → SESSİZ (KABUL)
    #4 → SESSİZ (KABUL)
    toplam otorite çağrısı: 10
    toplam kapanış: 0 []
  nasil_okunur: >
    "SESSİZ" burada R1'in kendisidir: kabul edilen emojide istemciye TEK BAYT
    gitmiyor, yankı change stream'den geliyor (`resign.ts` ile aynı gerekçe).
    Reddedilen çerçeve ise ANINDA `error` alıyor. "Otoriteye ULAŞAN çağrılar"
    listesi "yokluk" iddiasının DOLU eşi: 8 çerçeveden yalnız 5'i
    `pushEmoji`ye indi, fazlası veritabanına HİÇ ulaşmadı.

genel_sinirla_etkilesim:
  iki_sinir: |
    GENEL (WS-001, connection.ts `checkRate`) : 10 sn / 20 çerçeve → RATE_LIMITED
                                                10 sn / 40 çerçeve → close 4400
                                                KABUL+RET hepsini sayar
    EMOJİ  (W3-03, handlers/emoji.ts)         : 10 sn / 5 emoji    → RATE_LIMITED
                                                bağlantıyı KAPATMAZ
                                                yalnız KABUL EDİLENİ sayar
  neden_ikincisi_gerekli: >
    Genel bütçe oyun temposuna göre ölçülmüştü (seyrek hamle + 25 sn'de bir
    nabız). Bir saldırgan onun ALTINDA kalarak 10 saniyede 19 emoji
    gönderebilir; her biri gerçek bir Atlas yazması + gerçek bir change stream
    olayı üretir. Instance başına TEK stream olduğu için (ADR-0002) fatura
    saldırganın odasına değil, o instance'taki BÜTÜN odalara çıkar — WS-001'in
    `join` selinde bulduğu hatanın tıpatıp aynısı.
  cakisiyor_mu: >
    HAYIR. 5 < 20 olduğu için emoji seli DAİMA önce emoji kapısına çarpar;
    genel kapı emoji için pratikte hiç devreye girmez. Ters yönde de meşru
    kullanım kesilmiyor: 5 emoji + 1 nabız + 1 hamle = 7 çerçeve, genel
    bütçenin (20) üçte birinden az. Nabız zaten 25 sn'de bir
    (`WS_HEARTBEAT_MS`), yani 10 sn'lik pencereye en fazla bir kez düşer.
  kademelenme: >
    Emoji kapısı bağlantıyı kapatmaz — ısrar eden istemcinin RED çerçeveleri
    genel pencerede sayılmaya devam ettiği için 20'de genel `RATE_LIMITED`,
    40'ta 4400 gelir. Yani "nazik uyarı → sert kapanış" zinciri korunuyor,
    ama meşru bir kullanıcı asla zincire girmiyor.
  kanit_testleri: |
    ✓ GENEL sınırla çakışmaz: 5 emoji + nabız + hamle aynı pencerede temiz geçer
    ✓ emoji sınırı GENEL sınırdan ÖNCE ateşlenir (5 < 20) — bağlantı yaşar
      (19 emoji çerçevesi: genel bütçe hâlâ aşılmadı, emoji bütçesi çoktan;
       pushEmoji tam 5 kez çağrıldı — genel kapı ateşleseydi 19 olurdu)
    ✓ sınırı aşmak bağlantıyı KAPATMAZ; oturum çalışmaya devam eder
      (aşımdan sonra gönderilen `ping` normal `pong` alıyor)
  bilincli_sapma: >
    Genel sayaç reddedilen çerçeveyi de pencereye yazar; emoji sayacı YAZMAZ.
    Sebep: yazsaydı ısrarcı bir istemcinin penceresi kendi retleriyle sürekli
    dolu kalır ve kayan pencere KALICI CEZAYA dönüşürdü — 10 sn sonra meşru
    bir emoji atmak imkânsız olurdu. Bu ayrım `pencere kayınca meşru emoji
    YİNE geçer` testiyle ve 3 numaralı mutasyon sondasıyla kilitli.

davet_linki_oturumsuz_akis:
  zincir: '/davet/<KOD> → (oturum yok) → /giris?donus=%2Foda%2F<KOD> → giriş → /oda/<KOD>'
  neden_middleware_degil: >
    `/oda/:path*` middleware korumasında; davet linki DOĞRUDAN `/oda/<KOD>`e
    işaret etseydi oturumsuz kullanıcı yine `/giris`e düşerdi ama `/davet/*`
    ayrı bir yüzey olarak kalmıyor olurdu. `/davet` matcher'da YOK (AUTH-001)
    ve bu bir varsayım değil, test: `PROTECTED_ROUTE_PREFIXES` ve
    `MIDDLEWARE_MATCHER` içinde `/davet` yokluğu + `/oda` VARLIĞI birlikte
    iddia ediliyor.
  ucta_uca_sonda: >
    `app/davet/oturumsuz-akis.test.tsx` GERÇEK `GirisForm`u render ediyor:
    `inviteRedirect(code,false)` çıktısının sorgu dizesi `useSearchParams`e
    besleniyor, form gönderiliyor ve `router.push`un `/oda/ABC234` aldığı
    iddia ediliyor. Bu ara yüz (biri `encodeURIComponent` ile yazıyor, diğeri
    çözülmüş hâlini okuyor) iki dosyanın KENDİ testlerinin göremeyeceği yer:
    biri kodlamayı bıraksa ya da iki kez kodlasa ikisi de yeşil kalır,
    kullanıcı ise oda kodunu kaybederdi.
  test_ciktisi: |
    ✓ /davet middleware korumasının DIŞINDA — KK-121'in ön koşulu > korunan önek listesinde ve matcher'da /davet YOKTUR
    ✓ normalizeInviteCode — küçük harfli ve boşluklu kodu normalleştirir
    ✓ normalizeInviteCode — şemaya uymayan kodu null döner
    ✓ normalizeInviteCode — yol ayracı içeren bir kod ASLA geçmez (yol enjeksiyonu kapalı)
    ✓ inviteRedirect — girişliyse doğrudan odaya yollar
    ✓ inviteRedirect — girişsizse /giris'e yollar ve oda yolunu donus'ta TAŞIR
    ✓ inviteRedirect — donus'taki değer çözüldüğünde tam olarak oda yoludur (kod kaybolmaz)
    ✓ /davet/[kod] — girişli kullanıcıyı doğrudan odaya yönlendirir
    ✓ /davet/[kod] — oturumsuz kullanıcıyı /giris'e yollar ve oda kodunu donus'ta KORUR
    ✓ /davet/[kod] — oturum var ama user yoksa oturumsuz sayılır
    ✓ /davet/[kod] — geçersiz kod TEK bir hata-mesaji düğümü gösterir ve YÖNLENDİRMEZ
    ✓ /davet/[kod] — geçersiz kodda oturum HİÇ okunmaz (gereksiz auth() çağrısı yok)
    ✓ davet linki → giriş → oda — oda kodu giriş turunda KAYBOLMAZ
    ✓ davet linki → giriş → oda — başarısız girişte odaya YÖNLENDİRİLMEZ ama hata GÖRÜNÜR
    ✓ davet linki → giriş → oda — davet zinciri açık yönlendirmeye dönüşemez — donus DAİMA göreli
    Test Files  3 passed (3) · Tests  15 passed (15)
  tek_hata_bolgesi: >
    UI-002'nin ana sayfada bulduğu "iki `hata-mesaji` düğümü" tuzağı burada
    ÜRETİLMEDİ ve bu bir söz değil, iddia: geçersiz kod testi
    `getAllByTestId('hata-mesaji')` ile SAYIYOR ve `toHaveLength(1)` diyor.
    Sayfanın geçersiz-kod dalında tek bir `ErrorBanner` var, geçerli kod
    dalında hiç render yok (yönlendirme).
  acik_yonlendirme: >
    `donus` değeri kullanıcı girdisinden TÜREMİYOR: kod önce `roomCodeSchema`
    süzgecinden geçiyor (`//evil.com`, `https:`, `../../x` hepsi `null`), yani
    üretilebilecek tek biçim `/oda/<6 karakter>`. `GirisForm`un
    `safeRedirectTarget` sözleşmesi (göreli + protokol-göreli değil) ayrıca
    testte doğrulanıyor.

roomscreen_dokunulmadi_kaniti:
  komut: 'git diff --quiet main...HEAD -- <dosya>'
  cikti: |
    DOKUNULMADI  apps/web/components/room/RoomScreen.tsx
    DOKUNULMADI  apps/web/components/room/FriendAddButton.tsx     (W3-04 paralel)
    DOKUNULMADI  apps/web/lib/realtime/connection.ts              (kart: AÇILMAZ)
    DOKUNULMADI  apps/web/lib/realtime/handlers/index.ts          (kart: AÇILMAZ)
    DOKUNULMADI  apps/web/lib/realtime/session.ts
    DOKUNULMADI  apps/web/messages/tr.ts                          (TXT-001 donmuş)
    DOKUNULMADI  packages/shared/src/ws-protocol.ts               (donmuş)
    DOKUNULMADI  packages/shared/src/errors.ts                    (donmuş)
    DOKUNULMADI  packages/shared/src/constants.ts                 (donmuş)
    DOKUNULMADI  packages/db/src/models/room.ts                   (CORE-CFG-001 paralel)
    DOKUNULMADI  packages/db/src/rooms/index.ts                   (barrel donmuş)
  packages_shared_toplam: 'git diff --stat main...HEAD | grep packages/shared → BOŞ (tek satır bile değişmedi)'
  prop_sozlesmesi: >
    `EmojiTray({onSend, lastEmoji})` ve `InviteLink({roomCode})` imzaları
    UI-SKEL-001'in bıraktığı hâliyle KORUNDU; yeni prop İSTENMEDİ. Mount
    noktalarının hâlâ doğru çalıştığını `RoomScreen.test.tsx`in casus
    bileşenleri doğruluyor (21 bileşen dosyası, 150 test, hepsi yeşil).

mutasyon_sondalari:
  - sonda: '`handlers/emoji.ts` — emoji hız kapısı devre dışı (`if (!admitEmoji(…))` → `if (false)`)'
    uygulandi_mi: 'evet (diff -q + `grep -n "if (false)"` → satır 77)'
    sonuc: '`emoji.test.ts` 5 kırmızı (KK-124, pencere kayması, off-by-one, bağlantı-başına, genel-sınırdan-önce)'
  - sonda: '`handlers/emoji.ts` — off-by-one (`stamps.length >= count` → `> count`)'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: 'aynı 5 test kırmızı — eşiğin TAM yeri kilitli, "yaklaşık doğru" yeşil geçmiyor'
  - sonda: '`handlers/emoji.ts` — kayan pencere sabit pencereye çevrildi (cutoff filtresi silindi)'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '1 kırmızı: "pencere kayınca meşru emoji YİNE geçer (kalıcı ceza yok)" — kalıcı ceza regresyonu yakalanıyor'
  - sonda: '`db/rooms/emoji.ts` — `$inc: { version: 1 }` eklendi (kartın 1. kriterinin tersi)'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '2 kırmızı: "version ARTMAZ" ve "ikinci emoji … version yine sabit kalır"'
  - sonda: '`db/rooms/emoji.ts` — beyaz liste kapısı tamamen kaldırıldı'
    uygulandi_mi: 'evet (diff -q + grep: `emojiSchema.safeParse` satırı yok)'
    sonuc: '1 kırmızı: "palet DIŞI bir değer yazma katmanında da reddedilir"'
  - sonda: '`app/davet/invite-target.ts` — `if (signedIn)` → `if (!signedIn)`'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '8 kırmızı — üç davet test dosyasının tamamı (birim + sayfa + uçtan uca) tepki verdi'
  - sonda: '`components/room/InviteLink.tsx` — `/davet/` → `/oda/`'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '1 kırmızı: "panoya <origin>/davet/<KOD> yazar"'
  - sonda: '`components/room/EmojiTray.tsx` — `setHiddenAt(at)` → `setHiddenAt(null)` (balon hiç kaybolmaz)'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '3 kırmızı (3 sn kaybolma, süre sıfırlama, aynı emojinin yeniden belirmesi)'
  temizlik: >
    Her sonda ÖNCE ilgili düzeltme commit edildikten sonra uygulandı
    (conventions.md disiplini), `cp <yedek>` ile geri alındı, sonunda
    `git status --porcelain` BOŞ. Yedek dosyaları /tmp'den silindi.

kriter_kriter:
  1: >
    ✅ `pushEmoji` `version` ARTIRMIYOR — `casUpdateRoom` BİLEREK kullanılmadı
    (o yardımcı her yazmada `$inc`). Gerçek Atlas'ta ölçüldü: 30 → 30.
    `connection.ts` AÇILMADI; delta yolunun emojiyi sürüm kapısından ÖNCE ele
    aldığı, oturum testlerinde emojinin `move:applied` üretmemesiyle dolaylı,
    `connection.test.ts`in mevcut testleriyle doğrudan korunuyor.
    Sonda 4 bu kriteri tersine çevirip 2 kırmızı üretti.
  2: >
    ✅ KK-122 — palet TAM 8 emoji, `emoji-0`…`emoji-7`; 9. testid YOK.
    Balon `emoji-balonu` kancasıyla belirir ve 3 sn (2999 ms'de HÂLÂ var,
    3000 ms'de YOK) sonra kaybolur. "2 sn içinde belirir" tarafı istemci
    tarafında ANINDA (render'da türetiliyor, zamanlayıcı beklemiyor);
    uçtan uca gecikme bütçesi change stream'in işi (ölçülmüş p95 8.7 ms).
  3: >
    ✅ KK-123 — kontrol `EMOJI_PALETTE` üyeliği (`z.enum(EMOJI_PALETTE)`),
    uzunluk DEĞİL: `'💣'` (tek karakter, doğru uzunluk) ve `'a'` de
    reddediliyor. Reddedilen çerçeve `INVALID_MESSAGE` alıyor ve
    `pushEmoji` ÇAĞRILMIYOR — aynı testte önce geçerli bir emojinin
    otoriteye ULAŞTIĞI gösteriliyor (yokluk iddiasının dolu eşi).
    Beyaz liste artık ÜÇ katman: protokol · `connection.ts` yayın yolu ·
    `packages/db` yazma yolu. Handler'a dördüncüsü KONMADI (ulaşılamaz dal
    olurdu, gerekçe kodda yazılı).
  4: >
    ✅ KK-124 — yukarıdaki canlı sonda + 14 test + 3 mutasyon sondası.
    Sahte saat kullanıldı, gerçek bekleme yok (14 test 24 ms).
  5: >
    ✅ KK-120 — `<origin>/davet/<KOD>`, `data-kopyalandi="true"` 2 sn.
    Davranış `CopyButton`da (P0'da yazılmıştı) — kopyalama UX'i tek yerde.
  6: >
    ✅ KK-121 — girişliyse `/oda/<KOD>`, değilse `/giris?donus=%2Foda%2F<KOD>`.
    Rotanın middleware'de KORUNMADIĞI ayrıca test edildi (varsayım değil).
    Oturumsuz akış uçtan uca (gerçek `GirisForm` ile) koşuldu: kod kaybolmuyor.
  7: >
    ✅ `handlers/index.ts` ve `connection.ts` AÇILMADI (git ile doğrulandı).
    `handlers/index.test.ts` (TEST dosyası) güncellendi: "yazılmamış handler
    SERVER_ERROR döner" iddiası artık YANLIŞ davranışı kilitliyordu.
    Aynı sınıf: `packages/db/src/rooms/skeletons.test.ts`.
  8: '✅ `pnpm gates` EXIT=0. Ayrıca `pnpm --filter @xox/web build` yeşil.'

kart_disi_bulgular:
  - baslik: 'R1 sondasının kapsamı handler yazıldıkça SESSİZCE daralıyordu'
    detay: >
      `handlers/index.test.ts`teki "BAŞARILI bir geçişten sonra hiçbir yazma
      handler'ı mesaj göndermez" testi, denetlediği listeyi
      `!UYGULANAN.has(...)`'tan TÜRETİYORDU. Yani bir handler yazılıp
      `UYGULANAN`a eklendiği anda R1 denetiminden ÇIKIYORDU — W1-02
      `resign`/`rematch:*`i eklediğinde sonda fiilen yalnız `move`u
      denetliyordu. Liste ELLE yazılan `YAZAN_HANDLERLAR`a çevrildi (5 handler)
      ve uzunluğu ayrıca iddia ediliyor. Conventions'taki "türetilmiş test
      kapsamı otomatik genişletir ama silmeyi/daralmayı göremez" örüntüsünün
      ters yönü: burada türetme kapsamı otomatik DARALTIYORDU.
  - baslik: '`tr.chat.tooFast` anahtarının hiç tüketicisi yoktu'
    detay: >
      TXT-001 metni yazmış ama hiçbir yer okumuyordu. Hız sınırı hatasının
      `message` alanı artık bu anahtarı kullanıyor — Türkçe metin ikinci kez
      yazılmadı. (İstemci nasılsa `tr.errors.RATE_LIMITED`ı gösteriyor;
      `message` teşhis/log yüzeyi.) Aynı şekilde `tr.room.shareHint`
      `InviteLink`te ilk tüketicisini buldu.

decisions:
  - baslik: '`pushEmoji` tek gidiş-dönüş: önce OKUMA yapmaz, `findOneAndUpdate({code})` `null` dönüşünü `ROOM_NOT_FOUND` sayar'
    baglam: >
      Kardeş geçişler (`resign`, `rematch`, `detach`) önce `Room.findOne` ile
      okuyup sonra `casUpdateRoom` ile yazıyor — çünkü `expectedVersion`
      gerekiyor. Emoji `version` kullanmadığı için okumaya İHTİYACI YOK.
    karar: >
      Tek `findOneAndUpdate`. Odanın varlığı yazmanın kendi sonucundan
      anlaşılır; koltuk doğrulaması çağıran katmanda (`connection.seat()` +
      `session.ts`in koltuk kapısı).
    gerekce: >
      Instance başına TEK change stream var (ADR-0002) ve M0'da 100 işlem/sn
      bütçesi paylaşılıyor. Emoji, protokolün EN SIK çağrılabilen yazma yolu;
      her emojide iki Atlas işlemi yerine bir tane yapmak bu bütçeyi doğrudan
      koruyor. Aynı gerekçe WS-001'in `join` yazma amplifikasyonu blocker'ında
      kullanılmıştı.
    reddedilen: >
      Okuma + koşullu yazma (`extraFilter: {seats.<seat>: {$ne:null}}`) — koltuk
      kontrolünü DB'ye taşırdı ama `null` dönüşünü "oda yok" ile "koltuk yok"
      arasında ayırt edemezdi; ayırt etmek için ikinci bir okuma gerekirdi ve
      kazanç sıfırlanırdı. Koltuk zaten iki katmanda doğrulanıyor.
  - baslik: 'Emoji hız sınırı yalnız KABUL EDİLEN çerçeveyi sayar (genel sınırdan bilinçli sapma)'
    baglam: >
      `connection.ts`in genel kapısı kabul/ret ayırmadan HER çerçeveyi
      pencereye yazıyor.
    karar: 'Emoji penceresine yalnız otoriteye inen emoji yazılır.'
    gerekce: >
      Retler de sayılsaydı ısrarcı bir istemcinin penceresi kendi retleriyle
      sürekli dolu kalırdı: kayan pencere KALICI CEZAYA dönüşür, kullanıcı
      10 saniye sonra bile emoji atamazdı. Genel kapıda bu davranış İSTENİYOR
      (orada amaç bağlantıyı kademeli olarak kapatmak); emoji kapısında amaç
      yalnız hızı kesmek, cezalandırmak değil.
    reddedilen: >
      Retleri de saymak (basit ama kalıcı ceza) · reddedilen emojiyi genel
      pencereden düşürmek (`connection.ts` AÇILAMAZ ve zaten genel kademelenme
      isteniyor).
  - baslik: 'Emoji hız durumu `WeakMap<RoomConnection, number[]>` ile bağlantı NESNESİNE bağlanır'
    baglam: >
      Durum bir yere konmalı ama `connection.ts` kart gereği AÇILAMAZ (ve
      açılsaydı bile W3-03/W3-04 çakışırdı).
    karar: '`handlers/emoji.ts` içinde modül-düzeyi `WeakMap`, anahtar bağlantı nesnesi.'
    gerekce: >
      Oturum bitince bağlantı nesnesi çöp toplanır ve kayıt kendiliğinden
      düşer — sızıntı için ayrı bir temizlik yolu (ve onu unutma riski) yok.
      Oda kodu ya da userId anahtarlı bir `Map` uzun ömürlü bir Fluid
      instance'ında sonsuza kadar büyürdü.
    reddedilen: >
      `connection.ts`e alan eklemek (dosya donmuş, iki paralel görev çakışırdı)
      · `Map` + elle temizlik (`session.doEnd`e kanca gerekirdi, `session.ts` de
      benim çakışma kümemde değil).
  - baslik: 'Davet linki `/oda/<KOD>`e DEĞİL `/davet/<KOD>`e işaret eder'
    baglam: >
      `/oda/:path*` zaten middleware'de korunuyor ve oturumsuz kullanıcıyı
      `?donus=` ile `/giris`e yolluyor — yani teknik olarak `/oda/<KOD>` de
      "çalışırdı".
    karar: 'Ayrı bir `/davet/[kod]` iniş rotası; middleware matcher''ında YOK.'
    gerekce: >
      (a) Davet URL'si oda URL'sinden ayrı bir yüzey olarak kalıyor — ileride
      "X seni davet etti" ekranı, oda doluluk kontrolü veya tek kullanımlık
      davet kodu buraya eklenebilir, `/oda` sayfası bozulmadan. (b) Paylaşılan
      link ile giriş yönlendirmesi arasındaki sözleşme TEK bir dosyada
      (`invite-target.ts`) toplanıyor ve test edilebiliyor; middleware'e
      bırakılsaydı `next-auth` yüzünden Vitest'te hiç koşturulamazdı.
    reddedilen: >
      `/oda/<KOD>`ü doğrudan paylaşmak — bugün çalışır ama davet akışına
      herhangi bir şey eklemek `/oda` sayfasını ya da middleware'i açmayı
      gerektirirdi; ikisi de daha pahalı.
  - baslik: 'Beyaz listenin ÜÇÜNCÜ kapısı `packages/db` yazma yoluna kondu'
    baglam: >
      Güvenlik denetimi iki kapı saymıştı: protokol enum'u + `connection.ts`
      yayın yolu.
    karar: '`pushEmoji` de `emojiSchema.safeParse` yapar; başarısızsa `INVALID_MESSAGE`.'
    gerekce: >
      Mongoose şeması `emoji: { type: String }` — model TEK BAŞINA hiçbir şey
      engellemiyor ve `Emoji` tipi yalnız derleme zamanında yaşıyor.
      Serbest metin bir kez veritabanına girerse odaya bağlanan HERKESE
      dağıtılmaya aday olur (yayın yolu onu düşürse bile kalıcı veri kirlenir).
      `packages/db` ayrıca bağımsız tüketilebilen bir paket.
    reddedilen: >
      Handler'a dördüncü bir kapı koymak — `clientMessageSchema` palet dışını
      `session.ts`te zaten düşürdüğü için ULAŞILAMAZ bir dal olurdu (ölü kod +
      sahte kapsam).

gotchas:
  - baslik: '`react-hooks/set-state-in-effect`: "görünen"i değil "gizleneni" tut'
    detay: >
      Balonu göstermenin sezgisel yolu `useEffect(() => setVisibleAt(at), [at])`
      — ama bu efekt GÖVDESİNDE senkron `setState` demek ve ESLint kuralı
      (Next 16 + react-hooks) bunu HATA olarak reddediyor (her emojide fazladan
      bir render turu). Çözüm durumu TERSİNE çevirmek: "gizlenmiş olan damga"
      tutulur, görünürlük render sırasında TÜRETİLİR
      (`hiddenAt !== lastEmoji.at`), `setState` yalnız zamanlayıcı callback'inde
      (asenkron) çağrılır. Aynı kalıp süre dolunca gizlenen her UI parçası için
      geçerli.
  - baslik: 'Zaman damgalı UI durumunda efekt bağımlılığı NESNE olamaz'
    detay: >
      `useEffect(..., [lastEmoji])` yazmak cazip ama `roomClientReducer` HER
      sunucu mesajında yeni bir state nesnesi üretiyor — alakasız bir `pong`
      bile balon zamanlayıcısını sıfırlardı. Bağımlılık `lastEmoji.at`
      (ilkel değer) olmalı. Ters yönde, `lastEmoji.emoji`ye bakmak da yanlış:
      aynı emoji iki kez gelirse hiç değişmez ve balon ikinci kez HİÇ belirmez.
      Tek doğru ayırt edici sunucunun yazdığı `at`.
  - baslik: 'Bir iskeleti doldurmak, o iskeleti KİLİTLEYEN testleri de kırar — ve o testler hemen görünmez'
    detay: >
      `pushEmoji` gövdesi yazılınca `packages/db/src/rooms/skeletons.test.ts`
      ("açık bir hata fırlatır") ve `apps/web/lib/realtime/handlers/index.test.ts`
      ("yazılmamış her handler SERVER_ERROR döner") kırmızıya döndü. İkisi de
      görevin çakışma kümesinde DEĞİLDİ ve dosya adlarından ilişkileri belli
      değil; `pnpm gates` çalıştırılmasaydı fark edilmezdi. Bir iskeleti
      dolduran her görev, önce `grep -rn "henüz uygulanmadı"` ile o iskeleti
      kilitleyen testleri aramalı.
  - baslik: 'Vitest 4 `console.log`u YUTUYOR — canlı sonda çıktısı için `--disable-console-intercept` şart'
    detay: >
      Bir sondanın gerçek çıktısını görmek için yazılan `console.log`lar
      varsayılan koşumda hiç basılmıyor (test yeşil geçiyor, çıktı yok) —
      "sonda çalışmadı" diye yanlış okunabilir. `vitest run <dosya>
      --disable-console-intercept` ile basılıyor. Ayrıca pnpm sarmalayıcısı
      bazı durumlarda çıktıyı filtreliyor; sondayı `apps/web` içinden
      `../../node_modules/.bin/vitest` ile doğrudan koşturmak daha güvenilir.
  - baslik: 'Gerçek Atlas testlerinde `updatedAt` karşılaştırması aynı ms''de yanlış kırılır'
    detay: >
      `pushEmoji`nin change stream olayı üretmesi `timestamps:true`nun
      `updatedAt`i tazelemesine bağlı ve bunu test etmek için "önce/sonra"
      karşılaştırılıyor. Yazma yeterince hızlıysa iki damga AYNI milisaniyeye
      düşüyor ve `toBeGreaterThan` haksız yere kırılıyor. Teste 5 ms'lik
      bilinçli bir bekleme kondu.
  - baslik: 'Gece koşusu sırasında Atlas DNS''i (`querySrv ECONNREFUSED`) geçici olarak düşebiliyor'
    detay: >
      İlk `pnpm gates` koşusunda `@xox/db` testleri `querySrv ECONNREFUSED
      _mongodb._tcp.…mongodb.net` ile düştü ve turbo koşuyu saatlerce asılı
      gösterdi (`Duration 20243s`). Kod değişikliği YOKKEN ikinci koşu temiz
      geçti. Bu hata sınıfı kod hatasıyla karıştırılmamalı: `@xox/db` toplu
      düşüşü + `ECONNREFUSED` görülürse önce koşuyu tekrarla.

acik_borclar:
  - >
    `docs/memory/decisions.md` ve `docs/memory/gotchas.md` BİLEREK
    güncellenmedi: `docs/memory/**` çakışma kümemde değil ve dalgada üç görev
    daha uçuşta (CORE-CFG-001, W3-04, …). Yukarıdaki `decisions`/`gotchas`
    alanları memory-curator'ın (ya da lead'in) tek seferde birleştirmesi için
    hazır. Bu, agent talimatındaki "her karar decisions.md'ye yazılmalı"
    maddesinden CLAUDE.md kural 6 lehine bilinçli bir sapmadır.
  - >
    `docs/memory/api-contract.md` `chat:emoji` yolunu artık ANLATMALI
    (emojinin `version` artırmaması ve iki ayrı hız sınırı sözleşmenin
    parçası). Aynı çakışma gerekçesiyle bu görevde yazılmadı — önerilen metin:
    "`chat:emoji` bağlantı başına 10 sn / 5 ile sınırlıdır (`EMOJI_RATE_LIMIT`),
    aşımda `error{code:'RATE_LIMITED'}`; bağlantı KAPATILMAZ. Emoji odanın
    `version`ını ARTIRMAZ; yayın `updatedAt` tazelenmesiyle change stream'den
    gider."
  - >
    Uçtan uca (iki gerçek tarayıcı) emoji yayını `apps/e2e`nin işi: "A basar,
    B'nin ekranında 2 sn içinde `emoji-balonu` belirir, 3 sn sonra kaybolur"
    (KK-122'nin karşı-taraf yarısı). Bu görevde sunucu ve istemci yarıları ayrı
    ayrı kanıtlandı; iki instance arası bacak change stream'in genel kanıtına
    (E2E-001) dayanıyor.
  - >
    Emoji bütçesi bağlantı başına — aynı kullanıcı iki sekme açarsa iki bütçe
    alır. Koltuk sahipliği userId'ye ait olduğu için takeover (4409) ikinci
    sekmeyi zaten düşürüyor, yani pratikte istismar edilebilir bir yol
    görünmüyor; yine de userId başına bir sınır istenirse `WeakMap` anahtarı
    değişmeli ve o zaman elle temizlik gerekir.

blocked: null
```
