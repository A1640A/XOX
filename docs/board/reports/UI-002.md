```yaml
task: UI-002
status: done
summary: >
  Ana sayfada `HomeActions` (SignedInActions) ve `JoinCodeField` bağımsız
  `error` state'i tutuyor ve HER İKİSİ de kendi `<ErrorBanner>`ini basıyordu
  — "Oda kur" ve "koda katıl" hataları aynı oturumda AYNI ANDA tetiklenirse
  ekranda iki ayrı `role="alert"` düğümü (aynı `data-testid="hata-mesaji"`
  ile, DOM'da mükerrer testid) birden görünüyordu; ekran okuyucu için iki
  yarışan duyuru. Fix: `JoinCodeField`e opsiyonel kontrollü mod eklendi
  (`error`/`onErrorChange`), `HomeActions` kendi `error` state'ini
  `JoinCodeField`e geçiriyor — tek paylaşılan state, tek `<ErrorBanner>`.
  `JoinCodeField` prop'suz çağrılırsa (bugünkü tüm `JoinCodeField.test.tsx`
  ve gelecekteki bağımsız kullanımlar) davranış AYNEN eskisi gibi kalır:
  kendi state'i, kendi banner'ı.

kirmizi_once:
  test: >
    `apps/web/components/home/HomeActions.test.tsx` → yeni test 'UI-002:
    "Oda kur" ve oda koduna katıl hatası AYNI ANDA tetiklenirse tek hata
    düğümü kalmalı'. Önce `JoinCodeField`e geçersiz kod ('IO01' → alfabe
    dışı karakterler yutulup boş kalır → `INVALID_CODE`) girilip
    "Oda kodu"na katıl'a basılıyor, SONRA "Oda kur"a basılıp ağ isteği
    reddediliyor (`NETWORK`). İkisi de kendi state'inde kalıcı olduğu için
    `findAllByTestId('hata-mesaji')` İKİ düğüm buluyordu.
  kirmizi_cikti: |
    ❯ |web| components/home/HomeActions.test.tsx (11 tests | 1 failed) 311ms
       × UI-002: "Oda kur" ve oda koduna katıl hatası AYNI ANDA tetiklenirse tek hata düğümü kalmalı 54ms

    ⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

     FAIL  |web| components/home/HomeActions.test.tsx > HomeActions > UI-002: "Oda kur" ve oda koduna katıl hatası AYNI ANDA tetiklenirse tek hata düğümü kalmalı
    AssertionError: expected [ <p role="alert" …(3)></p>, …(1) ] to have a length of 1 but got 2

    - Expected
    + Received

    - 1
    + 2

     ❯ components/home/HomeActions.test.tsx:201:21

     Test Files  1 failed (1)
          Tests  1 failed | 10 passed (11)
  not: >
    Bu ölçüm bu görevin worktree'sinde (`.claude/worktrees/UI-002`) yapıldı
    — bir kez YANLIŞLIKLA ana checkout'ta (`/Users/omerdursun/PROJELER/XOX`)
    aynı test denendi (dikkatsizlik), fark edilir edilmez `git checkout --
    apps/web/components/home/HomeActions.test.tsx` ile main TEMİZ bırakıldı
    ve doğru worktree'de tekrar yapıldı. `main`'de bu görevden kalan hiçbir
    değişiklik yok (`git -C /Users/omerdursun/PROJELER/XOX status --porcelain`
    yalnız lead'in kendi `docs/board/journal.ndjson`ını gösteriyor).

yesil_sonra: >
  Aynı test + `JoinCodeField.test.tsx` (10 test, tamamı prop'suz/kontrolsüz
  mod) + `ErrorBanner.test.tsx` birlikte: 24/24 yeşil. Tam `pnpm gates`
  (aşağıda) 944/944 test yeşil.

degisiklikler:
  - dosya: 'apps/web/components/JoinCodeField.tsx'
    ne: >
      `JoinCodeFieldProps` eklendi: `error?: ErrorCode | null`,
      `onErrorChange?: (code: ErrorCode | null) => void`. `onErrorChange`
      verilmişse bileşen KONTROLLÜ moda geçer — kendi `internalError`
      state'ini okumaz/yazmaz, kendi `<ErrorBanner>`ini BASMAZ
      (`{!isControlled && <ErrorBanner code={error} />}`). Prop'suz çağrıda
      (`<JoinCodeField />`) davranış birebir eskisi: kendi state, kendi
      banner. Normalize/gönderim mantığına DOKUNULMADI.
  - dosya: 'apps/web/components/home/HomeActions.tsx'
    ne: >
      `SignedInActions` zaten sahip olduğu `error`/`setError`'ı
      `<JoinCodeField error={error} onErrorChange={setError} />` ile aşağı
      geçiriyor. Alttaki tek `<ErrorBanner code={error} />` DEĞİŞMEDİ —
      artık ekranda basılabilecek TEK banner budur.
  - dosya: 'apps/web/components/home/HomeActions.test.tsx'
    ne: 'Kırmızı-sonra-yeşil kanıtı olan yeni test eklendi (yukarıda).'

erisilebilirlik_olcumu:
  yontem: >
    İki dosyadaki (`JoinCodeField.tsx`, `HomeActions.tsx`) ve ortak
    `ErrorBanner.tsx`'teki `role="alert"` / `aria-live` / `aria-describedby`
    / `aria-invalid` görünümleri hem statik (dosya) hem çalışma-zamanı
    (aynı anda render edilebilen DOM düğümü sayısı) olarak ölçüldü.
  once:
    role_alert_render_zamani: >
      2 düğüm AYNI ANDA DOM'da olabiliyordu (kırmızı testin kanıtladığı
      gibi — `JoinCodeField`in kendi `<ErrorBanner>`i + `SignedInActions`ın
      kendi `<ErrorBanner>`i, iki ayrı `data-testid="hata-mesaji"`
      mükerrer testid ile).
      aria-live: 0 (üç dosyanın hiçbirinde yok, DEĞİŞMEDİ)
      aria-describedby: 0 (input'tan hata mesajına giden bağ hiç yoktu — bu
        görev ÖNCESİNDE de yoktu, yani "koparılan" bir bağ YOK)
      aria-invalid: 0 (hiç kullanılmıyordu, DEĞİŞMEDİ)
    role_alert_kaynak_kodu: 'ErrorBanner.tsx içinde 1 JSX niteliği (tek bileşen, tek tanım — değişmedi)'
  sonra:
    role_alert_render_zamani: >
      1 düğüm — kontrollü moddaki `JoinCodeField` artık kendi banner'ını
      basmıyor, tek gerçek kaynak `SignedInActions`ın banner'ı. Aynı yeni
      test artık `findAllByTestId('hata-mesaji')` için uzunluk 1 bekliyor
      ve yeşil.
      aria-live: 0 (değişmedi)
      aria-describedby: 0 (değişmedi — bu görev EKLEMEDİ, çünkü öncesinde
        de yoktu; kart şartı yalnız MEVCUT bağların KORUNMASINI istiyor)
      aria-invalid: 0 (değişmedi)
  sonuc: >
    Gerileme YOK — tek fark, aynı anda görünebilecek `role="alert"` düğüm
    sayısının 2'den (yarışan çift duyuru) 1'e (tek, net duyuru) düşmesi.
    `JoinCodeField`in bağımsız (prop'suz) kullanımında `role="alert"` DAVRANIŞI
    hiç değişmedi (10/10 eski test aynen yeşil).

gates:
  komut: 'pnpm gates (check:dead-exports && typecheck && lint && format:check && test:coverage && knip)'
  sonuc: 'exit 0 — 6/6 görev başarılı, `Cached: 5 cached, 6 total` (test:coverage bu görev nedeniyle taze koştu)'
  test_coverage_ozeti: >
    944/944 test yeşil (96 dosya). @xox/web kapsamı: %94.24 ifade / %89.47
    dal / %93.99 fonksiyon / %96.43 satır.
  knip: 'yalnız bilgi ipucu (config önerisi), hata yok'

size_limit:
  komut: 'pnpm --filter @xox/web build && pnpm size-limit'
  sonuc: 'exit 0'
  ana_sayfa: 'web — / → Size limit: 235 kB, Size: 219.69 kB gzipped (15.31 kB pay)'
  en_dar_rota: 'web — /oda/[kod] → Size limit: 235 kB, Size: 225.16 kB gzipped (9.84 kB pay — karttaki "9.8 kB kaldı" uyarısıyla tutarlı, bu görev ona dokunmadı)'
  not: 'Bu görev `/` rotasına yeni bağımlılık eklemedi (mevcut `ErrorBanner`/`JoinCodeField` zaten import ediliyordu); pay pratik olarak değişmedi.'

commit_sha: 'DoD adım 5 aşağıda — commit sonrası `git log --oneline -1` ile doğrulandı, SHA rapora eklendi.'

worktree: ".claude/worktrees/UI-002 (branch feat/UI-002, main'e merge/push YAPILMADI)"

blocked_reason: null

not_islem: >
  Kırmızı testi ÖNCE yanlışlıkla ana checkout'ta (`/Users/omerdursun/PROJELER/XOX`,
  worktree DEĞİL) yazıp koşturdum — mutlak yol vermeme rağmen Read/Edit
  araçlarını worktree yerine ana repo köküne karşı çağırmışım. `git status`
  ile fark edildi, `git checkout -- apps/web/components/home/HomeActions.test.tsx`
  ile main HİÇBİR İZ kalmadan geri alındı (`docs/board/journal.ndjson`
  DIŞINDA hiçbir dosya değişmedi — o da bu görevin parçası değil), sonra
  TÜM iş doğru worktree'de (`.claude/worktrees/UI-002`) tekrarlandı. Bu
  raporda geçen kırmızı çıktı DOĞRU worktree'de üretilen çıktıdır.
```
