```yaml
task: CTR-002
status: done
summary: >
  Tasarım §5.6'nın saf istemci indirgeyicisi (`room-client.ts`) ve enjekte
  soketle çalışan WS taşıma istemcisi (`ws-client.ts`) yazıldı. Tablonun 11
  satırının hepsi teste dönüştü; R1 değişmezi (kendi hamlesini sunucu yankısı
  olmadan kalıcı uygulamama) ayrı bir testle kilitlendi ve sonda ile kırmızıya
  döndürüldü. `@xox/shared` kapsamı %100 satır/dal/fonksiyon/ifade, 315 test
  (101 yeni); `pnpm gates` exit 0.

files_changed:
  - packages/shared/src/room-client.ts # YENİ — saf reducer, nextReconnectDelay, opponentLeftVisible
  - packages/shared/src/room-client.test.ts # YENİ — 76 test
  - packages/shared/src/ws-client.ts # YENİ — createRoomWsClient (soket/zaman/rng enjekte)
  - packages/shared/src/ws-client.test.ts # YENİ — 25 test, sahte soket + sahte zamanlayıcı
  - packages/shared/src/index.ts # iki yeni modül dışa verildi
  - packages/shared/package.json # ./room-client ve ./ws-client alt-yol export'ları

tests:
  added: 101
  passing: 315
  coverage: '%100 satır · %100 dal · %100 fonksiyon · %100 ifade (eşik %95)'
  mutation: 'kapsam dışı — Stryker yalnız game-core için kurulu; yerine 7 elle sonda koşuldu'

sondalar: # her biri uygulamayı bozup testin gerçekten kırmızı olduğunu görmek için
  - { sonda: 'ui:cell tahtaya taş yazsın (R1 ihlali)', sonuc: '1 test kırmızı — §5.6/1 R1' }
  - { sonda: '4409 dalı silinsin', sonuc: '2 test kırmızı — §5.6/8' }
  - { sonda: 'state mesajı tahtayı birleştirsin', sonuc: '1 test kırmızı — §5.6/6' }
  - { sonda: 'nabız eşiği 2 yerine 3 heartbeat', sonuc: '2 test kırmızı — heartbeat' }
  - { sonda: '4499 gecikmesizliği kaldırılsın', sonuc: '1 test kırmızı — yeniden bağlanma' }
  - { sonda: 'detach dinleyicileri sıfırlamasın', sonuc: '1 test kırmızı — çift kapanış' }
  - {
      sonda: 'kalıcı kapanış dalı silinsin',
      sonuc: '7 test kırmızı — §5.6/10 + ws-close örtüşme testi',
    }

tablo_kapsami: # tasarım §5.6, ELLE yazılmış beklentiler (kendine-referans yok)
  - {
      satir: 'hücreye basma kapısı',
      test: '§5.6/1 — 10 test: sıra/doluluk/bağlantı/devredildi/pending/bitmiş oyun/izleyici/aralık dışı',
    }
  - { satir: 'move:applied version+1', test: '§5.6/2 — 4 test' }
  - { satir: 'move:applied boşluk', test: '§5.6/3 — 2 test (resync)' }
  - { satir: 'move:applied eski/aynı', test: '§5.6/4 — 2 test' }
  - { satir: 'move:rejected', test: '§5.6/5 — 6 test (4 sebep kodu)' }
  - { satir: 'state tam yerine koyma', test: '§5.6/6 — 6 test' }
  - { satir: 'bitmiş state doğrudan sonuç', test: '§5.6/7 — 1 test' }
  - { satir: 'kapanış 4409', test: '§5.6/8 — 2 test + ws-client 1 test' }
  - { satir: 'kapanış 4499', test: '§5.6/9 — 2 test + ws-client 1 test' }
  - {
      satir: 'diğer kapanışlar',
      test: "§5.6/10 — 14 test (8'i ws-close üçlüsüyle örtüşme tablosu) + ws-client 3 test",
    }
  - {
      satir: '2 heartbeat içinde pong yok',
      test: 'ws-client heartbeat — 5 test (sahte zamanlayıcı)',
    }

decisions:
  - karar: '`now` yalnız `server` olayında taşınır, her olayda değil'
    gerekce: >
      Zamana ihtiyaç duyan tek geçiş `serverOffsetMs = serverTime - now`.
      Arayüz `dispatch({type:"ui:cell", index})` yazabiliyor, saat sızdırmıyor.
    reddedilen_alternatif: 'Her olaya `now` koymak — çağrı yerlerini kirletir, tek faydası tekbiçimlilik'
  - karar: 'Geri çekilme gecikmesi reducer içinde HESAPLANMAZ; efekt `{attempt, immediate}` taşır'
    gerekce: 'Gecikme `rng` ister; reducer rastgelelik alsaydı saflığı sözleşmede kaybolurdu. Hesabı taşıma yapar.'
    reddedilen_alternatif: 'Reducer imzasına `rng` eklemek — her çağrı yerinde enjeksiyon zorunluluğu, testte unutulursa sessiz rastgelelik'
  - karar: '`move:rejected` tahtaya DOKUNMAZ (spec "hücreyi boşalt" der)'
    gerekce: >
      R1 sayesinde reddedilen taş tahtaya hiç yazılmamıştır; `board[index]=null`
      yazmak `occupied` reddinde rakibin gerçek taşını silerdi. Test bunu kilitliyor.
    reddedilen_alternatif: 'Spec cümlesini birebir uygulamak'
  - karar: '`move:applied` sonrası durum `evaluateStatus` ile tahtadan türetilir'
    gerekce: >
      İnce yol yeni `status` taşımaz; sırayı elle çevirmek kural mantığını
      shared'a kopyalamak olurdu. Motor delege ediliyor (CLAUDE.md kural 4).
      Sunucunun bildirdiği çizgisiz bitiş (pes/süre/terk) korunur — geç yankı
      onu `playing`e döndüremez.
    reddedilen_alternatif: '`turn`u elle ters çevirmek; ya da `applyMove` çağırmak (kural ihlalinde FIRLATIR, reducer total olmalı)'
  - karar: 'Başlangıç `version: -1`'
    gerekce: 'İlk `state` gelmeden düşen `move:applied` (version >= 1) boşluk sayılır ve tam durum istenir; kör uygulama yok.'
    reddedilen_alternatif: '`0` — ilk hamleyi (version 1) boş tahtaya kör uygulardı'
  - karar: 'Nabız kaybında bağlantıyı istemci 4408 ile KENDİ kapatır'
    gerekce: 'Sunucunun 3 nabızlık 4408 eşiğini beklemek ekranı 25 sn daha donuk tutar; 4408 kalıcı değil, backoff doğal olarak işler.'
    reddedilen_alternatif: 'Yalnız soketin kendi kapanışını beklemek (KK-060 bütçesini aşar)'
  - karar: '4401 `reauth` efekti üretir, kendi başına bağlanmaz'
    gerekce: "Bilet almak G/Ç'dir; `@xox/shared` G/Ç yapmaz (ADR-0006). Çağıran bileti tazeleyip `connect(yeniUrl)` çağırır."
    reddedilen_alternatif: "`fetch` çağırmak — paketi tarayıcıya/RN'e bağlar, test edilemez hâle getirir"
  - karar: '`lastEmoji` alanı §5.6 arayüzüne eklendi'
    gerekce: 'Spec taslağında yoktu ama `chat:emoji` mesajı var; efekte çevirmek yerine duruma koymak arayüzü basit tutuyor (P2 hazır).'
    reddedilen_alternatif: 'Mesajı yoksaymak (veri kaybı) ya da ayrı efekt türü (taşımanın arayüze köprü kurması gerekirdi)'

gotchas:
  - >
    `nextReconnectDelay` yuvarlanmazsa `0.8 + 0.4*1 = 1.2000000000000002` üretir
    ve `600` beklentisi kırılır. Zamanlayıcı zaten tam sayı ms ister — `Math.round`
    hem doğru hem testi kırılgan olmaktan çıkarır.
  - >
    Soketi biz kapatırken (`close()`, nabız kaybı) dinleyicileri sıfırlamazsan
    gerçek `WebSocket` kendi `onclose`unu da ateşler: reducer iki kapanış görür,
    backoff sayacı iki kat artar ve İKİ yeniden bağlanma zamanlanır. Sunucu
    kapattığında ise sıfırlamaya gerek yok — yalnız referansı düşürmek yeterli,
    böylece geç düşen çerçeve `write` içinde sessizce yutulur.
  - >
    `pending` iki farklı şey demektir: "iyimser GÖSTERİM" serbest, "kalıcı
    UYGULAMA" yasak. R1 testi ikisini ayırt etmeli — yoksa `board[i]='X'` yazan
    bir uygulama testi geçer.
  - >
    `room-client.ts` 363 satır; konvansiyonun 250 satır eşiğini aşıyor ama
    bölünmedi: tasarım §5.6 dosya yolunu adıyla sabitliyor ve satırların büyük
    kısmı Türkçe gerekçe yorumu. Katmana göre bölmek (types/reducer/helpers)
    konvansiyonun yasakladığı bölme biçimi.

blocked_reason: null

next_suggestions:
  - >
    `move:applied` `turnDeadline`a DOKUNMUYOR: ince yolda sunucu yeni süre
    göndermiyor. P0'da `turnDeadline` hep null olduğu için etkisiz; Dalga 2'de
    süre açılınca ya protokole deadline eklenmeli ya da istemci
    `now + MOVE_TIMEOUT` türetmeli. Kararı DEADLINE kartına bırakıldı.
  - >
    `opponentLeftVisible(state, now)` ADR-0007'nin 2 sn eşiğini kilitliyor;
    web ve mobil `opponent:left` rozetini bu fonksiyona bağlamalı, kendi
    eşiğini yazmamalı.
  - >
    Reducer `resign`/`rematch`/`emoji` kapılarını da içeriyor; Dalga 1'in web ve
    mobil kartları yalnız `createRoomWsClient` + `dispatch` kullanmalı, ikinci
    bir istemci durum makinesi yazmamalı.
  - >
    `@xox/shared` için Stryker kurulu değil. Bu iki dosya mantık yoğun; bir
    dalga sonrasında `mutation` görev kapsamına alınabilir (game-core kalıbı:
    `plugins` + `inPlace: true`).
```
