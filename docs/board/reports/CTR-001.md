```yaml
task: CTR-001
status: done
summary: >
  `@xox/shared` sözleşme katmanı eksiksiz yazıldı: taşıma oyun durumu (ADR-0001
  değişmezi `superRefine` ile İKİ YÖNLÜ testli), 20 kodluk `ErrorCode` birliği,
  spec §2.0 testid sözleşmesi, tasarım §2.7'nin tüm sabitleri, 7 WS kapanış
  kodu, 12 sunucu + 7 istemci mesajı, §7'nin REST gövde/yanıt şemaları ve
  spec §5 metin ağacının anahtar listesi. Kapsam %100 (satır/dal/fonksiyon/
  ifade), 154 test; `pnpm gates` exit 0. `game-core`'a tek satır yazılmadı —
  yalnız tek yönlü köprü (`toTransportStatus` / `forfeitStatus`) ile tüketildi.

files_changed:
  - packages/shared/package.json # @xox/game-core workspace bağımlılığı geri eklendi
  - packages/shared/vitest.config.ts # kapsam eşiği 90 -> 95
  - packages/shared/src/index.ts
  - packages/shared/src/primitives.ts # YENİ
  - packages/shared/src/primitives.test.ts # YENİ
  - packages/shared/src/game-status.ts # YENİ
  - packages/shared/src/game-status.test.ts # YENİ
  - packages/shared/src/errors.ts # YENİ
  - packages/shared/src/errors.test.ts # YENİ
  - packages/shared/src/ws-close.ts # YENİ
  - packages/shared/src/ws-close.test.ts # YENİ
  - packages/shared/src/testids.ts # YENİ
  - packages/shared/src/testids.test.ts # YENİ
  - packages/shared/src/rest-contract.ts # YENİ
  - packages/shared/src/rest-contract.test.ts # YENİ
  - packages/shared/src/message-keys.ts # YENİ
  - packages/shared/src/message-keys.test.ts # YENİ
  - packages/shared/src/constants.ts # §2.7 sabitleri eklendi
  - packages/shared/src/constants.test.ts # YENİ
  - packages/shared/src/ws-protocol.ts # kırıcı genişletme
  - packages/shared/src/ws-protocol.test.ts
  - pnpm-lock.yaml # yalnız shared -> game-core link satırı
  - docs/board/reports/CTR-001.md

tests:
  {
    added: 143,
    passing: 154,
    coverage: '%100 satır · %100 dal · %100 fonksiyon · %100 ifade',
    mutation: 'yok — bu paket mutasyon kapsamında değil (yalnız game-core)',
  }

decisions:
  - karar: >
      Ortak zod ilkelleri (`playerSchema`, `cellSchema`, `boardSchema`,
      `cellIndexSchema`, `roomCodeSchema`, `epochMsSchema`) yeni bir
      `primitives.ts` dosyasına taşındı; `game-status.ts` ve `ws-protocol.ts`
      ikisi de oradan okuyor.
    gerekçe: >
      `ws-protocol` taşıma durumuna, taşıma durumu da oyuncu/indeks şemalarına
      ihtiyaç duyuyor. İlkeller `ws-protocol`'de kalsaydı
      `ws-protocol -> game-status -> ws-protocol` döngüsü oluşurdu ve
      `import-x/no-cycle` bunu reddederdi.
    reddedilen_alternatif: >
      İlkelleri `game-status.ts` içinde yeniden tanımlamak (iki ayrı doğruluk
      kaynağı) ya da döngüyü `import type` ile gizlemek (çalışma zamanı şeması
      gerektiği için mümkün değil).

  - karar: >
      `chat:emoji` mesajının `emoji` alanı serbest metin değil,
      `z.enum(EMOJI_PALETTE)`. Hem istemci hem sunucu mesajında.
    gerekçe: >
      KK-123 palet dışı her girdinin `INVALID_MESSAGE` almasını istiyor.
      Beyaz listeyi şemaya koymak XSS/istismar yüzeyini tek noktada kapatır;
      aksi hâlde her handler kendi kontrolünü yazar ve biri unutur.
    reddedilen_alternatif: >
      `z.string().min(1).max(MAX_EMOJI_LENGTH)` bırakıp doğrulamayı WS
      handler'ına ertelemek — kural mantığının dört uygulamaya dağılması demek.

  - karar: >
      `moveIndexSchema` adı `cellIndexSchema` olarak tekilleştirildi; eski
      `gameStatusSchema` ve `Seats` dışa aktarımları kaldırıldı.
    gerekçe: >
      Aynı 0..8 kısıtı için iki ad vardı ve `gameStatusSchema` artık
      `transportStatusSchema` tarafından geçersiz kılınıyor. Repo genelinde
      tüketicisi yoktu (`grep` ile doğrulandı: yalnız `ROOM_*` sabitleri
      kullanılıyor), yani kırıcı değişikliğin maliyeti bugün sıfır — ADR-0001'in
      "bu pencere Dalga 0'dan sonra kapanır" uyarısı gereği şimdi yapıldı.
    reddedilen_alternatif: >
      Geriye dönük takma ad bırakmak — knip/`no-unnecessary-condition` gürültüsü
      ve iki isimli tek kavram.

  - karar: >
      `message-keys.ts` yalnız anahtar listesi + `diffMessageKeys(tree)` +
      `MessageTree` tipi verir; Türkçe metinlerin kendisi web/mobil `tr.ts`
      dosyalarında kalır.
    gerekçe: >
      Boundaries mobil→web importunu yasaklıyor; iki tarafın da doğrulanabileceği
      tek ortak nokta anahtar kümesidir. `diffMessageKeys` fazla anahtarı da
      yakalar — tip kısıtı tek başına yalnız eksiği yakalardı.
    reddedilen_alternatif: >
      Metinleri `shared`'a taşımak: `ui-tokens`/i18n sorumluluğunu sözleşme
      paketine yükler ve mobil metin farklılaşmasını imkânsız kılar.

  - karar: '`ELO_START = 1200`, `DATA_ATTR`, `isReconnectableCloseCode` ek olarak dışa verildi.'
    gerekçe: >
      KK-081 (yeni kullanıcı 1200), KK-083 (`data-tema`), KK-120
      (`data-kopyalandi`) ve §2.6'nın "4409 sonrası yeniden bağlanma denenmez"
      kuralı dört ayrı yerde tekrarlanacaktı. Üçü de saf ve testli.
    reddedilen_alternatif: >
      Bunları tüketici paketlere bırakmak — aynı sabitin web ve mobilde iki kez
      yazılması.

gotchas:
  - >
    `boundaries/dependencies` var olmayan bir paketi (`@xox/yok-boyle-paket`)
    import eden dosyada SESSİZ kalır — çözülemeyen import "unknown" sayılır ve
    hiçbir politika ateşlenmez. Sonda ile doğrulandı. Yani "lint yeşil" bir
    importun gerçekten çözüldüğünü kanıtlamaz; kanıt `tsc`'dedir. Yasaklı yön
    (shared -> web) ayrı bir sondayla ateşlendi ve gerçekten hata verdi.
  - >
    `z.discriminatedUnion(...).superRefine(...)` çıktısı bir `discriminatedUnion`
    seçeneği OLAMAZ, ama bir `z.object` ALANI olarak sorunsuz kullanılır —
    `state.status` ve `game:over.status` bu yüzden değişmezi mesaj seviyesinde
    de dayatıyor (testli). `transportStatusInnerSchema` ADR-0001'in öngördüğü
    gibi ayrıca dışa veriliyor.
  - >
    zod 4'te `.partial()` bilinmeyen anahtarı reddetmez ve `.strict()`
    kullanımdan kalkma yolunda. `PATCH /api/profile` gövdesi bu yüzden
    `z.strictObject({ name: ...optional(), theme: ...optional() })`.
  - >
    `@typescript-eslint/consistent-indexed-object-style` iç içe eşleme tipini
    (`{ readonly [K in ...]: string }`) indeks imzası sanıp hata verir;
    `Readonly<Record<K, string>>` yazımı kuralı geçer, anlam aynı kalır.
  - >
    `packages/shared` `vitest`/`@vitest/coverage-v8`'i kendi `devDependencies`'ine
    yazmıyor, kök devDependency üzerinden çalışıyor. `game-core` yazıyor. Bu
    tutarsızlık bugün zarar vermiyor ama izole bir CI job'u paketi tek başına
    koşturursa kırılır.

blocked_reason: null

next_suggestions:
  - >
    `docs/memory/api-contract.md` §WebSocket + §REST tablosu artık gerçek
    şemalara işaret edebilir (`rest-contract.ts`, `ws-protocol.ts`,
    `message-keys.ts`). Dosya benim çakışma kümemin dışında olduğu için
    dokunmadım — lead ya da memory-curator güncellemeli.
  - >
    Tasarım §2 dosya listesindeki `room-client.ts` (saf reducer) ve
    `ws-client.ts` (yeniden bağlanma/heartbeat taşıması) bu kartın kabul
    kriterlerinde YOK; ayrı bir görev olarak planlanmalı. `WS_CLOSE` +
    `isReconnectableCloseCode` + `serverMessageSchema` onlar için hazır.
  - >
    UI-001 `tr.ts` testini `diffMessageKeys(tr)` üzerine kursun:
    `expect(diffMessageKeys(tr)).toEqual({ missing: [], extra: [] })` ve
    `const _: MessageTree = tr` tip kısıtı. `errors` grubu `ERROR_CODES`'un
    kendisidir, ayrıca listelemeye gerek yok.
  - >
    `/api/health` ve `/api/health/realtime` şemaları bilerek yazılmadı: health
    uç noktası zaten canlı ve `realtime` sondası RT-PROBE'un işi. İkisi de
    sözleşmeye girecekse ayrı bir kartla eklensin.
```
