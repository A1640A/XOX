```yaml
task: DB-004
status: done
summary: >
  DB-003'ün "apps/web'de mongoose modeline uzanan tek yer kapanır" iddiası
  YANLIŞTI — ve bu kartın kendi dört dosyalık listesi de EKSİKTİ. Gerçek
  tarama (`import { X } from '@xox/db'` içinde X ∈ {Room,User,Game,
  Friendship,MobileRefreshToken,WsTicket}) DOKUZ dokunma noktası buldu, kart
  yalnız dördünü biliyordu. Sınır artık İNCELEMEYLE değil `eslint.config.mjs`
  içindeki `no-restricted-imports` ile dayatılıyor: `@xox/db`'den bu altı ham
  Model adının apps/web'de içe aktarılması (test dosyaları hariç) hata.
  `allowTypeImports: true` sayesinde `import type { User as UserModel }` gibi
  çalışma zamanına hiç girmeyen tip importları serbest.

  KARARLAR (bu kartın çakışma kümesindeki 4 dosya için):
    - health/realtime/route.ts → İSTİSNA. RT-PROBE-001 sondası; prod'da 404,
      api-contract.md'de "KARAR KAPISI geçildi, Dalga 0'da yeniden yazılmaz"
      diye zaten dondurulmuş. Amacı ham sürücü/model davranışını (ready
      olayı, resumeToken sarmalayıcı/sürücü farkı) ölçmek — bir soyutlama
      tam da ölçülmek istenen şeyi gizlerdi.
    - lib/realtime/room-hub.ts → İSTİSNA (ADR-0002, kart zaten "zorunlu"
      diyordu). `Room.watch()` instance-başına-tek-stream kayıt defterinin
      TEK yeridir; Fluid instance ömrü + globalThis tekilleştirmesi web
      çalışma zamanı kaygısıdır, packages/db'ye taşınamaz. Aynı dosyadaki
      `Room.findOne({code}).lean()` (deps.findRoom) de aynı istisnaya dahil
      edildi — packages/db'de zaten 7 dosyada birebir aynı satır var
      (apply-move/join/resign/detach/settle/rematch/create), ayrı bir
      `findRoomByCode` sarmalayıcısı eklemek bu kartın izinli dosya kümesine
      YENİ bir packages/db dosyası gerektirir (bkz. aşağıdaki "taşınamadı"
      notu) — o yüzden bilinçli olarak İSTİSNA'ya dahil edildi, taşınmadı.
    - lib/auth/authorize.ts → İSTİSNA (taşınamadı, gerekçe aşağıda).
    - app/api/auth/register/route.ts → İSTİSNA (taşınamadı, gerekçe aşağıda).

  "TAŞINAMADI" GEREKÇESİ (authorize.ts + register.ts, dürüstçe): mimari
  olarak DOĞRU çözüm packages/db'ye `queries/auth.ts` gibi YENİ bir dosya
  eklemek olurdu (repo zaten `queries/friends.ts`, `queries/leaderboard.ts`,
  `queries/matches.ts` kalıbını kullanıyor) — ama bu kartın YETKİLİ çakışma
  kümesi packages/db içinde SADECE `src/index.ts`'i listeliyor, yeni bir
  dosya YASAK (CLAUDE.md: "kümenin DIŞINA çıkma... lead'e söyle"). İkinci,
  bağımsız gerekçe: `authorize.ts`'in sorgusu KK-005 sabit-zamanlı giriş
  mantığıyla (verifyFakePassword dalı) AYNI fonksiyonda olmak ZORUNDA —
  ikiye bölmek timing-safety garantisini iki modüle dağıtır. `register.ts`
  için ise tek kural zaten Mongo'nun `email_1` unique indeksi, uygulama
  kodunda korunan bir DOMAIN İNVARYANTI yok (yalnız pahalı argon2 hash'ten
  önce ucuz bir ön-kontrol). SONUÇ: bu iki dosya için gerçek "taşı" işi
  packages/db'de yeni dosya açma yetkisi olan AYRI bir kart gerektirir
  (öneri: DB-007 "User sorgularını packages/db/src/queries/auth.ts'e taşı").

  KEŞFEDİLEN 5 EK İHLAL (bu kartın DIŞINDA, dokunulmadı, geçici olarak
  lint istisnasına eklendi ki `pnpm gates` bu kartın yazma yetkisi olmayan
  dosyalar yüzünden kırmızı kalmasın):
    - apps/web/app/api/rooms/[code]/ws/route.ts → Room (deps.findRoom, aynı desen)
    - apps/web/app/api/profile/route.ts → User (findById)
    - apps/web/lib/theme.ts → User (findById, tema çözümü)
    - apps/web/app/api/auth/mobile/shared.ts → MobileRefreshToken (create)
    - apps/web/app/api/auth/mobile/refresh/route.ts → MobileRefreshToken (findOneAndDelete)
  Bunların HİÇBİRİ bu kartın çakışma kümesinde değildi; dosyaların kendisine
  DOKUNULMADI, yalnız eslint.config.mjs'deki `ignores` listesine "DB-004
  sırasında keşfedildi, takip kartı gerekir" notuyla eklendiler. Takip kartı
  önerisi: DB-005/006 numaraları dolu olduğu için DB-007 (ya da lead hangi
  numarayı uygun görürse) — "geri kalan 5 ihlali packages/db'ye taşı ya da
  ayrı ayrı gerekçelendir".

  TEST DOSYALARI (`apps/web/**/*.test.{ts,tsx}`, `*.spec.{ts,tsx}`) blok
  DIŞI bırakıldı: `presence.test.ts`, `double-execution.test.ts`,
  `mobile/refresh/route.test.ts` gerçek Atlas'a koşan entegrasyon testleri,
  kurulum/yıkım ve ham durum doğrulaması için MODELE bilerek doğrudan erişir
  — conventions.md zaten `boundaries/dependencies`'i test dosyaları için
  aynı şekilde gevşetiyor. `authorize.test.ts` zaten yalnız TİP import
  ediyordu (`import type { User as UserModel }`), `allowTypeImports: true`
  bunu ayrıca kapsıyor.

mekanizma:
  dosya: eslint.config.mjs
  kural: no-restricted-imports (paths: name '@xox/db', importNames Room/User/
    Game/Friendship/MobileRefreshToken/WsTicket, allowTypeImports: true)
  kapsam: 'apps/web/**/*.{ts,tsx}' EKSİ (test dosyaları + 9 gerekçelendirilmiş istisna)
  onemli_detay: >
    Bu blok apps/web dosyaları için no-restricted-imports'un TEK kaynağıdır
    (flat config aynı kuralı farklı bloklarda MERGE etmez, en spesifik/son
    eşleşen blok DEĞİŞTİRİR). Bu yüzden Playwright duvarının patterns'i
    (PLAYWRIGHT_PATTERNS) bu blokta TEKRAR taşındı — aksi halde apps/web'de
    Playwright duvarı sessizce kapanırdı. `apps/web/app/api/rooms/[code]/
    ws/route.ts` glob'unda `[code]` KARAKTER SINIFI olarak yorumlanabildiği
    için `\\[code\\]` olarak KAÇIRILDI (minimatch/eslint glob'u köşeli
    parantezi escape'siz literal almaz) — kanıt aşağıda.

kirmizi_kanit: |
  $ mkdir -p apps/web/app/api/_db004_probe && cat > .../route.ts <<'EOF'
  import { User, connectDb } from '@xox/db'
  export async function GET(): Promise<Response> {
    await connectDb()
    const u = await User.findOne({ email: 'x@example.com' }).lean()
    return Response.json({ u })
  }
  EOF
  $ pnpm exec eslint apps/web/app/api/_db004_probe/route.ts
  .../route.ts
    1:10  error  'User' import from '@xox/db' is restricted. apps/web ham
    Mongoose modeline erişemez (DB-004, bkz. docs/board/reports/DB-004.md).
    packages/db'nin rooms/*, queries/* fonksiyonlarından birini kullan; yoksa
    packages/db'ye ekle (yeni dosya = ayrı çakışma kümesi, ayrı kart)  no-restricted-imports
  ✖ 1 problem (1 error, 0 warnings)
  EXIT=1
  # (probe dosyası sonra silindi, git status temiz kaldı)

yesil_kanit_istisna: |
  $ pnpm exec eslint apps/web/app/api/health/realtime/route.ts \
      apps/web/lib/realtime/room-hub.ts apps/web/lib/auth/authorize.ts \
      apps/web/app/api/auth/register/route.ts
  EXIT=0
  # 4 dosya da Room/User import ediyor, hiçbiri hata vermedi — istisna listesi çalışıyor.

yesil_kanit_bracket_glob: |
  $ pnpm exec eslint "apps/web/app/api/rooms/[code]/ws/route.ts"
  EXIT=0
  # \\[code\\] escape'i olmadan bu dosya "[code]" karakter sınıfıyla
  # eşleşmeyip yanlışlıkla YAKALANIRDI (Room import'u var) — escape'li
  # haliyle doğru ignore edildiği doğrulandı.

files_changed:
  - eslint.config.mjs # no-restricted-imports (paths) + 9 istisna + kaçırılmış [code] glob'u
  - docs/board/reports/DB-004.md

dod:
  pnpm_gates: |
    check:dead-exports  OK (16 bilinen borç, yeni yok)
    typecheck (turbo --force benzeri, worktree'de tek sefer, 7/7, 0 cached ilk koşuda)  OK
    lint (eslint . --max-warnings=0)  OK
    format:check (prettier --check .)  OK (eslint.config.mjs'i prettier --write ile düzeltip yeniden koşuldu)
    test:coverage  OK — İLK koşuda apps/web'in 3 gerçek-Atlas testi
      (double-execution/presence/mobile-refresh) `.env.local` worktree'ye
      kopyalanmadığı için MONGODB_URI hatasıyla düştü (bilinen gotcha,
      "Worktree .env.local'ı ALMAZ" — bu kartın DEĞİŞİKLİĞİYLE İLGİSİZ).
      `.env.local` ana repo kökünden kopyalandıktan sonra TÜM 6 paket
      (game-core dahil, 248/248, %100 kapsam) yeşil.
    knip  OK (yalnız mevcut yapılandırma ipuçları, yeni sorun yok)
    exit: 0 (tüm zincir `&&` ile bağlı, tek komut exit 0)
  worktree_env: '.env.local ana repo kökünden kopyalandı (gitignore'lu, commit edilmedi)'
  merge_push: YAPILMADI — kart talimatına uygun. Reddedilen izin istemi olmadı.

not_lead_icin: >
  Bu kart "apps/web mongoose sınırı kapandı" konusunda İKİNCİ kez yanlış
  çıkan bir iddiayı düzeltiyor (DB-003 → bu kart bile eksikti). Gerçek sayı
  9 dokunma noktası, 4'ü bu kartta gerekçelendirildi+istisna edildi, 5'i
  keşfedildi ama DOKUNULMADI (yazma yetkim yoktu). ESLint kuralı ARTIK
  tüm 9'u da yakalıyor (test dosyaları hariç) — yeni bir 10. dosya
  eklenirse KIRMIZI olacak, ama mevcut 5'i düzeltmek için conflict set'i
  packages/db/src/queries/** ve o 5 dosyayı içeren AYRI bir kart gerekir.
```
