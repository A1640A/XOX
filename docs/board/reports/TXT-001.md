```yaml
task: TXT-001
status: done
summary: >
  `apps/web/messages/tr.ts` spec §5'teki Türkçe metin ağacının TAMAMINI (14 anahtar grubu +
  `chat` — board.json'daki kabul kriteri listesi görevin kendi metninde `chat`'i saymayı
  unutmuş ama `message-keys.ts`/board.json ikisi de `chat` grubunu zorunlu kılıyor, o yüzden
  dahil edildi) birebir spec metinleriyle içerecek şekilde yazıldı ve `tr: MessageTree`
  (`@xox/shared`) olarak tiplendi — bu atama tek başına derleme zamanında eksik/fazla anahtarı
  yakalıyor (excess-property + missing-property check). `apps/mobile/messages/tr.ts` AYNI
  ağacı, web'i IMPORT ETMEDEN, bağımsız bir dosya olarak taşıyor (boundaries: mobil web'e
  bağımlı olamaz). `apps/web/messages/tr.test.ts` her iki ağacı da DIŞ kaynağa
  (`@xox/shared`: `diffMessageKeys`, `MESSAGE_KEYS`, `errorCodeSchema.options`) karşı iki
  yönlü doğruluyor, ton kuralını ("siz" hitabı yasak) regex sondasıyla, yer tutucu biçimini
  (`{ad}`/`{saniye}`/`{tas}`) ve web↔mobil yer tutucu paritesini çıplak metin sondalarıyla
  kilitliyor. Mobil ağaç için AYRI bir `apps/mobile/messages/tr.test.ts` YAZILMADI — gerekçe
  `decisions`'ta, kanıt `sonda_output`'ta.
files_changed:
  - apps/web/messages/tr.ts
  - apps/web/messages/tr.test.ts
  - apps/mobile/messages/tr.ts
  - docs/board/reports/TXT-001.md
tests:
  {
    added: 19,
    passing: 38,
    coverage: 'apps/web messages/tr.ts: %100 (dosya kapsamı) — paket geneli statements 92.66%/branches 80.35%/functions 92.59%/lines 99.24% (eşik: 70/65/70/70, hepsi geçti)',
  }
decisions:
  - karar: >
      `apps/mobile/messages/tr.test.ts` yazılmadı; mobil ağacın parite/ton/yer-tutucu
      doğrulaması `apps/web/messages/tr.test.ts` içine taşındı (`trMobil` adıyla relative
      import: `../../mobile/messages/tr`).
    gerekçe: >
      Bu kartın çakışma kümesi YALNIZCA `apps/web/messages/**` ve `apps/mobile/messages/**`.
      `apps/mobile` bugün vitest'e sahip değil (`apps/mobile/node_modules`'ta `vitest` yok,
      `@types/node` yok, `package.json`'da `vitest` devDependency'si yok). Bir
      `apps/mobile/messages/tr.test.ts` dosyası `import { describe } from 'vitest'` yazsaydı,
      modül çözümlenemediği için `apps/mobile`'ın KENDİ `tsc --noEmit -p tsconfig.json`'ı
      (mobil tsconfig `include: ["**/*.ts", ...]` test dosyalarını da kapsıyor) KIRILIRDI —
      bunu düzeltmek `apps/mobile/package.json`'a `vitest`/`@vitest/coverage-v8` eklemeyi
      gerektirir ki bu dosya bu kartın çakışma kümesinin DIŞINDA (board.json'da
      `apps/mobile/**` W2-03'ün çakışma kümesi — `apps/mobile/messages/**` bilinçli olarak
      ondan ayrıştırılıp bu karta verilmiş, yani `package.json` W2-03'e ayrılmış). Bunun
      yerine iki bağımsız savunma kuruldu: (1) `tr: MessageTree` ataması mobilin KENDİ
      `typecheck` script'inde (zaten var, dokunulmadı) derleme zamanında anahtar paritesini
      zorunlu kılıyor — kanıtı aşağıda. (2) `apps/web/messages/tr.test.ts` mobil ağacı
      GERÇEKTEN İTHAL EDİP çalışma zamanında `diffMessageKeys`/`errorCodeSchema`/ton
      regex'iyle test ediyor — `pnpm --filter @xox/web test` altında koşuyor, board'un
      `pnpm gates`'ine dahil. Web'den mobile relative import, `boundaries/dependencies`
      kuralında yalnızca TEST dosyaları için (`**/*.test.{ts,tsx}`) zaten kapalı
      (`eslint.config.mjs`), üretim kodu değil.
    reddedilen_alternatif: >
      `apps/mobile/package.json`'a vitest eklemek — daha "doğru" mimari görünür ama bu kartın
      açıkça belirtilen çakışma kümesini ihlal eder ve W2-03'ün (apps/mobile/** sahibi, dalga
      2) alanına erken girer. Karar geri alınabilir: W2-03 vitest'i kurduğunda
      `apps/mobile/messages/tr.test.ts`'i eklemek tek dosyalık, önceki testlerin bir kopyası.
  - karar: >
      `tr.errors` ayrı bir `Record<ErrorCode, string>` tip anotasyonu yerine `tr: MessageTree`
      (bütün ağacı kapsayan tek anotasyon) ile tiplendi.
    gerekçe: >
      `MessageTree['errors']` zaten `Readonly<Record<MessageKey<'errors'>, string>>` ve
      `MessageKey<'errors'>` `MESSAGE_KEYS.errors` (= `ERROR_CODES` = `errorCodeSchema.options`)
      üzerinden `ErrorCode`'un kendisine eşit — yani kriter #2'nin istediği tipleme zaten
      sağlanıyor, ayrı bir anotasyon kod tekrarı olurdu. Runtime karşılaştırması (kriter #2'nin
      "VE çalışma zamanında" kısmı) yine de `errorCodeSchema.options`'a karşı AYRI ve dolaylı
      olmayan bir testle yazıldı (message-keys.ts'in ERROR_CODES'unu değil, doğrudan
      errors.ts'in errorCodeSchema'sını kullanıyor).
gotchas:
  - >
    Görevi veren üst mesajın serbest metni kabul kriterinde grup listesini sayarken `chat`'i
    ATLAMIŞ ("app, common, auth, home, computer, room, game, connection, rematch, profile,
    leaderboard, history, friends, errors" — `chat` yok). Ama board.json'daki TXT-001 kartının
    GERÇEK kabul kriteri metninde `chat` VAR, `@xox/shared/message-keys.ts` (CTR-001, dondu)
    `chat: ['sendEmoji', 'tooFast']` grubunu ZORUNLU kılıyor ve spec §5'in kendisinde de `chat`
    var. "tamamını" talimatına uyup `chat`'i dahil ettim — dahil etmeseydim `tr: MessageTree`
    ataması `chat` eksik olduğu için `tsc`'de TS2741 ile derleme hatası verirdi; gerçekten
    denedim (`chat` bloğunu sildim, `pnpm --filter @xox/web typecheck` çalıştırdım, hata aldım,
    geri ekledim) — çıktı `sonda_output`'ta "SİLME SONDASI 3".
  - >
    `pnpm gates` sonunda knip iki yeni "hint" (hata değil) veriyor: `@xox/shared apps/web` ve
    `@xox/shared apps/mobile` için "Remove from ignoreDependencies" — bu paket artık GERÇEKTEN
    kullanıldığı için knip.json'daki savunmacı ignore girdisi gereksiz hale geldi. Bilerek
    DOKUNULMADI (`knip.json` bu kartın çakışma kümesinde değil, OPS-002 sahibi); sıradaki
    knip.json'a dokunan kart bu iki satırı temizleyebilir.
sonda_output: |
  === SİLME SONDASI 1 — apps/web/messages/tr.ts'ten room.leave silindi ===
  $ (room.leave satırı silindi) && pnpm --filter @xox/web test
  FAIL messages/tr.test.ts > tr.ts — @xox/shared/message-keys ile parite (iki yönlü) > eksik ya da fazla anahtar yok
  AssertionError: expected { Object (missing, extra) } to deeply equal { missing: [], extra: [] }
  - Expected  + Received
    {
      "extra": [],
  -   "missing": [],
  +   "missing": [ "room.leave" ],
    }
  Test Files  1 failed | 2 passed (3)   Tests  1 failed | 37 passed (38)
  $ (room.leave geri eklendi) && pnpm --filter @xox/web test
  Test Files  3 passed (3)   Tests  38 passed (38)

  === SİLME SONDASI 2 — apps/mobile/messages/tr.ts'ten room.leave silindi ===
  $ (mobil room.leave silindi) && pnpm --filter @xox/mobile typecheck
  messages/tr.ts(63,3): error TS2741: Property 'leave' is missing in type '{ title: string; ... }'
    but required in type 'Readonly<Record<"resign" | "code" | ... | "leave", string>>'.
  [ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL] @xox/mobile@0.0.0 typecheck: exit 2
  $ pnpm --filter @xox/web test   # (aynı anda mobil tr.ts hâlâ bozukken, cross-import üzerinden)
  FAIL messages/tr.test.ts > tr.ts (mobil) — ... > eksik ya da fazla anahtar yok
  AssertionError: ... "missing": [ "room.leave" ] ...
  Test Files  1 failed | 2 passed (3)   Tests  1 failed | 37 passed (38)
  $ (mobil room.leave geri eklendi) && pnpm --filter @xox/mobile typecheck && pnpm --filter @xox/web test
  (typecheck: sessiz çıktı, 0 hata) — Test Files  3 passed (3)   Tests  38 passed (38)

  === SİLME SONDASI 3 — apps/web/messages/tr.ts'ten TÜM chat grubu silindi ===
  $ (chat bloğu silindi) && pnpm --filter @xox/web typecheck
  messages/tr.ts(17,14): error TS2741: Property 'chat' is missing in type '{ app: ...; errors: {...}; }'
    but required in type 'MessageTree'.
  [ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL] @xox/web@0.0.0 typecheck: exit 2
  $ (chat bloğu geri eklendi) && pnpm --filter @xox/web typecheck && pnpm --filter @xox/web test
  (typecheck: sessiz çıktı, 0 hata) — Test Files  3 passed (3)   Tests  38 passed (38)

  === TON SONDASININ KENDİSİ ÇALIŞIYOR MU? (yanlış pozitif/negatif kontrolü) ===
  SIZ_HITABI.test('Siz de mi buradasınız?')        -> true   (yakaladı)
  SIZ_HITABI.test('Emin misiniz?')                 -> true   (yakaladı)
  SIZ_HITABI.test('Hoş geldin, {ad}')              -> false  (doğru negatif)
  SIZ_HITABI.test('Henüz tamamlanmış oyunun yok.') -> false  (yanlış pozitif YOK — "Henüz"
                                                                 fiil eki değil, ilk denemede
                                                                 genel "n[iıuü]z" regex'i bunu
                                                                 yanlışlıkla yakalamıştı, dar
                                                                 sondaya (sınız/siniz/sunuz/
                                                                 sünüz) geçilerek düzeltildi)

  === pnpm gates — TAM ÇIKTI SONU ===
  $ pnpm gates ; echo "EXIT_CODE=$?"
  ... (typecheck 7/7 paket başarılı, lint 0 hata, format:check tüm dosyalar Prettier'e uygun,
       test:coverage 5/5 paket başarılı — @xox/web 38/38 test / statements 92.66%,
       branches 80.35%, functions 92.59%, lines 99.24% (eşik 70/65/70/70), knip yalnızca
       19 "Configuration hint", sıfır hata) ...
  EXIT_CODE=0
next_suggestions:
  - >
    W2-03 (`apps/mobile/**`, dalga 2) mobile'a vitest kurduğunda
    `apps/mobile/messages/tr.test.ts` eklenebilir — içerik
    `apps/web/messages/tr.test.ts`'teki mobil-özel `describe` bloklarının (parite,
    errorCodeSchema, ton) birebir taşınmasıdır; o zaman `apps/web/messages/tr.test.ts`'teki
    mobil-özel bloklar TEK KAYNAK ilkesine uymak için oradan silinebilir (yalnızca web/mobil
    yer-tutucu parite karşılaştırması web tarafında kalmalı, çünkü iki ağacı aynı anda
    karşılaştırmak zaten bir tarafın diğerini import etmesini gerektiriyor).
  - >
    `knip.json`'a dokunan bir sonraki kart `@xox/shared`'ı `apps/web` ve `apps/mobile`
    `ignoreDependencies` listelerinden çıkarabilir (artık gerçekten kullanılıyor, knip bunu
    kendisi "hint" olarak bildiriyor — hata değil, temizlik fırsatı).
```
