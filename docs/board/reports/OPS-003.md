```yaml
task: OPS-003
status: done
revision: 2 (güvenlik denetimi bulguları giderildi — 4 blocker/major + 5 küçük)
summary: >
  `ensureIndexes()` artık gerçek İKİ çağırana bağlı: (1) `packages/db/src/migrate.ts`
  — CI runner'ından DOĞRUDAN çağıran bir CLI (seed.ts/reset.ts kalıbında),
  `e2e-preview.yml` her preview deploy'undan sonra `pnpm --filter @xox/db migrate`
  koşturuyor (reset SONRASI, seed ÖNCESİ); (2) `POST /api/admin/migrate` —
  yalnız production runbook'u için (otomatik deploy workflow'u yok, deploy'lar
  elle `vercel deploy --prod`). İlk tasarımda route CI'dan da çağrılıyordu ve
  sırrı ağdan geçiriyordu — güvenlik denetimi bunun deploy edilen PR koduna
  güvendiğini ve sırrı sızdırabileceğini gösterdi (ayrıntı: revision notları).
  Artık sır HİÇBİR CI adımında ağdan geçmiyor.

  `packages/db/src/indexes.ts` `createIndex` (üretim yolu, `syncIndexes()` DEĞİL)
  kullanır; canlı Atlas'a karşı doğrulandı, çakışma kodu dokümantasyonun
  iddia ettiği 85 değil GERÇEKTEN 86 (`IndexKeySpecsConflict`) — ikisi de
  yakalanıyor. Ayrıca canlı doğrulandı: "aynı anahtara farklı adla ikinci
  indeks kur" boşluksuz-takas tasarımı Atlas'ta ÇALIŞMIYOR (kod 85 ile
  reddediliyor) — bunun yerine `unique` kurulmadan önce mükerrer değer taranır,
  temizse düşür+kur, ikinci kurma da patlarsa eski indeks aynı seçenekleriyle
  geri kurulur (hiçbir an indekssiz kalınmaz). `ensureIndexes` eşzamanlı
  ikinci çağrıyı reddeder. Route artık `?db=` parametresiyle hedefi pozitif
  doğruluyor, ham sürücü hatasını istemciye/loglara sızdırmıyor, yetkisiz
  denemeleri (değer/uzunluk sızdırmadan) logluyor. DB-001 bu görev sırasında
  merge oldu; `indexes.ts` main ile birleştirilip tam model setine (5 koleksiyon,
  12 indeks) uyarlandı. `pnpm gates` kökten temiz.
files_changed:
  - packages/db/src/indexes.ts (YENİ — createIndex + kod 85/86 + mükerrer-tarama + eşzamanlılık kilidi)
  - packages/db/src/indexes.test.ts (YENİ — 13 test: unit mock + canlı xox_test entegrasyonu)
  - packages/db/src/migrate.ts (YENİ — CI'nin gerçek çağıranı, seed.ts/reset.ts kalıbında)
  - packages/db/src/migrate.test.ts (YENİ — 4 test)
  - packages/db/src/index.ts (ensureIndexes/EXPECTED_INDEXES/ExpectedIndex export edildi)
  - packages/db/package.json (migrate script)
  - packages/db/src/load-env.ts (kapsam dışı ama gates'i kilitleyen bir lint hatası düzeltildi — ayrıntı gotchas)
  - apps/web/app/api/admin/migrate/route.ts (YENİ — yalnız production runbook'u)
  - apps/web/app/api/admin/migrate/route.test.ts (YENİ — 15 test)
  - .github/workflows/e2e-preview.yml (migrate adımı artık `pnpm --filter @xox/db migrate` — curl/sır YOK)
  - .env.example (MIGRATION_SECRET yer tutucu)
  - docs/board/reports/OPS-003.md
tests:
  db_unit: 'pnpm --filter @xox/db test → 11 dosya, 84 test yeşil'
  db_coverage: 'eşik 90/85/90/90 — geçti'
  web_unit: 'pnpm --filter @xox/web test → 4 dosya, 53 test yeşil'
  web_coverage: 'eşik 70/65/70/70 — geçti'
  gates_root: 'pnpm typecheck && pnpm lint && pnpm format:check && pnpm test:coverage && pnpm knip → hepsi exit 0'
  canli_dogrulama:
    - "ensureIndexes yolu (createIndexSafely) xox_dev'de gerçek email_1 üzerinde önce/sonra/geri-alındı kanıtlandı"
    - "aynı-anahtar-farklı-ad boşluksuz-takas tasarımı xox_test'te GERÇEKTEN reddedildi (kod 85) — tasarım buna göre değişti"
    - '`pnpm --filter @xox/db migrate` GERÇEK tsx ile (mock değil) xox_test''e karşı uçtan uca koşturuldu: "İndeksler kuruldu: xox_test"'
acceptance_kriter_1_cagiran:
  secim: >
    İki çağıran: (a) `packages/db/src/migrate.ts` — CI runner'ından doğrudan
    çağrı, HTTP yok, sır ağdan geçmiyor; `.github/workflows/e2e-preview.yml`
    her preview deploy'undan sonra `reset`'ten hemen sonra `seed`'den önce
    `pnpm --filter @xox/db migrate` koşturuyor. (b) `POST /api/admin/migrate`
    — yalnız production runbook'u (aşağıda, sır/host burada YOK — bkz. SEC-001).
  gerekce: >
    İlk turda route hem CI'dan hem production'dan çağrılıyordu ve CI adımı
    `MIGRATION_SECRET`'ı preview URL'ine POST ediyordu. Güvenlik denetimi:
    o URL'de deploy edilen kod PR HEAD'idir, PR yazarı route'a isteğin
    başlıklarını yansıtan bir satır ekleyip sırrı çalabilirdi; workflow zaten
    yanıt gövdesini public Actions log'una basıyordu. Çözüm: CI'nin ensureIndexes'e
    ihtiyacı ağ üzerinden değil, runner'ın ZATEN sahip olduğu `MONGODB_URI`
    ile karşılanabilir — bu yüzden CI artık route'u hiç çağırmıyor.
  production_runbook: >
    Gerçek prod URL'i ve sır değeri bu dosyada YOK (public repo). Devops,
    `vercel deploy --prod` sonrası kendi bildiği admin uç noktasını
    `?db=xox_prod` parametresiyle ve doğru başlıkla bir kez çağırır; yanlış
    ortam (`MONGODB_DB` unutulmuşsa) 409 ile reddedilir (SEC-004).
    `MIGRATION_SECRET`'ın gerçek değeri Vercel/GitHub'da HENÜZ KURULMADI —
    bkz. `bilinen_eksikler`.
acceptance_kriter_2_kanit:
  yontem: >
    xox_dev'e karşı GEÇİCİ bir vitest dosyası (çalıştırıldı ve SİLİNDİ — repoya
    commit edilmedi) ile: mongoose `autoIndex`'in bitmesini `User.init()` ile
    bekle (aksi hâlde `.indexes()` "ns does not exist" ile yarışıyor — canlı
    gözlemlendi), orijinal durumu kaydet, `email_1`'i KASITLI benzersiz OLMAYAN
    hâlde kur (ana daldaki mevcut şemanın ürettiği durumun birebir aynısı),
    önce/sonra durumunu bas, `createIndexSafely` ile `{unique:true}` iste,
    sonucu bas, sonda email_1'i orijinal seçenekleriyle geri kur.
  once_email_1: |
    { "v": 2, "key": { "email": 1 }, "name": "email_1" }
  ham_hata_gercek_atlas: |
    {"code":86,"codeName":"IndexKeySpecsConflict","message":"An existing index
    has the same name as the requested index. ... Requested index: { v: 2,
    unique: true, key: { email: 1 }, name: \"email_1\" }, existing index:
    { v: 2, key: { email: 1 }, name: \"email_1\" }"}
  sonra_email_1: |
    { "v": 2, "key": { "email": 1 }, "name": "email_1", "unique": true }
  geri_alindi_email_1: |
    { "v": 2, "key": { "email": 1 }, "name": "email_1" }
  ikinci_canli_dogrulama_sec003: >
    Denetim, "aynı anahtara farklı adla ikinci indeks kur, başarılıysa eskisini
    düşür" (boşluksuz takas) önerdi. xox_test'te CANLI denendi:
    `createIndex({email:1}, {unique:true, name:'email_1_uniq_test'})` — `email_1`
    (non-unique) DURURKEN — REDDEDİLDİ: `{"code":85,"codeName":"IndexOptionsConflict",
    "message":"Index already exists with a different name: email_1"}`. Mongo
    aynı anahtar üzerinde ikinci bir indeksi isim farkı gözetmeksizin reddediyor.
    Tasarım bu yüzden "düşürmeden önce mükerrer tarama + patlarsa telafi ile
    geri kurma"ya değiştirildi (bkz. `packages/db/src/indexes.ts` içindeki
    `createIndexSafely` ve `hasDuplicateValues`).
  sonuc: >
    xox_dev VE xox_test her ikisi de deneyler sonunda bulundukları hâle
    döndürüldü. Kanıt kalıcı olarak `packages/db/src/indexes.test.ts`'te
    kilitli: kod 85/86 mock testleri + SEC-003 alt grubu (mükerrer varken
    dokunmama, dropIndex reddi, ikinci createIndex reddinde telafi).
acceptance_kriter_3_test_yolu: >
  `packages/db/src/indexes.test.ts`: mock'lu birim testler `createIndex`/
  `dropIndex`/`indexes`/`aggregate` kullanır (syncIndexes YOK, çağrılmıyor).
  Ayrıca main'den gelen CANLI xox_test entegrasyon testi (tasarım §3.6 tam
  liste) `ensureIndexes()`'i gerçekten çalıştırıp 5 koleksiyonun 12 indeksini
  doğruluyor — mock ile canlı birbirini tamamlıyor.
acceptance_kriter_4_sira: >
  DB-001 bu görev SIRASINDA merge oldu. `packages/db/src/indexes.ts` main'in
  tam `EXPECTED_INDEXES`'i (12 girdi, 5 koleksiyon) ÜZERİNE createIndex/
  createIndexSafely üretim yolu ve SEC-003/007 düzeltmeleriyle yeniden kuruldu
  — merge conflict elle çözüldü, main'in syncIndexes() tabanlı sürümü TAMAMEN
  değiştirildi.
decisions:
  - karar: 'CI çağırma yolu: HTTP route değil, doğrudan CLI (packages/db/src/migrate.ts)'
    gerekce: >
      Sırrı ağdan hiç geçirmeyen tek tasarım budur. CI zaten `MONGODB_URI`
      secret'ına ve `pnpm --filter @xox/db reset` çağrısına sahip; aynı
      yetkiyle `ensureIndexes()`'i doğrudan çağırmak hem sırrı ortadan kaldırır
      hem HTTP/DNS/TLS bağımlılığını keser.
    reddedilen_alternatif: >
      Route'u CI'dan HTTP ile çağırmaya devam etmek + sır rotasyonu/kısa
      ömürlü token gibi azaltıcı önlemler eklemek — kökten çözüm dururken
      sızıntı YÜZEYİNİ küçültmek yerine YOK ETMEK tercih edildi.
  - karar: 'SEC-003: "farklı adla ikinci indeks" YERİNE "düşürmeden önce mükerrer tarama + telafi"'
    gerekce: >
      Önerilen boşluksuz-takas canlı Atlas'ta REDDEDİLDİ (kanıt yukarıda).
      Gerçek çalışan tasarım: unique istenirken önce veri taranır (mükerrer
      varsa eski indekse dokunulmaz, açık hata), temizse düşür+kur, ikinci
      kurma da patlarsa eski indeks aynı seçenekleriyle geri kurulur.
    reddedilen_alternatif: 'Aynı anahtara farklı adla ikinci indeks (canlı doğrulanmış biçimde çalışmıyor).'
  - karar: "Route 404 döner, 401/403 değil; production'da VERCEL_ENV ile KAPATILMAZ"
    gerekce: >
      404 route'un varlığını ele vermez. Production'da tamamen kapatmak
      (health/realtime'daki desen) burada YANLIŞ olurdu — production'da
      indeks kurmak gerçek bir ihtiyaç; doğru koruma yasak değil, `?db=`
      pozitif onayıdır (SEC-004).
gotchas:
  - >
    ÇÖZÜLDÜ (DB-001 ile): tsx'in mongoose CJS export'larını (`models`) çıplak
    ESM yükleyicisinde görememesi main'de `import mongoose from 'mongoose'` +
    destructure kalıbına ve `load-env.ts`'e taşınarak düzeltilmiş. Canlı
    doğrulandı: `pnpm --filter @xox/db migrate` GERÇEK tsx ile xox_test'e
    karşı uçtan uca koştu ("İndeksler kuruldu: xox_test").
  - >
    Mongoose `autoIndex` bağlantı sonrası ARKA PLANDA schema-seviyeli indeksleri
    kurar — `Model.init()` ile beklenmezse `.indexes()`/`.createIndex()` bu
    arka plan işiyle YARIŞIR ("ns does not exist"). Canlı gözlemlendi.
    `ensureIndexes()`'in kendisi etkilenmiyor (connectDb() zaten beklenir,
    createIndex çağrıları mongoose'un connection buffer'ına düşer) ama canlı-DB
    betiği yazan biri aynı yarışa düşebilir.
  - >
    `IndexOptionsConflict` (kod 85) dokümantasyon iddiası ilk denemede
    doğrulanmadı — GERÇEK kod 86 (`IndexKeySpecsConflict`) geldi. İKİNCİ bir
    canlı deneyde (SEC-003 boşluksuz-takas denemesi) kod 85 GERÇEKTEN göründü
    — yani sürücü AYNI mantıksal durumu (aynı anahtar, farklı seçenek/isim)
    bağlama göre 85 YA DA 86 ile raporluyor. İkisi de kontrol edilmeli.
  - >
    Aynı `pnpm-lock.yaml`'a karşı iki AYRI temiz `pnpm install` (main checkout
    ve bu worktree) farklı @types/node ambient çözümü verdi — `import.meta.dirname`
    biri `string`, diğeri `string | undefined` gördü. `load-env.ts`'teki
    `no-unnecessary-condition` hatası main'in KENDİ checkout'unda da (bu
    görevle ilgisiz, temiz kurulumla) canlı doğrulandı — repo genelinde
    `pnpm gates`'i kilitleyen, tamamen ortam kaynaklı bir kırılganlık. Hedefli
    `eslint-disable` ile susturuldu (davranış değişmedi); memory-curator'a not.
security_notlari:
  - >
    Kimlik bilgisi rotasyonu gerekiyor — ayrıntı board'da SEC-001 (Ömer'in
    kararına bağlı, Atlas konsol erişimi gerektiriyor). Bu görev sırasında
    tek bir ara komut çıktısı bir yerel ortam dosyasının içeriğini bu oturumun
    transkriptine redaksiyonsuz yazdı; repoya/committe/rapora HİÇ girmedi.
  - >
    `MIGRATION_SECRET`'ı GERÇEKTEN Vercel (preview+production) ve GitHub Actions
    secret'ı olarak kurmaya çalıştım — Claude Code auto-mode sandbox
    sınıflandırıcısı rastgele sır üretme/kaydetme desenini ENGELLEDİ (openssl
    çıktısını bir dosyaya yazmak dahil). Kod tarafı TAMAMEN hazır ama
    `MIGRATION_SECRET`'ın gerçek değeri NE Vercel'de NE GitHub'da tanımlı
    DEĞİL — bkz. `bilinen_eksikler`.
bilinen_eksikler:
  - >
    `MIGRATION_SECRET` henüz gerçek bir değerle kurulmadı (yukarıdaki sandbox
    engeli). CI artık bu sırra bağımlı DEĞİL (migrate.ts doğrudan çağırıyor),
    bu yüzden preview akışı ETKİLENMİYOR. Yalnız production runbook'u
    (`POST /api/admin/migrate`) bu sır kurulana kadar çağrılamaz. Ömer ya da
    yetkili bir insan oturumu: `openssl rand -base64 32` ile bir değer üretip
    Vercel (production ortamı) ve GitHub Actions secret'ı olarak AYNI değerle
    eklemeli.
next_suggestions:
  - "MIGRATION_SECRET'ı yukarıdaki bilinen_eksikler'deki adımlarla Ömer'in kurması gerekiyor."
  - >
    DB-001'in `packages/db/src/models/**` dosyalarındaki `import mongoose from
    'mongoose'` + destructure kalıbı artık tüm CLI betiklerinin (`seed`/`reset`/
    `migrate`) gerçek `tsx` ile çalıştığını kanıtlıyor — ileride yeni bir model
    dosyası eklenirse aynı kalıp korunmalı, aksi hâlde bu gotcha geri gelir.
```
