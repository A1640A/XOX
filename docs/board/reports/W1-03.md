```yaml
task: W1-03
status: done
summary: >
  Kopma / yeniden bağlanma / resync / takeover katmanı kapandı. Kartın on
  kriterinin dokuzu **davranışsal testle** kilitlendi, biri (kriter 9) dosya
  dokunulmazlığıyla; ayrıca kart dışı **bir MAJOR üretim hatası** bulunup
  düzeltildi.

  **Bulunan hata (kart dışı):** `detachConnection` okuma ile CAS yazması
  arasına giren HERHANGİ bir yazmada (rakibin hamlesi, tembel
  `settleDeadlines`, rakibin `join`i) `expectedVersion` tutmadığı için
  **sessizce hiçbir şey yazmıyordu**. Sonuç görünmez ama ölümcül:
  `presence[seat]` ölü bir `connId` ile takılı kalır, `disconnected` hiç
  damgalanmaz → rakip ne `opponent:left` görür ne de 30 sn sonunda terk
  galibiyetini (ADR-0004) alır; oyun sonsuza kadar "rakip düşünüyor"da donar.
  Pencere bir Atlas gidiş-dönüşü (~10-50 ms) ve "bağlantı koptuğu an rakip
  hamle yaptı" tamamen olağan bir senaryo. Sınırlı yeniden deneme eklendi;
  sahiplik koşulu (`extraFilter`) her denemede yeniden uygulandığı için
  takeover güvenliği ZAYIFLAMIYOR.

  **Kriter 2 ve 3 (kartın kalbi)** artık iki ayrı katmanda kanıtlı: `packages/db`
  düzeyinde koşullu yazma (`detach.test.ts`), ve `apps/web` düzeyinde GERÇEK
  `@xox/db` otoritesine karşı koşan uçtan uca senaryo (`presence.test.ts`) —
  orada rakibin mesaj listesinde `opponent:left`'in **yokluğu** iddia ediliyor
  ve aynı testin ikinci yarısında gerçek kopuşun onu **ürettiği** gösteriliyor.

  **Kriter 5** için `rotate` kablosunun üretimde gerçekten bağlı olduğu
  kanıtlandı: `session.ts`te `getDeadline`i koparan bir mutasyon sondası iki
  testi kırmızıya döndürüyor. Zincir (session → 4499 → istemci → tam state)
  tek bir sahte saat altında uçtan uca koşuyor.

files_changed:
  - packages/db/src/rooms/detach.ts # yeniden deneme döngüsü (MAJOR düzeltme)
  - packages/db/src/rooms/detach.test.ts # +4 test (2'si yarış), gerçek Atlas yarış enjeksiyonu
  - packages/db/src/index.ts # loadEnvLocal barrel'e eklendi (3. tüketici)
  - apps/web/components/room/ConnectionBadge.tsx # data-durum artık 4 değer yazıyor
  - apps/web/components/room/ConnectionBadge.test.tsx # +5 test, yanlış davranışı kilitleyen test düzeltildi
  - apps/web/lib/realtime/presence.test.ts # YENİ — 8 uçtan uca test, GERÇEK @xox/db
  - apps/web/lib/realtime/rotate.test.ts # +5 test — sahte saatle tam tur
  - docs/board/reports/W1-03.md

tests:
  {
    added: 22,
    passing: '490 (@xox/web · 48 dosya) · 137 (@xox/db · 19 dosya) · 342 (@xox/shared)',
    coverage: '@xox/web %94.61 ifade · %87.23 dal · %93.48 fonksiyon · %97.16 satır (eşik 70/65/70/70) · @xox/db %95.94/%90.2/%100/%98.32 (eşik 90/85/90/90)',
    mutation: '4 elle sonda, hepsi `diff -q` ile UYGULANDIĞI doğrulandı, hepsi kırmızı üretti, hepsi `git checkout --` ile geri alındı (`git status --porcelain` boş)',
  }

commits:
  - 8e71169 # fix(db): detachConnection kaybedilen version yarışında yeniden dener
  - 5235ac1 # feat(web): ConnectionBadge devredildi durumunu data-durum'a AYRI yazar
  - f9ff224 # test(web): presence/takeover/resync uçtan uca — GERÇEK @xox/db otoritesine karşı
  - 099370d # test(web): rotasyon tam turu — sunucu 4499 -> istemci gecikmesiz döner (sahte saat)

kriter_2_iki_yonlu_test:
  nerede: >
    `packages/db/src/rooms/detach.test.ts` (mevcut "İKİ YÖNLÜ #1/#2" korundu,
    üstüne 3 test eklendi) VE `apps/web/lib/realtime/presence.test.ts` ("İKİ
    YÖNLÜ — devredilmiş eski bağlantının KAPANIŞI hiçbir şey yazmaz…").
  gercek_cikti: |
    ✓ İKİ YÖNLÜ #1 — hâlâ AKTİF bağlantı: presence temizlenir, playing durumunda disconnected damgalanır, version+1
    ✓ İKİ YÖNLÜ #2 — DEVREDİLMİŞ (takeover edilmiş) eski bağlantı: HİÇBİR ŞEY yazılmaz — presence, disconnected, version aynı kalır (AC6)
    ✓ finished odada presence temizlenir ama disconnected YAZILMAZ (grace anlamsız)
    ✓ YARIŞ — okuma ile CAS arasına başka bir yazma girse bile disconnected YİNE damgalanır
    ✓ YARIŞ — araya giren yazma BİR TAKEOVER ise yeniden deneme yapılır ama HİÇBİR ŞEY yazılmaz
    ✓ YARIŞ — her denemede araya girilirse SESSİZCE pes eder (istisna fırlatmaz, sonsuz döngü yok)
    ✓ waiting durumunda kurucu ayrılırsa presence temizlenir ama disconnected YAZILMAZ (§3.10)
    ✓ presence zaten null ise (önceden ayrılmış) hiçbir şey yazmaz
    ✓ olmayan oda için hiçbir şey yapmadan döner (istisna fırlatmaz)
    Test Files  1 passed (1) · Tests  9 passed (9)

    ✓ İKİ YÖNLÜ — devredilmiş eski bağlantının KAPANIŞI hiçbir şey yazmaz ve rakipte hâlâ opponent:left OLUŞMAZ; ama GERÇEK kopuş onu ÜRETİR 1034ms
  oturum_duzeyinde_ne_iddia_ediliyor: >
    YÖN 1 (devredilmiş kapanış): `version` DEĞİŞMİYOR (`toBe(beforeStale.version)`),
    `presence.O` hâlâ yeni bağlantı, `disconnected` null, ve dağıtımdan sonra
    rakipte `opponent:left` YOK.
    YÖN 2 (aktif kapanış): `version` TAM 1 artıyor, `presence.O` null,
    `disconnected.seat === 'O'`, ve dağıtımdan sonra rakipte `opponent:left` VAR.
    İki yön AYNI testte, aynı odada, aynı akışta — "koşul hiç çalışmıyor" da
    "koşul her şeyi bloke ediyor" da yakalanıyor.

kriter_3_opponent_left_yoklugu:
  test: >
    `presence.test.ts` — "AC2+AC3 — takeover: eski bağlantı 4409 alır, RAKİBİN
    mesajlarında opponent:left OLUŞMAZ, disconnected damgalanmaz" (1333 ms,
    gerçek Atlas `xox_test`).
  kanit_satirlari: |
    // Eski bağlantı change stream olayından kendi connId'sinin gittiğini öğrendi:
    expect(kaanEski.sent).toContainEqual({ type: 'error', code: 'SESSION_TAKEOVER', message: expect.any(String) })
    expect(kaanEski.closes).toStrictEqual([{ code: 4409, reason: 'takeover' }])

    // ⚠️ KARTIN KALBİ — rakipte o olayın YOKLUĞU iddia ediliyor:
    expect(ada.types()).not.toContain('opponent:left')
    expect(ada.closes).toStrictEqual([])

    // Yeni bağlantı kesintisiz:
    expect(kaanYeni.types()).toStrictEqual(['state'])

    // Otorite tarafında grace HİÇ başlamadı:
    expect(room.disconnected).toBeNull()
    expect(room.presence.O).toMatchObject({ connId: 'conn-kaan-YENI' })
  neden_bos_bir_iddia_degil: >
    `ada.types()` gerçekten dolu bir liste (takeover yazması bir `version`
    artışı ürettiği için Ada tam `state` alıyor) — yani "hiç mesaj gelmedi, o
    yüzden opponent:left de yok" gibi vakum bir yeşil DEĞİL. Ayrıca aynı
    dosyadaki İKİ YÖNLÜ testin ikinci yarısı, gerçek kopuşta o mesajın
    ÜRETİLDİĞİNİ aynı yardımcıyla gösteriyor: assertion'ın kendisi çalışıyor.

kriter_5_rotate_kablosu:
  uretimde_bagli_mi: >
    ✅ EVET, ve kanıt mutasyonla. `app/api/rooms/[code]/ws/route.ts` →
    `getDeadline: () => getDeadline()` → `session.ts:runStart()` →
    `scheduleRotation({ getDeadline: deps.getDeadline, … })`. Sonda:
    `session.ts`te `getDeadline: deps.getDeadline` → `getDeadline: () => undefined`
    (diff -q ile uygulandığı doğrulandı).
  sonda_ciktisi: |
    × üretim kablosu bağlı: session rotasyonu KURAR ve süresi dolunca 4499 gönderir 15ms
    × 4499 sonrası yeniden bağlanma GECİKMESİ 0 ms olur ve tam state ile oyun sürer 2ms
    Tests  2 failed | 13 passed (15)
    → `cp /tmp/session.bak …` ile geri alındı; `git status --porcelain` yalnız kendi test dosyamı gösterdi.
  sahte_saat_testi_ciktisi: |
    ✓ üretim kablosu bağlı: session rotasyonu KURAR ve süresi dolunca 4499 gönderir 9ms
    ✓ 4499 sonrası yeniden bağlanma GECİKMESİ 0 ms olur ve tam state ile oyun sürer 2ms
    ✓ KARŞIT KANIT — sınıflandırılmamış 1006 gecikmesiz DEĞİL: 500 ms geri çekilme kurulur (4499 ayrımı gerçek) 1ms
    ✓ AC8 — sürüm boşluğunda bekleyen hamle gelen state`te VARSA onaylanır (0 ms, hata yok) 2ms
    ✓ AC8 — bekleyen hamle gelen state`te YOKSA SESSİZCE silinir (hata rozeti yanmaz) 1ms
    Test Files  1 passed (1) · Tests  15 passed (15)
  ne_kosuyor: >
    GERÇEK `createRoomSession` + GERÇEK `scheduleRotation` + GERÇEK
    `createRoomWsClient`/`roomClientReducer`, tek bir `vi.useFakeTimers()`
    saati altında (sunucu da istemci de `setTimeout` kullanıyor; hiçbir
    callback elle çağrılmıyor). Gerçek bekleme YOK: toplam 15 test 31 ms.
  gecikmesizlik_nasil_olculuyor: >
    Soket SAYIMI ile değil, istemcinin zamanlayıcıya verdiği GECİKME ile:
    4499'da `clientDelays` son değeri **0**, 1006'da **500**
    (`WS_RECONNECT_BASE_MS`, rng 0.5 → jitter 1.0). Sebep aşağıdaki gotcha:
    `@sinonjs/fake-timers` bir tick'in İÇİNDE kurulan 0 gecikmeli zamanlayıcıyı
    aynı `tickAsync(0)` çağrısında koşturmuyor.
  inMs_notu: >
    `ScheduledRotation.inMs()` hâlâ yalnız testten okunuyor — ama
    `scheduleRotation`ın KENDİSİ üretimde çağrılıyor ve artık uçtan uca
    kanıtlı. `inMs()` bir teşhis yüzeyi; silmedim çünkü mevcut birim testler
    gecikme hesabını onun üzerinden kilitliyor.

kk_064_regresyonu:
  test_ciktisi: |
    ✓ KK-064 — oda DOLUYKEN yeniden bağlanan aynı userId tam state alır, ROOM_FULL DÖNMEZ 848ms
    ✓ KARŞIT KANIT — ÜÇÜNCÜ bir userId aynı dolu odada 4403 ROOM_FULL alır (test boş değil) 306ms
  ne_iddia_ediliyor: >
    Oda `playing`, iki koltuk da SAHİPLİ, `disconnected.seat === 'O'` iken aynı
    `userId` YENİ bir `connId` ile bağlanıyor: `closes === []` (yani 4403 yok),
    `sent` TAM OLARAK `['state']`, `state.you === 'O'`. Ardından
    `disconnected` temizleniyor ve rakip `opponent:returned` alıyor.
    Karşıt kanıt aynı dolu odada ÜÇÜNCÜ bir `userId`nin `4403 room-full`
    aldığını gösteriyor — "her şeyi kabul ediyoruz" değil, sıralama doğru.
  sonda: >
    `join.ts`te üç dalın SIRASI ters çevrildi (doluluk kontrolü sahiplik
    kontrolünün ÖNÜNE alındı — spec §3.3'ün "klasik hata"sı). `diff -q`
    uygulandığını doğruladı; `presence.test.ts`te **7/8 test kırmızıya döndü**,
    aralarında KK-064. Geri alındı.

mutasyon_sondalari:
  - sonda: '`detach.ts` — sahiplik koşulu (hem okuma guard`ı hem `extraFilter`) TAMAMEN kaldırıldı'
    uygulandi_mi: 'evet (diff -q + grep: guard sayısı 0)'
    sonuc: '`detach.test.ts` 3 kırmızı (İKİ YÖNLÜ #2, takeover yarışı, presence-null) + `presence.test.ts` 1 kırmızı (İKİ YÖNLÜ)'
  - sonda: '`connection.ts` — `onRoomChange` içindeki `if (detectTakeover(room, seat)) return` silindi'
    uygulandi_mi: 'evet (diff -q + grep sayısı 2→1)'
    sonuc: '`presence.test.ts` 1 kırmızı (AC2+AC3 takeover)'
  - sonda: '`join.ts` — üç dalın sırası ters (ROOM_FULL, sahiplik kontrolünden önce)'
    uygulandi_mi: 'evet (diff -q + sed ile görsel doğrulama)'
    sonuc: '`presence.test.ts` 7/8 kırmızı'
  - sonda: '`session.ts` — `getDeadline: deps.getDeadline` → `() => undefined` (rotasyon kablosu koparıldı)'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '`rotate.test.ts` 2 kırmızı (yalnız yeni uçtan uca blok; eski birim testler YEŞİL kaldı — tam da bu yüzden yazıldı)'
  - sonda: '`room-client.ts` — `canAct` içindeki `connection === ''bagli''` → `!== ''kopuk''` (devredildi tahtayı açardı)'
    uygulandi_mi: 'evet (diff -q)'
    sonuc: '`room-client.test.ts` 1 kırmızı ("devredilmiş oturumda tahta salt-okunurdur") — kriter 4''ün salt-okunurluk şartı zaten kilitliymiş, doğrulandı'
  temizlik: 'her sondadan sonra `cp <yedek>` ile geri alındı; son durumda `git status --porcelain` BOŞ'

kriter_kriter:
  1: '✅ `detach.ts` koşullu + `version` TAM 1 artıyor (`detach.test.ts` İKİ YÖNLÜ #1, finished, waiting testleri)'
  2: '✅ iki yönlü, iki katmanda (db + oturum). Sonda ile kanıtlı.'
  3: '✅ 4409 + `SESSION_TAKEOVER`; rakipte `opponent:left` YOKLUĞU açıkça iddia ediliyor.'
  4: '✅ `data-durum` artık dört değer yazıyor; `devredildi`de düğme YOK, tahta salt-okunur (`room-client.test.ts`, sonda ile doğrulandı), yeniden bağlanma denenmiyor (`ws-client.test.ts` "4409 devralmada hiç yeniden bağlanılmaz": 60 sn ilerletildi, soket sayısı 1). Kod okuyarak DEĞİL, testle doğrulandı.'
  5: '✅ üretim kablosu mutasyonla kanıtlandı; tam tur sahte saatte, gerçek `setTimeout` beklemesi YOK.'
  6: '✅ KK-064 + karşıt kanıt.'
  7: '✅ dönen istemci TEK `state` alıyor; `move:applied` mesaj listesinde YOK; `state.version` boşluktan büyük.'
  8: '✅ iki yön (onay / sessiz iptal), 1 ms içinde uzlaşıyor (bütçe 2 sn), `lastError` null kalıyor.'
  9: '✅ `connection.ts`, `handlers/index.ts`, `lib/client/use-room.ts` AÇILMADI (yalnız geçici mutasyon sondaları, geri alındı). `RoomScreen.tsx`e de dokunulmadı.'
  10: '✅ `pnpm gates` EXIT=0 (typecheck + lint + format:check + test:coverage + knip).'

kapsam_degisimi:
  connection_ts: >
    Kart "`connection.ts`in iki `4403 seat-lost` dalı bugün %0 kapsamda" dedi.
    ÖLÇTÜM: o dallar aslında `connection.test.ts` tarafından zaten kapsanıyordu
    (satır kapsamı iki durumda da %100) — kartın verdiği satır numaraları eski
    bir revizyona ait. GERÇEKTEN kapsamsız olan `onForcedState` içindeki
    takeover dalıydı (`connection.ts:312`): `presence.test.ts` olmadan
    "Uncovered: …312…", varken kapsanıyor. Dal kapsamı %90.47 → %91.42.
    Abartmıyorum: kartın iddia ettiği iki dal zaten yeşildi.

decisions:
  - baslik: '`ConnectionBadge` `data-durum` DÖRT değer yazar (spec §2.0''ın üçlü tablosu genişletildi)'
    karar: >
      İskelet `devredildi`yi `kopuk`a eşliyordu ve bir test bunu KİLİTLİYORDU.
      Eşleme, davranışı TAM TERS olan iki durumu tek DOM değerine sıkıştırıyor:
      `kopuk` → üstel geri çekilmeyle yeniden bağlan + "Tekrar dene" göster
      (KK-062); `devredildi` → hiçbir yeniden bağlanma, düğme yok (§3.2, sonsuz
      takeover savaşı). Ayrım DOM'a yazılmazsa E2E "yeniden bağlanma
      denenmedi"yi ekrandan hiç doğrulayamaz. KK-062'nin `kopuk` iddiası
      etkilenmiyor: gerçek ağ kesintisi (1006) hâlâ `kopuk` üretiyor.
      Yanlış davranışı kilitleyen test, düzeltmeyle BİRLİKTE değiştirildi
      (gotchas #2'nin açıkça sorulmasını istediği soru).
  - baslik: '`detachConnection` sınırlı yeniden deneme yapar (3 deneme, çıplak sayı)'
    karar: >
      Sahiplik koşulu (`presence[seat].connId === connId`) korumanın TAMAMI;
      `expectedVersion` yalnız `casUpdateRoom`un tek-kapı disiplini için
      gerekli. Kaybedilen `version` yarışında yeniden okuyup denemek, koşullu
      yazmanın güvenliğini bozmadan detach'in sessizce kaybolmasını önlüyor.
      Alternatif olarak `casUpdateRoom`u atlayıp tek atomik aggregation-pipeline
      güncellemesi düşünüldü ve REDDEDİLDİ: `cas.ts`in "koşulsuz yazma
      YASAKTIR / tek geçiş noktası burasıdır" disiplinini delerdi ve `cas.ts`
      bu görevin çakışma kümesinde değil.
  - baslik: '`presence.test.ts` GERÇEK `@xox/db` geçişlerine karşı koşar; yalnız TAŞIMA sahte'
    karar: >
      Takeover/grace/KK-064 iddiaları koşullu yazma ile change stream yayınının
      BİRLEŞİMİNDEN doğuyor. Geçişler mock'lansaydı test kendi mock'unu
      doğrulardı (gotchas #2 · bu gece iki hata tam bu yüzden görünmedi).
      Change stream yerine deterministik bir "oku ve HER aboneye dağıt" ikizi
      kullanılıyor — ADR-0002'nin fan-out davranışının aynısı, ama zamanlaması
      testin elinde.
  - baslik: '`loadEnvLocal` `@xox/db` barrel''ine eklendi'
    karar: >
      `apps/web`in Atlas'a koşan tek testi ortam yüklemesine ihtiyaç duyuyor.
      Kendi kopyasını yazmak, 2026-08-24'te `seed`/`reset` CLI'sinde yaşanan
      "setup ile CLI farklı ortam yüklüyor" ayrışmasını bu kez web tarafında
      doğururdu. Test dosyası ayrıca `MONGODB_DB='xox_test'`i KOŞULSUZ zorluyor.

gotchas:
  - baslik: 'Mongoose query middleware MODEL DERLENMEDEN önce kayıtlı olmalı — `beforeAll` içinde eklenen hook HİÇ ateşlenmez'
    detay: >
      `detach`in okuma/yazma yarışını enjekte etmek için
      `Room.schema.pre('findOneAndUpdate', …)` `beforeAll` içinde kayıt edildi.
      Hook hiç çalışmadı ve test SESSİZCE "yarış olmadı" senaryosunu doğruladı
      (yeşil değil ama yanlış sebeple kırmızıydı: yarış hiç oluşmamıştı).
      Model `models/room.ts` import edildiği anda derleniyor; sonradan eklenen
      middleware o modele bağlanmıyor. **Yapılacak:** çalışma zamanı enjeksiyonu
      için `vi.spyOn(Model, 'findOneAndUpdate')` ile GERÇEK sorguyu sarmala
      (`.lean()`i Promise döndürecek şekilde ez) — mock değil, yalnız zamanlama.
  - baslik: '`restoreMocks: true` casusu HER TESTTEN sonra geri alır — `beforeAll`da kurulan spy yalnız İLK testte yaşar'
    detay: >
      `vitest.shared.ts` `restoreMocks: true` kullanıyor. Yarış enjektörü
      `beforeAll`daydı; ilk testten sonra sessizce kayboldu ve yarış testleri
      "yarış hiç olmadı" durumunu ölçtü. **Yapılacak:** kalıcı casusları
      `beforeEach`te kur.
  - baslik: '`@sinonjs/fake-timers`: bir tick İÇİNDE kurulan 0 ms zamanlayıcı aynı `tickAsync(0)` ile ÇALIŞMAZ'
    detay: >
      `advanceTimersByTimeAsync(20_000)` rotasyonu ateşledi, istemci
      `setTimeout(connect, 0)` kurdu, ardından `advanceTimersByTimeAsync(0)`
      onu KOŞTURMADI (soket açılmadı); `(1)` koşturdu. "Gecikmesiz yeniden
      bağlanma" iddiasını soket SAYIMINA dayandıran bir test bu yüzden yanlış
      kırmızı verir. **Yapılacak:** iddiayı gözlenebilir GECİKME değerine
      dayandır — istemcinin `setTimer`ına verilen `ms`i kaydet (4499 → 0,
      1006 → `WS_RECONNECT_BASE_MS`), sonra plumbing için 1 ms ilerlet.
  - baslik: 'Okuma+CAS deseninde `expectedVersion`, sahiplik koşulunun yerine geçmez — ama sahiplik koşulu VARKEN `expectedVersion` yazmayı KAYBETTİREBİLİR'
    detay: >
      `detachConnection` tam bu tuzağa düşmüştü: koşul doğruydu, ama araya
      giren ALAKASIZ bir yazma (rakibin hamlesi) CAS'ı düşürüp detach'i sessizce
      iptal ediyordu. `rooms/` altındaki her okuma-sonra-CAS fonksiyonu için
      sorulacak soru: "bu yazma kaybedilirse ne SESSİZCE olmaz?" — cevap
      "rakip terk galibiyetini hiç alamaz" ise yeniden deneme şart.
  - baslik: 'Kart bir dalın "%0 kapsamda" olduğunu söylüyorsa ÖLÇ — satır numaraları bayatlamış olabilir'
    detay: >
      Kart `connection.ts:248-253` ve `270-274`teki iki `4403 seat-lost` dalının
      %0 kapsamda olduğunu söyledi. Ölçüm: o dallar zaten kapsanıyordu; gerçek
      boşluk `onForcedState` içindeki takeover dalıydı (`:312`). Karta
      güvenip "kapattım" demek yanlış bir rapor olurdu.

blocked: []
```
