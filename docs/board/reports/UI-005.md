```yaml
task: UI-005
status: done
worktree: .claude/worktrees/UI-005
branch: feat/UI-005
summary: >
  `OpponentLeftBanner` artık gerçek yeniden bağlanma ile terk-ile-sonlanmayı
  ayırt ediyor. Kök neden (E2E-DIAG): `graceEndsAt`in non-null → null geçişi
  İKİ farklı sunucu olayının aynı sinyaliydi — (1) rakip gerçekten döndü
  (`packages/db/src/rooms/presence.ts` yolundan `disconnected:null`) VE (2)
  grace süresi DOLUP terk galibiyetiyle sonuçlandı (`packages/db/src/rooms/
  settle.ts` satır 58, `casUpdateRoom`ın `set` bloğunda `disconnected: null`
  KOŞULSUZ yazılıyor — reason `abandon` da olsa `timeout` da olsa). Bileşen
  bu ikisini tek başına ayıramıyordu, ikinci durumda da 5 sn boyunca yanlış
  "Rakip geri döndü." metnini basıyordu.

  Ayrımı SUNUCUDAN yeni bir alan istemeden, istemcide zaten var olan
  `RoomClientState.status.kind` (`packages/shared/src/room-client.ts`)
  üzerinden yaptım: terk/timeout galibiyeti aynı `state` mesajında hem
  `graceEndsAt`i null'a düşürür hem `status.kind`i `'playing'` olmaktan
  çıkarır (`won`/`draw`). Gerçek yeniden bağlanmada ise oyun `'playing'`
  kalmaya devam eder. `OpponentLeftBanner`'a `gameEnded?: boolean` (varsayılan
  `false`, geriye dönük uyumlu) eklendi; `graceEndsAt`in null'a düştüğü AYNI
  render'daki `gameEnded` değeri okunuyor — iki alan zaten atomik olarak
  birlikte değiştiği için (`settle.ts`'in tek CAS yazması) ek bir race
  koşulu yok.

  `packages/db/settle.ts`e DOKUNULMADI — istemci tarafında zaten yeterli bilgi
  vardı, sunucuda yeni bir alan gerekmedi.

ayrim_gerekcesi: >
  `dueSettlement` (`packages/db/src/rooms/deadlines.ts`) hem `abandon` hem
  `timeout` sebebiyle biten oyunlarda `settleDeadlines`e "kazanan" döndürüyor
  ve `settleDeadlines` (`settle.ts:47-60`) TEK bir `casUpdateRoom` çağrısında
  `state:'finished'`, `result` VE `disconnected:null`i AYNI ANDA yazıyor.
  Yani terk galibiyeti = grace dolduğunda `disconnected:null` + `status.kind`
  `'playing'`den çıkması TEK bir atomik olay. Bu yüzden `status.kind !==
  'playing'` kontrolü sağlam bir ayrım noktası: gerçek bir dönüşte oyun asla
  aynı anda bitmiyor (dönüş = rakip presence'ı geri geliyor, oyun sürüyor).
  `status.reason === 'abandon'` yerine `status.kind !== 'playing'` seçtim
  çünkü `dueSettlement`in "eşitlikte timeout kazanır" kuralı (satır 96-103)
  yüzünden grace tam turnDeadline ile aynı anda dolarsa reason `'timeout'`
  yazılıyor ama `disconnected:null` yine de KOŞULSUZ temizleniyor — yalnız
  `'abandon'`a bakmak bu kenar durumunu kaçırırdı. `status.kind !== 'playing'`
  her iki forfeit yolunu da (`abandon` VE `timeout`) doğru şekilde "bu bir
  dönüş değil" sayıyor.

conflict_set_notu: >
  RoomScreen.tsx (bileşenin tek çağrı noktası, `apps/web/components/room/
  RoomScreen.tsx:115`) BENİM KÜMEMDE DEĞİL — kart `conflictSet`i yalnız
  `OpponentLeftBanner.tsx` (+ testi). O dosya ayrıca "SICAK DOSYA DONDURMA #1"
  olarak işaretli. Bu yüzden `gameEnded` prop'unu RoomScreen'e KABLOLAMADIM;
  bileşen varsayılan `gameEnded=false` ile ESKİ davranışı AYNEN koruyor
  (geriye dönük uyumlu, hiçbir mevcut test/E2E kırılmadı). ÜRETİMDE hatayı
  gerçekten kapatmak için lead'in ya bana/başka bir agenta tek satırlık bir
  takip kartı açması ya da RoomScreen.tsx'i kendisinin şu satırı ekleyecek
  şekilde güncellemesi gerekiyor:

    <OpponentLeftBanner
      graceEndsAt={state.graceEndsAt}
      serverOffsetMs={state.serverOffsetMs}
      gameEnded={state.status.kind !== 'playing'}
    />

  Bu tek satır dışında başka hiçbir değişiklik gerekmiyor — `state.status`
  zaten `RoomScreen`de mevcut (`useRoom` döndürüyor, satır 36 ve sonrasında
  `state.status.kind` zaten birkaç yerde okunuyor).

kanit:
  terk_senaryosu_banner_cikmiyor: >
    Yeni test: "UI-005/E2E-DIAG: terk galibiyetiyle sonuçlanınca (gameEnded=
    true) 'Rakip geri döndü.' ASLA gösterilmez" — `graceEndsAt` non-null'dan
    null'a düşerken `gameEnded=true` geçiliyor, "Rakip geri döndü." hiç
    render edilmiyor (5 sn sonra da). Testten ÖNCE bu senaryo eski koda karşı
    kırmızıydı (component `gameEnded`i hiç bilmiyordu → her zaman gösterirdi).
  gercek_donus_hala_calisiyor: >
    Yeni test: "UI-005: gerçek yeniden bağlanmada (gameEnded=false, oyun HÂLÂ
    sürüyor) KK-071 metni değişmeden çıkmaya devam eder" — aynı geçiş ama
    `gameEnded=false`, "Rakip geri döndü." NORMAL şekilde görünüyor. Mevcut
    KK-071 testi ("rakip grace içinde dönünce...") de değiştirilmeden yeşil
    kaldı (varsayılan `gameEnded=false` sayesinde).
  ui004_sondalari_kirmizi_kaldi: >
    İki sonda talimatı uyarınca ÖNCE commit (1e0f02b) atıldı, SONRA sondalar
    uygulanıp `diff -q` ile değiştiği doğrulandı, test koşuldu, `git checkout
    --` ile geri alındı, `git status --porcelain` boş kontrol edildi:
      1. `opponentLeftVisible({graceEndsAt,serverOffsetMs}, now)` → ham
         `graceEndsAt !== null` kontrolüne değiştirildi: TAM 1 kırmızı
         ("ADR-0007 2 sn gösterim eşiğinin ALTINDA hiçbir şey göstermez").
      2. `graceEndsAt - (now + serverOffsetMs)` → `graceEndsAt - now`
         (serverOffsetMs kaldırıldı): TAM 1 kırmızı ("KK-070 ÇEKİRDEĞİ:
         istemci saati sunucudan sapmışken...").
    Her iki sondadan sonra `git status --porcelain` boş, dosya committed
    hâline birebir geri döndü (`diff -q` ile doğrulandı).

test_dosyasi: apps/web/components/room/OpponentLeftBanner.test.tsx (9 → 11 test)

gates:
  command: pnpm gates
  result: 'PASS (exit 0) — typecheck + lint + format:check + test:coverage (926 test, 93 dosya) + knip'
  ek_dogrulama:
    - 'pnpm --filter @xox/web typecheck → temiz'
    - 'pnpm --filter @xox/web lint → temiz'
    - 'pnpm lint apps/web → temiz (eslint . --max-warnings=0 apps/web)'
    - 'pnpm --filter @xox/web build → başarılı, tüm rotalar derlendi'

e2e_notu: >
  `apps/e2e/tests/timeout-abandon.spec.ts`teki KK-070/071 testlerine
  DOKUNULMADI (kümem dışı, E2E-005 paralel). Bileşen varsayılanı `gameEnded=
  false` olduğu ve RoomScreen henüz kablolanmadığı için bu testlerin mevcut
  davranışı DEĞİŞMEDİ — kırılma riski yok. Ama RoomScreen kablolandıktan
  SONRA, terk/timeout senaryosunu banner açısından da doğrulayan yeni bir
  E2E veya component-seviyesi entegrasyon testi eklenmesi önerilir (bu görev
  kapsamında değil).

commits:
  - sha: 1e0f02b
    message: 'fix(web): terk galibiyetinde yanlis "Rakip geri dondu" bannerini bastir'

blocked_reason: null

next_suggestions:
  - >
    **ZORUNLU takip:** `RoomScreen.tsx:115`e `gameEnded={state.status.kind
    !== 'playing'}` eklenmeden bu düzeltme ÜRETİMDE etkisiz kalır (bileşen
    varsayılanı eski davranışı koruyor). Dosya kümemde değil (frozen +
    conflictSet dışı) — lead'in ya küçük bir takip kartı açması ya da
    kendisinin eklemesi gerekiyor. Tek satır, `useRoom`'dan gelen `state`
    zaten `RoomScreen`de mevcut.
  - >
    Kablolama sonrası KK-071 E2E testinin (`timeout-abandon.spec.ts:270`)
    hâlâ yeşil kaldığı VE terk/timeout senaryosunda banner'ın artık
    çıkmadığı preview'da bir kez gözle doğrulanmalı — bu görev yalnız
    component-seviyesinde (Vitest) kanıtladı, gerçek WS akışı E2E-005/
    E2E-006 aralığında koşmadı.
```
