```yaml
task: OPS-002
status: done
revision: 2 (reviewer bulguları giderildi)
summary: >
  `apps/web/package.json` proje boyu ihtiyaç duyulacak bağımlılıklarla donduruldu:
  `@xox/shared`, `@xox/ui-tokens`, `@xox/game-core` (workspace), `mongodb@7.5.0`
  (mongoose@9.9.3'ün çözdüğü sürümle birebir), `next-auth@5.0.0-beta.32`,
  `@node-rs/argon2@2.1.0`, `jose@6.2.3` (bkz. review-fix #1 — 6.2.10 DEĞİL),
  `zod@4.4.3`, `@vercel/analytics@2.0.1`, `@vercel/speed-insights@2.0.0`,
  devDependency `@testing-library/react@16.3.2` + `@testing-library/user-event@14.6.6`.
  `dev:ws` script'i eklendi ve GERÇEKTEN çalıştırılıp WS echo turu doğrulandı
  (bkz. review-fix #2). Henüz kod tarafından kullanılmayan 11 bağımlılık
  `knip.json` → `apps/web.ignoreDependencies`'e eklendi; `pnpm knip` exit 0.
  `apps/web/vercel.json`'a WS route'u için `functions.maxDuration: "max"`
  eklendi (bkz. review-fix #3 — sayı DEĞİL). `.env.example` zaten istenen 5
  değişkenin tamamını içeriyordu, dokunulmadı. `apps/web/.gitignore` silindi
  (bkz. review-fix #4). `ws`+`@types/ws` doğrudan bağımlılık olarak
  dokunulmadan bırakıldı. `pnpm install` sıfır `allowBuilds` istemiyle temiz
  koştu. `pnpm gates` ve `pnpm --filter @xox/web build` (Turbopack) temiz.
review_fixes:
  - id: 1
    baslik: 'jose iki kopya — @auth/core ile çakışıyordu'
    onem: MAJOR
    once: |
      $ pnpm why jose
      jose@5.9.6   (vercel CLI'ın kendi devDependency ağacı)
      jose@5.10.0  (@vercel/oidc/@vercel/blob/@vercel/functions — CLI + runtime ortak)
      jose@6.2.3
      └─┬ @auth/core@0.41.3
        └─┬ next-auth@5.0.0-beta.32
          └── @xox/web@0.0.0 (dependencies)
      jose@6.2.10
      └── @xox/web@0.0.0 (dependencies)      <-- AYRI KOPYA, sorun buydu
      Found 4 versions of jose
    sonra: |
      $ pnpm why jose
      jose@5.9.6   (vercel CLI ağacı — apps/web runtime'ından izole)
      jose@5.10.0  (vercel CLI ağacı — apps/web runtime'ından izole)
      jose@6.2.3
      ├─┬ @auth/core@0.41.3
      │ └─┬ next-auth@5.0.0-beta.32
      │   └── @xox/web@0.0.0 (dependencies)
      ├─┬ @vercel/sandbox@3.0.0 → sandbox@4.0.0 → vercel@59.5.0 (CLI)
      └── @xox/web@0.0.0 (dependencies)        <-- TEK KOPYA, @auth/core ile aynı
      Found 3 versions of jose
    aksiyon: >
      `apps/web/package.json`'da `jose: "6.2.10"` → `jose: "6.2.3"` (yani
      `@auth/core@0.41.3`'ün çözdüğü sürüme sabitlendi — `mongodb`'de uygulanan
      disiplinle birebir aynı: bağımlı olduğu paketin transitive sürümünü
      doğrudan bağımlılıkta tekrarla). `pnpm install` sonrası tek kopya
      doğrulandı (yukarıdaki "sonra" çıktısı). Kalan `jose@5.9.6`/`5.10.0` iki
      kopyası `vercel` CLI'ın kendi bağımlılık ağacında (`@vercel/oidc`,
      `@vercel/blob` üzerinden) — bunlar devDependency'nin kendi araç zinciri,
      `apps/web`'in çalışma zamanı bundle'ına girmez, `instanceof` riskini
      taşımaz; dokunulmadı.
  - id: 2
    baslik: "dev:ws hiç çalıştırılmamıştı + temiz worktree'de proje bulamıyordu"
    onem: MAJOR
    aksiyon: >
      Script `"vc dev --listen 3000"` → `"vc dev --listen 3000 --local"` oldu.
      `-L/--local`, projeyi Vercel'e bağlamadan (`.vercel/project.json` aramadan/
      oluşturmadan) yerel bir dev sunucusu başlatıyor — hem etkileşimli "Set up
      and develop?" promptunu hem de her worktree'de ayrı `vercel link` ihtiyacını
      ortadan kaldırıyor (gece koşusu için güvenli, sıfır prompt riski). Bedeli:
      Development ortam değişkenleri Vercel'den çekilmez, yerel `.env.local`
      kullanılır — bu, WS/dev smoke testleri için kabul edilebilir (health/auth
      testleri zaten `.env.local` ister).
    kanit_next_dev_yetersiz: |
      $ pnpm exec next dev --turbopack --port 3101   (plain next dev, KARŞILAŞTIRMA)
      ▲ Next.js 16.3.2 (Turbopack) — Ready in 439ms
      $ node -e "new WebSocket('ws://localhost:3101/api/ws/echo')..."
      ERROR: socket hang up          <-- next dev WS upgrade'i desteklemiyor (gotcha doğrulandı)
    kanit_dev_ws_calisiyor: |
      Not: host'ta 3000 zaten dolu olduğu için (başka bir proje), script'in
      KENDİSİ değiştirilmeden (`--listen 3000 --local` sabit kaldı), test
      aynı komut 3100 portunda elle koşuldu — reviewer'ın izin verdiği yol.

      $ pnpm exec vc dev --listen 3100 --local   (apps/web içinde, .vercel YOKKEN)
      Vercel CLI 59.5.0 (Node.js 24.14.0)
      WARNING! Running dev server in local mode without a project setup:
        - Environment variables will not be pulled from Vercel
        - Project settings are defined by local configuration
      > Running Dev Command "next dev --port $PORT"
      ▲ Next.js 16.3.2 (Turbopack)
      - Local:  http://localhost:3100
      ✓ Ready in 487ms
      > Ready! Available at http://localhost:3100

      $ node -e "
        const ws = new (require('ws'))('ws://localhost:3100/api/ws/echo');
        ws.on('open', () => ws.send('merhaba'));
        ws.on('message', d => console.log('MESSAGE:', d.toString()));
        ws.on('close', (c,r) => console.log('CLOSE', c, r.toString()));
      "
      OPEN
      MESSAGE: echo:merhaba
      CLOSE 1005
    temizlik: >
      Test sonrası oluşan `apps/web/.vercel` (boş, gitignore'lu), otomatik
      üretilen `apps/web/AGENTS.md` + `apps/web/CLAUDE.md` (Next 16'nın yeni
      "agentRules" özelliği — `next.config`'de `agentRules: false` ile kapatılabilir,
      bkz. next_suggestions) ve `next-env.d.ts`'in dev-mode varyantı silinip/geri
      alınıp worktree commit öncesi temizlendi.
  - id: 3
    baslik: 'maxDuration sayı yerine "max" olmalı'
    onem: MAJOR
    aksiyon: >
      `apps/web/vercel.json`: `"maxDuration": 800` → `"maxDuration": "max"`.
      ADR-0007'ye şu satır eklendi: sabit bir Pro-tavanı sayısı yazmak, takım
      Hobby'ye düşerse (`800 > 300` limiti) HER deploy'u `invalid_function_duration`
      ile kırar; `"max"` her plana kendi tavanına çözülür ve `getDeadline()` zaten
      gerçek değeri çalışma anında okuduğu için davranış değişmez.
    dosya: docs/adr/0007-ws-baglanti-omru-ve-planli-rotasyon.md (madde 5'e paragraf eklendi)
  - id: 4
    baslik: 'apps/web/.gitignore silindi'
    onem: normal
    aksiyon: >
      `git rm apps/web/.gitignore`. Doğrulama: `git check-ignore -v apps/web/.env.example`
      artık `.gitignore:47:!.env.example` (kök dosya) üzerinden "izinli" dönüyor —
      önceden `apps/web/.gitignore:2:.env*` tarafından sessizce yutuluyordu.
  - id: 5
    baslik: '@testing-library/user-event eklendi'
    onem: normal
    aksiyon: >
      `apps/web/package.json` devDependencies → `@testing-library/user-event: 14.6.6`
      (npm dist-tag `latest`). `knip.json` → `apps/web.ignoreDependencies`'e eklendi
      (henüz hiçbir test dosyası import etmiyor). `pnpm knip` exit 0 doğrulandı.
files_changed:
  - apps/web/package.json
  - apps/web/vercel.json
  - apps/web/.gitignore (SİLİNDİ)
  - knip.json
  - docs/adr/0007-ws-baglanti-omru-ve-planli-rotasyon.md
  - pnpm-lock.yaml
  - docs/board/reports/OPS-002.md
tests:
  gates: 'pnpm gates → exit 0 (typecheck + lint + format:check + test:coverage + knip)'
  build: 'pnpm --filter @xox/web build → next build (Turbopack) başarılı, TS geçti'
  knip: 'exit 0 — yalnız görev öncesinden var olan 17 config hint kaldı (yeni unused-dependency yok)'
  dev_ws_smoke: 'bkz. review_fixes[1].kanit_dev_ws_calisiyor — OPEN → echo:merhaba → CLOSE'
  jose_dedup: "bkz. review_fixes[0].sonra — apps/web runtime'ında tek jose kopyası (6.2.3)"
decisions:
  - karar: '`-L/--local` bayrağı, `-y/--yes` yerine'
    gerekçe: >
      `-y` yalnız promptları otomatik onaylar; hâlâ bir Vercel projesine bağlanmayı
      DENER (API çağrısı, `.vercel/project.json` oluşturma) — ağ erişimi yoksa ya da
      proje eşleşmesi belirsizse gece koşusunda hâlâ kırılgan. `-L/--local` hiçbir
      Vercel API çağrısı yapmadan, proje bağlı olsun olmasın deterministik biçimde
      çalışır; bu, "her worktree'de sıfırdan çalışsın" gereksinimiyle birebir örtüşüyor.
    reddedilen_alternatif: '`-y/--yes` — hâlâ ağ bağımlı, hâlâ proje eşleştirme belirsizliği taşıyor.'
  - karar: '`.env.example`e dokunulmadı'
    gerekçe: >
      Dosya zaten `MONGODB_URI`, `MONGODB_DB`, `AUTH_SECRET`, `AUTH_URL`,
      `E2E_BASE_URL`'in tamamını gerçek değer olmadan belgeliyordu.
observations:
  - >
    `vc env ls` çıktısında bir tutarsızlık gözlendi: `AUTH_URL` yalnız Production'da
    tanımlı (Preview/Development'ta yok), `AUTH_SECRET` Preview+Production'da var
    ama Development'ta yok. Bilerek DOKUNULMADI — repoda henüz `auth.ts`/
    `auth.config.ts` yok; auth görevine not.
  - >
    Coordinator preview'ın gerçekten `xox_test`'e baktığını canlı deploy'la
    doğruladı (`{"ok":true,"db":"xox_test"}`) — bu görevde tekrar doğrulanmadı.
env_vercel_check: |
  $ vc env ls (izrandevu/xox, değerler maskeli — bu round'da değişmedi)
  AUTH_URL     Hidden  Sensitive      Production
  AUTH_SECRET  Hidden  Sensitive      Preview, Production
  MONGODB_DB   masked  Non-sensitive/Sensitive   Development, Preview, Production
  MONGODB_URI  masked  Non-sensitive/Sensitive   Development, Preview, Production
vc_version: '59.5.0'
plan: 'izrandevu (team) → billing.plan: pro'
gotchas:
  - >
    `jose` gibi bir paketin hem doğrudan bağımlılık hem de bir peer'ın transitive
    bağımlılığı olduğu her durumda `pnpm why <paket>` ile kopya sayısı kontrol
    edilmeli — `instanceof`/sınıf kimliği kontrolü yapan hata sınıfları (JWT
    hataları, custom Error alt sınıfları) kopyalar arasında sessizce false döner.
    `mongodb` + `jose` bu repoda ikinci örnek; kural artık genel: "bir peer'ın
    zaten çözdüğü paketi doğrudan eklerken, o peer'ın sürümüne sabitle."
next_suggestions:
  - >
    Next.js 16'nın "agentRules" özelliği (`next dev`/`vc dev` her koştuğunda
    `apps/web/AGENTS.md` + `apps/web/CLAUDE.md` otomatik üretiyor) HRD-003'ün
    `dev:ws` duman testinde de tetiklenecek. Bu iki dosya proje kökündeki gerçek
    `CLAUDE.md`'yi apps/web alt dizininde çalışan bir Claude Code oturumu için
    SESSİZCE GÖLGELEYEBİLİR. Öneri: `apps/web/next.config.ts`'e `agentRules: false`
    eklensin (next.config.ts benim çakışma kümemde değildi, dokunmadım) — ya da
    en azından kök `.gitignore`'a `apps/web/AGENTS.md` + `apps/web/CLAUDE.md`
    eklensin ki yanlışlıkla commit edilmesinler.
  - >
    Auth görevi `AUTH_URL`/`AUTH_SECRET` boşluklarını (Preview/Development) kapatmalı.
  - >
    `app/api/rooms/[code]/ws/route.ts` gerçekten oluşturulduğunda `vercel.json`
    içindeki `functions` glob'unun deploy sırasında eşleştiğini bir preview
    deploy'la doğrula.
```
