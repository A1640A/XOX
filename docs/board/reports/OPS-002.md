```yaml
task: OPS-002
status: done
summary: >
  `apps/web/package.json` proje boyu ihtiyaç duyulacak bağımlılıklarla donduruldu:
  `@xox/shared`, `@xox/ui-tokens`, `@xox/game-core` (workspace), `mongodb@7.5.0`
  (mongoose@9.9.3'ün çözdüğü sürümle birebir — `pnpm why mongodb` tek kopya
  doğruladı), `next-auth@5.0.0-beta.32`, `@node-rs/argon2@2.1.0`, `jose@6.2.10`,
  `zod@4.4.3`, `@vercel/analytics@2.0.1`, `@vercel/speed-insights@2.0.0`,
  devDependency `@testing-library/react@16.3.2`. `dev:ws` script'i eklendi
  (`vc dev --listen 3000`, Vercel CLI 59.5.0). Henüz kod tarafından kullanılmayan
  10 bağımlılık `knip.json` → `apps/web.ignoreDependencies`'e eklendi; `pnpm knip`
  exit 0. `apps/web/vercel.json`'a WS route'u için `functions.maxDuration: 800`
  eklendi (Pro plan doğrulandı, ADR-0007 gereği değer koda gömülmedi — `rotate.ts`
  yalnız `getDeadline()` okuyacak). `.env.example` zaten istenen 5 değişkenin
  tamamını içeriyordu, dokunulmadı. `vercel link` ile proje worktree'de bağlandı
  (`izrandevu/xox`, Root Directory `apps/web`) ve `vc env ls` ile ortam
  değişkenleri doğrulandı — MONGODB_URI/MONGODB_DB Development/Preview/Production
  üçünde de mevcut (değerler maskeli/gizli, içerik doğrulanmadı — repo public
  disiplini). `ws`+`@types/ws` doğrudan bağımlılık olarak dokunulmadan bırakıldı.
  `pnpm install` sıfır `allowBuilds` istemiyle temiz koştu (`@node-rs/argon2`
  önceden derlenmiş ikili — postinstall script tetiklemedi). `pnpm gates` ve
  `pnpm --filter @xox/web build` (Turbopack) temiz.
files_changed:
  - apps/web/package.json
  - apps/web/vercel.json
  - apps/web/.gitignore (vercel link ile otomatik oluştu — `.vercel`, `.env*`)
  - knip.json
  - pnpm-lock.yaml
  - docs/board/reports/OPS-002.md
tests:
  gates: 'pnpm gates → exit 0 (typecheck + lint + format:check + test:coverage + knip)'
  build: 'pnpm --filter @xox/web build → next build (Turbopack) başarılı, TS geçti'
  knip_before: '10 unused dependency ile exit 1 (apps/web.ignoreDependencies eklenmeden önce doğrulandı)'
  knip_after: 'exit 0 — yalnız önceden var olan 17 config hint kaldı (bu görevden önce de vardı)'
decisions:
  - karar: 'maxDuration = 800 (Pro plan tavanı)'
    gerekçe: >
      `vc project inspect` + Vercel takım API'si (`billing.plan: "pro"`) doğrulandı.
      ADR-0007 tablosunda Pro/Enterprise maksimumu 800 s. `rotate.ts` `getDeadline()`
      kullanacağı için bu sabit değer davranışı belirlemiyor, yalnız fonksiyonun
      gerçek tavanını Vercel'e bildiriyor; plan değişirse `vercel.json` tek satır
      güncellenir, kod değişmez.
    reddedilen_alternatif: >
      300 (Hobby varsayılanı) — takım zaten Pro'da, gereksiz yere daha sık
      rotasyon/resync tetikler; 1800 (beta) — resmi olarak dokümante edilmiş
      "maksimum" değil, ADR-0007 bunu bilinçli olarak reddetmişti.
  - karar: '`.env.example`e dokunulmadı'
    gerekçe: >
      Dosya zaten `MONGODB_URI`, `MONGODB_DB`, `AUTH_SECRET`, `AUTH_URL`,
      `E2E_BASE_URL`'in tamamını gerçek değer olmadan belgeliyordu;
      `.gitignore`'daki `!.env.example` istisnası bozulmadı.
  - karar: "`apps/web/.gitignore` (yeni, `.vercel` + `.env*`) commit'e dahil edildi"
    gerekçe: >
      `vercel link` çalıştırırken CLI bunu otomatik oluşturdu; kök `.gitignore`
      zaten `.vercel` ve `.env*`'i kapsıyor ama bu dosya proje köküyle
      `apps/web` (Vercel Root Directory) arasındaki standart CLI kalıbı —
      zararsız ve gelecekteki `vc link`/`vc dev` çalıştıran herkes için tutarlı.
observations:
  - >
    `vc env ls` çıktısında bir tutarsızlık gözlendi: `AUTH_URL` yalnız Production'da
    tanımlı (Preview/Development'ta yok), `AUTH_SECRET` Preview+Production'da var
    ama Development'ta yok. Görev tanımındaki "zaten ayarlı" notuyla tam örtüşmüyor.
    Bilerek DOKUNULMADI — repoda henüz `auth.ts`/`auth.config.ts` yok (Auth.js
    entegrasyonu sonraki bir dalga), bu yüzden doğru `AUTH_URL` stratejisini
    (örn. Preview'da hiç set etmeyip Auth.js'in `trustHost` ile `VERCEL_URL`'den
    çıkarması mı, yoksa açıkça mı) auth görevini üstlenecek agent kararlaştırmalı.
    Sonraki auth görevine not: bu boşluğu doldurmadan önce kontrol et.
  - >
    `MONGODB_URI`/`MONGODB_DB` üçü de (Development/Preview/Production) mevcut;
    Preview'ın gerçekten `xox_test`'e, Production'ın `xox_prod`'a işaret ettiği
    değerler maskeli olduğu için CLI çıktısından doğrulanamadı (Vercel `vc env ls`
    hassas değerleri hiç göstermiyor) — secret disiplini gereği değerleri açığa
    çıkarıp doğrulama yoluna gidilmedi. Görev tanımının "zaten ayarlı" ifadesine
    güvenildi; ilk gerçek DB bağlantısı kuran görev (health/realtime ya da auth)
    bunu davranışsal olarak (yanlış DB'ye yazma olmadığını gözlemleyerek) doğrular.
env_vercel_check: |
  $ vc env ls (izrandevu/xox, değerler maskeli)
  AUTH_URL     Hidden  Sensitive      Production   14h ago
  AUTH_SECRET  Hidden  Sensitive      Preview      14h ago
  AUTH_SECRET  Hidden  Sensitive      Production   14h ago
  MONGODB_DB   ●●●●●●  Non-sensitive  Development  14h ago
  MONGODB_DB   Hidden  Sensitive      Preview      14h ago
  MONGODB_DB   Hidden  Sensitive      Production   14h ago
  MONGODB_URI  ●●●●●●  Non-sensitive  Development  14h ago
  MONGODB_URI  Hidden  Sensitive      Preview      14h ago
  MONGODB_URI  Hidden  Sensitive      Production   14h ago
vc_version: '59.5.0 (pnpm exec vercel --version)'
plan: 'izrandevu (team) → billing.plan: pro'
gotchas: []
next_suggestions:
  - >
    Auth görevi `AUTH_URL`/`AUTH_SECRET` boşluklarını (Preview/Development) kapatmalı
    — `auth.config.ts`/`auth.ts` yazılırken Vercel preview URL stratejisine karar
    verilip ona göre `vercel env add` yapılmalı.
  - >
    `app/api/rooms/[code]/ws/route.ts` gerçekten oluşturulduğunda `vercel.json`
    içindeki `functions` glob'unun deploy sırasında eşleştiğini bir preview
    deploy'la doğrula (bu görevde route henüz yok, dosya sadece hazırlandı).
```
