````yaml
task: W2-05
status: done
summary: >
  `resolveTheme()` artık çerez YOKKEN (yeni cihaz/tarayıcı) oturumlu kullanıcının
  `users.theme` tercihini DB'den okuyup uyguluyor. Çerez hâlâ HIZLI YOL: varsa DB'ye
  hiç gidilmiyor. Anonim ziyaretçide de DB'ye hiç gidilmiyor (userId yoksa erken çıkış).
  DB sorgusu (bağlantı dahil) her ne sebeple düşerse düşsün yutuluyor, tema çözümü
  sayfa render'ını asla engellemiyor. `app/layout.tsx` diff'i TEK satırlık amaçla
  sınırlı: `auth()` artık tek kez çağrılıyor, aynı promise hem `resolveTheme`'e
  (userId çözmek için) hem `session`'a paylaşılıyor — ikinci bir `auth()` çağrısı yok.

kirmizi_once_kanit: |
  `apps/web/lib/theme.test.ts`e önce iki test eklendi (DB'den tema okuma + Promise
  olarak userId kabulü), DEĞİŞİKLİK YAPILMADAN çalıştırıldı:

  ```
  ❯ |web| lib/theme.test.ts (10 tests | 2 failed) 21ms
       × W2-05 KIRMIZI ÖNCE: çerez YOKKEN ve DB-de users.theme=koyu iken oturumlu
         kullanıcı için koyu döner 4ms
       × çerez YOKKEN userId bir Promise olarak da kabul edilir
         (layout.tsx paralel auth() paylaşımı) 0ms

   FAIL  |web| lib/theme.test.ts > resolveTheme > W2-05 KIRMIZI ÖNCE: çerez YOKKEN
   ve DB-de users.theme=koyu iken oturumlu kullanıcı için koyu döner
  AssertionError: expected 'acik' to be 'koyu' // Object.is equality
  Expected: "koyu"
  Received: "acik"
   ❯ lib/theme.test.ts:64:37
       64|     await expect(resolveTheme('u1')).resolves.toBe('koyu')

   FAIL  |web| lib/theme.test.ts > resolveTheme > çerez YOKKEN userId bir Promise
   olarak da kabul edilir (layout.tsx paralel auth() paylaşımı)
  AssertionError: expected 'acik' to be 'koyu' // Object.is equality
  Expected: "koyu"
  Received: "acik"

   Test Files  1 failed (1)
        Tests  2 failed | 8 passed (10)
  ```

  Düzeltmeden sonra aynı iki test + rapordaki diğer beş yeni test (toplam 7 yeni,
  10/10 dosya genelinde) yeşil (bkz. `test_ciktisi_ozet`).

layout_tsx_diff: |
  `git diff apps/web/app/layout.tsx` (TAM ÇIKTI — tek değişiklik bloğu,
  başka hiçbir satır dokunulmadı):

  ```diff
  --- a/apps/web/app/layout.tsx
  +++ b/apps/web/app/layout.tsx
  @@ -40,7 +40,15 @@ export default async function RootLayout({
   }: {
     children: React.ReactNode
   }): Promise<React.ReactElement> {
  -  const [tema, session] = await Promise.all([resolveTheme(), auth()])
  +  // W2-05: `auth()` TEK kez çağrılır, aynı promise hem `resolveTheme`'e
  +  // (çerez yoksa `users.theme`i okumak için) hem `session`'a paylaşılır —
  +  // çerez zaten varsa bu promise `resolveTheme` içinde hiç `await`
  +  // edilmez, ikinci bir `auth()` çağrısı da olmaz.
  +  const sessionPromise = auth()
  +  const [tema, session] = await Promise.all([
  +    resolveTheme(sessionPromise.then((s) => s?.user.id)),
  +    sessionPromise,
  +  ])

     return (
       <html lang="tr" data-tema={tema}>
  ```

  `<html data-tema>`, `SessionProvider`, `TopBar`, `metadata`, `Analytics`/
  `SpeedInsights` — hiçbiri değişmedi. Kartın "yalnız tema okumasıyla sınırlı"
  şartı bu diff'le mekanik olarak doğrulanabilir: tek satır kaldırıldı, sekiz
  satır (üçü yorum) eklendi, hepsi `auth()`/`resolveTheme()` çağrısının kendisiyle
  ilgili.

hizli_yol_korundu_kaniti: >
  `resolveTheme` önce çerezi okur ve `isTheme(cookieValue)` doğruysa DERHAL döner —
  `userId`'nin `Promise` dalı (dolayısıyla `sessionPromise`) bu durumda HİÇ `await`
  edilmez. Testle kanıtlandı: "çerez VARKEN oturumlu kullanıcı için bile DB-ye
  gidilmez (hızlı yol korunur)" — `mockConnectDb`/`mockFindById` çağrılmadığı
  doğrulanıyor.

anonim_sorgu_yok_kaniti: >
  Test: "anonim ziyaretçide (userId yok, çerez yok) DB-ye HİÇ gidilmez" —
  `resolveTheme(undefined)` çağrılıp `mockConnectDb`/`User.findById`'in HİÇ
  çağrılmadığı `expect(...).not.toHaveBeenCalled()` ile doğrulanıyor. Kaynak
  kodda bunun mekanizması: `resolvedUserId === undefined` erken çıkışı
  `connectDb()`'den ÖNCE gelir.

db_hatasi_patlamiyor_kaniti: >
  İki ayrı test: (1) `User.findById(...).select().lean()` reddedilirse, (2)
  `connectDb()`'in kendisi reddedilirse — ikisinde de `resolveTheme` `'acik'`e
  düşüyor, fırlatmıyor (`try/catch` `connectDb()`'i de kapsıyor).

db_maliyeti_olculdu: >
  Tahmin edilmedi, GERÇEK Atlas'a karşı ölçüldü (`packages/db/src/client.ts`
  `connectDb()` + `User.findById(id).select('theme').lean()`, seed'lenmiş
  `e2e-user-1` ile, worktree'nin kendi `.env.local`'i — Atlas free-tier cluster).
  İki senaryo:


  1. SICAK bağlantı (aynı serverless instance, havuz zaten açık — `connectDb()`
     her istekte çağrılıyor ama mongoose zaten bağlıysa no-op'a yakın): 30
     tekrar, mean=77.90ms p50=61.15ms p95=152.94ms. Bu, gotchas.md'deki
     "Istanbul-Atlas p50 63 ms" ölçümüyle TUTARLI (aynı büyüklük mertebesi) —
     ayrı bir sinyal bu ölçümü doğruluyor.
  2. SOĞUK bağlantı (her denemede yeni `MongoClient`/TLS el sıkışması, taze bir
     serverless instance'ın İLK çerezsiz isteğini taklit eder): 8 tekrar,
     mean=917.53ms min=863.89ms max=1012.96ms. Bu, çerezsiz VE oturumlu bir
     kullanıcının kök layout'unun İLK isteğinde (soğuk fonksiyon) görebileceği
     üst sınır — sonraki isteklerde bağlantı havuzu ısınır ve (1)'e düşer.

  Ölçüm scripti geçici (`/private/tmp/.../scratchpad/measure-theme-*.mjs`),
  worktree'ye KOPYALANIP koşuldu, ÇALIŞTIRMA SONRASI worktree'den silindi —
  repoya commit edilmedi (bkz. `dokunulan_dosyalar`).

  Not: bu, yalnız çerez YOKKEN VE oturum AÇIKKEN tetiklenir — kartın "her istekte
  DB'ye gitme" şartı korunuyor; çerezli/anonim istekte bu maliyet SIFIR (yukarıdaki
  kanıtlar).

size_limit_olculdu: >
  `pnpm --filter @xox/web build && pnpm size-limit` — en ağır rota `/oda/[kod]`:
  **225.16 kB gzip / 235 kB bütçe → 9.84 kB pay kaldı** (kart notundaki "~9.8 kB"
  ile örtüşüyor). Değişikliğin KENDİ maliyetini izole etmek için `git stash` ile
  bu üç dosyalık diff geçici olarak GERİ ALINIP aynı build+size-limit tekrar
  koşuldu: sonuç YİNE 225.16 kB — BİREBİR AYNI. Yani bu değişiklik istemci
  paketine SIFIR bayt ekliyor (beklenen: `@xox/db`/mongoose yalnız sunucu
  bileşeninde (`layout.tsx`, RSC) import ediliyor, next.js istemci paketine hiç
  girmiyor). Mevcut ~9.8 kB pay başka bir işten kalma, bu kartla İLGİSİZ.

test_ciktisi_ozet:
  theme_test: 'lib/theme.test.ts — 10/10 geçti (3 eski + 7 yeni: DB-den okuma, Promise-userId, anonim-sorgu-yok, çerez-önceliği, kullanıcı-silinmiş, iki DB-hata dalı)'
  layout_test: 'app/layout.test.tsx — 2/2 geçti, DOKUNULMADI (mevcut UI-003 testleri hâlâ geçiyor)'
  tum_paket: "pnpm --filter @xox/web test → 96 dosya / 956 test, hepsi yeşil (.env.local worktree'e kopyalandı, bkz. gotcha 2026-08-24)"
  typecheck: 'pnpm --filter @xox/web typecheck → temiz'
  gates: 'pnpm gates → exit 0 (check:dead-exports + typecheck + lint + format:check + test:coverage + knip)'
  force_dogrulama: >
    `pnpm exec turbo run typecheck --force` → 7/7 successful, Cached: 0 cached, 7 total.
    `pnpm exec turbo run test:coverage --force` → 6/6 successful, Cached: 0 cached, 6 total.
  coverage_ozet: '@xox/web statements 94.23% / branches 89.54% / functions 94.04% / lines 96.42% (eşik 70/65/70/70 üzerinde)'
  size_limit: 'pnpm size-limit → exit 0, tüm rotalar bütçenin altında (bkz. size_limit_olculdu)'

erisilebilirlik_notu: >
  Bu kart görsel/etkileşimli bir bileşen eklemedi (yalnız sunucu tarafı tema
  çözümü) — a11y değişmezleri (jsx-a11y, buton/aria-label) ilgisiz, hiçbir
  görünür metin/etkileşim eklenmedi.

dokunulan_dosyalar:
  degisen:
    - apps/web/lib/theme.ts
    - apps/web/app/layout.tsx
    - apps/web/lib/theme.test.ts
  gecici_silindi:
    - 'apps/web/measure-theme-cost.probe.mjs (ölçüm için kopyalandı, koşuldu, SİLİNDİ — commit edilmedi)'
    - 'apps/web/measure-theme-cold.probe.mjs (aynı)'
  dokunulmayan_dondurulmus: 'packages/ui-tokens/**, apps/web/app/globals.css, apps/web/messages/tr.ts (yeni anahtar gerekmedi — yeni görünür metin yok), apps/web/lib/theme-cookie.ts'

commit_sha: 'aşağıda — bu ajan merge/push YAPMADI, worktree feat/W2-05 dalında.'
````
