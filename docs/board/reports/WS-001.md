```yaml
task: WS-001
status: done
summary: >
  Gerçek zamanlı çekirdek tamamlandı ve **gerçek bir Vercel preview'da uçtan uca
  kanıtlandı**: iki ayrı oturum aynı odaya WS ile bağlandı, biri hamle yazdı,
  diğeri hamleyi change stream fan-out'undan **88.9 ms**'de aldı; yazan taraf
  kendi hamlesini **80.5 ms**'de yine change stream'den öğrendi (R1 canlı
  doğrulandı, süreç içi kısayol yok).

  `lib/realtime/room-hub.ts` instance başına EN FAZLA BİR `Room.watch()` tutar:
  tembel açılış (ilk `subscribe`), pipeline yalnız `operationType` üzerinde
  `$match`, oda filtresi süreç içinde `subscribers.get(code)`, son abone gidince
  `stream.close()`. Kopmada `resumeToken` **`resumeTokenChanged` olayından**
  saklanır (mongoose sarmalayıcısında `resumeToken` alanı YOK — tip yalan
  söylüyor), 500ms→10sn üstel geri çekilmeyle `startAfter` ile yeniden açılır ve
  **tüm yerel abonelere zorla tam `state`** yayınlanır (oda başına TEK okuma).

  `lib/realtime/connection.ts` §5.3'ün delta algoritmasının tamamını uygular:
  emoji `version` kapısından ÖNCE, takeover (`presence[seat].connId`) → 4409,
  türetilmiş olaylar (opponent:joined/left/returned · rematch:offered/cancelled ·
  game:over) KENDİ koltuğu filtreli, ince yol `move:applied` yalnız
  `version+1 && moves+1` iken, aksi hâlde tam `state`.

  `lib/realtime/handlers/index.ts` protokoldeki **yedi** istemci mesaj tipinin
  hepsini eşlenmiş bir tiple (`HandlerRegistry`) dondurur — bir giriş silinirse
  DERLEME kırılır. `join`/`move`/`ping` yazıldı; `resign`/`rematch:offer`/
  `rematch:accept`/`chat:emoji` tek satırlık `SERVER_ERROR` iskeletleri.
  `lib/realtime/timers.ts` no-op iskelet ama bağlantı yaşam döngüsünden ŞİMDİDEN
  çağrılıyor (kablolama testle kilitlendi).

  `app/api/rooms/[code]/ws/route.ts` ince: kimlik upgrade'den ÖNCE route'un
  kendi `Request`'inden çözülür (`experimental_upgradeWebSocket` handler'a
  `Request` vermiyor) ve closure ile taşınır; `allowTicket:true` YALNIZ burada
  geçilir ve dönen `identity.room` URL'deki oda koduyla KARŞILAŞTIRILIR.

files_changed:
  - apps/web/lib/game/deadlines.ts # YENİ — saf dueSettlement (§5.7)
  - apps/web/lib/game/deadlines.test.ts # YENİ — 12 test, çıplak sayılarla sınır/eşitlik
  - apps/web/lib/game/room-view.ts # YENİ — roomTransportStatus + toStateMessage
  - apps/web/lib/game/room-view.test.ts # YENİ — 10 test, stateMessageSchema'ya karşı
  - apps/web/lib/realtime/room-hub.ts # YENİ — tek change stream hub'ı + modül kapsamı `roomHub`
  - apps/web/lib/realtime/room-hub.test.ts # YENİ — 22 test, sahte sürücü
  - apps/web/lib/realtime/connection.ts # YENİ — RoomConnection, §5.3 delta
  - apps/web/lib/realtime/connection.test.ts # YENİ — 26 test
  - apps/web/lib/realtime/context.ts # YENİ — RoomTransitions/HandlerContext/HandlerRegistry (donmuş tip)
  - apps/web/lib/realtime/handlers/index.ts # YENİ — DONMUŞ kayıt defteri + dispatchMessage
  - apps/web/lib/realtime/handlers/index.test.ts # YENİ — 17 test (R1 sondası dahil)
  - apps/web/lib/realtime/handlers/join.ts # YENİ
  - apps/web/lib/realtime/handlers/move.ts # YENİ — başarıda HİÇBİR ŞEY göndermez
  - apps/web/lib/realtime/handlers/ping.ts # YENİ
  - apps/web/lib/realtime/handlers/resign.ts # YENİ — iskelet (W1-02)
  - apps/web/lib/realtime/handlers/rematch.ts # YENİ — iskelet (W1-02)
  - apps/web/lib/realtime/handlers/emoji.ts # YENİ — iskelet (W3-03)
  - apps/web/lib/realtime/session.ts # YENİ — §5.2'nin 10 adımı + çerçeve kuyruğu
  - apps/web/lib/realtime/session.test.ts # YENİ — 28 test
  - apps/web/lib/realtime/rotate.ts # YENİ — getDeadline() → 4499
  - apps/web/lib/realtime/rotate.test.ts # YENİ — 10 test, çıplak sayılar
  - apps/web/lib/realtime/timers.ts # YENİ — NO-OP iskelet (W2-01), yine de çağrılıyor
  - apps/web/lib/realtime/timers.test.ts # YENİ — 3 test
  - apps/web/app/api/rooms/[code]/ws/route.ts # YENİ — WS upgrade
  - docs/board/reports/WS-001.md

tests:
  {
    added: 128,
    passing: '265 (@xox/web tamamı) · 22 test dosyası',
    coverage: '%92.23 ifade · %84.96 dal · %91.42 fonksiyon · %95.95 satır (eşik 70/65/70/70)',
    mutation: '17 elle mutasyon sondası koşturuldu, hepsi `diff -q` ile UYGULANDIĞI doğrulandı (aşağıda)',
  }

commits:
  - d8234dc # feat(web): saf süre aşımı kararı ve oda→protokol görünümü
  - 6712acf # feat(web): instance başına tek change stream hub'ı (ADR-0002)
  - a9622a4 # feat(web): RoomConnection — §5.3 delta hesabı ve R1 saflığı
  - 163ab80 # feat(web): tam handler kayıt defteri + move/join/ping handler'ları
  - 33764be # feat(web): WS oturum yaşam döngüsü, planlı rotasyon ve süre zamanlayıcı iskeleti
  - e1a57e8 # feat(web): GET /api/rooms/[code]/ws — kimlik, bilet-oda doğrulaması, upgrade

tek_hub_sondasi:
  birim_kanit: >
    `room-hub.test.ts` — "ÜÇ abone eklendiğinde Room.watch TAM 1 kez çağrılır":
    iki farklı odaya üç abone eklendi, sahte sürücünün `watch` çağrı sayısı 1,
    `stats() = {watchCalls:1, openStreams:1, rooms:2, subscribers:3}`. Ayrıca
    `Promise.all` ile eşzamanlı üç `subscribe` de tek stream açıyor (açılış
    `opening` promise'iyle tekilleştirilmiş). Son abone gidene kadar
    `stream.close()` ÇAĞRILMIYOR, son abone gidince çağrılıyor
    (`openStreams: 0`).
  mutasyon: >
    İki ayrı guard (`ensureStream` + `openStream`) var; tek tek kaldırıldığında
    testler yeşil kalıyor (savunma derinliği, ikisi birbirini örtüyor) — bu
    yanıltıcı olduğu için İKİSİ BİRDEN kaldırıldı: `grep -c` ile 0 guard kaldığı
    doğrulandı, ardından **4 test kırmızıya döndü** ("ÜÇ abone…", "son abone
    gidince kapanır", "olay yalnız eşleşen odanın abonelerine gider", "zorla tam
    state"). Yani değişmez gerçekten korunuyor.
  kod_seviyesi: >
    Repo genelinde `Room.watch` çağrısı: `lib/realtime/room-hub.ts:339` (hub'ın
    varsayılan bağımlılığı) ve `app/api/health/realtime/route.ts:159`
    (RT-PROBE-001 gecikme sondası — production'da 404, `probeRunning` kilidiyle
    tek seferde bir stream, `finally`'de kapanıyor). Oyun yolunda TEK çağrı yeri.
  canli_gozlem: >
    Preview'da aynı odaya iki eşzamanlı bağlantı kuruldu ve ikisi de olayları
    aldı. **Instance başına stream sayısı uzaktan gözlemlenemiyor** (bunu
    ölçecek bir uç nokta yok) — canlı kanıt "fan-out çalışıyor" düzeyinde;
    "tek stream" kanıtı birim + mutasyon düzeyinde. Bunu abartmıyorum.

r1_sondasi:
  birim_kanit: >
    `handlers/index.test.ts` — "R1 SONDASI · yazan bağlantıya süreç içi kısayol
    YOK": TÜM geçişlerin `ok:true` döndüğü bir dünyada her yazma handler'ı
    (`move`, `resign`, `rematch:offer`, `rematch:accept`, `chat:emoji`)
    koşturuldu ve `error` dışında **sıfır** mesaj gönderdiği iddia edildi.
    Ayrıca "move başarılıysa TEK bir bayt bile gitmez": `sent === []` ve
    `closes === []`.
  mutasyon: >
    `move.ts`in başarı dalına süreç içi bir kısayol enjekte edildi
    (`connection.send({type:'move:applied', ...})`). `diff -q` mutasyonun
    uygulandığını doğruladı; **2 test kırmızıya döndü**. Yani R1 delinirse kapı
    çalıyor.
  canli_kanit: >
    Preview'da A hamleyi yazdı; A'nın KENDİ `move:applied`'ı 80.5 ms sonra
    geldi (yerel bir cevap olsaydı ~0 ms olurdu ve ağ turu görünmezdi).
    B'ninki 88.9 ms. İkisi de `version: 4` taşıyor — aynı change stream olayı.

close_4401_preview_dogrulamasi:
  sonuc: >
    ✅ **ULAŞIYOR.** ADR-0006'nın "Doğrulanmamış varsayım V1"i KAPANDI: geri
    çekilme planına (upgrade öncesi HTTP 401) GEREK YOK.
  yontem: >
    Preview `https://xox-5f1fi5zw8-izrandevu.vercel.app` (Vercel tarafında
    derlendi, `vercel deploy --archive=tgz`). `ws` istemcisiyle ham WS
    bağlantıları açıldı, `close` olayının `code` ve `reason` alanları okundu.
  olculen_kapanis_kodlari: |
    GECTI  kimliksiz                      beklenen=4401 gercek=4401 reason=unauthenticated  mesaj=[]
    GECTI  gecersiz bilet                 beklenen=4401 gercek=4401 reason=unauthenticated  mesaj=[]
    GECTI  gecersiz oda kodu              beklenen=4404 gercek=4404 reason=invalid-code     mesaj=[]
    GECTI  bilet BASKA odaya kesilmis     beklenen=4403 gercek=4403 reason=ticket-room-mismatch mesaj=[]
    GECTI  bilet DOGRU odaya, oda yok     beklenen=4404 gercek=4404 reason=room-not-found   mesaj=[]
    GECTI  cerezle, oda yok               beklenen=4404 gercek=4404 reason=room-not-found   mesaj=[]
  not: >
    Kapanış `reason` metni de istemciye ulaşıyor (bonus — protokolde
    kullanılmıyor, yalnız teşhis). Her vakada `mesaj=[]`: kimliksiz/yetkisiz
    bağlantıya **hiçbir oda mesajı gönderilmedi** (KK-008'in ikinci yarısı).
    Bilet sondaları için preview'da GERÇEK bir oturum açıldı (`e2e1@xox.test`,
    Auth.js credentials akışı fetch ile) ve GERÇEK bir bilet
    `POST /api/ws/ticket` ile alındı — bilet elle imzalanmadı, yani
    `ticket-room-mismatch` yolu uçtan uca sınandı.

fanout_canli_olcum:
  ortam: preview (fra1) + gerçek Atlas (xox_test)
  senaryo: >
    Oda doğrudan Atlas'a yazıldı (`POST /api/rooms` ROOM-API-001'de, henüz
    merge edilmedi), iki farklı kullanıcı çerezle bağlandı.
  cikti: |
    A ilk state:  {"type":"state","roomCode":"H3LADB","status":{"kind":"playing","turn":"X"},"you":"X",...}
    B ilk state:  you=O
    A opponent:joined aldı: {"type":"opponent:joined","userId":"e2e-user-2","seat":"O","name":"Test Oyuncu 2"}
    B move:applied gecikme = 88.9 ms  -> {"type":"move:applied","index":0,"by":"X","version":4}
    A (YAZAN) move:applied gecikme = 80.5 ms -> {"type":"move:applied","index":0,"by":"X","version":4}
    A ikinci hamle reddi: {"type":"move:rejected","index":1,"reason":"not-your-turn"}
    A gecersiz mesaj:     {"type":"error","code":"INVALID_MESSAGE","message":"Mesaj protokole uymuyor."}
    A pong aldi
    A, B'nin hamlesini 76.4 ms'de aldi -> {"type":"move:applied","index":4,"by":"O","version":5}
    son oda: state=playing version=7 board=[X,_,_,_,O,_,_,_,_] moves=2
             presence={X:null,O:null} disconnected={seat:O, graceEndsAt:+30sn}
  yorum: >
    KK-040 bütçesi 1500 ms; ölçülen 76–89 ms (bütçenin ~%6'sı). Kapanışta
    `detachConnection` koşullu yazması çalıştı: iki bağlantı da kapandı,
    `presence` temizlendi ve son kapanan (O) için `disconnected` damgalandı.
    Sonda odası ve `games` kaydı silindi; `xox_test.rooms` sonda sonrası 0.

vc_config_maxduration:
  komut: 'vercel build (rootDirectory=apps/web, target=preview)'
  cikti: |
    .vercel/output/functions/api/rooms/[code]/ws.func/.vc-config.json
      {"runtime": "nodejs24.x", "maxDuration": "max"}
    .vercel/output/functions/api/health/realtime.func/.vc-config.json
      {"runtime": "nodejs24.x", "maxDuration": 60}
    .vercel/output/functions/api/admin/migrate.func/.vc-config.json
      {"runtime": "nodejs24.x", "maxDuration": 30}
  yorum: >
    `apps/web/vercel.json`'daki `functions["app/api/rooms/[code]/ws/route.ts"]
    .maxDuration = "max"` GERÇEKTEN uygulanıyor — Vercel `app/` ile başlayan
    anahtarları hiç doğrulamadığı için tek mekanik kanıt budur ve alındı.
    `regions: ["fra1"]` da doğrulandı: preview isteklerinin `x-vercel-id`si
    `fra1::fra1::…` (bölge proje ayarından geliyor, `vercel.json`dan değil —
    gotcha korunuyor).

kabul_kriterleri:
  1_tek_watch: 'GEÇTİ — birim + çift-guard mutasyonu (yukarıda)'
  2_tembel_ve_pipeline: >
    GEÇTİ — hub kurulduğunda `watchCalls:0`; pipeline `toStrictEqual`
    ile TAM eşitlik iddia ediliyor (`operationType` $in dışında hiçbir şey);
    seçenekler ilk açılışta `{fullDocument:'updateLookup'}` (startAfter YOK).
  3_resume_token: >
    GEÇTİ — `resumeTokenChanged` olayından saklanıyor; `error`/`close`da
    500→1000→2000→4000→8000→10000→10000 ms geri çekilme (ÇIPLAK sayı testi);
    yeniden açılışta `startAfter` taşınıyor; ardından üç abonenin üçüne de
    `onForcedState` gidiyor ve `findRoom` ODA BAŞINA bir kez çağrılıyor.
    Geri çekilme sayacı yalnız GERÇEK bir olay geldiğinde sıfırlanıyor —
    "açıldı" tek başına sıfırlamıyor (CTR-002 dersi, ayrı testle kilitli).
  4_R1: 'GEÇTİ — birim sondası + mutasyon + canlı 80.5 ms ölçümü'
  5_delta_algoritmasi: "GEÇTİ — §5.3'ün altı dalı ayrı ayrı test edildi"
  6_10_adim: 'GEÇTİ — session.test.ts + canlı preview akışı'
  7_kimlik_upgrade_oncesi: >
    GEÇTİ — `resolveIdentity(request, …)` route handler'ın kendi `Request`'iyle
    çağrılıyor, sonuç closure ile handler'a taşınıyor. (Handler'a `Request`
    verilmediği `@vercel/functions` tipiyle de sabit:
    `handler: (ws: WebSocket) => void | Promise<void>`.)
  8_bilet_oda_dogrulamasi: 'GEÇTİ — canlı 4403 `ticket-room-mismatch`'
  9_settle_her_mesajda: >
    GEÇTİ — açılışta ve her GEÇERLİ mesajda; mutasyonla iki dal ayrı ayrı
    kırmızıya döndürüldü. `packages/db`nin `settleDeadlines` iskeleti HÂLÂ
    FIRLATIYOR; oturum hatayı yutuyor ve bağlantı ayakta kalıyor (ayrı test).
  10_zod_ve_4400: 'GEÇTİ — INVALID_MESSAGE kapatmıyor; 3. ardışıkta 4400; araya geçerli mesaj girerse sayaç sıfırlanıyor'
  11_move_yalniz_reddetmede: 'GEÇTİ'
  12_handler_kayit_defteri: >
    GEÇTİ — 7/7 giriş. Sonda: `chat:emoji` girişi silindi → `tsc` TS2741
    ("Property '\"chat:emoji\"' is missing … but required in type
    'HandlerRegistry'") + 4 test kırmızı. Beklenti tablosu ELLE yazıldı,
    `clientMessageSchema`dan türetilmedi.
  13_timers_iskeleti: 'GEÇTİ — no-op ama açılışta ve her oda olayında çağrılıyor, kapanışta iptal; kablolama mutasyonla doğrulandı'
  14_rotate_getDeadline: 'GEÇTİ — 800sn→790_000ms, 300sn→290_000ms (aynı kod), deadline yoksa zamanlayıcı yok, marginMs ezilebilir'
  15_dueSettlement: 'GEÇTİ — 12 test, eşitlikte timeout dahil'
  16_4401_preview: 'GEÇTİ — yukarıda, GERÇEK sonuç'
  17_vc_config: 'GEÇTİ — "max" okundu'
  18_yeniden_implementasyon_yok: >
    GEÇTİ — `apps/web`de `evaluateStatus`/`applyMove`/`isValidMove`/`nextPlayer`
    TANIMI yok (yalnız `@xox/game-core`dan import). Oyun yolunda koşulsuz
    `Room.updateOne` yok; tek yazma yüzeyi `RoomTransitions` ve arkasında
    `packages/db`nin `casUpdateRoom`u var. (Var olan tek koşulsuz yazma
    RT-PROBE-001'in gecikme sondasında — production'da 404, kendi tek kullanımlık
    odasını yazıp siliyor; bu görevin ürünü değil.)
  19_gates: >
    GEÇTİ — `pnpm gates` EXIT 0. Turbo sahte-yeşiline karşı ayrıca
    `turbo run typecheck --force` (7/7, **Cached: 0**) ve
    `turbo run test:coverage --force` (5/5, **Cached: 0**) koşuldu.
    Ek olarak `pnpm --filter @xox/web build` yeşil — route Next tarafından
    `ƒ /api/rooms/[code]/ws` olarak tanındı.

mutasyon_sondalari:
  yontem: 'Her sonda `perl -0pi` ile uygulandı, `diff -q` ile dosyanın GERÇEKTEN değiştiği doğrulandı; değişmediyse "kalıp tutmadı" denip yeniden yazıldı.'
  sonuclar: |
    HUB   iki guard birden kaldırıldı            -> 4 kırmızı
    HUB   startAfter silindi                      -> 1 kırmızı
    HUB   REOPEN_BASE_MS 500->400                 -> 4 kırmızı
    HUB   REOPEN_MAX_MS 10s->30s                  -> 1 kırmızı
    HUB   forceStateToAll çağrılmıyor             -> 1 kırmızı
    HUB   pipeline'dan replace/delete düşürüldü   -> 1 kırmızı
    HUB   delete olayı yok sayılıyor              -> 1 kırmızı
    CONN  emitEmoji çağrısı silindi               -> 2 kırmızı
    CONN  moveCount kontrolü düştü                -> 1 kırmızı (İLK denemede 0! aşağıya bak)
    CONN  takeover kapatmıyor                     -> 1 kırmızı
    CONN  ihlal eşiği 3->4                        -> 2 kırmızı
    CONN  noteValidMessage no-op                  -> 1 kırmızı
    CONN  version kapısı kaldırıldı               -> 6 kırmızı
    CONN  emitDerived çağrılmıyor                 -> 4 kırmızı
    CONN  opponentSeat = kendi koltuğu            -> 3 kırmızı
    HNDL  move başarısında move:applied gönder    -> 2 kırmızı (R1)
    HNDL  kayıt defterinden bir giriş silindi     -> tsc TS2741 + 4 kırmızı
    SESS  mesaj öncesi settle yok                 -> 1 kırmızı
    SESS  açılışta settle yok                     -> 2 kırmızı
    SESS  4400 kapatmıyor                         -> 1 kırmızı
    SESS  armIdle yok                             -> 1 kırmızı
    SESS  detachConnection çağrılmıyor            -> 2 kırmızı
    SESS  settlementTimer kablolaması yok         -> 1 kırmızı
    SESS  hub.unsubscribe yok                     -> 4 kırmızı
    SESS  abonelik join'den SONRA                 -> 2 kırmızı
    SESS  çerçeve kuyruğu kaldırıldı              -> 2 kırmızı
  bulunan_gercek_bosluk: >
    "CONN moveCount kontrolü düştü" mutasyonu İLK denemede 26 testin hepsini
    YEŞİL bıraktı. Sebep: mevcut test `moves: []` ile koşuyordu, `lastMove`
    `undefined` olduğu için kod zaten `state`e düşüyordu — test dalı hiç
    sınamıyordu. Yeni test yazıldı ("hamle listesi DOLU ama uzunluğu
    değişmemişse ESKİ hamle tekrar yayınlanmaz": hamleden sonra bir
    presence/disconnected yazımı) ve mutasyon o testi kırdı. Gerçek risk:
    tahtaya dokunmayan her yazmada aynı hamle ikinci kez `move:applied` olarak
    giderdi ve istemci sürüm boşluğu sanıp gereksiz resync isterdi.

kirmizi_yesil_kanit:
  - '`lib/game/deadlines.test.ts` modül yokken koşuldu → transform hatası (kırmızı), sonra implementasyon → 12/12 yeşil.'
  - >
    `session.test.ts` "art arda gelen iki hamle üst üste binmeden" testi ilk
    yazıldığında KIRMIZI verdi (`expected [] to equal ['giris-0']`) — kuyruk
    `queue.then()` üzerinden mikrogörev sınırına düştüğü için iddia senkron
    çalışıyordu. `vi.waitFor` ile düzeltildi; ardından kuyruk KALDIRILARAK
    mutasyon koşuldu ve test gerçekten kırmızı oldu.
  - >
    `connection.test.ts` takeover testi `expect.any(String) as unknown as string`
    kullanıyordu; `no-unnecessary-type-assertion` yakaladı. `toMatchObject`e
    çevrildi — lint kuralı gerçek bir gürültüyü temizledi.

decisions:
  - karar: >
      **Hub aboneliği `joinRoom` yazımından ÖNCE kurulur** (spec §5.2'nin
      5→7 sırasından bilinçli sapma). Anlık görüntü henüz yokken gelen olay
      `connection`da bekletilir ve `primeState` daha YENİ olanı seçer.
    gerekçe: >
      Spec'in sırasında `joinRoom` yazımı ile `hub.subscribe` arasında bir
      pencere var: o pencerede rakibin yazdığı olay hiçbir yere düşmez ve
      hemen ardından gönderdiğimiz tam `state` `joinRoom`un döndürdüğü (daha
      eski) dokümandan üretilir. Sonuç: bağlantı, bir sonraki olaya kadar
      BAYAT bir tahta gösterir. Abonelik önce kurulunca pencere kapanıyor;
      "anlık görüntü yokken gelen olay" durumu `seenBeforePrime` ile
      uzlaştırılıyor. Testle kilitlendi ("abonelikten önce gelen DAHA YENİ
      olay tam durumda kaybolmaz": join sırasında version 99 olayı gelir,
      joinRoom version 10 döner, gönderilen state version 99'dur).
    reddedilen_alternatif: >
      Spec'i harfiyen uygulayıp pencereyi kabul etmek — "nadiren olur" diye
      geçilecek bir yarış değil: `join` anı, rakibin "oyun başladı" yazmasıyla
      TAM OLARAK çakışan andır.
  - karar: >
      Change stream geri çekilme sayacı, stream'in yeniden AÇILMASIYLA değil
      **gerçek bir olay gelmesiyle** (`change`/`resumeTokenChanged`) sıfırlanır.
    gerekçe: >
      CTR-002 dersi (`gotchas.md`): "başarılı bağlantı sayacı sıfırlar" kuralı,
      açılır açılmaz ölen bir bağlantıda geri çekilmeyi TAMAMEN öldürür —
      sonsuza dek 500 ms'de bir denenir. Açılış tek başına yaşam kanıtı değildir.
  - karar: >
      `session.ts` (spec §5.1'de listelenmeyen yeni dosya) ve
      `lib/realtime/context.ts` eklendi; route dosyası ince bırakıldı.
    gerekçe: >
      Route `lib/auth/identity.ts` → `@/auth` → `next-auth` zincirini import
      ediyor; `next-auth` import eden hiçbir dosya Vitest'te ÇALIŞTIRILAMIYOR
      (repo konvansiyonu). Yaşam döngüsü mantığı route'ta kalsaydı 10 adımın
      hiçbiri birim testle sınanamazdı, tek kanıt E2E olurdu. `session.ts`
      next-auth'a hiç dokunmuyor ve 28 testle kaplandı.
  - karar: >
      `roomTransportStatus`, "oda `finished` ama tahta bitmemiş" durumunda
      `{kind:'draw'}` döner.
    gerekçe: >
      Pes/süre aşımı/terk sonucunun kazananı ve sebebi `rooms` dokümanında
      TUTULMUYOR (§3.2'de sonuç alanı yok; sonuç `games`e yazılıyor) ve
      `packages/db` bu görevde DONMUŞ. P0 bu dalı hiç üretmiyor (tek bitiş yolu
      `applyMove`in çizgi/beraberliği). Uydurma bir KAZANAN yazmaktansa oyunu
      sonuçsuz kapatmak seçildi.
    reddedilen_alternatif: >
      Motorun `{kind:'playing'}` sonucunu olduğu gibi yayınlamak — biten oyunu
      "sürüyor" göstermek tahtayı tıklanabilir bırakır. **W1-02 için borç:**
      pes/terk sonucunu `state`e taşıyacak bir yol gerekiyor (ya `RoomDoc`a
      sonuç alanı, ya `gameId` üzerinden `games` okuması). Bu, WS-001'in
      kapatamayacağı bir boşluk çünkü `packages/db` dondu.
  - karar: >
      Gelen çerçeveler `session` içinde bir **kuyrukta** sırayla işlenir.
    gerekçe: >
      `ws.on('message')` dinleyicileri sırayla ateşlenir ama gövdeleri
      asenkron. İki hamle art arda gelirse ikincinin `applyMove` okuması
      birincinin CAS'ından önce olabilir → sırası gelen oyuncu haksız yere
      `not-your-turn` yer. Kuyruk ayrıca `start()` bitmeden gelen bir mesajın
      koltuksuz işlenmesini engelliyor.
  - karar: >
      Bu görevin kararları/tuzakları `docs/memory/*`e YAZILMADI, yalnız bu
      rapora yazıldı.
    gerekçe: >
      Aynı dalgada üç agent (ROOM-API-001, UI-SKEL-001) paralel çalışıyor;
      `docs/memory/*` üçünün de yazacağı sıcak dosya. Kartın kendisi raporun
      `decisions`/`gotchas` alanlarını istiyor. Aşağıdaki `gotchas` girdileri
      `gotchas.md`e OLDUĞU GİBİ taşınmaya hazır.

gotchas:
  - baslik: "⚠️ `vc build` + `vercel deploy --prebuilt` macOS'ta ÇALIŞAN AMA ÖLÜ bir preview üretir"
    metin: >
      Yerelde (darwin-arm64) `vercel build` koşup `vercel deploy --prebuilt`
      ile yüklenen preview HTTP 200 ile deploy oldu ama HER fonksiyon 500
      döndü: `Failed to load external module @node-rs/argon2-…: Cannot find
      native binding`. Sebep: trace edilen çıktı yerel platformun ikilisini
      taşıyor, `linux-x64-gnu` ikilisi yok. Gövde BOŞ dönüyor (`content-length: 0`),
      yani belirti "kodum bozuk" gibi okunuyor. `vercel logs <url> --json`
      gerçek sebebi gösteriyor.
      **Yapılacak:** Sonda deploy'u `vercel deploy --archive=tgz` ile (Vercel
      TARAFINDA derlenerek) yap; `--prebuilt` yalnız CI'da (Linux runner)
      anlamlı. `.vc-config.json` okumak için `vc build` yeterli — deploy etmeye
      gerek yok.
  - baslik: "✅ Özel WS kapanış kodları (4xxx) Vercel'de İSTEMCİYE ULAŞIYOR — ADR-0006 V1 kapandı"
    metin: >
      Gerçek preview'da `ws.close(4401,'unauthenticated')` istemcide
      `close` olayına `code=4401`, `reason='unauthenticated'` olarak düştü;
      4403/4404 de aynı şekilde. Bağlantı upgrade EDİLİP hemen kapatıldığında
      istemci `1006` DEĞİL gerçek kodu görüyor ve hiçbir oda mesajı almıyor.
      ADR-0006'nın "geri çekilme planı: HTTP 401" maddesi artık gereksiz.
  - baslik: '`vercel pull` env dosyası ŞİFRELİ yer tutucu yazar — onunla token imzalayamazsın'
    metin: >
      `.vercel/.env.preview.local` içindeki hassas değerler `encrypted`
      (11 karakter) olarak geliyor. Onunla HS256 bilet imzalayıp preview'a
      göndermek sessizce 4401 üretir ve "bilet doğrulamam bozuk" diye YANLIŞ
      teşhis edilir. Yerel `.env.local`'daki `AUTH_SECRET` de preview'ınkinden
      FARKLI (`POST /api/ws/ticket` 401 verdi).
      **Yapılacak:** Bileti elle imzalama; gerçek oturum aç
      (`GET /api/auth/csrf` → `POST /api/auth/callback/credentials`) ve bileti
      `POST /api/ws/ticket`ten AL. Zaten daha iyi bir sonda: uçtan uca yolu
      sınar.
  - baslik: 'Yerel `.env.local` `xox_dev`, preview `xox_test` — aynı cluster, FARKLI veritabanı'
    metin: >
      Sonda odasını yerel `MONGODB_DB` ile yazarsan preview onu bulamaz ve
      `4404 room-not-found` alırsın; belirti "WS route odayı okuyamıyor" gibi
      görünür. Cluster aynı (`xox_dev`, `xox_test`, `xox_prod` yan yana).
      **Yapılacak:** Preview'a karşı sonda yazarken veritabanını AÇIKÇA
      `xox_test` seç ve sonda verisini sonunda sil.
  - baslik: 'İki ayrı guard aynı değişmezi korursa mutasyon sondası YANILTIR'
    metin: >
      `ensureStream` ve `openStream` ikisi de `if (stream !== null) return`
      içeriyordu. Tek tek kaldırıldıklarında 22 testin hepsi yeşil kaldı —
      "test bu değişmezi korumuyor" diye yanlış çıkarım yapmak çok kolay.
      İkisi birden kaldırılınca 4 test kırmızıya döndü.
      **Yapılacak:** Bir mutasyon hayatta kalırsa önce "aynı işi yapan İKİNCİ
      bir guard var mı" diye bak; savunma derinliği ile test boşluğu farklı
      şeylerdir.
  - baslik: '`Model.watch()` sarmalayıcısı `resumeToken` taşımıyor — DOĞRULANDI ve uygulandı'
    metin: >
      (Mevcut maddenin uygulama notu.) Hub token'ı `resumeTokenChanged`
      olayından saklıyor ve `startAfter` ile yeniden açıyor. `ready` olayı bu
      tasarımda hiç beklenmiyor: hub tembel açılıp olayları asenkron
      dağıttığı için "hazır mı" sorusunu sormasına gerek yok — ilk gerçek olay
      zaten yaşam kanıtı olarak kullanılıyor.

api_sozlesmesi_deltasi: >
  `docs/memory/api-contract.md`'nin WebSocket tablosuna eklenecek satır:
  `GET /api/rooms/[code]/ws` — oyunun gerçek zamanlı uç noktası.
  Kimlik: Bearer | Auth.js çerezi | `?ticket=` (YALNIZ burada kabul edilir,
  `room` claim'i URL'deki kodla eşleşmeli). Kapanış kodları:
  4400 (3 ardışık INVALID_MESSAGE) · 4401 (kimliksiz) · 4403 (oda dolu /
  koltuk yok / bilet-oda uyuşmazlığı) · 4404 (oda yok / geçersiz kod / oda
  silindi) · 4408 (75 sn sessizlik) · 4409 (takeover) · 4499 (planlı rotasyon).
  `maxPayload` 8 KiB. Bağlantı kurulur kurulmaz tam `state` gönderilir.

sonraki_dalgalara_borc:
  - 'W1-02: pes/süre aşımı/terk sonucu `rooms`ta tutulmuyor → `roomTransportStatus`un `finished`+bitmemiş-tahta dalı bugün `draw` dönüyor. Sonucu taşıyacak bir yol gerekiyor.'
  - "W2-01: `packages/db/src/rooms/settle.ts` hâlâ FIRLATIYOR; `session.ts` hatayı yutuyor (bağlantı ayakta kalıyor) ama süre aşımı P0'da hiç kesinleşmiyor. `timers.ts` gövdesi de W2-01'in."
  - "W3-03: `chat:emoji` handler'ı iskelet; `connection.ts` emoji YAYININI zaten uyguluyor (palet doğrulamasıyla), yalnız YAZMA tarafı eksik."
  - "E2E-001: preview'da elle koşturduğum akışın (giriş → oda → ikinci istemci → hamle → karşı tarafta gör) Playwright karşılığı. Oda kurma adımı ROOM-API-001'in `POST /api/rooms`'ini bekliyor."
  - "Hub'ın `stats()`'i bugün hiçbir uçtan okunmuyor; instance başına stream sayısını CANLI doğrulamak isteyen bir görev küçük bir teşhis ucu eklemeli."
```
