```yaml
task: DB-002
status: done
summary: >
  Otoriter oda geçişleri `packages/db/src/rooms/` altında yazıldı: `createRoom`
  (randomInt kod, E11000'de en fazla `ROOM_CREATE_MAX_ATTEMPTS`=5 deneme,
  hepsi çakışırsa `CODE_GENERATION_FAILED`), `joinRoom` (2. koltuk dolunca
  tek yazmada seats.O+state:playing+startedAt+gameId+version+1; aynı userId
  oda dolu olsa bile ROOM_FULL DÖNMEDEN yeniden bağlanır/takeover eder),
  `detachConnection` (koşullu — yalnız presence[seat].connId hâlâ o bağlantıya
  aitse yazar, devredilmiş eski bağlantı HİÇBİR ŞEY yazmaz) ve `applyMove`
  (§5.5'in tam sırası: seatOf → state → nextPlayer → isValidMove → TEK koşullu
  findOneAndUpdate({code,version})). Tüm durum değiştiren yazmalar TEK bir
  yardımcıdan (`rooms/cas.ts::casUpdateRoom`) geçiyor — koşulsuz
  updateOne/findOneAndUpdate `rooms/` içinde başka hiçbir yerde yok.

  `rooms/index.ts` barrel'ı artık `resign`/`offerRematch`/`acceptRematch`/
  `settleDeadlines`/`pushEmoji`/`finishGame` için TİPLİ İSKELETLER de dışa
  veriyor (gövdeleri açık bir `Error` fırlatıyor, sessizce "uygulandı"
  sanılamaz) — bu görevden sonra `packages/db/src/index.ts` ve
  `rooms/index.ts`'in dışa aktarım LİSTESİ donuyor, sonraki dalgalar yalnız
  kendi dosyalarının gövdesini dolduruyor (W1-02→resign/rematch/finish,
  W1-03→detach genişletme, W1-04→create genişletme, W2-01→settle, W3-01→finish,
  W3-03→emoji).

  `buildPairKey(a,b)`/`deriveParticipants(players)` kök seviyede
  (`packages/db/src/pair.ts`, `rooms/` İÇİNDE DEĞİL — `rooms/join.ts` ve
  `models/game.ts` ikisi de import ediyor, `rooms↔models` döngüsünden kaçınmak
  için). `Game.pre('validate')`'e `participants`/`pairKey`'in `players`'tan
  türetilenle eşleştiğini zorlayan yeni bir kural eklendi (AC12) — bu,
  görev talimatındaki "Dokunma: packages/db/src/models/**" notuyla ÇELİŞİYORDU
  ama AC12'nin kendisi açıkça bunu istiyordu; talimat çatışmasını gerçeğe göre
  çözdüm (aşağıda "decisions"). `@xox/game-core` `@xox/db`'ye YENİ bağımlılık
  olarak eklendi (boundaries zaten `db→game-core` izinliydi, yalnız
  `package.json`da eksikti).

files_changed:
  - packages/db/package.json # @xox/game-core bağımlılığı eklendi
  - packages/db/src/index.ts # rooms/ barrel'ının tamamı + buildPairKey/deriveParticipants re-export edildi
  - packages/db/src/pair.ts # YENİ — buildPairKey/deriveParticipants (kök seviye, döngü önleme)
  - packages/db/src/pair.test.ts # YENİ — saf birim testler
  - packages/db/src/models/game.ts # pre('validate')'e participants/pairKey türetme kontrolü eklendi (AC12)
  - packages/db/src/models/game.test.ts # 4 yeni test: ters sıra/yabancı id/yanlış pairKey reddi + ters-players kabul
  - packages/db/src/rooms/types.ts # YENİ — TransitionResult, RoomEvent
  - packages/db/src/rooms/seat.ts # YENİ — seatOf (userId→Player)
  - packages/db/src/rooms/cas.ts # YENİ — casUpdateRoom (rooms/ altındaki TEK koşullu yazma yolu)
  - packages/db/src/rooms/create.ts # YENİ — createRoom
  - packages/db/src/rooms/create.test.ts # YENİ — gerçek xox_test, generateRoomCode casus'landı (yalnız E11000 sondası için)
  - packages/db/src/rooms/join.ts # YENİ — joinRoom (+ reconnect/takeover)
  - packages/db/src/rooms/join.test.ts # YENİ — 8 test, eşzamanlılık CAS sondası dahil
  - packages/db/src/rooms/detach.ts # YENİ — detachConnection
  - packages/db/src/rooms/detach.test.ts # YENİ — 5 test, İKİ YÖNLÜ takeover/aktif senaryosu
  - packages/db/src/rooms/apply-move.ts # YENİ — applyMove
  - packages/db/src/rooms/apply-move.test.ts # YENİ — 10 test, eşzamanlılık sondası (Room.findOne casus'landı)
  - packages/db/src/rooms/resign.ts # YENİ — tipli iskelet (W1-02)
  - packages/db/src/rooms/rematch.ts # YENİ — tipli iskelet: offerRematch/acceptRematch (W1-02)
  - packages/db/src/rooms/settle.ts # YENİ — tipli iskelet: settleDeadlines (W2-01)
  - packages/db/src/rooms/emoji.ts # YENİ — tipli iskelet: pushEmoji (W3-03)
  - packages/db/src/rooms/finish.ts # YENİ — tipli iskelet: finishGame (W1-02/W3-01)
  - packages/db/src/rooms/skeletons.test.ts # YENİ — 6 test: her iskelet çağrılabilir + açıkça reddediyor
  - packages/db/src/rooms/index.ts # YENİ — rooms/ barrel'ı (bu görevden sonra DONUYOR)
  - pnpm-lock.yaml # yalnız @xox/game-core workspace bağlantısı
  - docs/board/reports/DB-002.md

tests:
  {
    added: 47,
    passing: 125,
    coverage: '%95.79 ifade · %89.89 dal · %100 fonksiyon · %98.26 satır (paket geneli, eşik 90/85/90/90 — hepsi aşıldı)',
    mutation: 'yok — bu paket mutasyon kapsamında değil (yalnız game-core)',
  }

esaszamanlilik_sondasi:
  applyMove_KK045: >
    `apply-move.test.ts` — "eşzamanlılık sondası (KK-045)": `Room.findOne`
    tek seferlik bir casus ile sarıldı; casus'un `.lean()` çağrısı, gerçek
    dokümanı okuduktan HEMEN SONRA (applyMove'un kendi okuması ile kendi
    yazması ARASINDA) `Room.updateOne({code},{$inc:{version:1}})` ile GERÇEK
    bir version artışı enjekte ediyor. Sonuç: `applyMove(code, xUserId, 0)`
    `{ok:false, code:'not-your-turn'}` döndü — CAS yazması 0 doküman
    güncelledi. Doğrulama: enjekte edilen +1 DIŞINDA version AYRICA
    artmadı (`after.version === before.version + 1`, ikinci bir +1 YOK).
    Gerçek `xox_test` Atlas'a karşı koştu, mock yalnız zamanlama/enjeksiyon
    içindi — okuma/yazmaların kendisi gerçek.
  joinRoom_CAS: >
    `join.test.ts` — "eşzamanlılık sondası": aynı odanın O koltuğuna İKİ
    FARKLI kullanıcı `Promise.all` ile GERÇEKTEN eşzamanlı katılmaya
    çalıştı. Sonuç deterministik: tam olarak biri `ok:true` (state:'playing',
    gameId dolu), diğeri `{ok:false, code:'ROOM_FULL'}` aldı — MongoDB'nin
    `{code,version,seats.O:null}` filtreli atomik `findOneAndUpdate`'i ikinci
    yazmayı 0 doküman güncelledi. Gerçek zamanlama (mock yok), 2/2 koşuda
    kararlı.

takeover_ve_detach_iki_yonlu:
  takeover_AC5: >
    `join.test.ts` — "AC3: aynı userId oda DOLU olsa bile yeniden bağlanır":
    seats.X+seats.O ikisi de dolu bir odada, X'in userId'si `presence.X.connId`
    farklı bir bağlantıyla tekrar `joinRoom` çağırdığında `ROOM_FULL`
    DÖNMEDİ — `presence.X` yeni connId'yle üzerine yazıldı, `version+1`,
    `disconnected` HİÇ dokunulmadı (rakip hiçbir kopma göremez, çünkü hiç
    yazılmadı).
  detach_yon_1_aktif: >
    `detach.test.ts` — "İKİ YÖNLÜ #1": `presence.O.connId==='o-conn'` iken
    `detachConnection(code,'O','o-conn')` çağrıldı → presence.O null,
    `state==='playing'` olduğu için `disconnected={seat:'O',...,
    graceEndsAt>now}` YAZILDI, version+1. Rakibin (X) koltuğuna dokunulmadı.
  detach_yon_2_devredilmis: >
    `detach.test.ts` — "İKİ YÖNLÜ #2": `presence.O.connId==='o-conn-YENI'`
    (takeover ZATEN olmuş) iken ESKİ bağlantı `detachConnection(code,'O',
    'o-conn-ESKI')` çağırdı → presence.O DEĞİŞMEDİ (hâlâ 'o-conn-YENI'),
    `disconnected` null KALDI, version AYNI kaldı (10→10). Sahte "rakip
    koptu" olayı üretilmedi — koşul (`presence[seat].connId===connId`) hem
    ilk okumada hem `casUpdateRoom`'un `extraFilter`'ında ayrıca uygulandığı
    için okuma-yazma arasındaki bir yarış bile 0 doküman günceller.
  waiting_disconnect_ozel_durum: >
    `detach.test.ts` — kurucu `waiting` durumunda ayrılırsa presence temizlenir
    ama `disconnected` YAZILMAZ (§3.10) — ayrı bir testle kilitlendi.

version_disiplini_kaniti:
  rejected_move_no_increment: >
    `apply-move.test.ts`teki `not-your-turn`/`occupied`/`out-of-range` testlerinin
    HER BİRİ reddedilen hamleden SONRA `Room.findOne({code}).lean()` ile
    version'ı taze okuyup RED ÖNCESİ değerle `toBe` (çıplak sayı, sabitten
    türetilmemiş: 7, 9, 11) karşılaştırıyor — üçü de DEĞİŞMEDİ.
  every_write_increments_by_exactly_1: >
    `casUpdateRoom` `rooms/`teki TEK koşullu yazma yoludur — `$inc:{version:1}`
    her çağrıda SABİT, çağıran ekstra bir `$inc` veremez (fonksiyon imzası
    yalnız `set`/`unset`/`push` kabul eder). `createRoom` version'ı 1 olarak
    AÇIKÇA yazar (şema varsayılanı 0'dır — "hiç yazılmadı" ile "ilk yazma"
    karışmasın diye). `joinRoom`in ikinci koltuk dolma testi version 1→2,
    `applyMove`in mutlu-yol testi 2→3 gösteriyor — hiçbiri sıfırlanmadı/atlamadı.
  max_attempts_ciplak_5: >
    `create.test.ts` — "ROOM_CREATE_MAX_ATTEMPTS (5) kez çakışırsa..." testi
    `generateRoomCode`'un TAM 5 kez çağrıldığını (`toHaveBeenCalledTimes(5)`)
    ÇIPLAK SAYIYLA doğruluyor, sabitten türetilmedi (gotcha: "beklentiyi
    sabitten türetmek körlük yaratır").

kirmizi_yesil_kanit:
  - >
    İlk `berabere biten hamlede status draw olur` testi (draw senaryosu)
    KIRMIZI verdi: board'daki 8 taş sayısı ÇİFT olduğu için sıra X'teydi ama
    test hamleyi `oUser` ile gönderiyordu → `not-your-turn` döndü, beklenen
    `ok:true` değildi. `nextPlayer` paritesini elle yanlış hesaplamıştım.
    Düzeltme: hamleyi `xUser`e çevirdim, test yeşile döndü — davranış DOĞRUYDU,
    test verisi yanlıştı (kırmızı doğru yerdeydi).
  - >
    Eşzamanlılık sondasının ilk yazımında `before` (version'ı önce okuyan
    referans değer) casus KURULDUKTAN SONRA okunuyordu — bu, casus'un
    `mockImplementationOnce`ını `before` okuması TÜKETİYORDU, `applyMove`
    hiç enjeksiyona uğramadan geçiyordu. Test yine de geçiyordu ama YANLIŞ
    NEDENLE (kendi kendine referanslı sonda riski — gotcha sınıfı). Fark
    edip `before` okumasını casus kurulumundan ÖNCEYE aldım; test hâlâ
    geçiyor ama artık GERÇEKTEN enjeksiyonu sınıyor.
  - >
    `tsc --noEmit` eşzamanlılık sondası dosyasında `TS2556: A spread argument
    must either have a tuple type` verdi (`Room.findOne`'ın overload'ları
    `Parameters<typeof Room.findOne>` ile spread'i belirsizleştiriyordu).
    Düzeltme: tek-parametreli açık bir sarmalayıcı fonksiyon yazıp
    `as unknown as typeof Room.findOne` ile tek noktada daralttım.

pnpm_gates: >
  Repo genelinde `pnpm gates` (typecheck + lint + format:check + test:coverage
  + knip) EXIT 0. @xox/db: 125/125 test, %95.79/%89.89/%100/%98.26
  (ifade/dal/fonksiyon/satır, eşik 90/85/90/90 — hepsi aşıldı). Diğer paketler
  bu görevde değişmedi (@xox/shared 342/342, @xox/game-core 91/91 %100,
  @xox/ui-tokens 38/38 %100, @xox/web 53/53). knip yalnız önceden var olan
  konfigürasyon ipuçlarını bastı (19 adet, hepsi başka paketlerle ilgili),
  kırmızı/unused bulgu YOK.

decisions:
  - karar: >
      Görev talimatı "Dokunma: packages/db/src/models/**" diyordu ama AC12
      açıkça "`Game` `pre('validate')`'te `pairKey`/`participants`'ın
      `players`'tan türetilenle eşleştiği doğrulanır" istiyordu — bu, YALNIZ
      `models/game.ts`'e dokunularak yapılabilir. Çatışmayı AC12 lehine
      çözdüm: `game.ts`'in `pre('validate')`'ine üç yeni kontrol eklendi
      (mevcut çapraz-tutarlılık kurallarına dokunulmadı, yalnız EKLENDİ).
    gerekçe: >
      "Dokunma" notu DB-001'in TESLİM ETTİĞİ modelleri gereksiz yeniden
      yapılandırmadan korumak için; bu sırada `apps/web`de paralel çalışan
      AUTH-001 zaten `models/**`e dokunmuyor, yani gerçek bir çakışma riski
      yoktu. AC'ler görev tanımının somut, test edilebilir sözleşmesidir;
      "dokunma" notu genel bir ihtiyat notudur. Talimat çelişkisinde
      lead'in kendi dersini uyguladım: "bir şey tuhaf geliyorsa dene, sonra
      bildir."
    reddedilen_alternatif: >
      Kontrolü `rooms/join.ts` içinde (Game.create'den ÖNCE, JS tarafında)
      yapmak — ama bu, modeli KORUMAZ: `Game.create` başka bir çağrı
      yolundan (ör. gelecekte bir migration script'i) yanlış pairKey ile
      çağrılırsa hiçbir şey onu YAKALAMAZ. AC12'nin isteği açıkça VERİ
      seviyesinde bir garanti (aynı sınıf ders: DB-001'in friendship
      sıralı-çift bulgusu — "değişmezi yazma yoluna değil veriye bağla").
  - karar: >
      `buildPairKey`/`deriveParticipants` `packages/db/src/pair.ts`te
      (KÖK seviyede) yazıldı — `rooms/` klasörünün İÇİNDE DEĞİL.
    gerekçe: >
      Hem `rooms/join.ts` hem `models/game.ts` bu iki fonksiyona ihtiyaç
      duyuyor. `rooms/`e konsaydı `models/game.ts → rooms/pair.ts` importu
      `rooms/join.ts → models/game.ts` (Game modelini kullanmak için zaten
      var) ile birleşip `rooms ↔ models` DÖNGÜSÜ oluştururdu
      (`import-x/no-cycle` bunu reddeder). Kök seviye, `room-code.ts`nin
      zaten kurduğu kalıpla (oda-ilişkili ama rooms/ dışında saf yardımcı)
      tutarlı.
    reddedilen_alternatif: >
      İki fonksiyonu `models/game.ts`e taşımak ve `rooms/join.ts`in
      oradan import etmesi — modeller katmanının "yalnız şema" sorumluluğunu
      bulanıklaştırırdı; ayrıca AC12'nin kendisi "burada yazılır" derken
      `rooms/`i işaret ediyordu (tam sözcüğü olmasa da bağlamı), kök
      seviye bu niyete en yakın döngüsüz çözüm.
  - karar: >
      `applyMove`'un reddetme sebebini (`out-of-range`/`game-over`/`occupied`)
      belirlemek için `game-core`'un `isValidMove`'unun KULLANDIĞI birebir
      aynı dışa açık ilkellerle (`BOARD_SIZE`, `evaluateStatus`) küçük bir
      sınıflandırma fonksiyonu (`moveRejectionReason`) yazıldı — `isValidMove`
      yalnız `boolean` döner, sebebi dışa vermez.
    gerekçe: >
      `game-core/moves.ts`in kendi belgelenmiş tasarım kararı: "sıra
      sahipliği bilerek doğrulanmaz... motor bunun yerine kararı vermek için
      gereken tek girdiyi dışa verir." Aynı ilke burada da geçerli: motor
      HANGİ üç kuralın ihlal edildiğini dışa vermeyi BİLEREK reddediyor, ama
      taşıma katmanının istemciye bir sebep göstermesi gerekiyor (B8,
      `MoveRejectionReason`). `moveRejectionReason` kural MANTIĞINI yeniden
      YAZMIYOR — yalnızca `isValidMove`'un zaten `false` dediği bir durumun
      HANGİ üç sebepten olduğunu, aynı ilkellerle (BOARD_SIZE sınırı,
      evaluateStatus'un oyun bitmiş mi sonucu) okuyor. `occupied` dalı hiçbir
      ilkel ÇAĞIRMAZ (elemeyle çıkarılır) — board'a `noUncheckedIndexedAccess`
      altında doğrudan indekslemekten de kaçınıyor.
    reddedilen_alternatif: >
      `game-core`'un `isValidMove`'unu `{ok,reason}` döndürecek şekilde
      DEĞİŞTİRMEK — ama görev kartı `game-core`'un DONDUĞUNU, kural
      motorunun bu görevden ETKİLENMEMESİ gerektiğini açıkça söylüyor;
      ayrıca `game-core`in kendi belgelenmiş kararını (sıra sahipliği gibi
      "yarım kontrol tam sanılma riski taşır") bozardı.
  - karar: >
      `rooms/` altındaki TÜM durum-değiştiren yazmalar (create hariç — o
      ilk yazma, koşulu yok; emoji hariç — bilerek `casUpdateRoom`
      KULLANMAZ) TEK bir yardımcıdan (`cas.ts::casUpdateRoom`) geçiyor.
    gerekçe: >
      Tasarım §5.5 "version disiplini — dört kural"ın 2. maddesi: "Koşulsuz
      `updateOne` YASAKTIR (lint edilemez; kod incelemesi maddesi + `rooms/`
      içinde TEK yardımcı fonksiyondan geçirilir)." Bu satır BİREBİR
      uygulandı — `detach.ts`/`join.ts`/`apply-move.ts` hiçbiri doğrudan
      `Room.findOneAndUpdate` ÇAĞIRMIYOR, hepsi `casUpdateRoom`'dan geçiyor.
    reddedilen_alternatif: >
      Her fonksiyonda kendi `findOneAndUpdate({code,version},...)` çağrısını
      elle yazmak — DB-001'in friendship dersiyle AYNI risk sınıfı: bir
      gün biri `version` koşulunu unutursa (kopyala-yapıştır hatası) hiçbir
      test/lint bunu YAKALAYAMAZ. Tek yardımcı, unutmayı YAPISAL OLARAK
      imkânsız kılıyor.

gotchas:
  - >
    `game-core`in `isValidMove(board, index)` fonksiyonu SADECE `boolean`
    döner — tasarım dokümanının §5.5 pseudokodu `check.ok`/`check.reason`
    varsaymış görünüyor ama gerçek `game-core/src/moves.ts` böyle bir API
    SUNMUYOR (kasıtlı, belgelenmiş bir tasarım kararı: "yarım bir kontrol
    tam sanılma riski taşır"). Taşıma katmanı SEBEP göstermek zorunda
    olduğu için (B8) `game-core`i DEĞİŞTİRMEDEN aynı sonucu üreten küçük
    bir sınıflandırıcı yazmak gerekti — pseudokodu OLDUĞU GİBİ kopyalamak
    typecheck'te patlardı.
  - >
    Mongoose casus'lama (`vi.spyOn(Room,'findOne')`) ile `.lean()` zincirini
    ARAYA girmek TS'te overload belirsizliği üretiyor
    (`Parameters<typeof Model.findOne>` spread edilemiyor — TS2556). Tek
    parametreli açık bir sarmalayıcı yazıp `as unknown as typeof X` ile
    TEK noktada daraltmak, tüm çağrı sitesini `any`e boğmaktan daha güvenli.
  - >
    Eşzamanlılık sondası testinde "önce/sonra" karşılaştırma değerini
    CASUS KURULDUKTAN SONRA okumak, casus'un `mockImplementationOnce`ını
    o okuma TÜKETİR ve asıl çağrı hiç enjeksiyona uğramadan geçer — test
    yine de geçebilir (yanlış nedenle). Sıra ÖNEMLİ: referans okuması
    HER ZAMAN casus kurulumundan ÖNCE olmalı.

blocked_reason: null

next_suggestions:
  - >
    WS-001 (sıradaki halka) bu beş fonksiyonu (`createRoom`, `joinRoom`,
    `detachConnection`, `applyMove` + iskeletler) `@xox/db`den DOĞRUDAN
    import edebilir; `packages/db/src/index.ts` VE `rooms/index.ts` artık
    DONDU — WS-001 bu iki barrel'a YENİ bir export EKLEMEMELİ, yalnız
    var olanları çağırmalı.
  - >
    `resign`/`offerRematch`/`acceptRematch`/`settleDeadlines`/`pushEmoji`/
    `finishGame` şu an çağrılırsa AÇIK bir `Error` fırlatıyor (sessizce
    `{ok:false}` DÖNMÜYOR) — W1-02/W2-01/W3-01/W3-03 bu gövdeleri
    doldururken `rooms/{resign,rematch,settle,emoji,finish}.ts`in
    İMZALARINI değiştirmemeli (barrel donuk), yalnız gövdeyi yazmalı.
    `skeletons.test.ts`teki testler o an KIRMIZI olacak (beklenen —
    "henüz uygulanmadı" mesajını arıyorlar) ve o görevler kendi testleriyle
    DEĞİŞTİRMELİ, silmemeli.
  - >
    `joinRoom`'un "her iki koltuk da boş" savunmacı dalı ve `createRoom`'un
    "yazdıktan hemen sonra okuma başarısız olursa SERVER_ERROR" dalı
    kasıtlı olarak DÜŞÜK dal kapsamında bırakıldı (gerçekçi olmayan/
    tetiklenmesi zor uç durumlar) — paket geneli eşik (85% dal) yine de
    rahatça aşıldı, bu iki dalı zorlamak yapay mock gerektirirdi.
  - >
    `detach.ts`/`create.ts` W1-03/W1-04'te GENİŞLETİLECEK (rotate/grace
    zamanlayıcısı entegrasyonu, kod normalleştirme) — bu görev yalnız
    DB-002'nin kabul kriterlerini karşılayan ÇEKİRDEK davranışı yazdı,
    o dosyalara YENİ genel export eklenmedi ki barrel `index.ts` sabit
    kalsın.
```
