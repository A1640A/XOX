```yaml
task: UI-004
status: done
summary: >
  `OpponentLeftBanner` iskeleti dolduruldu — KK-070/071 rakip kopma/dönüş
  bildirimi artık gerçekten DOM'a çıkıyor. Görünürlük kararı `@xox/shared`'ın
  saf `opponentLeftVisible(state, now)` yardımcısından TÜKETİLİYOR (yeniden
  YAZILMADI, `packages/**`'e hiç dokunulmadı); bileşen yalnız o kararı okuyup
  metni/geri sayımı render ediyor.

  Geri sayım `TurnTimer.tsx`nin desenini birebir izliyor: ham `Date.now()`
  KULLANILMIYOR, `serverOffsetMs` uygulanıyor, `clock` opsiyonel prop olarak
  enjekte ediliyor (varsayılan `Date.now`) — testte 3 dakikalık istemci saat
  sapması senaryosu deterministik kuruldu. Saniyede bir `setInterval` ile
  tazeleniyor, `graceEndsAt` `null` iken aralık hiç kurulmuyor.

  "Rakip geri döndü." (KK-071) `graceEndsAt`in dolu-iken-`null`'a düşme
  KENARINI (edge) yakalayarak gösteriliyor — `EmojiTray.tsx`nin "hiddenAt"
  desenine birebir uyularak (senkron `setState`-in-render'dan kaçınmak için)
  `RETURNED_VISIBLE_MS` (5 sn) sonra kendiliğinden kalkıyor; kalıcı bir banner
  rakip zaten normal oynarken ekranda asılı kalırdı.

  `RoomScreen.tsx`e DOKUNULMADI (zaten doğru `state.graceEndsAt`/
  `state.serverOffsetMs` ile mount ediyordu — kartın "casus bileşen" notu
  doğrulandı: `RoomScreen.test.tsx`teki `lastPropsOf(OpponentLeftBanner)`
  iddiası hâlâ birebir geçiyor, dosya hiç açılmadı). Metin `tr.connection.
  opponentDisconnected`/`opponentReturned`den okunuyor — `tr.ts` DONMUŞ
  (TXT-001), hiçbir anahtar eklenmedi/değiştirilmedi, yalnız TÜKETİLDİ.

  Yeni bir `TESTID` eklenmedi (frozen 31-anahtarlık liste, `packages/shared`
  kapsam dışı) — `apps/e2e`nin KK-070/071 testleri zaten `getByText(...)`
  kullanıyor, `role="status" aria-live="polite"` (ConnectionBadge/TurnTimer/
  EmojiTray ile aynı erişilebilirlik deseni) yeterli.

files_changed:
  - apps/web/components/room/OpponentLeftBanner.tsx # iskelet → gerçek gövde
  - apps/web/components/room/OpponentLeftBanner.test.tsx # 9 yeni test (İSKELET testi silinip yerine davranış testleri yazıldı)

tests:
  added: 9
  file: apps/web/components/room/OpponentLeftBanner.test.tsx
  passing: '9/9 (bu dosya) · apps/web toplamı 881/881 · pnpm gates genelinde 5/5 görev yeşil'
  coverage: 'apps/web %94.38 stmt / %89.73 branch / %94 fn / %96.55 line (eşiklerin üzerinde)'

opponentLeftVisible_tuketimi: >
  `OpponentLeftBanner.tsx` `@xox/shared`'dan `opponentLeftVisible` import
  ediyor ve `disconnected = opponentLeftVisible({ graceEndsAt, serverOffsetMs
  }, now)` şeklinde ÇAĞIRIYOR — 2 sn gösterim eşiği (ADR-0007,
  `OPPONENT_LEFT_DISPLAY_DELAY_MS`), `DISCONNECT_GRACE_SECONDS` karşılaştırma
  aritmetiği dosyada TEKRARLANMADI. `OpponentLeftBanner.test.tsx`teki "ADR-0007
  2 sn gösterim eşiğinin ALTINDA hiçbir şey göstermez" testi bu tüketimin
  gerçek eşiğe bağlı olduğunu (elapsed=0 → görünmez, elapsed=2000ms → görünür)
  kilitliyor — sabit kopyalanmış olsaydı da geçerdi, ama `packages/shared/src/
  room-client.ts`teki gerçek fonksiyonu import ettiği `grep`le doğrulanabilir
  (aşağıda).

serverOffsetMs_kaniti: >
  `OpponentLeftBanner.test.tsx` → "KK-070 ÇEKİRDEĞİ: istemci saati sunucudan
  sapmışken (serverOffsetMs) doğru görünürlük ve doğru geri sayım — ham
  istemci saati KULLANILMAZ" testi `TurnTimer.test.tsx`teki 3 dakikalık sapma
  senaryosunun BİREBİR karşılığı: `serverOffsetMs = -180_000`,
  `graceEndsAt = CLIENT_NOW - skewMs + 10_000` (sunucu zamanında ifade
  edilmiş bir kopma anı). Ham `clock()` kullanılsaydı kalan süre yanlış
  hesaplanırdı; test `serverNow = clock() + serverOffsetMs` düzeltmesiyle
  TAM 10 sn beklendiğini doğruluyor ve geçiyor.

sondalar: # commit f9c8ad8 sonrası UYGULANDI: diff -q ile değiştiğini doğrula → koş → git checkout -- → git status --porcelain boş
  - sonda: 'remainingMs hesabından serverOffsetMs çıkarıldı (graceEndsAt - now)'
    sonuc: '1 test kırmızı — KK-070 ÇEKİRDEĞİ (10 sn beklenirken 0 sn geldi)'
  - sonda: 'opponentLeftVisible çağrısı yerine ham (graceEndsAt !== null) kontrolü'
    sonuc: '1 test kırmızı — ADR-0007 2 sn eşik testi (elapsed=0da erken görünür oldu)'
  - sonda: 'RETURNED_VISIBLE_MS setTimeout efekti boş fonksiyona indirgendi (kalıcı banner)'
    sonuc: '1 test kırmızı — "kendiliğinden kalkar" testi (5 sn sonra hâlâ DOM'daydı)'
  - sonda: 'previousGraceEndsAtRef kenar yakalaması silindi (returnedAt hiç set edilmez)'
    sonuc: '2 test kırmızı — KK-071 metni hiç görünmedi (role="status" hiç DOM'a çıkmadı)'

kapilar: |
  pnpm gates → EXIT 0
    typecheck    7/7 (cache hit'ler dışında @xox/web cache miss, gerçek koşu)
    lint         eslint . --max-warnings=0 → temiz
    format:check prettier --check . → temiz (test dosyası --write ile 1 kez düzeltildi)
    test:coverage apps/web 85 dosya / 881 test — hepsi geçti
    knip         exit 0 (30 "configuration hint" — hepsi ÖNCEDEN VAR, bu kartla ilgisiz
                  knip.json ayar önerileri; unused-file/unused-export bulgusu YOK)
  pnpm --filter @xox/web build → başarılı (Next.js 16.3.2, tüm route'lar derlendi)

quarantine_kaldirma_talebi: >
  `apps/e2e/tests/timeout-abandon.spec.ts` içindeki `test.fixme` ile
  karantinaya alınmış iki test (KK-070/071 "terk bildirimi metni") artık
  gerçek bir DOM'a karşı koşabilir: metin `TXT.opponentDisconnectedPrefix`
  ('Rakibin bağlantısı koptu') ve `TXT.opponentReturned` ('Rakip geri döndü.')
  `getByText` ile bulunabilir hâlde render ediliyor. `apps/e2e` çakışma kümem
  DIŞINDA — karantinayı kaldırıp preview'a karşı koşmak lead'e/QA'ya kalıyor.

followups:
  - >
    `RETURNED_VISIBLE_MS = 5_000` sabiti kart tarafından İSTENMEDİ, tasarım
    kararı olarak eklendi (KK-071'in gövdesi yalnız "gösterilir" diyor,
    kalıcılık/kaybolma süresini belirtmiyor). E2E testleri bu süreyi
    sınamıyor (yalnız `toBeVisible`/`toHaveCount(0)` kontrol ediyor), yani
    preview koşusunu etkilemez; ama ileride tasarım bu süreyi değiştirmek
    isterse tek nokta burası.
```
