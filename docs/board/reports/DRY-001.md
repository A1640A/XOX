```yaml
task: DRY-001
status: done
summary: >
  `boardToString(board)` eklendi ve `index.ts`'ten dışa aktarıldı. Her hücreyi
  `X`, `O` ya da `.` olarak yazar; `EMPTY_BOARD` girdisinde `'.........'` döner.
  `board.test.ts`'teki mevcut `b(s)` yardımcısıyla gidiş-dönüş test edildi —
  metne çevrilip geri okunan tahta orijinaliyle birebir eşit.
files_changed:
  - packages/game-core/src/board.ts
  - packages/game-core/src/board.test.ts
  - packages/game-core/src/index.ts
  - docs/board/reports/DRY-001.md
tests: { added: 3, passing: 91, coverage: '100%', mutation: '98.56%' }
decisions:
  - karar: >
      Uygulama `cell ?? '.'` ile bir dizi push + join döngüsü olarak yazıldı
      (availableMoves ile aynı imperatif for-loop kalıbı, cellAt üzerinden).
    gerekçe: >
      `@typescript-eslint/prefer-nullish-coalescing` kuralı `cell === null ? '.' : cell`
      biçimindeki üçlü ifadeyi reddediyor; `??` hem kuralı geçiyor hem de gerçek
      mutasyon koşusunda (bkz. çıktı) yalnızca `&&` varyantı üretiliyor — bu da
      testlerce ölü mutant olarak yakalanıyor, eşdeğer mutant riski doğrulanmadı.
    reddedilen_alternatif: >
      `cell === null ? '.' : cell` üçlü ifadesi — lint kuralını ihlal ediyor;
      ayrıca tek satırlık `eslint-disable` ile bastırmak gereksizdi çünkü `??`
      hem daha sade hem de mutasyon koşusunda eşdeğer hayatta kalan mutant
      üretmedi.
  - karar: "`chars: string[]` biriktirip `.join('')` ile birleştirme (imperatif for-loop, cellAt kullanılarak)."
    gerekçe: >
      Paketteki mevcut `availableMoves` ile aynı kalıp (push + for-loop) —
      tutarlılık ve kanıtlanmış yüksek mutasyon skoru. `cellAt` kullanımı,
      indeks daraltmasının tek noktada kalması sözleşmesini korur.
    reddedilen_alternatif: >
      `board.map((c) => c ?? '.').join('')` — daha kısa ama `board[i]` doğrudan
      indekslemesi `noUncheckedIndexedAccess` altında güvenli değildir ve
      paketin "tek narrowing noktası cellAt" kuralını atlar.
gotchas:
  - >
    `@typescript-eslint/prefer-nullish-coalescing` kuralı `=== null ? a : b`
    kalıbını reddediyor; `??` kullanmak zorunlu. Bu paket için mutasyon
    testinde `??` üzerinde yalnızca `&&` varyantı üretiliyor (domain'de tüm
    olası `Cell` değerleri X/O/null olduğundan `||` varyantı zaten eşdeğer
    olurdu, ama gözlemlenen koşuda hiç üretilmedi) — gerçek çıktı ile
    doğrulandı, ölü mutant listesinde `LogicalOperator` mutantı görünüyor ve
    "killed" durumda.
next_suggestions:
  - >
    Diğer test dosyalarındaki (`status.test.ts`, `moves.test.ts`, `ai.test.ts`)
    elle yazılmış `b(s)` yardımcıları `boardToString`'in tersini yapıyor;
    isteğe bağlı bir takip görevi bu dosyalardaki tekrarı `board.ts`'ten
    (henüz yazılmamış) bir `stringToBoard`/`b` yardımcısıyla azaltabilir —
    ancak bu, DRY-001'in çakışma kümesi dışında kaldığı için burada
    yapılmadı.
```
