```yaml
task: W3-04
status: done
summary: >
  Arkadaşlar: istek, kabul, liste, çıkarma. `packages/shared`in ZATEN
  DONDURULMUŞ `friendRequestBodySchema`/`friendActionBodySchema`/
  `friendsResponseSchema`/`NOT_FRIENDS_ELIGIBLE`/`tr.friends.*` sözleşmesi
  KULLANILDI, hiçbiri değiştirilmedi/eklenmedi. `models/friendship.ts`in
  sıralı-çift değişmezi (bu gece güvenlik denetimiyle düzeltilen hâli)
  KORUNDU — yeni yazma yolları (`requestFriendship`/`respondToFriendRequest`/
  `removeFriend`) hep `sortPair`den geçen sıralı `{userA,userB}` filtresiyle
  çalışır, ham `updateOne`/`$set` ile ters sıra YAZILMAZ.

sirali_cift_degismezinin_korundugunun_kaniti:
  tasarim: >
    `packages/db/src/queries/friends.ts`teki TEK yardımcı `sortPair(a,b)`
    (`a < b ? [a,b] : [b,a]`) — `requestFriendship`/`respondToFriendRequest`/
    `removeFriend` üçü de filtre/`$setOnInsert` için BUNDAN geçiyor, hiçbiri
    kendi sıralamasını icat etmiyor. Ham `Friendship.updateOne`/
    `findOneAndUpdate` çağrıları YALNIZ zaten sıralı `{userA,userB}` ile
    yapılıyor (model testinde kanıtlanan "$set içindeki ters sıra da
    reddedilir" davranışına GÜVENİLMİYOR, ayrıca kendimiz de sırayı asla
    bozmuyoruz — savunma iki katmanlı).
  test_kaniti: >
    `packages/db/src/queries/friends.test.ts` "ters argüman sırasıyla
    çağrılsa da AYNI sıralı çifti hedefler" — `requestFriendship(b, a)`
    çağrısı sonrası `countDocuments({userA,userB})` (küçük/büyük sıralı) 1
    dönüyor, ters sıralı bir ikinci kayıt AÇILMIYOR. Model seviyesinde
    zaten var olan `friendship.test.ts`teki 10 test (updateOne/
    findOneAndUpdate/replaceOne + upsert ile ters sıra reddi) DEĞİŞTİRİLMEDİ
    ve hâlâ 10/10 yeşil (bu görev onlara dokunmadı, yalnız üstüne inşa etti).
  mutasyon_kaniti: >
    Aşağıdaki "sondadan" bölümünde `requestFriendship`in `$setOnInsert`ini
    `$set`e çevirip (idempotansı bozan bir mutasyon, sıralamayı DEĞİL) testin
    kırmızı olduğu doğrulandı — ayrı bir mutasyon olarak sıralamanın kendisini
    bozmadım çünkü o zaten modelin `pre('findOneAndUpdate')` hook'u tarafından
    sertçe reddediliyor (mutasyon modelin kendisinde patlardı, sorgu
    katmanında değil) — model testleri (friendship.test.ts) bunu zaten
    kanıtlıyor, tekrar kanıtlamak bu görevin kapsamı değil.

numaralandirma_ve_zamanlama_testi:
  tasarim: >
    `hasFinishedGameTogether(a,b)` TEK sorgu: `Game.exists({pairKey,
    finishedAt:{$ne:null}})`. `POST /api/friends` bu TEK sonucu kullanır —
    hedef `userId`'nin var olup olmadığına dair AYRI bir `User.findById`
    çağrısı YOK. Sonuç: var-olmayan bir `userId` ile var-olan-ama-hiç-
    oynanmamış bir `userId` AYNI kod yolundan, AYNI sayıda DB round-trip'iyle
    geçer ve AYNI 403 `NOT_FRIENDS_ELIGIBLE` gövdesini alır.
  test_kaniti_db_katmani: >
    `packages/db/src/queries/friends.test.ts` → "var OLMAYAN bir userId ile
    var olan ama HİÇ oynanmamış bir userId AYNI sonucu (false) ... üretir" —
    ikisi de `false`, ikisi de aynı fonksiyondan geçiyor.
  test_kaniti_route_katmani: >
    `apps/web/app/api/friends/route.test.ts` → "Numaralandırma/zamanlama
    karşıtı" testi: `hasFinishedGameTogether` mock'u `false` döndürürken
    hem "hiç var olmayan" hem "var ama uygun değil" görünen `userId` ile
    POST çağrılıyor, `toStrictEqual` ile İKİ YANITIN GÖVDESİ birebir
    karşılaştırılıyor + `hasFinishedGameTogether`in TAM 2 kez (fazladan bir
    varlık-kontrolü çağrısı OLMADAN) çağrıldığı doğrulanıyor. Ayrıca `@xox/db`
    mock'u BİLEREK `User` modelini export ETMİYOR — route bir varlık kontrolü
    için `User`'a dokunsaydı bu test `undefined` üzerinden çağrı hatasıyla
    KIRILIRDI (mekanik zorlama, yalnız iddia değil).
  mutasyon_kaniti: >
    `route.ts`teki `if (!eligible)` şartını `if (false)`e çevirip (uygunluk
    kontrolünü etkisiz kılan mutasyon) testler koşuldu: "KK-126" ve "kendine
    istek" testleri KIRMIZI oldu (200 bekleniyordu ↔ 403 bekleniyor,
    ters). Mutasyon `route.ts.orig` ile `diff -q`'dan geçirilerek GERİ
    ALINDI, `git status` boş.

idempotans_testi:
  requestFriendship: >
    `friends.test.ts` → "aynı isteği iki kez göndermek İKİNCİ bir kayıt
    AÇMAZ" (`countDocuments` 1) ve "zaten accepted olan bir çifte tekrar
    istek göndermek durumu DEĞİŞTİRMEZ" (`status` hâlâ `accepted`).
    `$setOnInsert` yalnız kayıt YOKSA yazdığı için ikisi de doğal sonuç.
  respondToFriendRequest: >
    "İSTEĞİ GÖNDEREN kendi isteğini kabul etmeye çalışırsa hiçbir şey
    değişmez" (filtre `requestedBy: requesterId` eşleşmiyor) ve "var olmayan
    bir isteğe yanıt vermek hata FIRLATMAZ, sessizce no-op" — route katmanında
    da PATCH "idempotans" testi aynı no-op'un 200 döndüğünü doğruluyor
    (numaralandırma yüzeyi açmıyor: başarı/no-op ayrımı istemciye sızmıyor).
  removeFriend: >
    "pending bir isteğe DOKUNMAZ (yalnız accepted hedeflenir)" ve route
    katmanında "var olmayan bir ilişkiyi silmeye çalışmak da idempotent 200
    döner".
  mutasyon_kaniti: >
    `requestFriendship`teki `$setOnInsert`i `$set`e çevirdim (upsert artık
    var olan bir kaydın `status`unu HER ÇAĞRIDA `pending`e geri döndürüyor) —
    "zaten accepted olan bir çifte tekrar istek göndermek durumu DEĞİŞTİRMEZ"
    testi KIRMIZI oldu (`accepted` beklendi, `pending` alındı). Mutasyon
    `friends.ts.orig` ile `diff -q`'dan geçirilerek GERİ ALINDI, `git status`
    boş.

roomscreen_dokunulmadiginin_kaniti: >
  `git diff 7cce531..HEAD -- apps/web/components/room/RoomScreen.tsx` boş
  (dosya bu görev boyunca HİÇ açılmadı/değiştirilmedi). Yalnız
  `FriendAddButton.tsx` (UI-SKEL-001'in bıraktığı iskelet gövdesi
  dolduruldu — `opponentId`/`visible` prop imzası AYNI kaldı) ve YENİ
  `FriendAddButton.test.tsx` dokunuldu. `RoomScreen.test.tsx` (var olan,
  `FriendAddButton`'ı `vi.mock` ile izole eden dosya) DEĞİŞTİRİLMEDİ ve
  hâlâ yeşil — bu, prop sözleşmesinin bozulmadığının ayrı bir kanıtı.
  `ResultPanel.tsx`/`EmojiTray.tsx`/`app/davet/**` (W3-03 paralel çakışma
  kümesi) da hiç açılmadı.

degisiklikler:
  - dosya: 'packages/db/src/queries/friends.ts (yeni)'
    ne: >
      `hasFinishedGameTogether`, `requestFriendship`, `respondToFriendRequest`,
      `removeFriend`, `getFriendsView`. `sortPair` tek sıralama kaynağı.
      `getFriendsView` `status:'pending'` satırlarını `requestedBy`'a göre
      `incoming`/`outgoing`'e ayırır; `User.find(..., 'name elo')` ile
      yalnız gereken alanlar projekte edilir (`passwordHash` zaten
      `select:false`, `aggregate` kullanılmadı).
  - dosya: 'packages/db/src/queries/friends.test.ts (yeni)'
    ne: 'Gerçek xox_test — 15 test (uygunluk/idempotans/numaralandırma/görünüm).'
  - dosya: 'packages/db/src/index.ts'
    ne: 'Yeni 5 fonksiyon + 2 tip export edildi (barrel).'
  - dosya: 'apps/web/app/api/friends/route.ts (yeni)'
    ne: >
      GET/POST/PATCH/DELETE. Hepsi `resolveIdentity(req)` — `allowTicket`
      HİÇBİR YERDE geçilmiyor. Hata gövdeleri `ErrorResponse` tipiyle
      `satisfies` ile kuruluyor (zod şemasından türetilen tip, elle `as`
      cast yok). `SERVER_ERROR` dalları `logError` ile sunucu tarafında
      loglanıyor, istemciye stack sızmıyor.
  - dosya: 'apps/web/app/api/friends/route.test.ts (yeni)'
    ne: >
      20 test — gerçek `resolveIdentity`, yalnız `@/auth`+`@xox/db` mock
      (KK-010 disiplini). `@xox/db` mock'u `User`'ı BİLEREK export etmiyor.
  - dosya: 'apps/web/components/room/FriendAddButton.tsx'
    ne: >
      İskelet gövdesi dolduruldu: `POST /api/friends`, `try/catch/finally`
      (`pending` finally'de düşer), başarı `role="status"`, hata
      `ErrorBanner` (zod `errorResponseSchema.safeParse`den geçirilmiş kod).
  - dosya: 'apps/web/components/room/FriendAddButton.test.tsx (yeni)'
    ne: '7 test (visible/opponentId dalları, başarı, 403, ağ hatası+pending kurtarma).'
  - dosya: 'apps/web/components/friends/FriendsContent.tsx (yeni)'
    ne: >
      `/arkadaslar`in tek ağ katmanı — `ProfileContent.tsx` ile AYNI
      disiplin (`useSession`, `session===null` güvenlik ağı, hata kodu
      zod'dan geçirilir). Yalnız DONDURULMUŞ `tr.friends.*` anahtarları
      kullanıldı, `tr.ts`e YENİ ANAHTAR EKLENMEDİ.
  - dosya: 'apps/web/components/friends/FriendsContent.test.tsx (yeni)'
    ne: '8 test (empty/hata/kabul/reddet/çıkar/ağ hatası).'
  - dosya: 'apps/web/app/arkadaslar/page.tsx (yeni)'
    ne: '`ProfilPage`/`OdaKatilPage` ile AYNI ince kalıp — middleware zaten `/arkadaslar`ı koruyor (önceden eklenmiş).'

kapsam_disi_bilinçli_karar: >
  `/arkadaslar` sayfası `outgoing` (gönderilen bekleyen) listesini AYRI bir
  bölümde GÖSTERMİYOR — kabul kriteri yalnız "bekleyen istekleri (kabul/
  reddet) ve arkadaş listesini (ad, ELO, çıkar)" istiyor, `outgoing` için
  bir UI şartı YOK ve `tr.ts` DONDURULMUŞ (TXT-001) olduğu için yeni bir
  bölüm başlığı metni eklenemezdi. `GET /api/friends` sözleşme gereği
  `outgoing`u yine de döner (API sözleşmesi tam), `FriendsContent` bunu
  state'te tutar ama render etmez — ileride bir metin eklenirse tek satırlık
  bir JSX bloğu yeterli olur.

test_kaniti:
  kirmizi_once: >
    Her üç dosya (queries/friends.test.ts, route.test.ts,
    FriendAddButton.test.tsx, FriendsContent.test.tsx) KOD ile BİRLİKTE
    yazıldı (TDD'nin "önce kırmızı" adımı ayrık commit'lerle değil, aynı
    oturumda yapıldı — ayrıca aşağıdaki mutasyon sondaları kırmızı/yeşil
    döngüsünü AYRICA kanıtlıyor).
  yesil: >
    `pnpm gates` (worktree `.claude/worktrees/W3-04`) TAMAMEN yeşil:
    typecheck (7/7 paket), lint (0 uyarı), format (temiz), test:coverage
    (@xox/web 64 dosya/663 test, @xox/db 24 dosya/184 test — hepsi yeşil),
    knip (yalnız bilgi ipucu, hata yok).
  coverage: >
    @xox/web %93.66 ifade / %88.33 dal / %93.09 fonksiyon / %96.07 satır.
    @xox/db %95.48 ifade / %89.13 dal / %98.68 fonksiyon / %97.75 satır.
  mutasyon_sondalari: >
    İki ayrı mutasyon uygulandı, KIRMIZI olduğu doğrulandı, `diff -q` ile
    orijinal dosyayla birebir eşleştiği kanıtlanarak GERİ ALINDI (bkz.
    yukarıdaki "numaralandirma_ve_zamanlama_testi" ve "idempotans_testi"
    bölümlerindeki mutasyon_kaniti alanları). Sondadan sonra
    `git status --porcelain` BOŞ.

commit_shas:
  - '0c5e6fa feat(db): arkadaşlık sorguları — istek/kabul/reddet/çıkarma (W3-04)'
  - '5748437 feat(web): /api/friends yüzeyi, FriendAddButton gövdesi, /arkadaslar sayfası (W3-04)'

worktree: ".claude/worktrees/W3-04 (branch feat/W3-04, main'den ayrıldı, main'e merge/push YAPILMADI)"

not_islem: >
  İlk üç dosya (queries/friends.ts + test + index.ts güncellemesi) YANLIŞLIKLA
  önce ana checkout'ta (main) yazıldı — fark edilince `git stash push
  --include-untracked -- <yollar>` ile main'den geri alındı, worktree
  `git worktree add` ile kurulduktan sonra `git stash apply` ile doğru
  worktree'ye taşındı ve stash düşürüldü. Aynı hata `apps/web/app/api/
  friends/{route.ts,route.test.ts}` için TEKRARLANDI (iki dosya yine main'e
  yazıldı) — bu kez basit `mv` ile worktree'ye taşındı (henüz stage
  edilmemişlerdi, stash'e gerek kalmadı). Her iki olayda da `main` çalışma
  ağacı işlem sonunda TEMİZ bırakıldı (`git status --porcelain` yalnızca
  lead'in kendi `docs/board/journal.ndjson` değişikliğini gösteriyor, bu
  görev onu HİÇ değiştirmedi).

blocked_reason: null

next_suggestions:
  - >
    Lead/QA-e2e: `/arkadaslar` ve oyun-sonu "Arkadaş ekle" akışı için bir
    E2E senaryosu (`apps/e2e`) düşünülebilir — KK-125/126/127 uçtan uca
    henüz Playwright'la doğrulanmadı (bu görev yalnız birim/entegrasyon
    seviyesinde, gerçek xox_test'e karşı kanıtlandı).
  - >
    İleride `outgoing` (gönderilen bekleyen istekler) için bir UI bölümü
    istenirse `tr.ts`e yeni bir anahtar (ör. `friends.sentPending`) eklenmesi
    gerekir — TXT-001 dondurmasını kaldırmak lead kararı gerektirir.
```
