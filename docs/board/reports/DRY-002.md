````yaml
task: DRY-002
status: done
summary: >
  `errorJson` (`{code,message}` gövdesi + verilen HTTP durumu) altı route'ta
  kopyalanmıştı. Çakışma kümesindeki dört route (`rooms`, `rooms/[code]`,
  `auth/register`, `ws/ticket`) tek kaynağa (`apps/web/lib/http/error-json.ts`)
  bağlandı; davranış birebir korundu (yalnız import/tanım satırları değişti,
  hiçbir route/durum kodu/gövde satırı değişmedi). `profile`, `friends`,
  `auth/mobile/refresh` kapsam dışı bırakıldı (kart talimatı — sırasıyla
  paralel kartların/agentların alanı).

yeni_modul:
  dosya: 'apps/web/lib/http/error-json.ts'
  govde: >
    `export function errorJson(code: ErrorCode, message: string, status: number): Response`
    — dört route'ta birebir aynı olan `Response.json({ code, message } satisfies
    ErrorResponse, { status })` gövdesinin AYNEN taşınmış hali (davranış
    değişikliği YOK, yalnız konum değişti).
  test: 'apps/web/lib/http/error-json.test.ts (2 test: gövde şekli+durum, ikinci kombinasyonla çıplak değer kanıtı — ilişki değil sabit değer doğrulanıyor)'
  cagiran_kapsam: >
    knip ölü kod uyarısı vermesin diye modülü TÜKETEN dört route +
    kendi test dosyası aynı commit'te; ayrıca yeni modülün kendi
    `error-json.test.ts`i knip'in "kullanılıyor" saydığı referans.

degisen_route_lar:
  - dosya: 'apps/web/app/api/rooms/route.ts'
    ne: 'Yerel `function errorJson(...)` silindi, `@/lib/http/error-json` import edildi. `ErrorCode`/`ErrorResponse` type importları artık gerekmediği için kaldırıldı (yalnız errorJson içinde kullanılıyordu).'
  - dosya: 'apps/web/app/api/rooms/[code]/route.ts'
    ne: 'Aynı desen — yerel tanım silindi, import eklendi.'
  - dosya: 'apps/web/app/api/auth/register/route.ts'
    ne: 'Yerel tanım silindi, import eklendi. `ErrorCode` tipi hâlâ `fieldErrorCode` fonksiyonunda kullanıldığı için o import KORUNDU, yalnız `ErrorResponse` kaldırıldı.'
  - dosya: 'apps/web/app/api/ws/ticket/route.ts'
    ne: 'Aynı desen — yerel tanım silindi, import eklendi.'

davranis_korumasi_kaniti:
  yontem: >
    Değişiklikten önceki dört `errorJson` gövdesi ve şimdiki tek modül
    satır satır karşılaştırıldı (aynı fonksiyon imzası, aynı
    `Response.json({code,message} satisfies ErrorResponse,{status})`).
    `git diff --stat` YALNIZ import/tanım satırlarını gösteriyor — hiçbir
    çağrı sitesi (`errorJson('...', '...', NNN)` argümanları) değişmedi:
    ```
    apps/web/app/api/auth/register/route.ts | 12 ++----------
    apps/web/app/api/rooms/[code]/route.ts  | 13 ++-----------
    apps/web/app/api/rooms/route.ts         | 12 ++----------
    apps/web/app/api/ws/ticket/route.ts     | 12 ++----------
    4 files changed, 8 insertions(+), 41 deletions(-)
    ```
  grep_kaniti_kopyalar_kalkti: >
    `grep -rn "function errorJson" apps/web/` çakışma kümesindeki dört
    dosyada ARTIK YOK; yalnız kapsam dışı üç dosyada (`profile`, `friends`,
    `auth/mobile/refresh` — kasıtlı olarak dokunulmadı) ve yeni modülde
    (`export function errorJson`) görünüyor.
  surucu_ayrintisi_sizmiyor_testleri_ayakta: >
    Dört testin tamamı DOKUNULMADAN geçti: `rooms/route.test.ts` ("createRoom
    istisna fırlatırsa 500 SERVER_ERROR döner, sürücü ayrıntısı sızmaz"),
    `rooms/[code]/route.test.ts` ("DB hatası fırlatırsa 500 SERVER_ERROR
    döner, sürücü ayrıntısı sızmaz"), `auth/register/route.test.ts`
    ("beklenmeyen DB hatası 500 SERVER_ERROR döner ve mesajı sızdırmaz"),
    `ws/ticket/route.test.ts` ("recordWsTicket (DB yazması) başarısız
    olursa istemciye 500 SERVER_ERROR ... döner"). Hiçbiri gevşetilmedi,
    hepsi refactor sonrası da yeşil (bkz. test_kaniti altında 887/887).

test_kaniti:
  yeni_testler: 'apps/web/lib/http/error-json.test.ts — 2 test (gövde şekli+durum kodu, sabit değer kontrolü).'
  tam_kosu: '@xox/web: 89 test dosyası, 887/887 test yeşil (2 yeni test dahil, hiçbir mevcut test silinmedi/gevşetilmedi).'
  coverage: '%94.23 ifade · %89.28 dal · %93.98 fonksiyon · %96.35 satır.'
  typecheck: 'tsc --noEmit temiz (apps/web).'
  lint: 'eslint . --max-warnings=0 apps/web packages/db temiz.'
  format: 'prettier --check temiz.'
  knip: 'ölü kod/kullanılmayan export uyarısı yok — yalnız bilgilendirici "Configuration hints" (mevcut, bu görevden bağımsız).'

pnpm_gates_ciktisi: >
  `pnpm gates` (typecheck + lint + format:check + test:coverage + knip)
  tamamı yeşil, exit 0. `test:coverage` turbo görevi ilk koşuda gerçek
  çalıştı (`Cached: 0 cached`), ikinci teyit koşusunda `Cached: 6 cached
  >>> FULL TURBO` (aynı worktree'de tekrar — merge sonrası ayrıca
  `--force` ile yeniden koşulmalı, bu integrator görevi).

kapsam_disi_dokunulmayanlar:
  - 'apps/web/app/api/profile/route.ts (yerel errorJson KORUNDU)'
  - 'apps/web/app/api/friends/route.ts (yerel errorJson KORUNDU)'
  - 'apps/web/app/api/auth/mobile/{authorize,callback,refresh}/route.ts (W2-03 alanı, kart talimatıyla dokunulmadı)'
  - 'apps/web/auth.ts (SEC-005 paralel, dokunulmadı)'
  - 'apps/web/components/room/OpponentLeftBanner.tsx, apps/web/messages/tr.ts (UI-004 paralel, dokunulmadı)'

commit_shas:
  - 'ba76fd2 refactor(web): errorJson tek kaynağa indirildi (DRY-002) — feat/DRY-002'

worktree: ".claude/worktrees/DRY-002 (branch feat/DRY-002, main'den ayrıldı, main'e merge/push YAPILMADI)"

blocked_reason: null

next_suggestions:
  - >
    Lead isterse ayrı bir kart açıp `profile`/`friends`/`auth/mobile/refresh`
    içindeki kalan üç kopyayı da aynı `apps/web/lib/http/error-json.ts`'e
    bağlayabilir — bu kartın kapsamı dışında bırakıldı (conflictSet dışına
    çıkmama kuralı gereği).
````
