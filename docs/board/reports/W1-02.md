---
task: W1-02
title: 'Sonuç, pes etme, rövanş (finishGame idempotansı dahil)'
agent: xox-dev-realtime
status: done-with-blocked-subcriterion
branch: feat/W1-02
worktree: .claude/worktrees/W1-02
base: b1301bc (good/wave-0)
commits:
  - sha: 4328a55
    message: 'feat(db): sonuç/pes/rövanş geçişleri + finishGame CAS idempotansı'
  - sha: b0e9a17
    message: 'feat(web): pes/rövanş handler gövdeleri + sonuç paneli + room-view borcu'

gates:
  command: pnpm gates
  exit_code: 0
  turbo: '7/7 (typecheck+lint+format:check) · 5/5 (test:coverage) · knip temiz'
  db_coverage: 'statements 95.05% · branches 88.93% · functions 98.52% · lines 97.52%'
  totals: 'db 165 test / 23 dosya · web 519 test / 51 dosya'
  new_tests: 'db +36 (finish 9, resign 7, rematch 17, apply-move-finish 3), web +58 dosya toplamı'

files_written:
  - packages/db/src/models/room.ts # YENİ ALAN: RoomResult + result
  - packages/db/src/rooms/finish.ts # finishGame CAS + stats + toRoomResult
  - packages/db/src/rooms/resign.ts
  - packages/db/src/rooms/rematch.ts
  - packages/db/src/rooms/apply-move.ts # terminal dalda result + finishGame
  - packages/db/src/rooms/{finish,resign,rematch,apply-move-finish}.test.ts # YENİ
  - packages/db/src/rooms/skeletons.test.ts # 5 iskelet -> 1 (pushEmoji)
  - packages/shared/src/game-status.ts # forfeitStatus dönüş tipi daraltıldı
  - apps/web/lib/game/room-view.ts # BORÇ: sonuç rooms.result'tan okunuyor
  - apps/web/lib/realtime/handlers/{resign,rematch}.ts
  - apps/web/lib/realtime/handlers/{resign,rematch}.test.ts # YENİ
  - apps/web/components/room/ResultPanel.tsx
  - apps/web/components/room/{ResultPanel.test.tsx,status-text.test.ts} # YENİ
  - apps/web/{lib/game/room-view,lib/realtime/{connection,session,room-hub},lib/realtime/handlers/index}.test.ts # fixture'lara result:null + UYGULANAN kümesi
  - apps/web/components/room/RoomScreen.test.tsx # data-kazanan TAM 3 hücre iddiası

files_not_touched:
  - apps/web/lib/realtime/handlers/index.ts # kriter 13 — açılmadı
  - apps/web/lib/realtime/connection.ts # kriter 13 — açılmadı
  - packages/db/src/index.ts # kriter 13 — açılmadı
  - packages/db/src/rooms/detach.ts # W1-03
  - apps/web/lib/realtime/{presence,rotate}.ts # W1-03
  - apps/web/components/room/{ConnectionBadge,RoomScreen}.tsx # W1-03 / DONMUŞ
  - apps/web/app/oyna/** # W1-01

acceptance:
  1_finishGame_CAS: ok
  2_idempotans_dokuman_farki: ok
  3_games_dokumani_tam: ok
  4_stats_tam_1: ok
  5_resign_line_null: ok
  6_rematch_offer_state_ve_olay: 'ok (≤2 sn ölçümü preview E2E'nin işi — birim testte yol doğrulandı)'
  7_koltuk_takasi: ok
  8_version_sifirlanmaz: ok
  9_tembel_dusme: ok
  10_karsilikli_teklif_mutabakat: ok
  11_rakip_ayrilinca_iptal: BLOCKED
  12_ResultPanel_metin_ve_data_kazanan: ok
  13_donmus_dosyalar_acilmadi: ok
  14_xox_test_yesil_gates_temiz: ok

blocked:
  criterion: 11
  what: "rematch:cancelled{reason:'opponent-left'} + teklif edenin `Rakip ayrıldı.` ekranı"
  why: >
    Üç yazma yüzeyinin de bu kartta AÇILMASI YASAK ve gereken değişiklik
    tamamen onların içinde:
      (a) `packages/db/src/rooms/detach.ts` (W1-03'te) — bugün `state:'finished'`
          iken YALNIZ `presence[seat]=null` yazıyor; bekleyen `rematch` teklifi
          rakip ayrılınca odada AYNEN kalıyor. Ayrıca `disconnected` yalnız
          `state==='playing'` iken damgalandığı için rövanş aşamasında teklif
          edene HİÇBİR sinyal gitmiyor (`opponent:left` de üretilmiyor).
      (b) `apps/web/lib/realtime/connection.ts:205-208` — sebep SABİT KODLU
          `'expired'`; yorumu bile "süresi dolması tek P0 yolu" diyor. Bu satır
          değişmeden `'opponent-left'` tele hiçbir koşulda çıkamaz.
      (c) `apps/web/components/room/RoomScreen.tsx` (DONDURULDU) — `ResultPanel`
          `status/you/rematch` + iki geri çağrı alıyor; rakibin ayrıldığını
          gösteren bir prop (ör. `state.graceEndsAt !== null`) mount noktasına
          eklenmeden panel bu metni SEÇEMEZ.
  evidence:
    - "grep 'opponent-left' → yalnız packages/shared (şema + şema testi); üreten kod YOK"
    - "detach.ts:26 `if (room.state === 'playing')` — finished odada disconnected yazılmıyor"
  what_i_did_instead:
    - "Protokol zaten `reason:'opponent-left'`i taşıyor; şemada değişiklik gerekmiyor."
    - "ResultPanel'e HER sonuçta görünen `Ana sayfa` çıkışı eklendi: rakip yanıt
       vermezse (ya da ayrılırsa) kullanıcı sonuç ekranında kilitlenmiyor."
  handoff: |
    1. detach.ts: `if (room.state === 'finished' && room.rematch !== null)
       set['rematch'] = null`  (+ istenirse finished odada da `disconnected`
       damgalamak — o karar W1-03/W2-01'in).
    2. connection.ts:205-208: sebebi türet —
       `const reason = room.presence[opponentSeat] === null ? 'opponent-left' : 'expired'`.
    3. RoomScreen.tsx: `<ResultPanel opponentLeft={state.graceEndsAt !== null} …>`;
       panel `tr.connection.opponentLeft` + `tr.rematch.newRoom` + `tr.common.home`
       gösterir. Metinler `tr`de HAZIR, yeni anahtar gerekmiyor.

proofs:
  idempotans_dokuman_farki:
    yesil: |
      expect(docDiff(afterFirst, afterSecond)).toStrictEqual({})   → PASS
      (iddia geçici olarak `.not.` ile ters çevrildi, vitest ÇIKTISI:
       "AssertionError: expected {} to not strictly equal {}
        Compared values have no visual difference." — yani gerçek fark BOŞ)
    mutasyon_altinda: |
      # sonda: finish.ts CAS filtresinden `finishedAt: null` KALDIRILDI
      AssertionError: expected { …(2) } to strictly equal {}
      + {
      +   "1332cf54-aca8-49ee-ac20-754138bc6245": {
      +     "stats": {
      +       "sonra": { "draws": 1, "losses": 0, "wins": 1 },
      +       "önce":  { "draws": 0, "losses": 0, "wins": 1 },
      # yani ikinci `finishGame` sayacı GERÇEKTEN bozuyor; test bunu görüyor.
  cas_mutasyonu:
    sonda: "perl -0pi -e 's/{ _id: gameId, finishedAt: null }/{ _id: gameId }/' finish.ts"
    dosya_gercekten_degisti: 'diff -q → exit 1 (Files differ) · grep ile satır 72 doğrulandı'
    sonuc: '3 test kırmızı (İDEMPOTANS · ÜÇ ardışık çağrı · CAS kaybı) — 13 yeşil kaldı'
    geri_alma: 'git checkout -- packages/db/src/rooms/finish.ts → git status --porcelain BOŞ'
  version_monotonikligi_mutasyonu:
    sonda: "startRematch sonuna `Room.updateOne({code},{$set:{version:0}})` eklendi"
    dosya_gercekten_degisti: 'diff -q → exit 1 · grep "version: 0" satır 91-92'
    cikti: |
      × version rövanşta SIFIRLANMAZ — monotonik artmaya devam eder (KK-058)
      AssertionError: expected +0 to be 32 // Object.is equality
      - 32
      + 0
    sonuc: '1 kırmızı / 16 yeşil — yalnız hedef test düştü (yanlış-pozitif yok)'
    geri_alma: 'git checkout -- rematch.ts → git status --porcelain BOŞ'
  koltuk_takasinda_seat_lost:
    test: "apps/web/lib/realtime/handlers/rematch.test.ts → 'KOLTUK TAKASINDA seat-lost/takeover TETİKLENMEZ'"
    nasil: >
      Ada (u1/c1, X) ve Kaan (u2/c2, O) için iki gerçek `createRoomConnection`
      kuruldu, ikisi de `primeState` ile bitmiş odayı aldı; sonra ikisine de
      takas edilmiş oda `onRoomChange` ile verildi.
    sonuc: |
      ada.closes = []   kaan.closes = []          (ne 4403 seat-lost ne 4409)
      ada.seat() = 'O'  kaan.seat() = 'X'
      son mesaj: {type:'state', you:'O', version:32} / {type:'state', you:'X', version:32}
      tahta TÜMÜYLE boş geldi (ince delta DEĞİL — KK-047)
    negatif_kontrol: >
      Aynı akış presence TAKAS EDİLMEDEN koşturulunca `closes = [{code:4409,
      reason:'takeover'}]` üretiyor. Yani "tetiklenmedi" tesadüf değil: presence
      takası düşerse test kırmızıya döner.
  borc_kapanisi:
    acilan_alan: 'rooms.result → `RoomResult { kind, winner, line, reason }`'
    neden_bu_sekil: >
      `TransportStatus`un (ADR-0001) alanlarıyla BİREBİR. İkinci bir eşleme tipi
      seçilseydi iki ayrı dönüştürücü (db→şekil, şekil→taşıma) olur ve sessizce
      sapabilirdi. Bu şekilde okuma tarafı doğrudan `transportStatusSchema` ile
      doğruluyor — `reason:'line' ⟺ line !== null` değişmezi VERİTABANINDAN
      gelen veride de çalışma zamanında kontrol ediliyor.
    room_view_nereden_okuyor: |
      apps/web/lib/game/room-view.ts → roomTransportStatus():
        1. `room.result` varsa `transportStatusSchema.safeParse` ile doğrulanıp DÖNER
        2. yoksa tahtadan `evaluateStatus` (line/draw)
        3. tahta bitmemiş + state==='finished' + result yok → console.error
           ("oda finished ama rooms.result BOŞ …") + `{kind:'draw'}`
      Yazan taraf: `applyMove` (line/draw) ve `resign` (resign) `state:'finished'`
      ile AYNI CAS'ta `result` yazıyor. Yani 3. dal artık gerçekten ulaşılamaz —
      ama sessiz değil.
    ek_koruma: >
      Bozuk bir kayıt (ör. reason:'resign' + dolu line) şemayı geçemez: gürültü
      çıkar ve tahtaya düşülür. Bunun testi var.

decisions:
  - id: W1-02-D1
    karar: '`rooms` dokümanına `result` alanı eklendi (RoomDoc + şema).'
    neden: >
      Pes/süre aşımı/terk sonucunun kazananı TAHTADAN OKUNAMAZ. Canlı katman
      yalnız `rooms`u görüyor (change stream odayı taşır, `state` mesajı odadan
      üretilir); sonucu yalnız `games`e yazmak, `room-view.ts`in "berabere"
      uydurmasına yol açıyordu (WS-001 borcu). Alan `state:'finished'` ile AYNI
      CAS'ta yazılıyor: iki yazma olsaydı arada bir change stream olayı düşer ve
      istemci bir an "kazananı olmayan bitmiş oyun" görürdü.
    alternatif_reddedildi: >
      "Sonucu `games`ten oku" — `apps/web` odayı change stream'den alıyor; her
      olayda ikinci bir `games` sorgusu R1'i (tek okuma kaynağı) bozar ve
      instance başına tek stream bütçesine ek Atlas işlemi bindirirdi (M0'da
      100 işlem/sn).
  - id: W1-02-D2
    karar: '`applyMove` terminal dalı `finishGame`i ÇAĞIRIYOR (apply-move.ts açıldı).'
    neden: >
      Kartın çakışma kümesinde apply-move.ts yoktu ama kriter 3/4 (games dolar,
      stats artar) yalnız `resign` yoluyla karşılansaydı NORMAL biten oyunlar
      hiç kesinleşmezdi: `finishedAt:null` oyunlar birikir, istatistik hiç
      artmazdı. `finishGame` kendi CAS'ıyla idempotent olduğu için ek çağıran
      risk taşımıyor. Dosya bu dalgada başka kimsede değil (W1-01 offline oyun,
      W1-03 presence).
  - id: W1-02-D3
    karar: 'Rövanşta `presence` de koltukla BİRLİKTE takas ediliyor.'
    neden: >
      Koltuk sahipliği `seats[*].userId`ye, bağlantı geçerliliği
      `presence[seat].connId`ye bakıyor. Yalnız `seats` takas edilseydi her iki
      oyuncu da kendi YENİ koltuğunda "başka bir bağlantı" görür ve
      `detectTakeover` ikisini birden 4409 ile kapatırdı — rövanş her seferinde
      iki tarafı da düşürürdü. Negatif kontrol testi bunu kanıtlıyor.
      Aynı gerekçeyle `disconnected.seat` de takas ediliyor (kayıt "şu
      koltuktaki kişi koptu" demek; koltuk etiketi değişince aynı kişiyi
      göstermeye devam etmeli).
  - id: W1-02-D4
    karar: "Rövanş yüzeyinde durum hatası `INVALID_MESSAGE` (GAME_OVER DEĞİL)."
    neden: >
      `GAME_OVER`ın Türkçesi "Oyun bitti." — oyun bitmeden gelen bir rövanş
      teklifinde tam TERSİNİ söylerdi. `INVALID_MESSAGE` ("Geçersiz istek.")
      "bu çerçeve bu durumda geçersiz" anlamını doğru veriyor. Zod ihlali
      sayacını tetiklemiyor (o sayaç yalnız ayrıştırma hatalarında artıyor).
  - id: W1-02-D5
    karar: 'Kendi teklifini tekrarlamak / kendi teklifini kabul etmek YAZMA ÜRETMEZ.'
    neden: >
      `join.ts`teki yazma-amplifikasyonu kapısının aynısı: her tekrar bir CAS +
      bir change stream olayı + odadaki her bağlantıya tam `state` üretirdi.
      Change stream instance başına TEK olduğu için tek soketin seli o
      instance'taki BÜTÜN odaların olay dağıtımını geciktirir.
  - id: W1-02-D6
    karar: 'Süresi geçmiş teklif okunurken `rematch:null` GERÇEKTEN YAZILIYOR (2 sürüm artışı olsa da).'
    neden: >
      Kriter 9 "yazılır" diyor ve sebebi mimari: teklifin düştüğünü karşı taraf
      yalnız change stream'den öğrenebilir (R1). Yazmayıp sadece "yok say"
      deseydik, teklif eden ekranında sonsuza kadar "Rövanş yanıtı bekleniyor…"
      kalırdı. Yazma sayesinde `connection.ts` zaten
      `rematch:cancelled{reason:'expired'}` üretiyor.
  - id: W1-02-D7
    karar: '`forfeitStatus` dönüş tipi `Extract<TransportStatus,{kind:"won"}>`e daraltıldı.'
    neden: >
      Aksi hâlde her çağıranın ölü bir `kind !== "won"` dalı yazması gerekiyordu
      (kapsamı düşüren, hiç koşulmayan kod). Daraltma geriye dönük uyumlu.
  - id: W1-02-D8
    karar: 'Rövanş CAS yarışını kaybedersek az önce açılan `Game` SİLİNİYOR.'
    neden: >
      Aksi hâlde `finishedAt:null` bir oyun sonsuza dek `games`te kalır ve maç
      geçmişi/eşleşme sayımlarını (KK-113) kirletirdi.

gotchas:
  - baslik: 'Koltuk takası `presence` takas edilmezse İKİ oyuncuyu birden 4409 ile düşürür'
    detay: >
      `seats` ve `presence` aynı koltuk etiketiyle indeksleniyor ama farklı
      şeyleri temsil ediyor (kim oturuyor / hangi soket geçerli). Rövanşta
      `seats` takas edilip `presence` bırakılırsa `connection.detectTakeover`
      her iki bağlantıda da "presence.connId benim değil" görür ve
      SESSION_TAKEOVER ile kapatır. `seat-lost` (4403) dalı ise HİÇ tetiklenmez
      çünkü `seatOf` hâlâ bir koltuk buluyor — yani belirti "oda boşaldı" değil
      "iki oyuncu da devredildi sanıp salt-okunura düştü" olur, teşhisi zor.
      Sonda: presence takasını kaldır, `rematch.test.ts`in negatif kontrolü
      4409 üretir.
  - baslik: 'Mongoose `findOneAndUpdate` `pre("validate")` hook`unu ATLAR — GameDoc çapraz tutarlılığı update yolunda ÇALIŞMIYOR'
    detay: >
      `Game`in `pre('validate')` hook`u (isDraw⇒winner null, endReason==="line"
      ⇒winLine dolu) yalnız `doc.save()`/`insertMany` yolunda koşuyor.
      `finishGame` CAS`ı `findOneAndUpdate` kullandığı için bu hook HİÇ
      çalışmıyor; tutarlılık "yapı gereği" sağlanıyor (alanların tümü tek bir
      doğrulanmış `TransportStatus`tan türetiliyor) + `runValidators: true` ile
      alan-seviyesi doğrulayıcılar açık. Biri ileride `finishGame`e elle alan
      eklerse hook onu YAKALAMAZ.
  - baslik: 'Yeni bir zorunlu `RoomDoc` alanı 5 test dosyasının fixture`ını kırar (kaynak kodu değil)'
    detay: >
      `result` eklenince `apps/web`de 5 `makeRoom` literali TS2322/TS2741 verdi
      (room-view, connection, handlers/index, room-hub, session testleri).
      Üretim kodu hiç kırılmadı — yani "RoomDoc`a alan eklemek ucuz" sanıp
      yalnız `packages/db` typecheck`ine bakmak yanıltıcı; alan ekleyen görev
      `apps/web` typecheck`ini de koşmak zorunda.
  - baslik: 'Rövanş sonrası ince delta gönderilemez — `moves` boşaldığı için tam `state` şart'
    detay: >
      `emitBoardDelta`ın "ince" koşulu `version+1 && moves.length+1`. Rövanşta
      `moves` 5`ten 0`a düşüyor, yani koşul doğal olarak bozuluyor ve tam durum
      gidiyor (KK-047 istemci tahtayı TÜMÜYLE değiştirir). Bu bir tesadüf değil
      ama koda yazılı da değil — `moves`u korumaya çalışan bir "optimizasyon"
      istemcinin tahtasında eski taşları bırakırdı.
  - baslik: '`rematch:cancelled` sebebi bugün SABİT KODLU — `opponent-left` protokolde var, üreten kod YOK'
    detay: >
      `connection.ts:207` her düşmede `reason:'expired'` yazıyor. Şema
      (`ws-protocol.ts:97`) ve şema testi `'opponent-left'`i tanıyor, yani
      "protokol destekliyor" görüntüsü var ama hiçbir kod yolu üretmiyor.
      Ayrıca `detach.ts` `state:'finished'` odada `disconnected` DAMGALAMIYOR,
      dolayısıyla rövanş beklerken rakip ayrılınca teklif edene hiçbir sinyal
      gitmiyor. Şemada bir değerin bulunması onun üretildiği anlamına gelmez.

notes_for_lead:
  - 'İki mutasyon sondası da UYGULANDIĞI `diff -q` ile doğrulandı, koşuldu ve `git checkout --` sonrası `git status --porcelain` BOŞ. Sondadan önce gerçek düzeltmeler commit edilmişti (4328a55).'
  - '`skeletons.test.ts` 5 iddiadan 1`e indi (yalnız `pushEmoji`/W3-03 kaldı). `settle.test.ts` tetikleyicisine DOKUNULMADI.'
  - '`handlers/index.test.ts` içindeki `UYGULANAN` kümesi ve "iskelet DOKUNMAZ" testi güncellendi (artık `chat:emoji` üzerinden). Kaynak `handlers/index.ts` AÇILMADI.'
  - 'ELO/`rated` bilerek yazılmadı: `rated:false`, `eloDelta:{X:0,O:0}` kalıyor — W3-01`in işi. `finishGame` sırası (§9) ELO adımının araya girebileceği şekilde bırakıldı.'
  - 'Kriter 6`nın ≤2 sn bütçesi ağ ölçümüdür; birim testte yalnız "oda değişti → rematch:offered üretildi" yolu doğrulandı. Preview E2E ölçmeli.'
---
