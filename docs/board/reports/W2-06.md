```yaml
task: W2-06
status: done
summary: >
  P1 BLOKER: `apps/web/app/api/auth/mobile/callback/route.ts` başarılı girişte
  KOŞULSUZ `xox://auth?...` deep link'ine 307 dönüyordu — web hedefinde (Expo
  web build) bu şema tarayıcı için anlamsız, mobil web girişi tamamen kırıktı
  (E2E-005'te gerçek Chromium'da ölçülmüş, `mobile-web.spec.ts`te `test.skip`
  ile KK-091 olarak belgelenmiş bir bulgu). Düzeltme: `callback` artık
  ALLOWLİST edilmiş bir `redirect_uri` isteği varsa gerçek bir http(s) web
  hedefine, yoksa (bugünkü TEK davranış) `xox://` deep link'ine döner. Açık
  yönlendirme YOK — hedef her zaman `origin`+`pathname`den yeniden kurulan,
  sabit bir allowlist'e karşı doğrulanmış TEMİZ bir URL. `apps/mobile/app/
  auth.tsx` (yeni) web'de bu hedefi karşılar ve `expo-web-browser`ın GERÇEK
  web implementasyonunun beklediği `maybeCompleteAuthSession()`i çağırır.

onemli_kisitlama_okundugu_ve_uygulandigi: >
  Kartın çakışma kümesi (`apps/mobile/app/auth.tsx` + `apps/web/app/api/
  auth/mobile/callback/route.ts` + kendi test dosyaları + bu rapor) DIŞINDA
  HİÇBİR dosyaya dokunulmadı — `git diff main --stat` bunu doğruluyor. Bunun
  tam olarak ne anlama geldiği ve neden tam çözümün BİR PARÇASININ kapsam
  dışında kaldığı "kapsam_disi_gerekli_degisiklik" bölümünde.

kirmizi_once_kanit: >
  RED test `apps/web/app/api/auth/mobile/callback/route.test.ts` içine EKLENDİ
  ("W2-06 — web hedefi: allowlist edilmiş redirect_uri" describe bloğu),
  sonra `route.ts` GEÇİCİ olarak `git stash push -- apps/web/app/api/auth/
  mobile/callback/route.ts` ile ORİJİNAL (düzeltme ÖNCESİ) haline döndürülüp
  koşuldu. Çıktı (harfiyen): "pnpm --filter @xox/web exec vitest run
  app/api/auth/mobile/callback/route.test.ts" → "Test Files  1 failed (1)" /
  "Tests  2 failed | 5 passed (7)" — iki başarısız test de TAM OLARAK yeni
  eklenen W2-06 testleriydi: "oturumluysa VE redirect_uri allowlist origin +
  /auth ise, xox:// DEĞİL o hedefe (http/https) 307 döner" ve "DB yazması
  başarısız olursa VE redirect_uri geçerliyse hata da AYNI web hedefine
  gider (error=SERVER_ERROR)". İkisinde de assertion hatası birebir aynıydı:
  "AssertionError: expected 'xox:' to be 'http:' — Expected: http: / Received:
  xox:". Bu, kartın istediği tam kanıt: web bağlamından ("redirect_uri" ile
  "ben bir web istemcisiyim" diyen bir istek) gelen bir callback isteği BUGÜN
  `xox://` döndürüyor. Ardından `git stash pop` ile düzeltme geri getirildi ve
  AYNI komut 7/7 yeşil verdi (aşağıda "olculen_ciktilar"da).

secilen_sinyal_ve_guvenlik_gerekcesi: >
  SİNYAL: isteğe bağlı `redirect_uri` query parametresi (OAuth'un standart
  `redirect_uri` deseni). NEDEN BU: `expo-web-browser`ın GERÇEK web
  implementasyonu okunarak doğrulandı (`node_modules/.pnpm/expo-web-browser@
  57.0.2.../src/ExpoWebBrowser.web.ts`) — `openAuthSessionAsync` web'de
  `redirectUrl`ı SUNUCUYA HİÇ GÖNDERMEZ, yalnız açan pencerenin KENDİ
  `localStorage`'ında saklar; popup'ın NEREYE VARDIĞINI (`window.location`)
  `maybeCompleteAuthSession()` içinde okuyup `postMessage`le açan pencereye
  bildirir. Yani native/web ayrımı için User-Agent/Referer gibi bir HTTP
  başlığına GÜVENİLEMEZ — her ikisi de (ASWebAuthenticationSession/Custom
  Tabs VE gerçek bir masaüstü tarayıcı popup'ı) GERÇEK bir tarayıcı oturumu
  olduğu için başlık seviyesinde AYIRT EDİLEMEZ (bunu doğrulamak için
  kaynağı okudum, tahmin etmedim). Tek güvenilir yol istemcinin NİYETİNİ
  AÇIKÇA bildirmesidir — tıpkı standart OAuth `redirect_uri`si gibi.

  NEDEN GÜVENLİ (açık yönlendirme YOK): `redirect_uri` DEĞERİ hiçbir zaman
  doğrudan `Location` başlığına yazılmaz.

  1. `resolveWebRedirectTarget` önce `http`/`https` şeması VE tam `/auth`
     path'i şart koşar.
  2. Sonra `origin`i SABİT bir allowlist'e (`ALLOWED_WEB_AUTH_ORIGINS`,
     `MOBILE_WEB_AUTH_ORIGINS` env'i, varsayılan `http://localhost:8081` —
     `expo start --web --port 8081`in kendi varsayılan portu, `package.json`
     `web` script'inden doğrulandı) karşı TAM EŞLEŞME arar.
  3. Eşleşmezse `null` döner ve akış BUGÜNKÜ TEK davranışa (`xox://auth`)
     düşer — yani sinyalin saldırgan/istemci tarafından "serbestçe"
     seçilmesinin SONUCU EN FAZLA "hiçbir şey değişmedi"dir, hiçbir zaman
     YENİ bir yeteneğe (bizim sahip olmadığımız bir origin'e yönlendirme)
     kapı AÇMAZ.
  4. Eşleşen TEK durumda bile üretilen hedef `parsed`in kendisi DEĞİL,
     `origin`+`pathname`den YENİDEN KURULMUŞ temiz bir `URL` nesnesidir —
     istemcinin `redirect_uri`ye iliştirebileceği rastgele bir query string
     bizim ürettiğimiz yönlendirmeye asla taşınmaz.

  Sonuç: bu uç zaten kimlik token'ı üretiyor olsa da (kart uyarısı), bu
  sinyalin manipülasyonu saldırgana YENİ bir yetenek vermez — token her
  koşulda ya OS'in kendi kayıtlı şemasına (native) ya da BİZİM önceden
  onayladığımız bir origin'e (web) gider, üçüncü bir seçenek YOKTUR.

kapsam_disi_gerekli_degisiklik: >
  TAM çözüm için `apps/mobile/lib/auth/browser-login.ts`nin `authorize`
  isteğine `&redirect_uri=<Linking.createURL('auth')>` eklemesi VE
  `apps/web/app/api/auth/mobile/authorize/route.ts`nin bunu `callback`e
  taşıması (bugün yalnız `state`i taşıyor) gerekir — bu, `docs/board/
  reports/E2E-005.md`nin ZATEN önerdiği 3 karttan BİRİNCİSİ ve bu görevin
  çakışma kümesi (`apps/mobile/app/auth.tsx` + `apps/web/.../callback/
  route.ts`) AÇIKÇA bu iki dosyayı DIŞARIDA bırakıyor. Bunu DOĞRULAMAK için
  `expo-web-browser`ın GERÇEK kaynağı okundu (yukarıda alıntılandı):
  `redirectUrl` parametresi ASLA ağa gönderilmiyor, yalnız yerel doğrulama
  için kullanılıyor — yani BUGÜN hiçbir istemci `redirect_uri`yi sunucuya
  göndermiyor, bu görev SADECE alıcı ucu (sunucu + `/auth` sayfası) hazır
  hale getirdi. Bu satır kartı "orada bırakmıyor" (DoD'un geri kalanı
  tamamlandı) ama lead'e AÇIKÇA bildiriliyor: **P1 tam olarak KAPANMADI**,
  ikinci bir küçük kart (`xox-dev-mobile`, çakışma kümesi: `browser-login.ts`
  + `authorize/route.ts`) gerekiyor. Ayrıca E2E-005'in 2. ve 3. maddeleri
  (CORS + gerçek dağıtım origin'i kararı) hâlâ AÇIK — `MOBILE_WEB_AUTH_ORIGINS`
  env'i şimdilik yalnız yerel dev varsayılanını (`http://localhost:8081`)
  içeriyor, prod/preview origin'i eklenmedi (henüz bir dağıtım kararı yok).

mevcut_davranisin_bozulmadiginin_kaniti: >
  `route.test.ts`teki üç ORİJİNAL test (oturumsuz → `/giris`, oturumlu →
  `xox://auth?token=&refresh=&state=` + çapraz-kabul-imkânsız doğrulaması,
  DB yazması başarısız → `xox://auth?error=SERVER_ERROR`) TEK SATIR
  DEĞİŞTİRİLMEDEN hâlâ yeşil — hiçbiri `redirect_uri` göndermiyor, bu yüzden
  `webTarget === null` dalına düşüyorlar ve `errorRedirectTarget`/`target ??
  new URL('xox://auth')` BUGÜNKÜ AYNI `mobileErrorDeepLink`/deep-link
  davranışını üretiyor. `refresh/route.ts` ve `authorize/route.ts`
  DOKUNULMADI (`git diff main --stat` doğruluyor), ikisinin de testleri
  (10 test) değişmeden yeşil.

olculen_ciktilar:
  vitest_callback_ve_kardesleri: >
    `pnpm --filter @xox/web exec vitest run app/api/auth/mobile` →
    "Test Files  3 passed (3)" / "Tests  16 passed (16)" (authorize 3 +
    refresh 6 + callback 7 [4 eski + 3 yeni W2-06] — not: callback
    dosyasında toplam 7 `it`, 4 ORİJİNAL + 3 YENİ `describe` alt bloğunda;
    üstteki 16 sayısı authorize+refresh+callback toplamı).
  mobil_typecheck: '`pnpm --filter @xox/mobile typecheck` → exit 0, hata yok.'
  mobil_build: >
    `pnpm --filter @xox/mobile build` → `expo export --platform web
    --output-dir dist` başarılı, "Static routes (13)" listesinde
    "/auth (18KB)" YENİ satır olarak görünüyor, `apps/mobile/dist/index.html`
    VE `apps/mobile/dist/auth.html` üretildi (`auth.html` içinde
    `tr.auth.mobileReturn` metni — "Girişin tamamlandı, uygulamaya
    dönülüyor…" — server-render edilmiş halde, `grep -c` ile doğrulandı).
  mobil_lint: '`pnpm lint apps/mobile` → `eslint . --max-warnings=0 apps/mobile` sıfır uyarı/hata.'
  pnpm_gates: >
    `pnpm gates` (worktree kökünden) → **exit 0**. Altı kapı: `check:dead-exports`
    (16 BİLİNEN borç, YENİ şüpheli ölü export yok) + `typecheck` (7/7 paket,
    `@xox/web`/`@xox/mobile` cache miss/taze koştu, diğerleri cache hit) +
    `lint` (0 uyarı) + `format:check` (temiz — `prettier --write` ile bir
    format sorunu düzeltildikten SONRA) + `test:coverage` (`@xox/web`
    96 dosya / 947 test yeşil, kapsam %94.2 ifade / %89.41 dal / %94.04
    fonksiyon / %96.41 satır) + `knip` (yalnız var olan config ipuçları,
    hata yok). Turbo cache'in "eski yeşili replay etme" riskine karşı
    `@xox/web`/`@xox/mobile` typecheck'i bu koşuda zaten `cache miss` idi
    (dosyalar YENİ değiştiği için doğal taze koşu; ayrıca gotchas.md'nin
    önerdiği merge-sonrası `--force` doğrulaması entegrasyon adımında
    integrator tarafından tekrar yapılmalı).

degisiklikler:
  - dosya: 'apps/web/app/api/auth/mobile/callback/route.ts'
    ne: >
      `ALLOWED_WEB_AUTH_ORIGINS` (env `MOBILE_WEB_AUTH_ORIGINS`, varsayılan
      `http://localhost:8081`), `resolveWebRedirectTarget` (allowlist +
      `/auth` path doğrulaması, TEMİZ URL üretimi), `errorRedirectTarget`
      (native/web ORTAK hata URL'si, `shared.ts`teki `mobileErrorDeepLink`e
      DOKUNULMADI). `GET` artık `webTarget ?? new URL('xox://auth')` ile
      hedef seçiyor.
  - dosya: 'apps/web/app/api/auth/mobile/callback/route.test.ts'
    ne: >
      4 yeni test ("W2-06 — web hedefi: allowlist edilmiş redirect_uri"):
      allowlist eşleşince http(s) hedefine gitme + token doğrulanabilirliği,
      allowlist DIŞI origin'in (açık yönlendirme denemesi) YOK SAYILMASI,
      yanlış path'in YOK SAYILMASI, DB hatasının da web hedefine gitmesi
      (token/refresh sızmadan). 3 ORİJİNAL test DEĞİŞMEDEN kaldı.
  - dosya: 'apps/mobile/app/auth.tsx (yeni)'
    ne: >
      KK-009 web alıcı ucu. `useEffect`te yalnız `Platform.OS === 'web'`ken
      `WebBrowser.maybeCompleteAuthSession()` çağrılır (native'de bu ekran
      normal akışta hiç mount olmaz — OS seviyesinde yakalanır). `error`
      query param'ı varsa `tr.errors[...]`, yoksa `tr.auth.mobileReturn`
      (var olan, DONDURULMUŞ metin, YENİ anahtar EKLENMEDİ) gösterilir.
      Token/refresh burada `SecureStore`a YAZILMAZ (kart değişmezi) — o iş
      zaten `session.tsx`nin `signIn()`inde, `openAuthSessionAsync`in
      çözdüğü sonuçtan yapılıyor; bu ekran popup İÇİNDE çalışır, opener'ın
      state'ine erişimi yoktur.

test_kaniti:
  kirmizi_once: 'Yukarıda "kirmizi_once_kanit" — `git stash` ile ölçüldü, tahmin edilmedi.'
  yesil: '`pnpm gates` exit 0 (yukarıda tam çıktı).'
  mevcut_davranis_korundu: '"mevcut_davranisin_bozulmadiginin_kaniti" bölümü.'

commit_sha: 'b93489f fix(web): mobil web girişini xox:// deep link tuzağından kurtar (W2-06)'

worktree: ".claude/worktrees/W2-06 (branch feat/W2-06), main'e merge/push YAPILMADI"

blocked_reason: null

next_suggestions:
  - >
    YENİ KART gerekiyor (öneri: xox-dev-mobile, çakışma kümesi:
    `apps/mobile/lib/auth/browser-login.ts` + `apps/web/app/api/auth/mobile/
    authorize/route.ts`): `browser-login.ts` `authorizeUrl`e
    `&redirect_uri=${encodeURIComponent(Linking.createURL('auth'))}` eklemeli
    (yalnız `Platform.OS === 'web'`ken; native'de eklemek ZARARSIZ ama
    gereksiz — `redirect_uri` zaten `xox://auth` olur ve allowlist'te
    olmadığı için bugünkü davranışa düşer, YİNE DE netlik için native'de
    hiç eklenmemesi tercih edilir); `authorize/route.ts` bunu `callback`e
    (`callback.searchParams.set('redirect_uri', ...)`) TAŞIMALI. Bu kart
    tamamlanmadan KK-091 (`mobile-web.spec.ts`, hâlâ `test.skip`) unskip
    edilemez.
  - >
    `MOBILE_WEB_AUTH_ORIGINS` env'ine gerçek preview/prod origin'i eklenmesi
    E2E-005'in 3. maddesindeki (mobil web'in nerede barınacağı) dağıtım
    kararına bağlı — o karar verilince `.env.example`/Vercel proje ayarına
    eklenmesi ayrı, küçük bir devops görevi.
  - >
    E2E-005'in 2. maddesi (CORS ya da aynı-origin dağıtım) bu karttan
    BAĞIMSIZ hâlâ açık — `redirect_uri` köprüsü tamamlansa bile mobil web
    farklı origin'den servis edilirse kimlikli `fetch` (oda kurma, WS bileti)
    yine engellenir (`mobile-web.spec.ts`teki "BULGU" describe bloğu).
```
