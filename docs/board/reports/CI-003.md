```yaml
task: CI-003 + CI-004
status: done
summary: >
  ÖLÇÜM (2026-08-28, dispatch anında): CI-004'ün allowlist düzeltmesi (eb2be58,
  2026-08-25) ZATEN main'de mergeli ve trace-zip dışlaması (6742241) da mergeli —
  bu iki alt-görev kod düzeyinde ÖNCEDEN kapanmıştı. CI-003'ün "iş hiç koşmuyor"
  iddiası kısmen YANLIŞ ÇIKTI: `gh run list` son 100 koşuda 81 skipped / 10
  failure / 9 success gösteriyor — yani iş ZATEN gerçek koşular üretiyor, ama
  yalnız bir branch/PR origin'e push edildiğinde. Bugünkü (2026-08-28) 27
  koşudan 25'i main (doğru şekilde skip — main = Vercel Production Branch,
  environment="Production") ve 2 tanesi GERÇEK "Preview" deployment_status
  olayıydı: biri geçti (run 33133885483), biri gerçek E2E kırmızısı üretti
  (run 33134842205, 3 test failed + 1 flaky, timeout-abandon.spec.ts + profile.spec.ts).

  KÖK NEDEN (asıl, düzeltilen kısım): kod hatası değil SÜREÇ boşluğuydu. Normal
  dalga döngüsünde worktree branch'leri (feat/<id>) LOKAL kalıyor, origin'e hiç
  push edilmiyor — dolayısıyla deployment_status için gerçek bir "Preview" olayı
  üretecek bir tetik zaten oluşmuyordu. Mekanizmanın kendisi (allowlist filtresi)
  sağlıklı; sorun onu tetikleyecek bir olayın SOP'ta hiç doğmamasıydı. İş yalnız
  biri kasıtlı olarak bir branch/PR push ettiğinde (feat/CI-002, chore/gece2-*)
  çalışıyordu.

  DÜZELTME: `.github/workflows/e2e-preview.yml`'e `workflow_dispatch` girdisi
  (`preview_url`, required) eklendi — artık deployment_status olayını beklemeden,
  devops/qa-e2e istediği preview URL'ine karşı işi ELLE tetikleyebilir. Girdi
  önce bir guard adımından geçiyor: `xox.omerdursun.com` (production alan adı)
  içeriyorsa ya da `https://` ile başlamıyorsa iş `exit 1` ile durur — E2E_BASE_URL
  yanlışlıkla production'a işaret edip gerçek kullanıcı verisiyle çakışan test
  trafiği üretmesin diye (MONGODB_DB zaten sabit xox_test, ama HTTP trafiği ayrı
  bir risk). Ayrıca dosyanın başına CI-003 teşhisini özetleyen bir yorum bloğu
  eklendi — ileride biri "hiç çalışmıyor" diye tekrar yanlış teşhis koymasın.

  ⚠️ YENİ EKLENEN `workflow_dispatch` YOLU DOĞRULANMADI: bu iş yalnız GitHub'ın
  bildiği bir ref'te (ör. main, ya da origin'e push edilmiş bir branch) tanımlıysa
  tetiklenebilir. `feat/CI-003` origin'e hiç push edilmedi (DoD gereği "merge/push
  YAPMA") ve GitHub bu ref'i bilmiyor — dolayısıyla `gh workflow run` ile canlı
  deneyemedim. `actionlint` (v1.7.12, brew ile kuruldu) dosyayı ve tüm
  `.github/workflows/*.yml`'i temiz geçti (exit 0), if/env ifadeleri sözdizimsel
  olarak doğru, ama GERÇEK bir GitHub Actions çalıştırması ile kanıtlanmadı.
  Entegratör main'e merge ettikten sonra `gh workflow run "E2E (preview)" -f
  preview_url=<gerçek preview url>` ile tek seferlik doğrulanmalı.

  Buna karşılık `deployment_status` tetikleyicisinin GERÇEKTEN çalıştığı iddiası
  UYDURMA DEĞİL — aşağıdaki `gercek_ci_kosulari` bölümündeki run ID'leri bu
  görev başlamadan ÖNCE (bugün, 01:46 ve 02:05'te) zaten üretilmişti; ben onları
  yeniden ürettim değil, `gh run list`/`gh api` ile ÖLÇTÜM.
files_changed:
  - .github/workflows/e2e-preview.yml (workflow_dispatch girdisi + production-alan-adı guard adımı + teşhis yorumu; deployment_status davranışı DEĞİŞMEDİ)
  - docs/board/reports/CI-003.md
olcum_bugun_2026-08-28:
  son_100_kosu: "81 skipped, 10 failure, 9 success (gh run list --workflow='E2E (preview)' --limit 100)"
  bugunku_27_kosu: '25 skipped (hepsi main, environment=Production — DOĞRU davranış), 1 success, 1 failure (ikisi de gerçek Preview deployment_status olayı)'
  main_uzerinden_YANLIS_kosan_tarihsel_olay: >
    Son 100 koşuda headBranch=main VE conclusion != skipped olan yalnız 4 kayıt
    var (runs 32823623383, 32823634080, 32824251611, 32824258151) — hepsi
    2026-08-25T07:5x, yani CI-004'ün düzelttiği DENYLIST hatasının canlı kanıtı
    (o gün main'e push production URL'ine karşı gerçekten koştu). Fix commit'i
    (eb2be58, 2026-08-25T11:29:48+03:00) SONRASI main için tek bir non-skipped
    kayıt yok — allowlist Aug 25'ten beri kesintisiz doğru çalışıyor.
kok_neden:
  - 'main = Vercel Production Branch → main''e her push GitHub''a environment:"Production" bildirir; allowlist bunu (doğru şekilde) atlar. Bu bir hata DEĞİL.'
  - "Normal dalga SOP'u worktree branch'lerini origin'e hiç push etmiyor → gerçek bir Preview deployment_status olayı çoğu zaman zaten doğmuyor. Mekanizma sağlıklı, tetikleyici nadiren geliyordu."
  - 'Bir branch/PR bilerek push edildiğinde (feat/CI-002 x6, chore/gece2-* x2, chore/size-limit-yorum-duzeltme x3, chore/yeni-secret-dogrulama, chore/trace-dislama-dogrulama) iş GERÇEKTEN çalışıyor ve gerçek Playwright sonucu üretiyor.'
gercek_ci_kosulari:
  - run: 'https://github.com/A1640A/XOX/actions/runs/33133885483 — deployment_status, environment=Preview, chore/gece2-preview-dogrulama (sha 69f56bf), 2026-08-28T01:46:32Z, conclusion=success. Bu görev BAŞLAMADAN ÖNCE üretilmiş, ölçülerek doğrulandı.'
  - run: "https://github.com/A1640A/XOX/actions/runs/33134842205 — deployment_status, environment=Preview, chore/gece2-karantina-sonrasi (sha 6da73f2), 2026-08-28T02:05:23Z, conclusion=failure. Gerçek Playwright çıktısı: '3 failed, 1 flaky, 43 passed' — timeout-abandon.spec.ts KK-072 ve KK-070/071, profile.spec.ts KK-083 (tema kalıcılığı) flaky. Kapı ÇALIŞIYOR ve gerçek kırmızı üretiyor; testleri buradan yeşile boyamadım."
  - run: "https://github.com/A1640A/XOX/actions/runs/32824258151 (ve 3 kardeşi, 2026-08-25T07:5x) — CI-004'ün düzelttiği DENYLIST hatasının tarihsel kanıtı: main push'u production URL'ine karşı gerçekten koştu."
  - not: 'workflow_dispatch (yeni eklenen) için run ID YOK — DOĞRULANMADI, gerekçe yukarıda.'
pnpm_gates:
  exit_code: 0
  detay: "check:dead-exports (16 bilinen borç, yeni yok) + typecheck (7/7 cached, Cached: 7 cached) + lint + format:check (prettier --write ile e2e-preview.yml düzeltildi) + test:coverage (@xox/web: 96 dosya/943 test yeşil, statements 94.23/branches 89.4/functions 93.99/lines 96.43) + knip (yalnız var olan config-hint'leri, yeni ölü export yok)"
  not: "Bu görev yalnız bir YAML dosyasına dokunduğu için typecheck/test farkı beklenmiyordu; turbo cache hit'leri (7 cached, 6 cached) bunu doğruluyor — kod tarafında regresyon yok."
actionlint:
  surum: '1.7.12 (brew ile bu görevde kuruldu)'
  sonuc: 'e2e-preview.yml tek başına ve .github/workflows/*.yml tümü exit 0, 0 bulgu'
oneri_ayri_kart:
  - "E2E-005 (öneri): timeout-abandon.spec.ts KK-070/071/072 (terk/grace akışı, 3 test) ve profile.spec.ts KK-083 (tema kalıcılığı, flaky) preview'da GERÇEKTEN kırmızı/kararsız çıktı (run 33134842205). Bu kart onları düzeltmiyor — kapsam dışıydı (yalnız .github/workflows/e2e-preview.yml + bu rapor). xox-qa-e2e'ye ayrı kart olarak devredilmeli."
  - "CI-005 (öneri): normal dalga SOP'u branch'leri origin'e push etmediği için deployment_status tetikleyicisi PRATİKTE nadiren ateşleniyor. Ya integrator her merge öncesi feat/<id>'yi kısa süreliğine push edip PR açsın (Preview üretir, sonra silinir) ya da yeni eklenen workflow_dispatch entegratörün DoD adımına (merge sonrası zorunlu manuel tetik) eklensin. Karar lead'e bırakıldı."
izin_reddi: 'Bu görevde reddedilen bir izin isteği OLMADI.'
secret_disiplini: "VERCEL_AUTOMATION_BYPASS_SECRET ve MONGODB_URI değerleri hiçbir yerde okunmadı/yazdırılmadı; yalnız GitHub secret referansı olarak (${{ secrets.* }}) kaldı. Trace zip dışlaması (!apps/e2e/playwright-report/data/*.zip) DOKUNULMADAN doğrulandı — hâlâ satır 136'da (git diff ile teyit edildi)."
commit: 'ci(ci): E2E preview kapisi olculdu — sureç boşluğu tespit edildi, manuel tetik eklendi (CI-003+CI-004)'
```
